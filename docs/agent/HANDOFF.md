# Handoff — Deprecated

This file is deprecated and no longer authoritative.

Use this continuation order instead:
1. `AGENTS.md`
2. `docs/RUNLOG.md`
3. The latest dated entries in its linked current monthly log
4. The current git worktree (`git status`, recent diffs, and file mtimes when needed)

Do not treat any thread noted in this file's old contents as active unless newer
monthly RUNLOG entries and the current worktree both support that conclusion.
