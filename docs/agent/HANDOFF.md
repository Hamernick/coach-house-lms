# Handoff — Deprecated

This file is deprecated and no longer authoritative.

Use this continuation order instead:
1. `AGENTS.md`
2. The latest dated entries in `docs/RUNLOG.md`
3. The current git worktree (`git status`, recent diffs, and file mtimes when needed)

Do not treat any thread noted in this file's old contents as active unless newer
`docs/RUNLOG.md` entries and the current worktree both support that conclusion.
