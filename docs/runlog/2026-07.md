# RUNLOG — 2026-07

Monthly session history beginning at the 2026-07-14 log migration. Earlier July
entries remain in the [legacy archive](archive/legacy-through-2026-07-14.md).

2026-07-14 14:14 EDT - replaced the 3.89 MB single-file RUNLOG with a stable index and monthly append-only logs.

- Preserved the original 30,722-line history byte-for-byte in `docs/runlog/archive/legacy-through-2026-07-14.md` (SHA-256 `00bb1f5e0c5dd14f5ae0c93915c9818091886aee683338144c5db1d5690558f7`).
- Updated `AGENTS.md` and active workflow docs, added index/month/archive size limits, documented rollover, and added continuation-contract tests.
- Passed focused Prettier, ESLint, Vitest (`2 files`, `5 tests`), `pnpm check:large-files`, direct size checks, `git diff --check`, and live `/` plus `/find` probes.
- A broad Markdown Prettier check still reports pre-existing formatting drift in several legacy docs; they were not reformatted. No application, database, environment, deployment, or unrelated dirty-worktree changes were made.

2026-07-14 15:00 EDT - began the source-verified public resource enrichment and release pipeline.

- Verified the live/local mismatch: live has 64 external resources plus 13 platform organizations; localhost has 4,124 external candidates plus 13 organizations. Local preview bypasses review by reading the candidate JSONL; production reads only approved canonical records.
- Audited all 4,249 candidates. None meets the publication contract. All lack eligibility, access instructions, two completed comparison passes, and approved enrichment verification; 4,242 lack useful summaries.
- Added deterministic enrichment readiness checks, a hard `--publish` gate, safe provider-page evidence collection, structured two-pass OpenAI draft/verification code, citation validation, AI field provenance, and a private RLS enrichment-run ledger migration.
- Fetched all 81 official Chicago Public Library pages without publishing. All loaded; 321 field comparisons matched after address normalization. Two real conflicts remain blocked: a `Washtington` title typo and Rogers Park carrying Scottsdale's email.
- Fixed library-hour inference (`Noon-8` to `12:00-20:00`; `1-5` to `13:00-17:00`), made reprocessing repair stale structured hours, and wrote a separate ignored 4,249-record reprocessed JSONL.
- Added a sanitized paginated public RPC and server batching up to 5,000 records, removing the old 500-record live truncation point.
- Stopped publication from creating false verification timestamps and propagated the real human approver into canonical payloads.
- Updated `AGENTS.md` to link the durable resource enrichment contract for new conversations.
- Passed focused Prettier and ESLint, 83 focused Vitest tests, structured-output schema generation, all-source evidence collection, corrected candidate reprocessing, 81/81 AI readiness dry-run, `git diff --check`, linked migration inspection, and localhost `/find` HTTP 200. Full TypeScript checking still fails on extensive pre-existing dirty-worktree errors; local Supabase SQL lint could not run because port 54322 is not active.
- No Supabase writes, migration application, Vercel changes, production deployment, or resource publication occurred. The two new migrations are confirmed local-only. `OPENAI_API_KEY` is not configured, so the real five-record AI pilot remains blocked.

2026-07-14 15:34 EDT - made resource-map promotion atomic and retry-safe.

- Added `20260714201500_resource_map_atomic_promotion.sql` with one locked promotion RPC, an accepted-duplicate serialization trigger, and a partial accepted-match index.
- The RPC requires an approved staged record, identified reviewer, real verification timestamp, two source comparisons, approved completed verification ledger entry, and no unsupported claims or contradictions. It creates canonical organization/service/children, copies staged evidence, updates promotion state, and writes the curation event in one transaction.
- Accepted duplicate matches atomically block promotion; completed retries return existing canonical IDs. Promoted contacts and links are forced private regardless of client payload.
- Replaced the client-side multi-write promotion path with one RPC call and added the generated Supabase function contract plus focused migration/script tests.
- Passed focused Prettier, ESLint, Vitest (`4 files`, `27 tests`), Node syntax, and `git diff --check`. The migration compiled and ran in a temporary PostgreSQL 14 database: promotion, evidence propagation, private visibility, idempotent retry, duplicate blocking, and post-promotion duplicate rejection all passed.
- No connected Supabase migration, import, publication, environment, or deployment mutation occurred. Local Supabase lint remains unavailable because port `54322` is not running; the new migration and enrichment ledger must be applied and populated before real promotion.

2026-07-14 15:48 EDT - added the evidence-first resource-map administrator review surface.

- Added `/admin/platform/resource-map` with breadcrumbs, URL-selected and paginated review state, compact queue loading, record-scoped raw/evidence/AI queries, responsive empty/loading/error states, and the minimal high-contrast Geist treatment from `docs/design.md`.
- Reviewers can compare authoritative values with extracted/AI output, inspect citations, verification passes, conflicts, blockers, completeness, duplicate matches, reviewer identity, and explicit canonical contact/link visibility decisions. Unsafe external URL schemes are blocked.
- Mirrored approval gates server-side: incomplete source evidence, missing citations/comparison passes, failed verification, enrichment-ledger errors, and unresolved duplicate matches cannot be bypassed through a direct form request. This surface does not promote or publish records.
- Added keyboard-visible focus, mobile 44px targets, pending/error feedback, unsaved-reason warnings, private-by-default visibility messaging, and safe degradation while the enrichment ledger migration is unapplied.
- Passed focused Prettier, ESLint, Vitest (`1 file`, `7 tests`), touched-file TypeScript filtering, feature/boundary/route/threshold/raw-button/interaction/workspace guards, and scoped `git diff --check`. Local Playwright confirmed the admin route is protected on mobile and laptop viewports; authenticated visual review was unavailable because the test browser had no admin session. The optional `agent-browser` CLI was not installed.
- Full TypeScript and structure checks remain blocked by pre-existing unrelated dirty-worktree errors and oversized files. No migration, import, promotion, publication, environment, or deployment mutation occurred.

2026-07-14 18:29 EDT - replaced API-dependent library enrichment with source-specific enrichment and improved all 64 live library resources.

- Confirmed the linked Supabase database has the enrichment-ledger, public-pagination, and atomic-promotion migrations applied.
- Added a deterministic Chicago Public Library enrichment pass that uses the retained official branch pages without an OpenAI key. It corrects raw titles and contacts, creates plain-language summaries and service offerings, states eligibility/source omissions safely, explains access, preserves structured hours, adds accessibility details and `America/Chicago`, records two comparison passes, and writes claim citations plus verification evidence.
- Generated and audited an ignored 81-record library cohort: all 81 have coordinates, address, categories, contact, provider link, useful description, access information, two completed source checks, and approved deterministic verification; all 81 pass the publication contract with zero warnings. Seventeen are not yet published and were not imported or promoted.
- Added a dry-run-first, backup-first sync for already-published library records. Applied it to the 64 existing canonical records after a six-record pilot. The final backup is `data/resource-map/.engine/backups/library-public-sync-2026-07-14T22-25-55-721Z.json`.
- Live database verification now shows 64/64 clear library titles, summaries, eligibility, cost guidance, access instructions, accessibility notes, timezone, and real verification timestamps; 128/128 phone/email contacts are public; 64/64 official website links are public; raw source links remain private. The ledger contains 64 completed draft and 64 completed approved verification passes, provider `deterministic`.
- Corrected Rogers Park from the erroneous Scottsdale email to `rogerspark@chipublib.org`, corrected both Daley branch names, and normalized Legler/Woodson regional-library titles.
- Updated the local public endpoint serializer to retain useful service details and the existing detail surface to show `Who it helps`, `Eligibility`, `Cost`, `How to access`, and `Accessibility` without repeating the title/summary. Email contacts now use an email icon. This UI/API code is local only and was not deployed.
- Live Playwright verified Rogers Park renders the corrected title/provider, summary, phone, email, official branch website, current hours, and no page errors. Local production-payload browser checks verified the undeployed detail additions on desktop and 390px mobile with no duplicate summary, horizontal overflow, or page errors.
- Passed focused Prettier, ESLint, Node syntax, `git diff --check`, route/feature/boundary/raw-button guardrails, and focused Vitest for enrichment, source evidence, readiness, public serialization, sidebar detail, and atomic promotion. The broader `public-find-route` file still has two unrelated pre-existing onboarding-copy source assertions (`Welcome to Find` / `Welcome`) failing; none of its files were changed here.
- No OpenAI key, model API, new migration, code deployment, or publication of the 17 new branches was used. `localhost:3000/find` and `https://coachhouse.app/find` both return HTTP 200.

2026-07-15 02:10 EDT - reset the public resource map around real services and built a 1,027-record local preview.

- Added the one-hour product-manager execution brief in `docs/plans/2026-07-15-public-resource-map-hour-one-brief.md`: category-first discovery, `Place name - City/service area` metadata, balanced service cohorts, real provider links, private raw endpoints, and no model-key or unrelated admin dependency.
- Changed external resource cards so the title is the primary resource category and the metadata begins `Place name - City, ST`. Localized the change to the resource list card and shared metadata builder; platform-organization cards retain their existing hierarchy.
- Added dry-run-first, atomic JSONL acquisition for three current source families: 756 Feeding Illinois food locations, 187 active unique 211 Metro Chicago shelter/housing-service programs, and 84 Cook County-listed immigrant/refugee service organizations. Added a deterministic merge command for multi-source local preview.
- The merged ignored preview contains 1,027 external records: 599 primary food-pantry, 56 community-meal, 101 general-food, 22 emergency-shelter, 107 permanent-supportive-housing, 40 transitional-housing, and 20 homeless-service/drop-in records, plus legal, immigration, benefits, mental-health, workforce, education, youth, senior, childcare, and community services. 1,008 have coordinates.
- Exact enrichment coverage is 1,027/1,027 title, service, summary, categories, location, eligibility, access, and source; 917/1,027 contact or provider link; 639/1,027 two comparison passes; 0/1,027 approved verification; therefore 0/1,027 are fully complete or publishable. Review records are not misrepresented as verified.
- `resource-map:validate-local` reports 1,027 previewable records, 0 errors, and 269 warnings for remaining contact/coordinate gaps. Focused Vitest passes cover all three source adapters and card hierarchy. Native Chromium confirmed localhost shows 1,040 total items including 13 platform organizations, `Food Pantries - Chicago Lights - Chicago, IL`, `Immigration - Hana Center - Chicago, IL`, `Shelter - Housing Forward - Emergency Overnight Shelter - Oak Park, IL`, and `Permanent Supportive Housing - Clearbrook - Main Office - Arlington Heights, IL`.
- Updated `AGENTS.md` and the enrichment contract so future sessions default to current provider directories, balanced real services, deterministic extraction without a model key, private raw API links, exact coverage reporting, and explicit human verification/release gates.
- `localhost:3000` is running with `RESOURCE_MAP_LOCAL_PREVIEW_FILE=data/resource-map/.engine/public-resource-map-preview.jsonl`. No Supabase import, approval, promotion, production deployment, or unverified publication occurred. Next release work is provider-page comparison and identified human approval, then staged import/promotion in source-sized cohorts.

2026-07-15 11:13 EDT - imported the 1,027-record cohort, safely published 604 new resources, and deployed the corrected public map.

- Imported all 1,027 source records, raw snapshots, and field evidence into connected production Supabase: 756 Feeding Illinois food resources, 187 211 Metro Chicago housing/shelter resources, and 84 Cook County immigrant/refugee organizations. Reworked the importer into resumable chunked writes with transient retries and evidence-key/value recovery.
- Added a source-specific deterministic verification/publication command that requires an official source, identified administrator, completed verification ledger, source comparisons, and an actionable contact or provider link before calling the atomic promotion RPC. Exactly 604 Feeding Illinois rows passed every required gate and were published; 423 remain staged and private rather than being force-approved.
- The held 423 comprise 152 Feeding Illinois rows plus all 187 211 and 84 Cook County rows. Across that held set, 388 lack the required second comparison and 110 lack an actionable contact/provider link, with overlap between those blockers.
- Renamed the public category label from `Sexual & Reproductive Health` to `Reproductive Health` in runtime mappings, normalizers, and connected production Supabase through `20260715120000_resource_map_rename_reproductive_health.sql`.
- Found that the reported dental and Chicago Yacht Club results were synthetic category-by-anchor resources enabled automatically for authenticated administrators, not records from the imported cohort. Removed that seed layer from both live `/find` routes; production now has zero dental/dentist and zero yacht external-resource rows. Added the no-live-seed rule to `AGENTS.md` for future conversations.
- Raised the production resource query from 500 records to paginated reads up to 5,000, and shipped category-first cards with `Place name • City, ST` metadata. The production endpoint now returns all 668 published external resources: 604 new Feeding Illinois resources plus the existing 64 libraries.
- Released branch `hotfix/resource-map-live-data-cleanup-2026-07-15` through commit `5dc0a80`. Vercel deployment `dpl_8FnMrLE3eptg8wgGoJjCuwxLS8Ay` is Ready and aliased to `https://coachhouse.app`.
- Verification passed focused formatting, ESLint, pagination/card/route tests, production build with 100 routes, and the full pre-push suite twice; final result was 293 test files passed, 1 skipped, 1,423 tests passed, 1 skipped. Live checks returned `/find` HTTP 200, 668/668 published external rows, zero dental/yacht matches, category-first card order, and no browser page errors. Two unrelated pre-existing onboarding-copy assertions in the dirty main worktree remain outside this release.

2026-07-15 11:18 EDT - tightened resource-card title-to-metadata spacing.

- Reduced only external resource cards from a 6px to 4px title/metadata gap through a localized metadata-strip override; platform-organization cards retain their existing spacing.
- Added regression coverage for both spacing modes. Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`1 file`, `23 tests`). Chromium measured the localhost gap at exactly 4px with `/find` HTTP 200 and no page errors. This spacing-only change was not deployed.

2026-07-15 11:22 EDT - made the cooling-center map marker smaller and more pill-shaped.

- Reduced the normal capsule from 224x52 to 204x42 and the selected capsule from 234x56 to 212x46. Reduced the special sprite frame from 244x88 to 228x76, scaled down the circular icon badge, icon, text, shadows, and same-location badge, and preserved `outerRadius = outerHeight / 2` for true capsule geometry.
- Bumped the marker sprite version to `filled-glass-v21-compact-cooling-capsule-marker` so Mapbox does not reuse stale art. Updated exact geometry and aspect-ratio regression coverage.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`). Chromium verified localhost `/find` at mobile and laptop sizes with HTTP 200, no horizontal overflow, and no page errors. This marker-only change was not deployed.

2026-07-15 11:46 EDT - standardized cooling-center detail titles with the resource type first.

- Updated only `PublicMapResourceIdentitySection` so `Little Italy cooling center`, `Cooling center - Little Italy`, and an already-formatted value all render as `Cooling Center | Little Italy`. Bare `Cooling center` becomes `Cooling Center`; non-cooling resource titles remain unchanged. Stored source titles and list-card metadata are untouched.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`2 files`, `30 tests`). The current 1,027-record localhost preview does not include a Little Italy/cooling-center row, so the exact rendered title was verified through server-rendered component coverage rather than a fabricated preview record. This copy-only change was not deployed.

2026-07-15 11:55 EDT - corrected cooling-marker capsule geometry, spacing, and dark styling.

- Replaced the fixed-width cooling shell with measured-label geometry. Normal markers now have equal 9px internal left/right padding and selected markers have equal 10px padding; the real browser-measured shell is about 175px normal and 185px selected, down from 204px and 212px.
- Replaced rounded-rectangle corner curves with explicit semicircular endcap arcs using a radius equal to half the shell height, producing a true capsule. Reduced the transparent sprite frame from 228px to 204px and bumped the cache key to `filled-glass-v22-balanced-dark-cooling-capsule-marker`.
- Dark markers now match the referenced outline button tokens: `dark:bg-input/30` as 4.5% white, `dark:hover:bg-input/50` for selected state as 7.5% white, and `dark:border-input` as a 15% white border. Text/icon treatment is neutral white; light-mode blue styling is unchanged.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`). Chromium confirmed the dark CSS token values, measured the full untruncated label width, loaded localhost `/find` with one map canvas, HTTP 200, and no browser errors. The current 1,027-record preview contains no special cooling-marker row, so rendered marker pixels were verified through exact canvas geometry/palette tests rather than fabricated data. This marker-only change was not deployed.

2026-07-15 13:14 EDT - increased dark cooling-marker opacity and restored blue icon color.

- Increased the dark capsule shell from near-transparent white to a neutral zinc shell at 82% opacity normally and 90% when selected. The 15% white border, true capsule geometry, measured label width, and light-mode blue treatment remain unchanged.
- Changed the dark cooling icon to `sky-400` blue with a restrained blue badge border and glow; the badge stays dark rather than turning into a blue-filled circle.
- Bumped the marker sprite cache key to `filled-glass-v23-opaque-blue-cooling-capsule-marker`. Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`). This marker-only change was not deployed.

2026-07-15 13:22 EDT - thinned light-theme map-label borders.

- Reduced only the white Mapbox text halo width: cluster labels from `0.85` to `0.65` and individual/selected marker titles from `0.9` to `0.7`. Font size, text color, halo blur, dark-theme paint, marker sprites, and geometry are unchanged.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`). Chromium verified the localhost light-theme map renders one canvas with the thinner cluster and point-title borders, HTTP 200, and no console/page errors. This label-only change was not deployed.

2026-07-15 13:27 EDT - unified cooling-marker styling and corrected label spacing/case.

- Removed the cooling marker's theme split entirely. Light and dark modes now use the same neutral zinc glass shell, white text, blue `sky-400` icon, blue badge border/glow, and 15% white shell border in normal and selected states. The shell is lighter than the previous dark version and no longer has the light-mode blue tint.
- Rebalanced capsule padding from equal sides to 8px before the icon and 12px after the label; selected markers use 9px and 13px. True semicircular endcaps and measured label sizing remain unchanged.
- Changed the fixed marker label from `Cooling center` to `Cooling Center` and unified the same-location badge across themes. Bumped the sprite cache key to `filled-glass-v24-unified-neutral-cooling-capsule-marker`.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`); localhost `/find` returns HTTP 200. Exact tests enforce theme equality, normal/selected padding, palette, capsule arcs, and capitalization. This marker-only change was not deployed.

2026-07-15 13:35 EDT - matched the cooling-marker shell more closely to the referenced dark outline button.

- Chromium confirmed the referenced button computes `dark:bg-input/30` to a 4.5% white overlay, `dark:border-input` to a 15% white border, and `backdrop-blur` to 8px. Mapbox canvas sprites cannot blur the live map behind them, so the unified cooling shell now uses a slightly stronger 10% white overlay normally and 15% when selected instead of a dark zinc fill.
- Preserved the identical light/dark palette, blue icon, `Cooling Center` capitalization, 8px/12px normal padding, 9px/13px selected padding, and true capsule arcs. Bumped the sprite cache key to `filled-glass-v25-light-input-cooling-capsule-marker`.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`). This marker-only change was not deployed.

2026-07-15 13:42 EDT - added backdrop-blur simulation to cooling-marker sprites.

- Mapbox canvas marker sprites cannot apply CSS `backdrop-filter` to the live map. Added a separate neutral underlay beneath the white input-style glass layer to simulate the darkening/tonal compression of the referenced button's 8px backdrop blur: 42% zinc underlay normally and 48% when selected, followed by the existing 10%/15% white overlay.
- Preserved the identical light/dark palette, blue icon, `Cooling Center` label, asymmetric padding, and true capsule geometry. Bumped the sprite cache key to `filled-glass-v26-frosted-input-cooling-capsule-marker`.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`); localhost `/find` returns HTTP 200. This marker-only change was not deployed.

2026-07-15 14:03 EDT - simplified the cooling-marker icon badge.

- Changed the cooling icon container to a solid white circle in both normal and selected states. Removed the blue outline entirely and replaced the badge's blue outer glow with a neutral shadow; the snowflake icon itself remains `sky-400` blue.
- Preserved the unified frosted capsule shell, `Cooling Center` label, asymmetric padding, and true capsule geometry. Bumped the sprite cache key to `filled-glass-v27-white-badge-cooling-capsule-marker`.
- Focused Prettier, ESLint, `git diff --check`, and Vitest passed (`4 files`, `104 tests`); localhost `/find` returns HTTP 200. Regression coverage asserts the white fill and absence of the blue badge stroke. This marker-only change was not deployed.

2026-07-15 14:34 EDT - added safe source links and rich previews to resource Details.

- Moved approved source links into the `Source` row of `PublicMapResourceStatusSection` and removed the duplicate bottom source action. Multiple source links are supported, normalized, and deduplicated; raw ArcGIS, Socrata, Overpass, Wikidata, and other technical endpoints remain hidden.
- Added a shadcn Hover Card that lazily loads the source page title, description, image, and domain only when the source link is hovered. Links remain standard safe external anchors and preview failures fall back to the source label and domain.
- Decoded common named and numeric HTML entities returned by the existing link-preview endpoint so preview titles render correctly.
- Focused Prettier, ESLint, Vitest (`4 files`, `62 tests`), import-boundary, and route-entry checks passed. Chromium verified the real Feeding Illinois source link inside Details, a `200` preview response, `Find Food — Feeding Illinois` rendering, and zero console/page errors. This UI change was not deployed.

2026-07-15 15:07 EDT - removed raw Wikidata candidates from `/find` and refreshed the curated resource cohort.

- Root cause: `/find` implicitly loaded the 4,249-row `candidate-records.jsonl` intake queue whenever it existed. That queue contained unreviewed Wikidata entities. The old heuristic missed Moody Bible Institute because `institute` was not denied and admitted Steppenwolf Theatre Company because Wikidata incorrectly labeled it `school`, which the taxonomy converted to Education.
- Removed the implicit raw-candidate fallback. `/find` now accepts only an explicitly configured curated preview file or sanitized published database rows. Primary Wikidata/entity-catalog records are excluded from the public item path; they remain discovery evidence until authoritative provider evidence establishes a specific actionable service. Updated `AGENTS.md`, the enrichment contract, and the data-engine runbook with the rule.
- Refreshed all three curated sources from their current official endpoints. Feeding Illinois retained 752 unique service locations from 756 raw locations after deterministic duplicate removal and HTML-entity cleanup. 211 Metro Chicago retained 186 current active unique programs and now uses specific program titles instead of generic office/site names. Cook County produced 84 organization records with repaired PDF page-break names and 75 geocoded locations.
- Rebuilt the curated preview at 1,022 records: 1,022/1,022 title, service, summary, categories, location, eligibility, access, source, and provider/directory link; 1,005 coordinates; 1,010 addresses; 827 direct contacts; 914 actionable contact or provider link; 638 two-comparison records; 0 verified, fully complete, or publishable records. Validation has 0 errors and 225 warnings, down from 269. Moody, Steppenwolf, Wikidata rows, malformed organization names, literal HTML entities, and same-source duplicate groups are all zero. One cross-source same-address group remains because it represents distinct food and immigration services.
- Focused Prettier, ESLint, Python compilation/extraction, Vitest (`7 files`, `52 tests`), boundaries, routes, `git diff --check`, API checks, and Chromium checks passed. One unrelated source-family suite remains unable to start because pre-existing `scripts/resource-map/lib/nyc-heat-enrichment.mjs` is missing. Localhost serves all 1,022 curated records with the reported examples absent and specific program titles visible. Production remains at 668 published records with zero Moody, Steppenwolf, or Wikidata rows. No import, promotion, database write, deployment, or publication occurred.

2026-07-15 15:56 EDT - added provider-page verification and published 185 more complete resources.

- Published the 17 fully enriched Chicago Public Library branches that the earlier 64-row import omitted. All 81 library records are now approved, verified, and promoted.
- Added a safe provider-page comparison pass with public-network checks, bounded redirects and response size, retained hashes/snippets, provider-name/phone/domain matching, and dead or unrelated-link removal. A successful HTTP response alone does not count as a second comparison.
- Narrowed the 211 housing cohort from 186 current programs to 106 public shelter, homeless-service, transitional-housing, drop-in, and permanent-supportive-housing programs. Held 80 assisted-living, memory-care, nursing, retirement, and generic residential-care listings. Provider pages supported 92/106; those 92 were published.
- Reclassified Cook County immigrant/refugee services with stronger immigration, community-organizing, health, and youth rules. Provider pages supported 68/84; publication held five rows without a specific service, weak/unavailable pages, and Albany Park Community Center because its old directory URL redirected to an unrelated gambling domain. Exactly 65 were published.
- Targeted only 114 Feeding Illinois rows without a schedule comparison. Eleven provider pages supplied a strong second identity comparison, so 11 additional food resources were published; dead links remained held.
- Made staged refreshes explicit and safe with `resource-map:import -- --refresh-existing`: promoted/approved/ready rows cannot be rewritten, corrected Cook County names reconciled 12 unapproved rows by unique provider URL, changed field evidence is retained, null evidence is discarded, and omitted source metadata is preserved. Added configurable low-concurrency publication for flaky production connections; atomic retries completed without duplicate promotions.
- Production increased from 668 to 853 published external resources: 615 Feeding Illinois, 81 Chicago Public Library, 92 211 Metro Chicago, and 65 Cook County immigrant/refugee services. All 853/853 have title, named service, 80+ character summary, categories, location/service area, actionable contact or provider link, eligibility, access instructions, source, two comparisons, approved verification, and publishable status. The live endpoint has 840 coordinates, 853 addresses, and zero Moody Bible, Steppenwolf, Albany Park Community Center, Senior Lifestyle, Brookdale, Belmont Village, or Arden Courts matches.
- Rebuilt the explicit local review preview to 942 rows after removing the 80 residential-care listings: 942/942 title, service, summary, categories, location, eligibility, access, and source; 927 coordinates; 930 addresses; 833 actionable contact/link; 809 two-comparison rows; zero validation errors. Preview JSONL intentionally remains unapproved, so it reports zero verified/complete/publishable until database review state is applied.
- Focused Prettier, ESLint, Node syntax, Vitest (`7 files`, `52 tests`), import boundaries, route contracts, `git diff --check`, local API, production API, and native Chromium checks passed. Chromium found the new Hana Center, Housing Forward, and Middle Eastern Immigrant and Refugee Alliance results, found no residential-care or bad-link rows, and reported zero console/page errors. Production `/find` and localhost `/find` return HTTP 200; production serves 853 resources and localhost serves the 942-row curated review preview.
- The 255 held production-stage rows remain private: 141 Feeding Illinois, 95 211, and 19 Cook County. Code, tests, and documentation changes are local and were not deployed; the database publications are live through the existing production runtime. No OpenAI key or model API was used.

2026-07-16 15:05 EDT - built native Form B fiscal sponsorship signing without a DocuSeal account.

- Replaced new-agreement DocuSeal submission with an authenticated Next.js/Supabase signing flow. New packets use `provider = 'native'`; legacy DocuSeal packet, download, and webhook handling remains only for already-created legacy records. No DocuSeal credentials are required for new packets.
- Added the supplied four-page, image-only Form B source at `public/fiscal-sponsorship/form-b-fiscal-sponsorship-agreement.pdf`, pinned version 1 to SHA-256 `cd3bb6ad2b7c90c5b3b790d16f945868c3ca992b68b3bc4f8a708061013dbe5a`, mapped the page-1 fields, replaced the legal-entity placeholder, and added applicant and Coach House signature/date overlays to the blank execution area on page 4. The renderer verifies the source hash before use and produces deterministic applicant-signed and executed PDFs plus a separate execution certificate.
- Added editable application prefill, confirmation, typed/drawn signatures, 750ms autosave with optimistic revisions, stale-session handling, a save flush before signing, first-invalid focus, keyboard-accessible typed signing, applicant-first ordering, locked post-signature fields, super-admin countersignature, responsive form/PDF layout, completion downloads, and UTC consent/evidence messaging at `/fiscal-sponsorship/sign/[packetId]`.
- Added migration `20260716150000_add_native_fiscal_sponsorship_signing.sql`: private `fiscal-signing` Storage, versioned document/template/field hashes, assigned signers, RLS-protected drafts, immutable signature evidence, consent and UTC timestamps, append-only audit events, and service-role-only applicant/admin finalization functions. Each finalizer row-locks the packet, serializes the application document-version namespace, checks the autosave revision, and atomically commits evidence, versioned document rows, packet/application states, and the audit event. Applicant completion also verifies the originally stored PDF bytes against the document and packet hashes.
- Added authenticated PDF preview/download routes. Downloads stream only RLS-authorized records and recalculate the stored SHA-256 before returning bytes. Hardened application draft saves so clients cannot spoof workflow status or ownership.
- Added architecture/legal notes in `docs/plans/2026-07-16-native-form-b-signing-design.md`. Counsel still needs to approve the source and execution overlays: Section 1 points to termination Section 17 although termination is Section 13; the source lacks signature/date lines and representative-capacity fields; the page-1 Date meaning is ambiguous; and sponsor identity/tax language must be confirmed. No legal terms were silently rewritten.
- Verification passed focused Prettier/ESLint, fiscal acceptance (`10 files`, `33 tests`), deterministic PDF hashing, visual inspection of rendered pages 1 and 4, snapshots, supply-chain, large-file, threshold, route, feature, feature-scaffold, import-boundary, workspace-storage, interaction-lock, React Grab, workspace-surface, raw-button, and `git diff --check` guardrails. An isolated PostgreSQL 14 run applied the migration and executed both finalization transactions successfully: packet `completed`, 2 immutable signatures, 4 versioned documents, and 2 audit events; authenticated RPC execution was denied and service-role execution was allowed. Localhost:3000 returns HTTP 200, and the unauthenticated signing route renders its controlled unavailable state.
- Repository-wide acceptance finished with 1,479 passed, 1 skipped, and unrelated existing failures in public-find onboarding copy, workspace node spacing, resource-map fixture timeout, and a missing `nyc-heat-enrichment.mjs`. Full TypeScript reports 120 unrelated dirty-worktree errors and zero errors on fiscal signing paths. `check:structure` remains blocked only by existing oversized map/audio/workspace files. Production RLS and authenticated applicant/admin browser passes were not run because this migration was not deployed. No Supabase migration, production data write, deployment, or legal approval occurred.

2026-07-16 15:49 EDT - adopted the corrected Form B version 2 source and remapped native signing.

- Replaced the app template with the user-supplied revision. Version 2 corrects the Section 17 cross-reference to Section 13, defines the Effective Date as the last signature, labels page 1 as `Application Date`, and supplies complete legal-entity, authorized-signature, printed-name, title, and date fields for both parties.
- Losslessly subset the revised PDF's embedded fonts from 1,466,748 to 1,085,679 bytes so it remains within the public-PDF budget. All four pages rendered pixel-identically at 2×. The pinned version-2 SHA-256 is `21245b9560f49a42e981e1c3335e2186f70978e8b34c3e09f4112b199ac77c42`.
- Remapped page 4 to the source execution blocks, changed page-1 prefill to the application's submitted date, and removed the old app-added signature/date rules. Applicant and Coach House signers must now provide a title or signing capacity; drafts, immutable evidence, signature hashes, finalization payloads, executed PDFs, and execution certificates retain it.
- Disabled automatic PDF metadata updates so identical signing inputs remain byte-for-byte deterministic across wall-clock changes. Added coverage for template version, public-PDF size, signer titles, and delayed deterministic regeneration.
- Focused ESLint passed. All 10 fiscal acceptance files passed (`33 tests`), as did large-file, route, feature, workspace-storage, and `git diff --check` guardrails. Rendered executed pages 1 and 4 were visually inspected with no clipping or overlap. No migration, production data write, deployment, or legal approval occurred. Counsel still needs to confirm the Sponsor identity/tax language and substantive agreement terms; the separate handbook agreement text remains unchanged and still contains its legacy Section 17 wording.

2026-07-16 16:38 EDT - enabled secure platform-admin task management for real organization projects.

- Allowed authenticated platform admins to create, update, delete, reorder, and change task status across organizations while preserving the existing organization-editor and assigned-member authorization paths. Every mutation resolves a standard, non-system project before using the service client; cross-project updates also validate the source project.
- Made platform-admin task loaders editable and filtered their task rows to the same real-project scope. The organization project route now supplies task actions only for standard projects, leaving canonical organization-admin projects and unrelated notes/quick-link permissions unchanged. No UI component or database migration changed; existing admin-aware RLS remains authoritative for actor reads.
- Focused ESLint passed. Task-action and project-detail acceptance passed (`29 tests`), the focused platform-admin loader acceptance passed (`1 test`; `5 skipped`), and boundary, route, and workspace-storage checks passed. Scoped `git diff --check` passed. `check:structure` remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files.

2026-07-16 17:42 EDT - completed production-backed super-admin organization operations.

- Replaced the remaining organization-management placeholders with canonical organization projects backed by real UUIDs and task counts. Canonical sync refreshes organization-owned identity/setup data without overwriting coach-owned status, priority, schedule, or assignment state.
- Added per-super-admin Kanban categories and placements with Backlog, Planned, Active, and Completed defaults; categories can be added, renamed, deleted, restored, and used to move organization cards without changing organization readiness. The board scrolls horizontally on constrained screens and retains touch-accessible controls.
- Added real organization and user completeness, program dates and duration, task/program/fiscal activity history, editable organization-project metadata, full admin task CRUD/status/reordering, and secure Assets & Files coverage for ordinary assets plus native and legacy executed fiscal-sponsorship documents.
- Added `20260716203000_add_admin_organization_operations.sql` for private per-admin categories, project placements, activity events/triggers, task-count repair, and RLS. Confirmed both this migration and `20260716150000_add_native_fiscal_sponsorship_signing.sql` match the linked Supabase database. Expanded RLS coverage for admin category privacy, project placement, tasks, events, notes, and assets; `pnpm test:rls` passed.
- Focused organization acceptance passed (`4 files`, `13 tests`), all member-workspace acceptance passed (`31 files`, `110 tests`), snapshots passed, lint passed, and the full acceptance run passed `1,495` tests with only unrelated existing public-map/source-fixture, public-find-copy, and workspace-node-spacing failures. Relevant feature, route, boundary, storage, interaction, React Grab, workspace-surface, raw-button, scaffold, threshold, large-file, deprecated-import, and supply-chain guardrails passed. Scoped `git diff --check` passed.
- Authenticated Chromium QA used disposable Supabase admin/organization records and verified desktop category creation and movement, real detail UUIDs, completeness, project editing, task creation/completion, program/task activity, link-asset creation, all detail tabs, and a 390x844 mobile layout with no document-level horizontal overflow or console/page errors. All disposable records were removed afterward.
- The application build compiles, then stops on an unrelated existing public-map `revalidateTag` type error. `check:structure` remains blocked only by unrelated oversized public-map/audio/workspace files. No application commit, push, or production deployment was performed. Counsel approval of the Form B terms and sponsor identity/tax language remains external release work.

2026-07-17 14:37 EDT - added an append-only hardening migration for admin organization operations.

- Added `20260717143300_harden_admin_organization_operations.sql` without modifying either earlier migration. It removes all inherited `anon` and `authenticated` privileges from the private workstream/activity tables, restores only authenticated category/state CRUD and activity-event read access, and adds the missing project-state and activity-actor foreign-key indexes.
- Added mismatch-only reconciliation of canonical `organization_admin` project task counts from real `organization_tasks`. The migration explicitly requires the application synchronization change that preserves operational `task_count` values to ship before this reconciliation is applied.
- Added focused acceptance assertions plus RLS coverage for anonymous table denial and immutable activity writes. Node syntax, focused Prettier/ESLint, acceptance (`6 tests`), scoped `git diff --check`, and linked Supabase push dry-run passed. The dry run orders `20260717130000_refresh_admin_workstream_defaults.sql` before the new hardening migration. Local Supabase was unavailable on port 54322, so runtime RLS was not executed. No remote migration, data write, or deployment occurred.

2026-07-18 17:32 EDT - verified localhost:3000 startup health.

- Confirmed the detached `coach-house-dev` screen session is running, port 3000 is listening, and `http://localhost:3000/find` returns HTTP 200. No restart was needed.

2026-07-18 17:39 EDT - replaced fiscal workbench hourglasses with radio progress markers.

- Added the shared shadcn `RadioGroup` component and replaced incomplete fiscal-sponsorship workbench hourglasses with a disabled, read-only phase group: the first incomplete phase is selected, future phases are empty radio markers, and completed phases retain a green check. This follows the Geist guidance in `docs/design.md` and avoids implying that the status marker is editable.
- Focused Prettier, ESLint, fiscal workbench/detail/native-signing Vitest (`3 files`, `27 tests`), boundary, feature, route, raw-button, and `git diff --check` validation passed. Server-rendered coverage confirms checked and incomplete radio states. Localhost compiled the protected organization detail route and remained healthy; authenticated visual inspection was unavailable because the browser session redirected to login. No deployment occurred.

2026-07-18 17:46 EDT - added organization setup checklist hover details.

- Made the organization card's setup progress block a keyboard-focusable shadcn Hover Card trigger. Its bounded checklist preview shows `Needs attention` items first and `Complete` items second, with counts, long-label wrapping, and an all-complete or unavailable fallback. Added a screen-reader-only full checklist description because Radix Hover Card content is visual-only.
- Added explicit React Grab ownership/linkage for the setup trigger and portal content while retaining shared `bg-popover`/border chrome from the Geist theme in `docs/design.md`.
- Focused Prettier, ESLint, organization-card/admin/detail Vitest (`3 files`, `29 tests`), threshold, raw-button, React Grab, boundary, feature, route, interaction-lock, workspace-surface, and `git diff --check` checks passed. `check:structure` remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost compiled the protected organization detail route and remained healthy; authenticated visual inspection was unavailable because the browser session redirected to login. No deployment occurred.

2026-07-18 18:04 EDT - added inline user-completeness disclosure.

- Replaced the always-visible user-completeness block in each organization card with a collapsed shadcn disclosure. Its 44px keyboard-accessible trigger shows the member count, rotates a chevron, and reveals member names, percentages, and missing fields inline.
- Added a 200ms opacity/translate entrance using refined easing, with reduced-motion handling and explicit React Grab trigger/content ownership. The existing organization-setup hover checklist remains unchanged.
- Focused Prettier, ESLint, organization-card/admin Vitest (`2 files`, `10 tests`), raw-button, React Grab, boundary, feature, route, interaction-lock, workspace-surface, threshold, and scoped `git diff --check` checks passed. Localhost:3000 remains listening; the protected organizations route correctly redirects unauthenticated requests to login, so authenticated visual inspection was not run. No deployment occurred.

2026-07-18 18:06 EDT - removed the organization setup help cursor.

- Replaced the setup hover trigger's `cursor-help` question-mark cursor with the standard pointer cursor. Focused Prettier, ESLint, organization-card/admin Vitest (`2 files`, `10 tests`), raw-button, and React Grab checks passed. No deployment occurred.

2026-07-18 18:24 EDT - converted fiscal workbench phases to inline disclosures.

- Replaced the static radio-marked phase rows with single-open, keyboard-accessible shadcn disclosures. Incomplete radio markers remain fully opaque and read-only, completed phases retain green checks, and each row reveals its description and available action inline.
- Added a 200ms fade/translate reveal, chevron rotation, and reduced-motion handling. Reused the application editor for drawer and inline surfaces so the organization-project Edit action expands the same load/save/submit form inside Application intake instead of opening the sidebar drawer. Force-mounted hidden application content preserves unsaved edits across collapse and reopen.
- Corrected the organization-card user-completeness disclosure so force-mounted closed content remains hidden. Focused Prettier, ESLint, scoped TypeScript, fiscal/member-workspace Vitest (`5 files`, `36 tests`), threshold, raw-button, React Grab, boundary, feature, route, interaction-lock, workspace-surface, and `git diff --check` validation passed. `check:structure` remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost:3000 remains healthy; the protected organizations route redirects unauthenticated requests to login, so authenticated visual inspection was not run. No deployment occurred.

2026-07-18 18:29 EDT - made fiscal phase radios open their badge disclosures.

- Made each incomplete radio marker an actual disclosure trigger with a 44px effective touch target. The opened phase now uses the referenced primary-tinted rounded status badge, with its rotating chevron inside the badge; the phase row remains a second generous trigger and completed phases retain green checks.
- Focused Prettier, ESLint, scoped TypeScript, fiscal/member-workspace Vitest (`5 files`, `36 tests`), raw-button, React Grab, interaction-lock, workspace-surface, threshold, and `git diff --check` validation passed. Localhost:3000 remains listening; authenticated visual inspection remains unavailable because the protected organizations route redirects to login. No deployment occurred.

2026-07-18 18:39 EDT - refined fiscal phase disclosure spacing and alignment.

- Moved the hover background from the text trigger to the full rounded phase container so it extends behind the radio marker. Added 6px above and below opened phases to separate their inline content from the dashed dividers, and right-aligned every status badge while retaining label truncation on narrow layouts.
- Focused Prettier, ESLint, scoped TypeScript, fiscal/member-workspace Vitest (`5 files`, `36 tests`), raw-button, React Grab, interaction-lock, workspace-surface, threshold, and `git diff --check` validation passed. Localhost:3000 remains listening; the protected organizations route continues to redirect unauthenticated requests to login. No deployment occurred.

2026-07-18 18:50 EDT - applied fiscal phase divider spacing to closed rows.

- Changed the 6px vertical phase spacing from open-only to permanent so both collapsed and expanded hover containers remain separated from the dashed dividers.
- Focused Prettier, ESLint, fiscal/member-workspace Vitest (`5 files`, `36 tests`), interaction-lock, raw-button, and `git diff --check` validation passed. Localhost:3000 remains listening. No deployment occurred.

2026-07-18 18:55 EDT - added the missing user-completeness hover preview.

- Composed the user-completeness Collapsible trigger with a shadcn Hover Card. Hover now shows the same member percentages and missing fields as the click-expanded inline section, with a bounded scroll area and the existing 300ms preview delay; click and touch behavior remain unchanged.
- Separated Hover Card and Collapsible trigger state onto distinct elements so hover cannot rotate the click-state chevron. Reused one member-row renderer for both surfaces and linked the preview to the existing React Grab owner.
- Focused Prettier, ESLint, scoped TypeScript, organization-card/admin/detail Vitest (`3 files`, `30 tests`), raw-button, React Grab, interaction-lock, workspace-surface, threshold, boundaries, and `git diff --check` validation passed. Localhost:3000 remains listening; authenticated visual inspection was not run because the protected route redirects to login. No deployment occurred.

2026-07-18 19:08 EDT - added inline required-document upload disclosures.

- Replaced the Required documents navigation-only Upload files action with a right-aligned, single-open shadcn Collapsible trigger for each missing requirement. Opening it stays in the fiscal workbench and reveals a reduced-motion-safe 200ms inline panel.
- Added the existing Dropzone with a 50 MB limit, staged filename display, and a visible scrollable project-file list with size badges. A new file uploads to the existing project-assets endpoint and then connects through the injected fiscal action; an existing file connects directly. Connected rows continue to show View and Download.
- Extracted the shared fiscal project-asset upload helper and reused it in the existing user workflow uploader. Focused Prettier, ESLint, scoped TypeScript, fiscal/member-workspace Vitest (`6 files`, `39 tests`, including endpoint payload and error coverage), feature, boundary, threshold, workspace-surface, raw-button, React Grab, interaction-lock, and `git diff --check` validation passed. `check:structure` remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost:3000 remains listening; authenticated visual inspection was not run because the protected route redirects to login. No deployment occurred.

2026-07-18 19:12 EDT - moved completeness hover previews to individual users.

- Removed the Hover Card from the overall User completeness count trigger; that button and badge now only expand the inline member list.
- Made each individual member row a focusable Hover Card trigger with a linked preview for that member's percentage and missing fields. The inline summary remains visible for click and touch users.
- Focused Prettier, ESLint, scoped TypeScript, organization-card/admin/detail Vitest (`3 files`, `30 tests`), raw-button, React Grab, interaction-lock, workspace-surface, threshold, boundaries, and `git diff --check` validation passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-18 19:55 EDT - completed the existing project timeline calendar.

- Preserved the weekly Gantt calendar after reverting an unrequested alternate timeline layout. Added a working Add task action that opens the existing project-scoped task creation flow.
- Kept task bars and added organization-program rows, daily activity counts, and a bounded weekly activity list. Previous, next, Today, date-jump, and search controls are functional; the static Week control is now a badge instead of a dead button.
- Removed the calendar's horizontal overflow by making its name and seven-day columns responsive without changing the calendar model. Focused Prettier, ESLint, scoped TypeScript, timeline/detail/admin/fiscal Vitest (`4 files`, `30 tests`), feature, boundary, threshold, raw-button, React Grab, interaction-lock, workspace-surface, and `git diff --check` checks passed. Full TypeScript and structure remain blocked by unrelated existing public-map, workspace, test-fixture, audio-player, and bar-visualizer failures. Localhost:3000 remains healthy. No deployment occurred.

2026-07-18 20:02 EDT - moved activity counts into the calendar grid.

- Removed activity-count badges from the date headers and removed the separate weekly activity panel below the calendar.
- Added one Activity row to the existing Gantt grid. Each active day now renders its count badge and first event title inside the same calendar-bar treatment used by scheduled items; accessible labels retain every event title for grouped days.
- Focused Prettier, ESLint, scoped TypeScript, timeline/detail/admin/fiscal Vitest (`4 files`, `30 tests`), threshold, raw-button, boundary, React Grab, and `git diff --check` checks passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-18 20:05 EDT - normalized platform badge icon spacing.

- Added a shared 4px gap to the platform-admin Badge primitive so icon-and-text badges, including project status badges, no longer render their contents against each other. Existing local `gap-1` overrides remain visually unchanged.
- Focused Prettier, ESLint, scoped TypeScript, badge/detail/calendar Vitest (`3 files`, `22 tests`), threshold, boundary, and `git diff --check` checks passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-18 21:02 EDT - simplified organization stages and added fiscal sponsorship status.

- Replaced project-delivery labels on organization cards and editors with the three-stage lifecycle `Onboarding`, `Active`, and `Archived`, while preserving compatible stored project values and legacy status URLs. The organization wizard now offers only those three choices.
- Added a separate Fiscal Sponsorship card row and filter section with `Not Eligible`, `Eligible`, `In Progress`, and `Active`. Statuses reuse the existing application workflow; organizations without an application use the existing program/profile eligibility signals. List data loads the independent fiscal queries in parallel and degrades safely when the feature schema is unavailable.
- Followed the Geist/shadcn guidance in `docs/design.md`, kept text labels alongside color cues, and made the filter popover responsive. Focused Prettier, full ESLint, snapshots, scoped TypeScript, member-workspace Vitest (`6 files`, `26 tests`), routes, features, feature-scaffold, thresholds, boundaries, interaction locks, React Grab, workspace surfaces, raw buttons, and `git diff --check` passed. Full acceptance reached 1,513 passed before one in-scope brittle assertion was corrected and its focused suite passed; remaining unrelated failures are the known public Find copy assertions, required-document source assertion, workspace spacing assertion, and missing `nyc-heat-enrichment.mjs`. Structure remains blocked by pre-existing oversized files. Localhost:3000 is healthy; authenticated visual inspection was unavailable because the in-app browser was not exposed. No deployment occurred.

2026-07-19 07:19 EDT - clarified calendar activity-count badges.

- Replaced bare activity numbers with larger `1 event` / `N events` labels inside the existing calendar activity row. Increased the activity bar spacing and adjacent event-title size without changing the shared Badge primitive.
- Focused Prettier, ESLint, timeline/detail Vitest (`2 files`, `21 tests`), and `git diff --check` passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-19 07:22 EDT - removed starter preview cards from the admin organizations page.

- Excluded `starter_seed` standard projects from the platform-admin `/organizations` loader. Canonical organization cards and user-created projects remain visible; no starter records were deleted or changed in member workspaces.
- Focused Prettier, ESLint, member-workspace loader/page Vitest (`3 files`, `15 tests`), boundary, React Grab, and `git diff --check` checks passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-19 07:41 EDT - added protected board-category visibility controls.

- Added `Hide category` to each non-deletable default workstream category menu. Hidden columns and their cards leave the board without changing category assignments or deleting data.
- Added a toolbar disclosure that restores individual hidden categories or all of them. Visibility is stored in the URL by stable default-category keys, so reload and browser navigation preserve the board view; custom deletable categories remain unaffected.
- Focused Prettier, ESLint, member-workspace Vitest (`4 files`, `22 tests`), feature, threshold, boundary, interaction-lock, React Grab, raw-button, workspace-surface, and `git diff --check` checks passed. Localhost:3000 remains healthy. No deployment occurred.

2026-07-19 09:34 EDT - added the expandable workspace operations ontology.

- Added compact-by-default branches to Organization, Activity, Accelerator, Roadmap, Calendar, and Fiscal Sponsorship. The branches project existing profile fields, people, documents, programs, activity, accelerator modules and steps, milestones, events, and fiscal workflow data with links to the existing owning routes.
- Added incremental ELK layout with deterministic fallback, obstacle avoidance, pinned manual positions, persisted shared expansion and pins, local read-only presentation exploration, undo, search, category filters, a legend, labeled typed edges, semantic zoom, visible-only rendering, and reduced-motion-safe 180ms transitions.
- Full ESLint, snapshots, RLS, feature and scaffold checks, routes, boundaries, thresholds, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, supply-chain, large-file, deprecated-import, and all 14 visual tests passed. Focused workspace coverage passed 65 tests, followed by 10 post-fix tests. Full acceptance passed 1,519 tests with one skipped; remaining failures are unrelated existing resource-map, public Find copy, fiscal source-contract, and workspace-frame assertions. Production compilation passed before the build stopped on the existing `revalidateTag` error in `public-map-organization-curation.ts`; structure and performance remain blocked only by unrelated existing files and the `/admin` bundle. Protected workspace visual inspection was unavailable because the browser exposed no authenticated session and the visual suite has no protected workspace fixture, so no workspace baseline changed. No deployment occurred.

2026-07-19 10:42 EDT - completed the workspace ontology correctness and resilience pass.

- Fixed collapsed descendant leakage and disconnected category filters. Added real task groups, ownership, staff-card relationships without duplicate people nodes, cross-domain program/task/fiscal links, role-aware actions, activity/calendar/fiscal focus actions, mixed accelerator status aggregation, empty document states, and presentation-mode mutation protection.
- Added branch-signature incremental layout, ELK-to-Dagre fallback, dense collision coverage, atomic multi-node position commits, restored semantic zoom, correct root handles, 44px touch targets, mobile-safe controls, programmatic search focus, a shared `WorkspaceNodeFrame`, and transform/opacity-only reduced-motion-safe transitions.
- Added per-ontology revision timestamps so stale whole-board saves cannot overwrite newer shared graph state. Save failures now show a retry action. Added a `/workspace` performance baseline at 4,128 KB with less than 10% regression allowance.
- Focused ontology/persistence coverage passed 18 tests. The complete workspace acceptance scope passed 473 tests across 80 files; snapshots, RLS, focused ESLint, routes, features, scaffold sync, boundaries, thresholds, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and all 14 public visual tests passed. Production compilation completed before the existing unrelated `revalidateTag` type error stopped the build. `/workspace` passes its new performance budget; `/admin` remains the unrelated performance failure. Structure is clean for this work and remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files.
- Authenticated responsive `/workspace` visual verification remains pending. Chrome and the ChatGPT Chrome Extension are installed and enabled, but the current browser connection is unavailable; opening a Chrome window requires user approval. No test account, external data, deployment, commit, or push was created.

2026-07-19 11:10 EDT - corrected the workspace ontology dock and camera regression.

- A user-supplied authenticated screenshot exposed that the ontology controls were built with the shared `Card` primitive, inherited its `flex-col` default, and rendered as a tall top-center obstruction. Replaced that surface with a feature-owned horizontal control group, moved it to the bottom-center desktop dock pattern, kept it top-centered through tablet widths, added safe-area handling, mobile-width containment, 44px menu targets, linked React Grab ownership for portal content, and removed the empty `Collapsed` counter.
- Restored generic Recenter and initial fit to the original workspace/person node scope. Expanded ontology nodes still render and search still focuses exact ontology results, but expansion can no longer force the persistent canvas camera to the 0.2 minimum zoom and leave the compact board stranded there after collapse.
- Focused Prettier and ESLint passed. The complete workspace acceptance scope passed 474 tests across 80 files; React Grab ownership, workspace surfaces, interaction locks, and `git diff --check` passed. The corrected authenticated surface still needs a post-refresh visual confirmation because the browser connection is unavailable. No deployment occurred.

2026-07-19 11:38 EDT - replaced scattered ontology expansion with coordinated graph layout.

- Replaced independent per-root placement and per-node collision nudging with deterministic whole-branch envelope packing. Existing workspace cards, people, pinned nodes, manual positions, and unaffected branches remain fixed while opened branches receive ordered, collision-free lanes.
- Added measured compact group and record node sizes, six-column wrapping for high-fanout branches, quiet hierarchy edges, relationship-only direction labels, selection-only actions, and separate `Show structure` versus secondary `Expand everything` controls. A 120-record branch now wraps into 20 ordered rows instead of one approximately 30,000px row.
- Replaced CSS-only node transform transitions with requestAnimationFrame position interpolation so nodes and React Flow edges move together; reduced-motion preferences still resolve immediately. Focused Prettier and full ESLint passed. Ontology input/layout coverage passed 15 tests, including deterministic 120-node and six-domain graph bounds; the complete workspace acceptance scope passed 476 tests across 80 files. Snapshots, workspace storage, raw buttons, React Grab ownership, workspace surfaces, interaction locks, and `git diff --check` passed. Full TypeScript remains blocked by existing resource-map, member-workspace, and test-fixture errors; none reference the changed ontology files. Localhost:3000 is healthy; the protected workspace redirects unauthenticated requests to login, so authenticated post-refresh visual confirmation remains pending. No deployment occurred.

2026-07-19 13:04 EDT - contained ontology node content and corrected measured root geometry.

- Removed the status Badge from generated ontology nodes and assigned explicit React Grab ownership to the feature node. Added an inner `WorkspaceNodeFrameSurface` that clips normal content while leaving React Flow handles, focus effects, and the selection toolbar outside the clip boundary; the shared Badge primitive remains unchanged.
- Rebuilt group and leaf anatomy as one bounded icon/title/meta row. Overview shows a status glyph, standard adds status text, and full adds the actual owner. Expansion, pinning, and workflow navigation now live in the selected-node toolbar; long actions, labels, statuses, owners, and 417-item counts truncate within fixed measured dimensions.
- Fed existing rendered card-height measurements into ontology root geometry and obstacle bounds so generated branches cannot be placed underneath auto-height Organization, Accelerator, Calendar, or fiscal cards.
- Added a development-only real React Flow visual fixture, light/dark screenshot baselines, and an independent six-variant Playwright bounds test using extreme content. The visual test now sets its stored theme before navigation so the theme provider cannot race the dark baseline. The complete workspace scope passed 477 tests across 80 files. Focused ESLint, route, feature, scaffold, threshold, workspace storage, interaction lock, React Grab, workspace surface, raw button, and `git diff --check` checks passed. All three ontology visual tests passed; the full 17-test visual run passed 16 tests and had one unrelated `/find` 768px navigation timeout, which passed immediately on focused rerun. Authenticated `/workspace` confirmation still requires a browser refresh because the currently loaded browser chunk predates these source changes. No deployment occurred.

2026-07-19 13:56 EDT - made the workspace graph self-guiding, canonical, and root-aware.

- Replaced the duplicated category legend with a compact Search, Filter, Help, Structure, and Undo dock. Filter is now the only area list and shows present-area counts. Help separately explains Needs information, Blocked, In progress, Complete, hierarchy lines, cross-area arrows, and category dots. Search uses user-facing planning-board language, indexes names, owners, statuses, and work areas, and reveals a result without filtering or reflowing the graph.
- Added managed versus manual root-position ownership. Organization, Programs, Accelerator, Roadmap, Calendar, and Fiscal Sponsorship now participate in the same deterministic scene packing and 180ms animation as their expanded branches; direct or selection dragging marks a primary card manual and preserves its exact position. Managed positions persist atomically without overwriting manual cards.
- Enforced one canonical identity per graph node, edge ID, and endpoint pair. Calendar now solely owns event nodes, Activity links to those canonical events, duplicate placed-person IDs produce one staffing relationship, root-ID collisions are rejected, and orphan, self, or repeated relationships are removed at projection and final canvas merge boundaries.
- Routed labeled cross-area relationships to a dedicated bottom target handle so dashed arrows and their labels avoid same-rank cards. Added a rendered expanded-scene baseline showing primary cards, child nodes, and the cross-area route without node overlap, plus light/dark dock, Help, and extreme-content containment baselines.
- Focused acceptance passed 81 tests and focused visual coverage passed 5 tests. Full ESLint, snapshots, routes, features, feature scaffold, boundaries, thresholds, workspace storage, interaction locks, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Full acceptance remains blocked by unrelated fiscal-source and public-Find copy assertions plus a missing resource-map enrichment module; structure and TypeScript remain blocked by the existing unrelated repository errors reported above. The protected `/workspace` route still requires an authenticated browser session for a post-refresh live check. No deployment occurred.

2026-07-20 10:36 EDT - rebuilt the workspace ontology as a horizontal operations graph.

- Changed ELK and Dagre to left-to-right ranks and replaced dense sibling rows with bounded vertical columns. When structure is open, managed Organization, Programs, Accelerator, Roadmap, Calendar, and Fiscal Sponsorship cards now form one ordered left lane with dependency columns to the right; the all-collapsed state remains a compact row. Manual roots and pinned nodes remain fixed.
- Moved hierarchy routing from bottom-to-top handles to right-to-left handles, kept orthogonal edges, and limited cross-area labels to full detail. Removed decorative category rails/dots from generated cards and removed their redundant Help entry while preserving explicit status icons and labels.
- Added horizontal-branch, no-rail, no-overlap, long-content, 120-node, six-domain, light, and dark visual coverage. The reference and revised dark fixture were compared together; `design-qa.md` records a passing result. Position-keyed fixture remounting made the fit-view baseline deterministic across repeated runs.
- Full ESLint, snapshots, RLS, 490 workspace tests across 82 files, 23 focused ontology tests, six focused visual tests, routes, features, feature scaffold, boundaries, thresholds, workspace storage, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. Full acceptance passed 1,546 tests with one skipped and remains blocked only by four unrelated existing fiscal, public Find, and resource-map assertions plus the missing NYC heat enrichment module. Structure and TypeScript remain blocked by the existing unrelated repository errors; no changed ontology source is reported by TypeScript. No deployment occurred.

2026-07-20 10:48 EDT - collapsed accelerator ontology branches to lesson-level entities.

- Replaced the accelerator's one-card-per-screen projection with one card per lesson/module. Internal videos, resources, assignment sections and questions, and the synthetic completion step now contribute only to the lesson summary, search terms, aggregate status, and workflow destination; they never become graph nodes.
- Completed lessons expose one `Review lesson` action and unfinished lessons expose one `Open lesson` action. Added the lesson-level granularity rule to the ontology design contract and regression coverage with explicit question, video, resource, and completion-step inputs.
- Focused ESLint and Prettier passed. All 491 workspace acceptance tests across 82 files passed, along with feature, boundary, threshold, workspace-surface, workspace-storage, interaction-lock, React Grab, and raw-button checks. No deployment occurred.

2026-07-20 12:56 EDT - integrated the workspace ontology into one measured horizontal scene.

- Moved each root Show/Hide control fully below its primary card in normal flow with a consistent 8px gap, included the control in measured root geometry, and replaced `sections` with `details`. Generated group and leaf cards now share one 64px height and use the existing node-frame anatomy without duplicated shell styling or decorative rails.
- Opening any ontology branch now temporarily arranges all visible Organization, Activity, Accelerator, Roadmap, Calendar, and Fiscal Sponsorship roots with their generated nodes as horizontal atomic scenes, regardless of saved manual card positions. Open-scene positions are render-only, root dragging is disabled during the managed view, and collapsing restores saved coordinates without persisting the temporary arrangement.
- Added controller-owned node and edge presence phases so new graph elements fade/translate in together, departing elements remain inert for their exit, and roots/retained nodes interpolate for 180ms. Reduced motion resolves immediately. Camera visibility now includes generated ontology nodes and fits once after a structural layout settles.
- Extracted accelerator ontology assembly and scene animation/layout into focused modules. Full ESLint and snapshot verification passed; focused ontology acceptance passed 22 tests. The full acceptance run passed 1,548 tests with one skip and remains blocked by three unrelated fiscal/public-Find assertions plus the missing NYC heat enrichment module. Feature, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-buttons passed. Structure is clear for this work and remains blocked by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost:3000 is healthy. Browser-rendered screenshot comparison remains pending because the in-app browser is unavailable and permission to use the Playwright visual runner is pending. No deployment occurred.

2026-07-20 13:16 EDT - removed the workspace ontology drag regression.

- Fixed collapsed primary cards being overwritten by stale ontology root positions during drag. Temporary managed root coordinates now apply only while structure is open; collapsed cards remain controlled by React Flow and persist once on drag stop.
- Removed pointer-frequency React renders for both workspace and generated ontology positions. Transient drag coordinates stay in React Flow's internal store, settled positions reconcile once, stable handler and visible-ID references no longer wake the panel or camera effects, and pin callbacks read current positions through a ref.
- Replaced JavaScript frame-by-frame scene interpolation and per-node Motion subscriptions with one final layout commit plus scoped CSS transform/presence transitions. Ontology edges hide during coordinate movement and return when nodes settle so lines never appear detached; generated nodes cannot be dragged while layout is settling. Search reveal suppresses the redundant full-scene fit, and a new drag cancels pending automatic fitting.
- Full ESLint, snapshots, focused workspace acceptance (`98 tests`), thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and `git diff --check` passed. Full acceptance passed `1,548` tests with one skip and remains blocked by the same unrelated fiscal/public-Find/resource-map failures plus one resource-map fixture timeout. Structure remains blocked only by pre-existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost:3000 and the ontology fixture route are healthy. Browser motion profiling remains pending under the active browser policy. No deployment occurred.

2026-07-20 13:20 EDT - refined dark workspace relationship labels.

- Rebuilt ontology rail labels as compact glass pills using the existing background and foreground tokens: full radius, 8px horizontal and 4px vertical padding, a quiet border and shadow, 10px Geist text, truncation, and a darker translucent blurred surface in dark mode.
- Focused Prettier, ESLint, ontology acceptance (`18 tests`), and `git diff --check` passed. The local ontology fixture returns 200. Rendered dark-mode comparison remains pending because the in-app browser currently exposes no browser surface; no Playwright fallback was used without permission. No deployment occurred.

2026-07-20 13:32 EDT - integrated the staff tree into ontology scene placement.

- Preserved saved staff coordinates while treating vertically related person obstacles as one reserved lane during expanded ontology layout. Root scenes still pack left to right when unobstructed, use a compact horizontal detour when it fits, and move below the staff lane instead of being pushed arbitrarily far to the right.
- Moved the ontology controls from the drawer-obscured bottom dock to a top-left safe-area anchor with mobile and desktop containment. The visual fixture now exercises the same anchor and updated light/dark baselines.
- Focused Prettier, ESLint, ontology acceptance (`21 tests`), workspace surfaces, interaction locks, and React Grab ownership passed. The panel-focused visual checks passed in light and dark. The six-domain graph remains within its existing 900px height bound. Authenticated `/workspace` visual confirmation remains pending; no deployment occurred.

2026-07-20 14:16 EDT - cleared ontology relationship labels and completed search selection.

- Aggregated authoritative accelerator assignment labels, descriptions, placeholders, options, lesson notes, and module-resource labels into the owning lesson's search terms without creating question or resource cards.
- Routed cross-area ontology relationships through dedicated bottom source and target handles. Dashed relationship labels now occupy a clear corridor below the node rows and no longer compete with Accelerator or Organization expansion controls.
- Search results now reveal, select, focus, and fit the exact generated node so its selection toolbar and destination action are immediately available. Ontology selection state remains separate from pointer-frequency position updates.
- Focused ESLint passed. Ontology, identity, root-layout, staff-layout, drag-policy, input, and selection coverage passed `40 tests`; all `7` focused light, dark, transition, and bounds visual tests passed. Workspace surfaces, interaction locks, React Grab ownership, and raw-button checks passed. `/workspace` remains within its first-load budget at `4,128 KB`; the repo-wide performance command remains blocked only by the unrelated `/admin` route at `1,582.9 KB` against its `1,000 KB` budget. Chrome exposes no available authenticated browser instance, so the production-data workspace check remains pending. No deployment occurred.

2026-07-20 14:23 EDT - bounded the holistic workspace scene and simplified staff links.

- Replaced the expanded layout's unlimited horizontal row with a `4,800px` scene band. Large managed graphs now wrap into additional horizontal lanes while each root's branch still grows left to right. The narrower `2,800px` staff-detour limit remains in place, so broad staff trees send ontology scenes below the reserved people lane instead of pushing Accelerator arbitrarily far right.
- Reduced the People branch's cross-area links to roots of the placed staff forest. Existing report-to edges continue the hierarchy, removing redundant People-to-every-person fan-out; disconnected trees and cyclic components retain one deterministic anchor.
- Kept the top-left control dock compact and raised its mobile search field to 16px so opening board search does not trigger iOS viewport zoom.
- Added combined six-root, nine-person, and two-utility geometry coverage. The complete protected scene remains collision-free and bounded, and saved staff and utility coordinates remain unchanged. No deployment occurred.
- Final focused verification passed ESLint, feature, threshold, and boundary checks; `42` ontology and canvas acceptance tests; and all `7` light, dark, bounds, label-corridor, and transition visual tests. The visual suite now waits for the settled horizontal root order before capture, eliminating the stale pre-layout dark baseline race.

2026-07-20 14:51 EDT - removed the floating workspace ontology controls.

- Deleted `WorkspaceOntologyPanel`, its canvas mount, exports, fixture surface, and panel-only search, filter, help, global expansion, undo, history, and focus runtime. Root-card Show/Hide controls and selected-node toolbars remain as the contextual interaction model.
- Removed the obsolete Help screenshot and regenerated the light and dark ontology node baselines without the detached panel. Focused ESLint passed, `41` ontology acceptance tests passed, all `6` focused ontology visual tests passed, and feature, threshold, boundary, React Grab, workspace-surface, and scoped diff checks passed. No deployment occurred.

2026-07-20 17:36 EDT - completed contextual workspace ontology actions and layout hardening.

- Kept the floating ontology panel deleted and replaced dead or misleading node actions with contextual destinations. Calendar nodes open the exact event, fiscal nodes open an existing workflow phase, and task nodes open the matching objective editor; task groups open the objectives workspace. Read-only viewers cannot enter a transient editor, and missing fiscal workflows no longer show nonfunctional actions.
- Switched task projection from the initial seed snapshot to the live board tracker, added pinned generated nodes to primary-card collision obstacles, replaced ELK/Dagre imports with deterministic horizontal branch packing, and constrained selected-node toolbars at narrow widths. Objectives controls now meet mobile touch sizing inside their Sheet, and Sheet motion respects reduced-motion preferences.
- Focused ESLint passed. `43` focused acceptance tests and all `7` ontology visual tests passed, including narrow-viewport containment, dark mode, reduced-motion transitions, exact fourth-or-later calendar-event selection, and live tracker projection. Thresholds, boundaries, workspace surfaces, raw buttons, and interaction locks passed. Production build and repository TypeScript remain blocked by existing unrelated public-map, member-workspace, and fixture errors; structure remains blocked only by existing oversized public-map, audio, bar-visualizer, and workspace-action files. No deployment occurred.

2026-07-20 18:01 EDT - removed the React Flow render-phase store update.

- Replaced `WorkspaceCanvasReactFlowErrorBootstrap`'s render-time `store.setState` call with a shared commit-phase bootstrap. React Flow now remains gated until the filtered error handler is installed in a layout effect, preventing `EdgeRenderer` subscribers from being updated while the bootstrap renders.
- Added a development-only Strict Mode fixture and browser regression covering a real custom-node edge, node-type replacement, a bootstrap remount under a persistent provider, a full provider remount, store-level meaningful-error delegation, React Flow CSS containment, console warnings/errors, and page exceptions.
- Focused ESLint and Prettier passed. `24` focused acceptance tests passed, and the rendered lifecycle regression passed three consecutive runs. Routes, features, boundaries, thresholds, workspace surfaces, interaction locks, raw buttons, and scoped diff checks passed. Structure and repository TypeScript remain blocked only by existing unrelated oversized files and public-map/member-workspace/test-fixture errors; no changed bootstrap file is reported. No deployment occurred.

2026-07-20 18:30 EDT - replaced intrusive ontology toolbars with card-first actions.

- Removed the selected node's detached three-action toolbar. Generated group cards now expand or collapse from the card itself; actionable leaves open their exact route, editor, workflow phase, task sheet, calendar event, or lesson; dead-end leaves focus the owning primary card. Enter and Space provide the same behavior from the focused React Flow node, while modifier clicks preserve multi-selection.
- Kept only a quiet 28px reset control for a selected node that was manually dragged and pinned. Its event is isolated from the card action. Added exact activation descriptions to React Flow accessibility labels and kept all interaction wiring inside the ontology/canvas feature instead of changing the shared node-frame primitive.
- Restarted the stale local development process that was still serving the deleted panel and previous toolbar bundle, then regenerated the light and dark ontology baselines from the current source. Visual coverage now explicitly rejects any toolbar on an unpinned selected card and asserts the card-level open and detail actions.
- Focused Prettier and ESLint passed. `18` focused acceptance tests and all `7` ontology visual tests passed. Thresholds, boundaries, routes, features, workspace surfaces, interaction locks, and raw buttons passed. The edited canvas surface is back within its component line budget; structure remains blocked only by unrelated existing oversized public-map, audio, bar-visualizer, and workspace-action files. Repository TypeScript remains blocked by existing public-map, member-workspace, and fixture errors; none reference the card-first ontology changes. No deployment occurred.

2026-07-20 18:35 EDT - removed ontology pin actions.

- Deleted the generated-node pin toolbar, pin icon, button dependency, node callback, canvas data wiring, and controller toggle action. Ontology cards now have one interaction model: click or use the keyboard to expand details or open the exact destination.
- Kept manual drag coordinates as internal persisted placement state without exposing a pinning concept. The selected visual fixture now exercises that persisted state while explicitly requiring zero React Flow toolbar elements.
- Focused Prettier, ESLint, ontology acceptance (`16 tests`), all `7` ontology visual tests, thresholds, boundaries, workspace surfaces, interaction locks, raw buttons, and scoped diff checks passed. No deployment occurred.

2026-07-20 18:58 EDT - eliminated repeated ontology expansion drift.

- Removed the generated-node position feedback loop that treated previously solved descendants as fresh root-layout obstacles. Generated ontology nodes are now exclusively layout-managed and non-draggable; primary cards and people retain their existing manual placement. A 4px drag threshold protects movable workspace nodes from click jitter.
- Replaced recursive subtree-width placement and the second branch-repacking pass with deterministic horizontal depth ranks. Each rank uses at most three columns, grows vertically in bounded bands, and is solved once with its owning root. Staff-lane vertical fallback now resets to the canonical row origin instead of retaining an accumulated horizontal detour.
- Scoped automatic post-expansion fitting to the changed ontology scene roots and descendants instead of every visible workspace node. Legacy generated pins and coordinates normalize away but remain readable in the serialized state shape for backward compatibility.
- Added regressions for legacy saved coordinates, dense 120-node ranks, staff obstacles, reverse expansion order, deep collapse/reopen cycles, and exact coordinate restoration. Focused ESLint passed; `57` workspace/ontology acceptance tests and all `8` focused visual tests passed. Features, routes, thresholds, boundaries, workspace storage, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. Repository TypeScript and structure remain blocked only by existing unrelated public-map, member-workspace, test-fixture, audio-player, bar-visualizer, and workspace-action failures; no changed ontology file is reported. No deployment occurred.

2026-07-20 19:35 EDT - completed the workspace ontology interaction and resilience audit.

- Corrected accelerator lesson destinations to open the next unfinished step. Removed silent activity truncation and grouped every upcoming calendar event under compact month nodes while keeping resources, questions, videos, and completion screens aggregated inside their lesson.
- Added one blurred hierarchy rail label per parent, retained every cross-area relationship label, and corrected React Flow's double translation so labels occupy the branch corridor without overlapping root or detail cards. Updated the intentional light/dark graph baselines.
- Committed accepted target geometry and signatures before rendering transition-only items. A new 20-toggle, 20ms regression restores the exact node coordinates and camera with zero overlaps instead of leaking departing-node geometry into another branch.
- Removed ontology edges from keyboard tab order, exposed group expansion state on the focused React Flow node, added reliable Space activation to root branch controls, and changed narrow-screen fitting to focus the changed root/group at readable scale rather than shrinking the entire graph into an unusable overview.
- Focused ESLint passed. `95` focused workspace/ontology acceptance tests and all `10` ontology visual/bounds tests passed. Features, routes, feature scaffold, thresholds, boundaries, workspace storage, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. Repository TypeScript and structure remain blocked by existing unrelated public-map, member-workspace, test-fixture, audio-player, bar-visualizer, and workspace-action failures; no changed ontology source is reported by TypeScript. Authenticated `/workspace` verification remains unavailable because the in-app browser exposes no signed-in browser. No deployment occurred.

2026-07-20 19:45 EDT - restored the local development server and reviewed continuation context.

- Read `AGENTS.md`, `docs/RUNLOG.md`, the latest July entries, and the existing dirty worktree. Started the detached `coach-house-dev` session after confirming port 3000 was down; `/find` now returns HTTP 200. No product code, deployment, commit, or push was changed.

2026-07-20 20:39 EDT - refined the workspace ontology into an anchored, action-first view.

- Moved root and group expansion into bounded `workspace-details` and `workspace-groups` URL parameters with refresh and Back/Forward restoration. Ontology exploration no longer writes shared board state; legacy persisted fields remain readable for compatibility.
- Kept primary workspace cards at their saved coordinates and limited deterministic layout to generated detail lanes around fixed roots, staff, and utility obstacles. Removed temporary root-position write plumbing and the obsolete controller function-limit exception.
- Sorted branches blocked, missing, in-progress, then complete. Root controls surface blocked/missing priority counts; generated cards use real links or buttons, always-visible status and action text, taller measured frames, bottom hierarchy handles, and opaque 11px edge labels in collision-free corridors.
- Updated the ontology feature and design contracts, added URL, anchored-layout, attention-order, semantic-action, browser-history, root-stability, dense-content, mobile-focus, and first-time priority-task regressions, and regenerated the intentional light/dark baselines.
- Focused acceptance passed `55` tests across `9` files; all `11` ontology visual tests, focused ESLint, snapshots, RLS, routes, features, feature scaffold, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and scoped diff checks passed. `/workspace` remains within budget at `4,182.1 KB` against `4,550 KB`; the repo-wide performance command is blocked only by the unrelated `/admin` bundle.
- Full acceptance passed `1,559` tests with one skip and remains blocked by four unrelated fiscal/public-Find assertions plus a missing NYC heat enrichment module. Structure and full TypeScript remain blocked by existing unrelated oversized and public-map/member-workspace/test-fixture work; a filtered post-fix TypeScript run reports no ontology, ontology integration, node-frame, or ontology persistence errors.
- Localhost:3000 and the visual fixture return 200. `/workspace` correctly preserves ontology URL state through its unauthenticated login redirect, but the in-app browser exposes no browser surface, so the authenticated production-data first-time-user check remains pending. No deployment, commit, or push occurred.

2026-07-20 21:00 EDT - completed the ontology requirement audit and mobile wayfinding correction.

- The requirement audit found that narrow-screen expansion still centered the root instead of the new priority action and that the visual fixture used separate camera logic. Added one shared wayfinding hook for production and fixtures: opening focuses the first revealed blocked/missing item at readable zoom, including reduced motion; collapsing or browser Back returns to the owning root.
- Replaced the generic fallback “View card” with exact action text such as “Go to Fiscal sponsorship card.” The same action-description function now owns visible and accessible labels.
- Split the approaching-limit `workspace-canvas-ontology-input.ts` from `430` lines to `169` and moved Organization and Activity/Programs projection into focused `161`- and `119`-line adapters. All six ontology adapters are now `119–182` lines, and the ontology-specific structure warning is gone.
- Focused acceptance passed `55` tests across `9` files. All `11` ontology visual tests passed, including an end-to-end first-time task that identifies the blocked item, activates its workflow link, and verifies the preserved login redirect, plus mobile priority focus and parent restoration. Focused ESLint, features, thresholds, boundaries, snapshots, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and scoped diff checks passed. `/workspace` remains within its performance budget; only unrelated repository structure, `/admin` performance, full acceptance, and full TypeScript failures remain.
- The in-app browser still exposes no browser surface, so authenticated production-data validation remains the only incomplete objective gate. No deployment, commit, or push occurred.

2026-07-20 21:01 EDT - isolated the final ontology validation blocker.

- Rechecked localhost:3000, the ontology fixture, the URL-preserving `/workspace` login redirect, scoped diffs, and the available browser inventory. Localhost and the fixture remain healthy, but the in-app browser exposes no browser surface for the third consecutive goal turn.
- All implementation and automated acceptance gates remain complete. The sole unfinished requirement is the signed-in, realistic-data first-time-user task; completing it now requires the user to expose the in-app browser and sign into localhost:3000. No alternate browser was used to bypass authentication. No deployment, commit, or push occurred.

2026-07-20 21:15 EDT - kept ontology cards opaque on hover.

- Replaced the generated node card's 20%-opacity hover fill with the solid themed muted surface, so hovering increases contrast without exposing the canvas through the card.
- Added source-contract coverage and light/dark browser assertions that the hovered background remains opaque. Focused ESLint and Prettier passed, `19` ontology acceptance tests passed, and the hover checks passed in both visual themes. The full `11`-test ontology visual file passed `10`; its unrelated rapid-toggle exact viewport comparison missed by `0.000554` scale once, then passed twice consecutively. Feature, threshold, boundary, interaction-lock, React Grab, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 21:27 EDT - removed legacy accelerator destinations from the ontology and retired the old route.

- Replaced ontology lesson and accelerator-activity links derived from legacy `/accelerator` URLs with exact `/workspace/accelerator?step=...&module=...` destinations. Users without access receive the current workspace paywall route.
- Reduced the legacy accelerator layout from `202` lines to a direct redirect, so `/accelerator` and every child URL now redirect to `/workspace/accelerator` before the retired shell can render. Updated the app-shell and route contracts accordingly.
- Focused ESLint and Prettier passed; `50` acceptance tests across `8` route, ontology, node-frame, and layout files passed. Route, feature, threshold, and boundary checks passed. Direct localhost requests confirmed both the legacy root and a nested legacy lesson return `307` to `/workspace/accelerator`; the canonical deep link preserves its query through login. The visual file passed `10/11`; the same unrelated rapid-toggle exact-camera assertion remains flaky while all action and navigation tests pass. No deployment, commit, or push occurred.

2026-07-20 21:30 EDT - restored frosted ontology branch controls.

- Kept the fix in `WorkspaceOntologyBranchToggle`, the React Grab semantic and class-assembly owner. Added explicit ownership metadata so future grabs resolve the component instead of reporting an unknown owner.
- Overrode the shared outline button's 30%-opacity dark fill with a 90%-opaque dark surface, retained a 95%-opaque light surface, added a 12px backdrop blur, and increased opacity on hover. Browser-computed styles confirmed the blur and intended alpha in both themes.
- Focused ESLint and Prettier passed, `23` acceptance tests passed, both intentional light/dark board baselines passed after update, and threshold, React Grab, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 21:52 EDT - restored smooth primary-card dragging.

- Removed the node-change regression that discarded every live `dragging: true` position update. Organization, Accelerator, and Activity now update continuously under the pointer, while transient drag frames bypass the full workspace-node reconciliation pass.
- Kept frosted branch controls at rest but disabled their backdrop filter while their owning card moves, avoiding a per-frame canvas repaint; the opaque surface and blur return on drop. Confirmed the drag-only rule is present in the served localhost CSS bundle.
- Focused ESLint and Prettier passed, `26` focused acceptance tests passed, and thresholds, boundaries, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. The full quality gate reached and passed lint, then stopped at unrelated existing oversized public-map, audio-player, bar-visualizer, and workspace-action files. The Chrome extension bridge remained unavailable after a permitted reconnect, so signed-in live drag confirmation is pending. No deployment, commit, or push occurred.

2026-07-20 22:07 EDT - compacted ontology branch controls.

- Replaced count-heavy control text with one- or two-word labels: `Show details`, `Hide details`, or the priority count such as `2 priorities`. Preserved the card name, full descendant count, and action in the accessible label.
- Added a small sky-colored hierarchy icon on the left while retaining the directional chevron on the right, 44px mobile target, focus ring, opaque/frosted surface, and drag-time blur optimization.
- Focused ESLint and Prettier passed, `19` acceptance tests and all `11` ontology visual tests passed, and intentional light/dark baselines were regenerated. Threshold, React Grab ownership, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 22:19 EDT - aligned ontology nodes with the main-card system.

- Rebuilt generated ontology nodes on the existing workspace-node frame anatomy with the main-card visual language: a muted 2rem outer shell, compact header, inset background body, and explicit footer action button. Preserved native links, full accessible labels, React Flow handles, card-level activation, focus rings, and reduced-motion behavior.
- Increased detail-node height from `92` to `152` pixels for the new structure, let dense depth bands use up to five columns, and moved relationship routes and labels beneath the taller rows. Dense branches remain deterministic, bounded, and collision free.
- Focused ESLint and Prettier passed, `37` acceptance tests and all `11` ontology visual tests passed, and intentional light/dark baselines were regenerated and then passed without update mode. Threshold, boundaries, interaction locks, React Grab ownership, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 22:23 EDT - optically balanced ontology node insets.

- Increased only the generated node shell's vertical inset from `8` to `10` pixels while retaining its `8`-pixel horizontal inset, creating visually even spacing around the curved 2rem frame without changing node geometry or interaction behavior.
- Focused ESLint and Prettier passed, `23` acceptance tests and the `4` affected light/dark visual tests passed after baseline regeneration and again without update mode. Threshold, React Grab ownership, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 22:30 EDT - combined ontology status and action controls.

- Replaced the separate inset status surface and footer action with one native link/button row. Status and optional owner remain on the left; the action label and directional icon occupy the right, with independent truncation and the existing complete accessible label.
- Reduced generated node height from `152` to `112` pixels and proportionally returned relationship routes and labels to the compact scene corridor. Aligned the visual fixture's initial fit settings with shared desktop wayfinding so repeated expansion and collapse now restore the same camera deterministically.
- Focused ESLint and Prettier passed, `37` acceptance tests and all `11` ontology visual tests passed after baseline regeneration and again without update mode. Threshold, boundaries, interaction locks, React Grab ownership, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-20 22:36 EDT - clarified accelerator readiness rails.

- Strengthened the existing Fundable and Verified segment tints using the green and blue families documented in `docs/design.md`. Added a centered dollar icon to Fundable and a badge-check icon to Verified on the rail and in their tooltips; text and accessible segment labels remain the primary status cues.
- Increased the shared rail height from `12` to `20` pixels so the markers remain legible without changing checkpoint math, progress behavior, or consuming card actions.
- Focused Prettier and ESLint passed, `11` accelerator/card acceptance tests passed, snapshots matched, and thresholds, routes, features, scaffold sync, boundaries, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. Structure remains blocked only by unrelated existing oversized public-map, audio, bar-visualizer, and workspace-action files. Localhost:3000 is healthy; the Chrome extension connection was unavailable, so signed-in visual confirmation remains pending. No deployment, commit, or push occurred.

2026-07-20 22:50 EDT - moved Team Access beside the calendar.

- Moved the existing Team Access avatar and realtime trigger into the shell header's right action slot, immediately left of Calendar. Preserved its hover details, member management, invitations, pending-access state, tutorial callout, accessible summary, and React Grab ownership; the hover panel now aligns inward from the right edge.
- Removed the collapsible Team Access right-rail section and its tutorial auto-open helper. The workspace right rail now contains only Strategic Roadmap; the stored legacy collapse preference remains readable for backward compatibility.
- Focused ESLint and Prettier passed, `49` acceptance tests passed, snapshots matched, and routes, thresholds, boundaries, interaction locks, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-20 22:53 EDT - centered the documents banner.

- Reorganized `DocumentsBanner` into a centered vertical lockup with the folder graphic above the heading, balanced copy width, and centered view-only messaging.
- Added a single 600ms transform-and-opacity entrance to the folder graphic when the banner mounts. Reduced-motion users receive the static graphic, and the banner now exposes explicit React Grab ownership.
- Focused Prettier and ESLint passed, the documents-banner acceptance test passed, snapshots matched, and thresholds, boundaries, React Grab ownership, raw buttons, and scoped diff checks passed. Localhost:3000 remains healthy. No deployment, commit, or push occurred.

2026-07-20 22:55 EDT - restored the dark ontology action surface.

- Overrode the shared outline button's translucent dark fill in `WorkspaceOntologyNode`. The combined status/action row now keeps an opaque `background` surface in dark mode and an opaque `accent` hover surface without changing the shared button primitive.
- Added source and computed-style regressions requiring the dark action row to remain opaque and darker than its muted node frame. Focused Prettier and ESLint passed, `19` ontology acceptance tests passed, both affected dark visual baselines were regenerated and passed again without update mode, and thresholds, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. No deployment, commit, or push occurred.

2026-07-20 22:57 EDT - removed icons from the accelerator rail.

- Corrected the Fundable and Verified treatment by removing the visible dollar and badge markers embedded inside the progress segments. Restored the compact 12px rail while retaining the stronger green and blue segment colors, accessible segment labels, and icons inside hover tooltips only.
- Focused Prettier and ESLint passed, `22` accelerator acceptance tests passed, and thresholds, boundaries, raw buttons, and scoped diff checks passed. No deployment, commit, or push occurred.

2026-07-20 23:16 EDT - compacted the canvas viewport controls.

- Matched the zoom, recenter, and integrated help controls to the left shortcut rail's 36px desktop buttons. Retained 44px targets on mobile and the existing icon, focus, tooltip, and touch behavior.
- Added direct React Grab ownership to zoom-out, zoom-in, and recenter so future selections resolve to the actual viewport-control assembly file.
- Focused Prettier and ESLint passed, `30` workspace acceptance tests passed, both affected light/dark workspace visual tests passed, and thresholds, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. No deployment, commit, or push occurred.

2026-07-20 23:22 EDT - refined ontology branch motion.

- Coordinated the branch toggle, generated nodes, edges, layout, and camera around a 240ms ease-out-quint transition. The trigger now has restrained press feedback, a softened label change, a smoother chevron turn, and a subtle expanded-state hierarchy cue.
- Opening nodes now glide and scale outward from the parent; closing nodes return toward the parent with a faster exit. Edges follow with a short delayed fade, and reduced-motion behavior remains instant. All animation properties are limited to transform and opacity outside existing color/shadow transitions.
- Focused Prettier and ESLint passed, `24` ontology acceptance tests passed, snapshots matched, and thresholds, boundaries, interaction locks, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. All `11` ontology browser tests passed after intentional baseline regeneration; the verification rerun passed `10/11` before the existing mobile width assertion missed by `0.08px`, and that isolated test passed immediately on rerun. No deployment, commit, or push occurred.

2026-07-20 23:58 EDT - clarified ontology setup controls and completed Accelerator handling.

- Replaced generic branch labels with status-aware `Start setup`, `Finish setup`, `Review setup`, and `Hide setup` actions. Preserved the priority count, moved it to the action's right side, tightened the left icon inset, and added completed-count data so the labels reflect actual progress.
- Added an eye-off circle immediately left of the Accelerator setup pill once its selected class reaches 100%. It hides through the existing card-visibility reducer and remains recoverable from the shortcut rail. Changed the direct card editor action from an expand glyph to a compact pencil-and-`Edit` control, including the overflow-menu icon.
- Focused Prettier and ESLint passed, `29` focused acceptance tests and all `11` ontology visual tests passed, snapshots matched, and routes, features, scaffold sync, thresholds, boundaries, workspace storage, interaction locks, React Grab ownership, workspace surfaces, and raw buttons passed. Full TypeScript and structure checks remain blocked by unrelated existing resource-map/test type failures and oversized public-map/audio/workspace-action files. Localhost:3000 is healthy. Chrome is running with the extension installed, but its tab bridge exposed no tabs; live signed-in verification awaits permission to open a connected Bandto-profile window. No deployment, commit, or push occurred.

2026-07-21 00:09 EDT - enlarged and recolored ontology setup controls.

- Increased the branch pill from 32px to 40px on desktop and from 44px to 48px on mobile, with 14px labels, 28px icon circles, larger hierarchy and chevron icons, and a 12px count while preserving the tight left inset and accessible touch behavior.
- Replaced the blue hierarchy treatment with semantic orange for incomplete setup and green for completed setup. The icon circle and right-side count share the state color, while text and labels keep color-independent meaning. Shifted root relationship labels eight pixels lower to clear the larger controls and updated the visual fixture's reserved root height.
- Focused Prettier and ESLint passed, `20` ontology acceptance tests and all `11` ontology visual tests passed after forced intentional baseline regeneration and a no-update verification pass. Thresholds, boundaries, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 00:11 EDT - promoted the direct card Edit action.

- Changed the local direct editor link from a compact ghost action to the established small primary-button treatment: primary fill, primary foreground, 32px height, 14px label, 16px pencil, and 12px horizontal padding. The shared Button and Card primitives remain unchanged.
- Focused Prettier and ESLint passed, `7` card-header acceptance tests passed, snapshots matched, and thresholds, React Grab ownership, raw buttons, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 00:34 EDT - added the generated-path wave and speed-ramped board camera.

- Staggered ontology connectors and nodes outward in semantic path order, with each connector arriving before its node. Collapse now retracts from the deepest node back toward the root, caps long-branch wave spans, and keeps rapid-toggle canonical-scene recovery plus instant reduced-motion behavior.
- Started desktop pull-back while a branch generates, retained readable mobile focus, and lengthened React Flow's native d3 cubic-in-out camera ramp: 420ms manual zoom, 460–480ms focus moves, and 520ms fit/recenter moves. All shared viewport commands now honor reduced motion.
- Focused acceptance passed `31` tests and all `11` ontology browser tests passed without baseline changes. Full ESLint, snapshots, routes, features, scaffold sync, thresholds, boundaries, workspace storage, interaction locks, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Full acceptance passed `1,564` tests with one skip but remains blocked by four unrelated fiscal/public-Find/resource-map assertions and a missing NYC heat module; full TypeScript and structure remain blocked by unrelated existing public-map/member-workspace/test typing and oversized public-map/audio/workspace-action files. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 00:38 EDT - moved completed-Accelerator hiding to the Organization pill.

- Routed Accelerator completion and its hide callback to the Organization node, placing the existing eye-off circle immediately left of the Organization setup pill instead of the Accelerator pill. The action appears only while Accelerator is complete and visible, then disappears after hiding it; shortcut-rail recovery remains unchanged.
- Focused Prettier and ESLint passed, `26` acceptance tests passed, and thresholds, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 00:58 EDT - removed the completed-Accelerator hide button.

- Removed the eye-off circle from the ontology control row entirely and deleted its dedicated completion state, callback, memo-comparison, and card-data plumbing. Accelerator remains hideable and recoverable through the existing board controls and shortcut rail.
- Focused Prettier and ESLint passed, `30` acceptance tests passed, and thresholds, boundaries, React Grab ownership, workspace surfaces, raw buttons, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 01:03 EDT - clarified the Organization documents relationship.

- Replaced the vague Organization-to-Documents rail label `supported by` with the precise `documented by`. Avoided `verified by`, which would overstate what uploaded records prove, and preserved the relationship direction and layout.
- Focused Prettier and ESLint passed, `28` ontology acceptance tests passed, and thresholds, boundaries, and scoped diff checks passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 01:18 EDT - reframed the Documents rail as an objective step.

- Changed the Organization-to-Documents rail label from the ownership-oriented `documented by` to the action-oriented `Next step`, matching the board's command-center model. Kept the broader AI-controlled relationship vocabulary conceptual until its module model exists.
- Focused Prettier and ESLint passed, `28` ontology acceptance tests passed, and thresholds and boundaries passed. Localhost:3000 is healthy. No deployment, commit, or push occurred.

2026-07-21 10:01 EDT - made ontology steps land on exact highlighted work.

- Added URL-backed focus targets to workspace editor paths. Organization profile steps now open the Company tab in edit mode, scroll the requested field or logo control into view, move accessible focus to its group, and hold a blue focus treatment for 3.2 seconds; smooth scrolling honors reduced motion.
- Routed document leaves to their exact row in `/organization/documents`, preserving the focus target through login, and routed People and ownership to the actual `/people` manager instead of the read-only profile tab. Existing exact Program, Accelerator, task, calendar, and fiscal destinations remain unchanged.
- Focused Prettier and ESLint passed, `45` acceptance tests passed, snapshots matched, and feature, scaffold, threshold, boundary, route, interaction-lock, React Grab, workspace-surface, and raw-button checks passed. The full typecheck remains blocked by unrelated existing public-map, member-workspace, and test typing failures; no changed file appears in the filtered errors. Localhost:3000 is healthy and both authentication return paths preserve their focus query.
- Signed-in Chrome verification remains pending: Chrome is running and the ChatGPT Chrome Extension plus native host are installed and enabled, but the extension bridge exposed no tabs after its permitted retry. No deployment, commit, or push occurred.

2026-07-21 11:46 EDT - verified the platform-admin account model and provisioning path.

- Confirmed that the app's highest privilege is a normal Supabase Auth user whose `public.profiles.role` is `admin`; it is separate from organization membership roles. `requireAdmin()` and `public.is_admin()` enforce the same profile-role boundary in server routes/actions and RLS, while dashboard state uses it to expose platform navigation and bypass member onboarding/access gates.
- Confirmed the recommended local-manifest provisioner creates or updates confirmed Auth users through the service-role client, upserts their admin profile, optionally resets passwords, and fails closed when a missing user has no password. Existing one-off create and promote scripts remain available; revocation changes the profile role away from `admin`.
- Review only; no account, role, password, environment, or application code was changed. No live platform-admin roster was queried and no deployment, commit, or push occurred.

2026-07-21 13:30 EDT - added developer and coach platform-access levels.

- Added the RLS-protected `platform_staff_members` contract with `developer` and `coach` levels. Developers retain all existing platform-admin authority; coaches receive only Workspace, Find, and Organizations capabilities. Server authorization, dashboard state, sidebar visibility, direct-route redirects, member-workspace loaders/actions, fiscal-sponsorship operations, and staff assignee lists now use the capability boundary while preserving the legacy admin fallback until migration.
- Prepared the migration and provisioners to keep Caleb as developer and assign Paula, `fs@coachhousesolutions.org`, and `joel@amorejustchicago.org` as coaches. The migration keeps broad `is_admin()` RLS authority developer-only and gives each staff user read access to their own level. Coach organization access is intentionally all-organizations until coach-to-organization assignment is developed next.
- Focused ESLint passed; `83` focused acceptance tests and the existing remote RLS suite passed. Features, scaffold sync, routes, thresholds, and boundaries passed. The full quality gate remains blocked by unrelated existing size violations; full acceptance has unrelated fiscal/public-Find/resource-map failures, and build/typecheck remains blocked by unrelated public-map/workspace typing. No changed production file appeared in the filtered typecheck errors.
- Live roles were not changed and no deployment, commit, or push occurred. Applying the role migration before deploying the matching authorization code would remove current production access for the affected existing admins, while deploying this heavily dirty worktree would ship unrelated unfinished changes.

2026-07-22 19:53 EDT - moved the local UI server to port 3002.

- Read the canonical continuation docs and inspected the existing dirty worktree. Replaced the repo's detached port-3000 dev session with `coach-house-dev-3002` to avoid concurrent Next.js cache conflicts.
- Confirmed a listener on `localhost:3002` and a successful `/find` response after its initial compile. No application code, deployment, commit, or push changed.
- Created and switched the existing worktree to `codex/ui-work-2026-07-22` for today's UI work; all pre-existing uncommitted changes remain intact.

2026-07-22 20:57 EDT - removed the public-home fake map fallback.

- Deleted the fabricated land polygons, grid, and marker clusters from `HomeFindMapMini`. When the Mapbox preview is loading or unavailable, the hero now stays on its quiet dark base; the map label and selected-resource card appear only with the real map.
- Added source-level regression coverage. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Browser verification on `localhost:3002` confirmed zero fallback elements, no map-only overlay in the unavailable state, and no console errors. No deployment, commit, or push occurred.

2026-07-22 22:44 EDT - removed the public-home fake selected-map card.

- Deleted `HomeMapSelectedPreview`, including its fabricated Community Arts Initiative profile and program, people, and file counts. The real map and its small resource-map label remain unchanged.
- Added regression coverage requiring the fake card and its data hook to stay absent. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Restored the repo's detached local server to the requested port 3002 and confirmed its listener and homepage health. No deployment, commit, or push occurred.

2026-07-22 23:04 EDT - removed the home-map compact attribution icon.

- Replaced the compact top-left Mapbox attribution control with its required text-only form in the bottom-right and returned the Mapbox logo to the bottom-left. Removed the obsolete top-left control positioning overrides.
- Updated the public-home regression contract. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Live browser verification on `localhost:3000` confirmed the attribution button is hidden, the control is non-compact in the bottom-right, the real map reached ready state, and the console had no errors. No deployment, commit, or push occurred.

2026-07-22 23:07 EDT - removed the public-home map label pill.

- Deleted the floating `Public resource map` pill from `HomeFindMapMini` without changing the real map, markers, or required Mapbox attribution.
- Added regression coverage requiring the label to stay absent. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Live browser verification on `localhost:3000` confirmed zero matching labels and no console errors. No deployment, commit, or push occurred.

2026-07-22 23:15 EDT - minimized the required Mapbox attribution footer.

- Preserved the required `© Mapbox`, `© OpenStreetMap`, `Improve this map`, and satellite-source attribution, but restored the Mapbox map root class that React had dropped and reduced the footer to a quiet 10px treatment with a dark translucent surface.
- Added regressions for the stable Mapbox root class, compact footer styling, and the required improve-map link remaining visible. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Live browser verification reduced the control from about `445 × 24` to `269 × 17` pixels and the improve link from `134` to `83` pixels; the map reached ready state with no console errors. No deployment, commit, or push occurred.

2026-07-22 23:18 EDT - restored the public-home map after the attribution sizing regression.

- Removed the explicit `mapboxgl-map` class from the React-owned map container. Mapbox's CSS had overridden the required absolute positioning, collapsing the ready map to zero height; the local 10px attribution styling remains intact.
- Updated regression coverage to require the absolute map container and reject the conflicting class. Focused Prettier, ESLint, the public-home acceptance suite, thresholds, and React Grab ownership passed. Live browser verification confirmed a ready `974 × 925` map and canvas, absolute positioning, the compact attribution footer, visible satellite imagery, and no console errors. No deployment, commit, or push occurred.

2026-07-23 11:45 EDT - started the local development server on port 3000.

- Read the canonical continuation docs and inspected the existing dirty worktree. Started detached `coach-house-dev`, confirmed a listener on `localhost:3000`, and verified `/find` returns `200`. No application code, deployment, commit, or push changed.

2026-07-23 12:59 EDT - lightened the workspace React Flow canvas.

- Changed the live workspace canvas and its initial onboarding surface from light-mode `zinc-100` to `zinc-50`; dark mode remains `zinc-800`.
- Focused ESLint passed and `25` workspace acceptance tests passed. The production canvas has no targeted visual baseline. Prettier passes for the live canvas file; the onboarding file retains unrelated existing formatting differences. No deployment, commit, or push occurred.

2026-07-23 13:18 EDT - lightened the workspace canvas to white.

- Changed the live workspace canvas and initial onboarding surface from light-mode `zinc-50` to Tailwind `white`; dark mode remains `zinc-800`.
- Focused ESLint and `25` workspace acceptance tests passed. Localhost returned `200`. No deployment, commit, or push occurred.

2026-07-23 13:20 EDT - softened the white workspace canvas.

- Changed the live workspace canvas and initial onboarding surface from pure white to `#fcfcfc` in light mode; dark mode remains `zinc-800`.
- Focused ESLint and `25` workspace acceptance tests passed. No deployment, commit, or push occurred.

2026-07-23 13:51 EDT - audited workspace ontology focus and hierarchy.

- Reviewed the live dense-board screenshot against the ontology adapters, projection, layout, wayfinding, node-frame contract, and current interaction tests. The main UX problem is progressive disclosure: same-depth nodes wrap across horizontal columns, completed history has equal visual weight, multiple branches can remain expanded, and desktop wayfinding fits the full graph until actions become unreadably small.
- Recommended a guided default mode with one active root, one active group, up to three actionable leaves, completed-item rollups, strict depth columns, and a secondary full-map mode. Accelerator should preserve its existing `groupTitle` hierarchy; operational activity should become a capped temporal feed rather than a 44-node tree. Review only; no product code, tests, deployment, commit, or push changed.

2026-07-23 15:08 EDT - implemented the guided workspace ontology canvas.

- Added URL-backed Focus and Map modes. Focus now keeps one root and one canonical group path, shows the three highest-priority actions, summarizes extra and completed siblings, dims unrelated roots and sibling branches, and supports group entry from Map plus Back, Forward, and Escape focus restoration. User pan, zoom, or drag cancels pending camera framing; reduced motion disables structural and cosmetic transitions.
- Preserved each primary card's saved coordinates and replaced same-depth wrapping with strict left-to-right depth columns. Added differentiated group, action, more, and completed-rollup cards, sky active paths, calm frosted controls, and exact `#fcfcfc` light and zinc-800 dark canvases.
- Restored Accelerator phase hierarchy through the canonical lesson-group helpers and capped operational activity at the three newest records with archived counts. Added the design contract and focused acceptance coverage.
- Full ESLint, snapshots, routes, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and RLS passed. Focused ontology acceptance passed `47/47`; Playwright ontology and production node-bounds coverage passed `9/9`; the complete acceptance run passed `1,580` tests with three unrelated public-Find/fiscal failures and one unrelated missing resource-map enrichment module.
- Repository-wide typecheck remains blocked by existing public-map, member-workspace, accelerator-test, and other unrelated errors; filtered ontology output is clean. Structure and feature/scaffold gates remain blocked by unrelated oversized audio/public-map/workspace files and the incomplete `organization-kanban-visibility` feature. Localhost:3000 remains healthy. No deployment, commit, or push occurred.

2026-07-23 15:14 EDT - removed tinted workspace ontology surfaces.

- Replaced group, action, rollup, and more-node fills with the opaque `bg-background` surface while retaining status through borders, icons, and text. Removed the branch toggle's translucent background and backdrop blur.
- Focused ESLint and ontology acceptance passed; Playwright ontology and production node-bounds coverage passed `9/9`. Localhost:3000 remains healthy. No deployment, commit, or push occurred.

2026-07-23 16:41 EDT - collapsed Focus-mode ontology nodes into guided lists.

- Replaced repeated Focus-mode group and action cards with one compact list node per active hierarchy level. Lists retain direct navigation, nested group expansion, status, sibling de-emphasis, and footer actions for additional and completed work. Map and search continue to expose the complete individual-node graph.
- Updated the guided-canvas design and visual baseline. Focused formatting, ESLint, boundaries, React Grab, workspace surfaces, raw-button checks, and `45` ontology acceptance tests passed; Playwright ontology and production node-bounds coverage passed `9/9`. Filtered TypeScript output is clean, and localhost:3000 remains healthy. No deployment, commit, or push occurred.

2026-07-23 16:45 EDT - removed objectives and tasks from the ontology board.

- Deleted the ontology-only task adapter, removed the generated `Objectives and tasks` branch, and removed its Programs relationship. The existing objectives/task editor remains unchanged.
- Focused formatting, ESLint, `34` ontology acceptance tests, boundaries, React Grab, workspace surfaces, raw-button checks, and thresholds passed. Localhost:3000 remains healthy. No deployment, commit, or push occurred.

2026-07-23 17:06 EDT - opened ontology document rows in the workspace bottom sheet.

- Replaced direct list-row links with controller-owned button activation. Organization document leaves now expand the existing Storage drawer, select Documents, and focus the exact document record without changing the workspace URL; other ontology destinations retain their existing behavior.
- Focused formatting and ESLint passed, `27` ontology and drawer acceptance tests passed, and workspace-surface, React Grab, and interaction-lock checks passed. Live Chrome verification confirmed the URL remained unchanged, Documents was selected, and State Registration received focus.
- The repository-wide typecheck remains blocked by unrelated existing public-map, member-workspace, and fixture typing errors; no changed file appeared in its failures. No deployment, commit, or push occurred.

2026-07-23 17:47 EDT - removed draft and onboarding-only content from the ontology map.

- Carried canonical Accelerator class/module publication state into workspace steps and filtered unpublished modules plus workspace-onboarding modules from the ontology input. Matching Accelerator activity entries and exact draft-status program objects are also excluded, preventing Kick-off from reopening organization setup after onboarding.
- Focused ESLint, `48` acceptance tests, boundaries, React Grab, and workspace-surface checks passed. Live Map verification reduced the current graph from `74` to `70` rendered nodes and confirmed Draft Class, Draft, Kick-off, Organization setup, and both offending module IDs are absent while `45` published Accelerator nodes remain.
- The repository-wide typecheck remains blocked by unrelated existing errors; no changed file appeared in the filtered failures. No deployment, commit, or push occurred.

2026-07-23 18:00 EDT - fixed ontology Accelerator node routing.

- Completed ontology nodes were targeting hidden `lesson` steps, so the Accelerator controller rejected the requested step and fell back to its first visible step: organization setup.
- Added one shared visible-step rule for the Accelerator controller and ontology adapters. Module and activity links now select a visible video, resource, or assignment step instead of a hidden lesson or completion step.
- Focused ESLint and `52` acceptance tests passed; formatting, boundaries, interaction locks, React Grab, and workspace-surface checks passed. Live browser verification confirmed a completed node retained its requested `video` URL and did not show Create your organization. No deployment, commit, or push occurred.

2026-07-23 18:49 EDT - embedded the Accelerator in the workspace drawer.

- Added an Accelerator tab to the existing workspace bottom sheet with lazy mounting, responsive checklist/viewer layouts, access handling, persistent tab and progress state, and the existing video, resource, assignment, notes, and completion experiences.
- Canvas Accelerator cards and ontology nodes now open the exact requested module step in the drawer without changing the workspace URL. Direct Accelerator URLs remain available as deep links and modifier-click destinations.
- TypeScript, full ESLint, snapshots, RLS, focused formatting, `67` focused acceptance tests, routes, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks passed. Live Chrome verification opened exact video and assignment steps, preserved the URL and tab state, and unmounted inactive Accelerator media. The complete acceptance run passed `1,587` tests; unrelated fiscal-sponsorship, public-Find, and resource-map failures remain. Structure and feature/scaffold gates remain blocked only by unrelated existing files. No deployment, commit, or push occurred.

2026-07-24 13:31 EDT - moved Accelerator to the third workspace drawer tab.

- Reordered the workspace drawer tabs to People, Documents, then Accelerator without changing selection persistence or Accelerator routing.
- Focused formatting, ESLint, `8` drawer acceptance tests, React Grab, workspace-surface, and interaction-lock checks passed. No deployment, commit, or push occurred.

2026-07-24 13:35 EDT - removed Documents result key warnings.

- Moved each document row key before its focus-prop spread in the desktop table and mobile card renderers, preserving deep-link focus behavior while restoring React's static-child key handling.
- Focused formatting, ESLint, `10` acceptance tests, raw-button, and React Grab checks passed. Live Chrome verification rendered `31` Documents table rows with no new unique-key warnings from either renderer. No deployment, commit, or push occurred.

2026-07-24 13:40 EDT - added the Accelerator drawer banner.

- Added a compact neutral introduction banner above the Accelerator drawer content and access state, matching the Documents banner pattern without additional animation or fabricated progress data.
- Focused formatting, ESLint, `9` acceptance tests, boundaries, React Grab, workspace-surface, and raw-button checks passed. No deployment, commit, or push occurred.

2026-07-24 13:55 EDT - moved the organization profile editor into the workspace drawer.

- Added a lazy Organization drawer tab first in the list while retaining Accelerator in third position. Existing `/workspace?view=editor` links now open the requested profile tab, program, or focused field inside the bottom sheet instead of replacing the canvas.
- Organization-card edit actions and ontology profile links now open the drawer locally while modifier clicks retain the canonical deep link. The existing editor, edit permissions, program wizard, and full-height drawer control are reused.
- Focused formatting, ESLint, `57` acceptance tests, routes, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, and raw-button checks passed. Live Chrome verification confirmed the Programs deep link selected Organization and Activity with no console errors. Repository-wide TypeScript remains blocked by unrelated existing public-map, member-workspace, resource-map, and fixture errors. No deployment, commit, or push occurred.

2026-07-24 14:01 EDT - removed the organization profile banner close button.

- Removed the top-right `Return to workspace` X over the organization banner and its obsolete close callback plumbing. Restored and retained `View map profile`, Edit, Cancel, and Save.
- Focused ESLint and `17` acceptance tests passed. No deployment, commit, or push occurred.

2026-07-24 14:25 EDT - simplified the Accelerator drawer lesson layout.

- Kept the existing drawer width, safe-left spacing, canvas dimensions, shortcut size, and React Flow zoom behavior. Removed only the Accelerator drawer's lesson-view two-column split so its checklist no longer remains beside and pushes the lesson viewer right.
- Focused ESLint, `21` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured a single full-width Accelerator lesson column, the original `16px` shortcut-rail inset, a `36px` shortcut button, unchanged canvas zoom, and no console errors. No deployment, commit, or push occurred.

2026-07-24 14:29 EDT - moved canvas shortcuts behind the workspace drawer.

- Lowered the desktop and mobile shortcut overlays to `z-10` so drawer content covers them instead of canvas shortcuts appearing on top. Drawer width, spacing, shortcut size, and canvas zoom remain unchanged.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification confirmed the shortcut rail below the expanded drawer with no console errors. No deployment, commit, or push occurred.

2026-07-24 14:32 EDT - centered workspace drawer tab content.

- Removed the obsolete one-sided `4.25rem` shortcut-rail inset and replaced it with equal `32px` desktop gutters on the tab header and panels. The drawer shell width, canvas dimensions, shortcut size, and React Flow zoom remain unchanged.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification confirmed equal gutters, unchanged drawer and canvas bounds, shortcuts below the drawer, and no console errors. No deployment, commit, or push occurred.

2026-07-24 14:37 EDT - capped workspace drawer main content width.

- Centered Organization, People, Accelerator, and Documents panels within a shared `max-w-7xl` boundary while leaving the drawer shell and tab header full width.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured all four panels at `1280px` inside the unchanged `1462px` drawer with equal `91px` outer gutters and no console errors. No deployment, commit, or push occurred.

2026-07-24 14:42 EDT - centered organization profile header actions.

- Centered the View map profile and Edit action group within the organization profile header without changing button styling or drawer layout.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured a `0px` center offset with no console errors. No deployment, commit, or push occurred.

2026-07-24 14:46 EDT - stabilized organization profile tab width.

- Changed the embedded organization panel to a column layout so its profile stretches to the available width instead of shrinking to each tab's content.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured About, Activity, and People at the same `1192px` width with no console errors. No deployment, commit, or push occurred.

2026-07-24 14:51 EDT - narrowed the organization profile panel.

- Centered the embedded Organization panel within a `max-w-6xl` boundary without changing the drawer or other drawer-tab widths.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured a stable `1152px` panel and `1128px` profile width across About, Activity, and People with no console errors. No deployment, commit, or push occurred.

2026-07-24 14:53 EDT - hid the Accelerator banner during lessons.

- Connected the drawer panel to the Accelerator runtime state so the introductory banner is hidden whenever the lesson viewer is open and returns after the lesson closes.
- Focused ESLint, `10` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification confirmed the banner absent during a lesson, present after closing it, and no console errors. No deployment, commit, or push occurred.

2026-07-24 15:06 EDT - stabilized full-height drawer snapping.

- Constrained drawer updates and restored preferences to the three supported snap points. Invalid or undefined cycle requests now preserve the current snap instead of corrupting Vaul's controlled state.
- Focused ESLint, `15` acceptance tests, workspace-surface, interaction-lock, and React Grab checks passed. Live Chrome reproduction confirmed a full-height handle double-click now preserves exact canvas top and bottom bounds with no console errors. No deployment, commit, or push occurred.

2026-07-24 15:14 EDT - corrected organization header action placement.

- Returned View map profile and Edit to the profile header's top-right after clarifying that the requested centering applied to the overall organization profile content, not its actions.
- The centered `max-w-6xl` Organization panel remains unchanged. Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. Live Chrome verification measured a `24px` right inset, a centered `1152px` panel, and no console errors. No deployment, commit, or push occurred.

2026-07-24 15:28 EDT - restored the drawer Storage title.

- Reverted the unrequested top-left drawer-title copy change from Workspace back to Storage without changing tabs or shared drawer primitives.
- Focused ESLint, `9` acceptance tests, workspace-surface, and React Grab checks passed. No deployment, commit, or push occurred.

2026-07-24 15:49 EDT - surfaced newly added budget items.

- Updated Add Item so the new budget row scrolls into view and focuses its Category field in both stacked and grid layouts.
- Focused formatting, ESLint, `4` acceptance tests, interaction-lock, and React Grab checks passed. Live Chrome verification confirmed the new row was visible, its Category input retained focus after the drawer settled, and no console errors occurred. No deployment, commit, or push occurred.

2026-07-24 16:08 EDT - compacted the program budget step.

- Switched the program wizard budget editor from stacked cards to the shared TanStack grid and placed its three summary metrics in one responsive row above the table.
- Focused formatting, ESLint, `4` acceptance tests, interaction-lock, and React Grab checks passed. Live Chrome verification measured three equal `274px` summary columns on one row, confirmed the editable table rendered, and found no console errors. No deployment, commit, or push occurred.

2026-07-24 17:02 EDT - routed global search into current workspace surfaces.

- Replaced legacy Accelerator, organization, people, documents, programs, supporters, and roadmap search destinations with canonical workspace drawer or current workspace routes. Added drawer deep-link hydration so direct search navigation opens the requested tab, module, program, document, or roadmap section.
- TypeScript, focused ESLint, `20` acceptance tests, routes, thresholds, boundaries, interaction locks, React Grab, workspace surfaces, and raw-button checks passed. Live Chrome verification opened an exact Accelerator module and Documents inside the Storage drawer under `/workspace` with no console errors. No deployment, commit, or push occurred.

2026-07-24 17:13 EDT - compacted organization profile tabs.

- Changed the desktop profile tab list to content width, left alignment, compact spacing, and non-growing triggers while preserving the mobile picker and drawer-level tabs.
- Focused ESLint, `8` acceptance tests, thresholds, React Grab, and workspace-surface checks passed. Live Chrome verification measured a `272px` tab group within the `1128px` profile, aligned `24px` from its left edge with no console errors. No deployment, commit, or push occurred.

2026-07-24 17:38 EDT - aligned the organization tab separator.

- Moved the desktop separator to a full-width wrapper while keeping the tab list compact, and positioned the active indicator directly above the separator.
- Focused ESLint, `8` acceptance tests, thresholds, React Grab, and workspace-surface checks passed. Live Chrome verification measured the separator at the full `1128px` profile width and the indicator ending `1px` above its border with no console errors. No deployment, commit, or push occurred.

2026-07-27 14:34 EDT - completed the in-app fiscal sponsorship signing workflow.

- Added the verified March 2024 IRS Form W-9, an authenticated in-app completion route, field validation, typed or drawn electronic signature, complete penalties-of-perjury certification, private server-side PDF generation, immutable versioned storage, SHA-256 integrity evidence, redacted database metadata, applicant and Coach House access, review controls, and signed-copy view/download actions.
- Assigned Form B signing to the application primary-email account instead of assuming the organization owner, added direct in-app and email signing notifications, allowed authorized Coach House staff to retrieve final documents and countersign, locked applicant application edits after submission, and blocked agreement generation until an executed W-9 is accepted.
- Focused fiscal acceptance passed `16/16`; full ESLint, snapshots, routes, thresholds, boundaries, workspace-storage, interaction-lock, React Grab, workspace-surface, and raw-button checks passed. The generated signed W-9 was flattened, hash-checked, and visually inspected; localhost served the protected route and official PDF correctly.
- The complete acceptance run passed `1,596` tests with only unrelated public-Find/resource-map failures. Repository structure and feature/scaffold checks remain blocked by unrelated pre-existing oversized files and the incomplete `organization-kanban-visibility` feature; repository TypeScript remains blocked by unrelated existing errors. Recovered four remote-only migration files locally, removed the duplicate down migration generated by Supabase CLI, restored unintended CLI rewrites, applied `20260727130000` remotely, and verified migration parity with a clean dry run. The full remote RLS suite passed. No application deployment, commit, or push occurred.

2026-07-27 16:30 EDT - linked fiscal intake to the program budget.

- Replaced the fiscal application’s legacy three-column budget editor with the shared program-builder grid for category, description, cost type, unit, quantity, unit cost, total, reordering, and removal.
- Preserved structured budget rows from the selected activity through fiscal prefill and draft metadata, synchronized saved fiscal rows and totals back to the source program, and reloaded the draft when the selected activity changes.
- Filtered zero-only legacy summaries and empty program rows so `$0.00; $0.00` no longer appears as editable content. CSV import, budget-support uploads, derived totals, and agreement-ready expense summaries remain connected.
- Focused ESLint, `15` focused acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace surfaces, raw-button checks, and diff checks passed. Live Chrome verification confirmed the selected Idea to Impact Accelerator budget populated the complete grid with a `$7,000` total, no malformed zero summary, and no fiscal-flow console errors. Repository-wide TypeScript, structure, and feature checks remain blocked only by unrelated pre-existing errors, oversized files, and the incomplete `organization-kanban-visibility` feature. No deployment, commit, or push occurred.

2026-07-27 16:41 EDT - contained uploaded fiscal documents in the workflow drawer.

- Constrained the fiscal workflow scroll root, Radix viewport wrapper, tab content, and uploaded-document text so long filenames cannot expand or clip beyond the drawer’s right edge. Shared Sheet and ScrollArea primitives remain unchanged.
- Focused formatting, ESLint, `10` acceptance tests, boundaries, thresholds, interaction locks, React Grab, and workspace-surface checks passed. Live Chrome verification against the existing uploaded PDFs measured equal bounded viewport/content widths with no horizontal overflow and no fiscal-flow console errors. No deployment, commit, or push occurred.

2026-07-27 17:35 EDT - repaired the fiscal sponsorship client/server boundary.

- Replaced the fiscal action re-export barrel with explicit async Server Action wrappers, keeping application drafts, document uploads, native signing, W-9 completion, workflow review, and agreement actions out of the client module graph.
- Moved account-settings active-organization lookup behind an authenticated Server Action so the browser no longer imports the server-only cookie helper.
- Removed the stale 19 GB Next cache and restarted localhost on port 3000. Authenticated Chrome verification loaded the organization workbench, opened the fiscal application editor, and completed saved-draft loading without a Runtime Error or Build Error.
- Focused ESLint, boundaries, routes, `33` fiscal/member-workspace acceptance tests, and the Turbopack production compilation passed. The full build remains blocked during TypeScript checking by the unrelated existing one-argument `revalidateTag` call in `src/actions/public-map-organization-curation.ts`; structure and feature checks remain blocked by existing oversized files and the incomplete `organization-kanban-visibility` scaffold. No deployment, commit, or push occurred.

2026-07-28 18:36 EDT - aligned fiscal document badges and actions.

- Updated the fiscal sponsorship document and signing rows so status badges use the established amber treatment and each action group stays aligned to the far right.
- Focused formatting, ESLint, `3` acceptance tests, boundaries, thresholds, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-28 18:55 EDT - completed fiscal workbench controls and recent updates.

- Made the workbench Edit control open and scroll to the inline application, replaced agreement-generation wording with Prepare agreement and Send for signature, hid unavailable actions, surfaced action failures inline, and prevented duplicate agreement preparation.
- Moved Recent updates above the right-column Time card with a separator, four-item shadcn pagination, reduced-motion-aware page animation, real document links, and in-page destinations for application, uploads, review, and signing activity.
- Focused formatting, ESLint, `25` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Repository-wide TypeScript, structure, and feature checks remain blocked by unrelated existing errors, oversized files, and the incomplete `organization-kanban-visibility` scaffold. No deployment, commit, or push occurred.

2026-07-28 19:56 EDT - corrected the accepted W-9 agreement gate.

- Confirmed through a read-only remote check that the accepted W-9 row actually referenced `Form-B-Fiscal-Sponsorship-Agreement-Clean-Blank.pdf` as a draft application document, so it did not satisfy the completed W-9 evidence gate.
- Added explicit W-9 review handling: accepted uploaded PDFs become executed tax-form records, obvious Form B agreements are rejected with a precise error, assigned reviewers use the scoped server-side review path, and legacy mismatches display Needs signed W-9 instead of Accepted.
- Agreement preparation now stays unavailable until an executed W-9 is accepted, the next step names that requirement, and reviewers can replace an incorrect W-9 PDF in place. Focused ESLint, `47` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed. No database record was changed. No deployment, commit, or push occurred.

2026-07-28 20:11 EDT - removed the browser auth lock timeout.

- Upgraded `@supabase/supabase-js` from `2.97.0` to the Node 20-compatible `2.109.0`, whose auth client uses lockless refresh coordination instead of the default Navigator LockManager path that produced the ten-second runtime timeout.
- Confirmed the installed Supabase auth implementation is `2.109.0` and has no default `navigator.locks` mutex. Fiscal sponsorship, native signing, W-9, and notification regressions passed `18/18`; the restarted localhost returned `200` on `/find`.
- Production compilation passed, then the build remained blocked by the unrelated existing one-argument `revalidateTag` call in `src/actions/public-map-organization-curation.ts`. No deployment, commit, or push occurred.

2026-07-28 21:14 EDT - opened roadmap sections in the workspace drawer.

- Routed workspace roadmap table-of-contents and search links to the existing Storage drawer, added a Roadmap tab with the embedded editor, preserved exact section deep links, and synchronized section changes while the drawer remains mounted.
- Removed the redundant Return To Workspace control while preserving direct roadmap routes for bookmarks.
- Focused ESLint, `31` acceptance tests, routes, boundaries, interaction locks, React Grab, workspace surfaces, raw-button, and diff checks passed. Localhost accepted the protected roadmap drawer URL and redirected unauthenticated requests correctly. Production compilation passed; the full build remains blocked by the unrelated existing one-argument `revalidateTag` call in `src/actions/public-map-organization-curation.ts:55`. No deployment, commit, or push occurred.

2026-07-28 21:20 EDT - removed the visible workspace drawer title.

- Removed the visible Storage label from the workspace drawer while retaining a screen-reader-only title and explicit grid placement for the centered handle and right-aligned fullscreen control.
- Focused ESLint, `10` acceptance tests, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-28 21:27 EDT - made drawer-header double-click collapse reliably.

- Disabled Vaul's delayed handle snap cycling for this workspace drawer and routed a header or handle double-click directly to its compact collapsed snap point; fullscreen-button double-clicks remain isolated.
- Focused ESLint, `10` acceptance tests, interaction-lock, React Grab, workspace-surface, and diff checks passed. No deployment, commit, or push occurred.

2026-07-28 21:58 EDT - diagnosed intermittent workspace drawer width.

- Confirmed the settled drawer and its canvas portal container share exact `1,171px` left/right bounds, their scroll widths equal their client widths, and the `1,501px` viewport has no horizontal overflow.
- Traced the intermittent wide first paint to asynchronous right-rail registration: `RightRailSlot` declares presence in a passive effect while the drawer mounts from its canvas ref independently, and the shell then closes/reopens or reallocates the desktop right rail. The drawer correctly uses `w-full` against the temporarily wider canvas before the shell settles.
- No implementation code changed. The appropriate future fix is to stabilize right-rail presence before the drawer can paint, with a container-width clamp as defensive hardening. No deployment, commit, or push occurred.

2026-07-28 22:15 EDT - stabilized workspace drawer loading geometry.

- Made right-rail state initialize identically on the server and client, removing the shell hydration mismatch, and delayed drawer content until the real workspace rail reaches its first open state.
- Suppressed the embedded roadmap editor's duplicate right-rail registration and added explicit minimum/maximum width containment through the workspace canvas, flow frame, and drawer without narrowing the existing full-bleed shell.
- Focused ESLint, `22` acceptance tests, boundaries, interaction locks, React Grab, workspace surfaces, raw-button, and diff checks passed. Authenticated reload verification measured matching `1,171px` drawer/canvas bounds, a `272px` right rail, one Strategic Roadmap rail, no horizontal overflow, and no new hydration or application errors. Production compilation passed; the full build remains blocked by the unrelated existing one-argument `revalidateTag` call in `src/actions/public-map-organization-curation.ts:55`. No deployment, commit, or push occurred.

2026-07-28 23:06 EDT - renamed the roadmap navigator trigger.

- Changed the `RoadmapNavigatorSection` header button label from Strategic Roadmap to Build without altering its behavior, styling, or ownership metadata.
- Focused formatting, ESLint, `16` acceptance tests, React Grab, raw-button, and diff checks passed. No roadmap visual snapshot covers this trigger, so no baseline changed. No deployment, commit, or push occurred.

2026-07-28 23:10 EDT - hid canvas viewport controls behind the fullscreen drawer.

- Hid the complete canvas viewport-control cluster whenever the workspace drawer reaches its full-height snap point, using the drawer's existing fullscreen state without adding duplicate React state or effects.
- Focused formatting, ESLint, `12` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification confirmed the cluster changed from visible to `display: none` at full height and returned after restoring the drawer, with no console errors. No visual snapshot exercises this drawer state, so no baseline changed. No deployment, commit, or push occurred.

2026-07-29 12:54 EDT - widened the organization setup rail.

- Increased the project detail sidebar's desktop width cap from `320px` to `384px`, giving the organization setup section more horizontal room without changing mobile layout or shared HoverCard primitives.
- Focused formatting, ESLint, `25` acceptance tests, React Grab, workspace-surface, and diff checks passed. Authenticated Chrome verification measured the setup trigger increasing from `229px` to `293px` at the existing `1,501px` viewport with no application console errors. No visual baseline changed. No deployment, commit, or push occurred.

2026-07-29 13:20 EDT - removed source-card duplicates from the ontology map.

- Added source-card-only ontology visibility and applied it to the Fiscal Sponsorship application plus every Accelerator phase and module already represented inside the primary Accelerator card. Hidden branches no longer create canvas nodes, edges, counts, or search results; Required documents remains available.
- Focused formatting, ESLint, `37` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification found zero Accelerator detail nodes, zero Fiscal Application nodes, the Fiscal documents branch preserved, and no application console errors. No visual baseline changed. No deployment, commit, or push occurred.

2026-07-29 14:04 EDT - distilled the live workspace canvas.

- Disabled the dense ontology projection on the live canvas without deleting its data or implementation, removed the Focus and Map mode controls, and kept ontology details available through their owning drawer surfaces. This follows `docs/design.md` by restoring a restrained hierarchy with primary content on neutral surfaces.
- Hid the Accelerator card from the normal canvas while preserving tutorial rendering. Moved its progress summary into the Accelerator drawer, directly left of the class picker in a right-aligned responsive control cluster.
- Added a viewport-layout revision that clears only the stale pre-simplification camera once, preserving other drawer and people preferences before fitting the remaining Organization and Activity cards.
- Focused formatting, ESLint, `54` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification found zero ontology nodes, zero Accelerator canvas cards, zero Focus or Map controls, both remaining cards visible, and the progress rail left of the picker at the drawer's right edge. Repository-wide TypeScript remains blocked by existing unrelated errors; no touched file appeared in its failures. No deployment, commit, or push occurred.

2026-07-29 14:09 EDT - left-aligned accelerator drawer progress.

- Expanded the Accelerator drawer header across the available width so progress anchors left and the class picker remains right, with balanced vertical spacing.
- Promoted the checklist label to a `text-sm` semibold section heading.
- Focused formatting, ESLint, `24` acceptance tests, boundaries, thresholds, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification measured 12px top/bottom header padding, a 512px left-aligned progress cluster, a right-aligned picker, and 14px/600 checklist typography. No deployment, commit, or push occurred.

2026-07-29 14:11 EDT - traced the unexpected Accelerator class label.

- Confirmed `Published Class Updated` is a leaked synthetic RLS-test class in the configured remote Supabase project, not product curriculum. The test creates `Published Class`, renames it during its admin-update assertion, and only cleans fixtures at the successful end of the runner.
- The workspace checklist repeats the selected class group title, while the legacy-class filter only recognizes the pre-update title or fixed `published-class` slug; the generated updated fixture therefore remains visible after an interrupted run.
- No database record or implementation code was changed pending explicit cleanup authorization.

2026-07-29 14:14 EDT - hid the live Accelerator shortcut.

- Removed the Accelerator button from the normal desktop and mobile workspace shortcut rails while preserving its implementation for the Accelerator tutorial step.
- Focused formatting, ESLint, `28` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification found zero Accelerator shortcuts while Activity and Data remained available. No deployment, commit, or push occurred.

2026-07-29 14:29 EDT - temporarily restored the Accelerator canvas card.

- Forced the Accelerator card back into the live and tutorial canvas card sets despite persisted hidden state, while keeping its shortcut and ontology details hidden.
- Focused formatting, ESLint, `37` acceptance tests, boundaries, thresholds, workspace-surface, and diff checks passed. Authenticated Chrome verification found one Accelerator canvas node and zero Accelerator shortcuts after recentering. No deployment, commit, or push occurred.

2026-07-29 15:22 EDT - re-hid Accelerator and simplified its drawer controls.

- Removed the temporary live/tutorial visibility override so the Accelerator card is hidden from the normal canvas again while its authored tutorial scenes remain available.
- Removed the duplicate checklist title row, retaining the embedded picker path used by the tutorial, and enlarged the badge picker from 28px to 32px with 12px side padding and a wider width cap.
- Focused formatting, ESLint, `68` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated Chrome verification found zero Accelerator canvas nodes, zero Accelerator shortcuts, zero duplicate checklist header rows, and a 32px picker. No deployment, commit, or push occurred.

2026-07-29 15:30 EDT - removed the leaked Accelerator test class.

- Deleted the exact synthetic `Published Class Updated` RLS-test class and its dependent fixture rows from the configured Supabase project, then verified the class no longer exists. Recovery would require a database backup.
- Added one shared non-product class filter for the legacy title, updated title, fixed slug, and generated `published-<8 hex>` test slugs; applied it to workspace progress, the Accelerator overview, and the app sidebar.
- Focused formatting, ESLint, `36` acceptance tests, boundaries, thresholds, and diff checks passed. Authenticated browser verification found one picker, 11 real options, and zero `Published Class Updated` options. No deployment, commit, or push occurred.

2026-07-29 16:02 EDT - renamed the roadmap navigator trigger.

- Changed the `RoadmapNavigatorSection` header label from Build to Core Documents without altering behavior, styling, or ownership metadata.
- Focused formatting, ESLint, `17` acceptance tests, and diff checks passed. No deployment, commit, or push occurred.

2026-07-29 16:49 EDT - hid the Roadmap drawer tab.

- Removed the Roadmap trigger while preserving its drawer content for direct Core Documents links, and reordered the visible tabs to Organization, People, Documents, Accelerator.
- Focused ESLint, `16` acceptance tests, interaction locks, React Grab, workspace surfaces, and diff checks passed. Authenticated browser verification found exactly those four visible tabs in that order and zero Roadmap triggers. No deployment, commit, or push occurred.

2026-07-29 16:59 EDT - made Core Documents open instantly at full height.

- Replaced route navigation from the workspace Roadmap navigator with a synchronous drawer request shared by the right rail and canvas card, retaining URL navigation only as a fallback outside the workspace surface.
- Handled drawer requests before paint and forced Roadmap requests to the full-height snap point while selecting the requested document.
- Focused formatting, ESLint, `17` acceptance tests, boundaries, interaction locks, React Grab, workspace surfaces, and diff checks passed. Authenticated browser verification opened Need in the full-height drawer with the workspace URL unchanged; the selected panel was ready when the click returned in `283ms`. No deployment, commit, or push occurred.

2026-07-29 17:08 EDT - contained organization link cards.

- Added minimum-width containment to shared organization profile fields and link cards so long URLs truncate inside their grid column instead of overlapping neighboring cards.
- Focused formatting, ESLint, `13` acceptance tests, boundaries, thresholds, and diff checks passed. Authenticated browser measurement showed the Facebook and LinkedIn cards shrink from `396px` and `350px` to their `308px` columns, with zero remaining intersections. No deployment, commit, or push occurred.

2026-07-29 17:12 EDT - stopped brand-image edge clipping.

- Inset image-based `BrandLink` content by 4px inside its existing 80px bordered card so rounded overflow no longer clips the asset edges.
- Focused formatting, ESLint, `13` acceptance tests, boundaries, thresholds, and diff checks passed. Authenticated browser measurement confirmed a consistent `5px` rendered inset on every side. No deployment, commit, or push occurred.

2026-07-29 17:13 EDT - shortened the organization Need label.

- Changed Need statement to Need in both internal and public organization profile displays. The supplied `config.ts` pointer was stale; the visible copy is owned by the two profile renderers.
- Focused ESLint, `13` acceptance tests, boundaries, and diff checks passed. No deployment, commit, or push occurred.

2026-07-29 17:38 EDT - restored rounded brand-image clipping.

- Reverted the internal `BrandLink` image inset so image assets fill the existing 80px rounded, overflow-hidden card and sharp file corners are masked again; retained link-card width containment.
- Focused formatting, ESLint, `13` acceptance tests, boundaries, thresholds, and diff checks passed. Authenticated browser verification measured an 80px rounded, overflow-hidden container with the contained image filling its 78px interior. No deployment, commit, or push occurred.

2026-07-29 17:44 EDT - made the Brand Kit logo fill its preview.

- Changed the image-based `BrandLink` from contain to cover, matching the organization header logo behavior so the asset fills the rounded card without exposing square file corners.
- Focused formatting, ESLint, `13` acceptance tests, boundaries, thresholds, and diff checks passed. No deployment, commit, or push occurred.

2026-07-29 17:47 EDT - clarified the Accelerator banner heading.

- Changed the banner heading to `Build your organization with guided lessons.` and corrected the supplied spelling.
- Focused formatting, ESLint, the banner acceptance test, and diff checks passed. No deployment, commit, or push occurred.

2026-07-29 18:08 EDT - routed People management into the workspace drawer.

- Replaced both stale `/people` destinations in the organization profile People panel with the canonical workspace People drawer destination.
- Focused formatting, ESLint, `14` acceptance tests, boundaries, and diff checks passed. Authenticated browser verification confirmed the button opens the workspace drawer's People tab instead of the legacy page. No deployment, commit, or push occurred.

2026-07-29 18:30 EDT - completed the Organization Brand Kit editor.

- Added an inline Brand Kit section to Organization edit mode with named Primary, Dark, Light, and Accent color controls; heading and body font pickers; immediate-save Primary and Mark logo uploads; and structured Audience, Tone, Style, Personality, Guidelines, and Avoid fields that use the existing Organization Save/Discard flow.
- Replaced visible Boilerplate UI in the Organization profile, Brand Kit panel, Brand Kit sheet, and public profile with the structured Brand Voice model. Updated profile hydration, validation, sanitization, readiness, download manifests, and plain-text exports while retaining the legacy stored field for backward compatibility.
- Focused formatting, ESLint, `6` Brand Kit acceptance tests, boundaries, thresholds, React Grab, raw-button, workspace-surface, and diff checks passed. Authenticated browser verification confirmed the full edit surface and working color popover without saving profile changes. Repository-wide TypeScript, structure, and feature-contract checks remain blocked by existing unrelated dirty-worktree failures; no touched file appeared in the TypeScript failures. No visual baseline changed. No deployment, commit, or push occurred.

2026-07-29 18:34 EDT - expanded the database Brand Kit contract.

- Added and applied migration `20260729223500_expand_org_brand_kit_profile_contract.sql` to the linked Supabase project. The migration documents all Brand Kit asset, color, typography, and Brand Voice keys stored in the existing additive `organizations.profile` JSONB column; it does not rewrite data or alter RLS.
- Added database-to-UI hydration and migration-contract coverage for the complete Brand Kit shape. Focused formatting, ESLint, `8` acceptance tests, and diff checks passed. Supabase migration history confirms `20260729223500` is present locally and remotely. No application deployment, commit, or push occurred.

2026-07-29 18:37 EDT - compacted the Organization Brand Kit section.

- Removed the Brand Kit-only stacked `FormRow` override from edit and read-only profile views, returning the section to the standard label-left/content-right profile layout without changing the shared primitive.
- Reduced the nested Brand Kit container radius, padding, and section gaps. Focused formatting, ESLint, `8` acceptance tests, and diff checks passed. Authenticated browser verification confirmed the compact split layout in both view and edit modes without saving profile changes. No deployment, commit, or push occurred.

2026-07-29 18:40 EDT - rounded the Brand Kit color swatch.

- Rounded and border-normalized the native Chromium and Firefox color swatches inside the existing Brand Kit color input, leaving the shared Input primitive unchanged.
- Focused formatting, ESLint, `8` acceptance tests, and diff checks passed. Authenticated browser verification confirmed the selected color now fills a rounded inner swatch. No profile changes were saved; no deployment, commit, or push occurred.

2026-07-29 19:19 EDT - surfaced calendar event creation.

- Moved the existing Add event menu from the Core Documents calendar agenda footer into the month header so it remains immediately visible without duplicating the control or changing calendar permissions.
- Focused ESLint, `15` acceptance tests, React Grab, raw-button, and diff checks passed. Authenticated browser verification confirmed the enabled header button opens all four existing event choices. No event was created; no deployment, commit, or push occurred.

2026-07-30 16:31 EDT - replaced the Budget prose editor with the shared budget table.

- Kept Budget in Core Documents but replaced its rich-text body with the existing responsive TanStack budget table, including line-item creation, calculated totals, row reordering/removal, manual save, autosave, mobile stacked rows, and read-only behavior.
- Added normalized `budgetRows` persistence to the existing roadmap section data stored in `organizations.profile` JSONB. Existing Budget prose remains stored but is no longer displayed, and Program budgets remain separate.
- Focused ESLint, `40` roadmap/program tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated browser verification confirmed one enabled editable table, visible Add line item, disabled Saved state, and zero rich-text toolbar controls. Repository-wide TypeScript and structure checks remain blocked by existing unrelated failures; no scoped TypeScript errors were found. No budget data was changed during browser QA; no deployment, commit, or push occurred.

2026-07-30 16:42 EDT - stabilized the calendar event drawer lifecycle.

- Stopped the shared controlled drawer from changing Vaul's modal mode during its closing render. Vaul's overlay conditionally skips a hook in non-modal mode, so the previous open-to-closed mode change caused the reported hook-order crash after saving or closing an event.
- Retained the existing interaction-lock cleanup and added a regression assertion against the unsafe modal expression. Focused ESLint, `19` calendar/interaction-lock tests, interaction locks, React Grab, raw-button, and diff checks passed. Browser automation was unavailable for this verification; no event data changed. No deployment, commit, or push occurred.

2026-07-30 17:22 EDT - tightened the workspace drawer tab spacing.

- Reduced the drawer tab header's bottom padding from 6px to 2px, keeping a small divider separation while removing excess space below the tab row.
- Focused ESLint, `10` drawer tests, React Grab, workspace-surface, raw-button, and diff checks passed. Browser automation was unavailable for this verification. No deployment, commit, or push occurred.

2026-07-30 17:23 EDT - flattened the team-access header action.

- Removed the drop shadow from the workspace team-access trigger while retaining its border, background, and hover treatment.
- Focused ESLint, `2` acceptance tests, React Grab, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-30 18:49 EDT - repaired the fiscal sponsorship W-9-to-agreement handoff.

- Traced the live Coach House Solutions Group application: it is approved, but all three accepted required-document records were blank Form B agreements; the W-9 record was an application draft rather than an executed tax form, so agreement preparation correctly remained locked while the UI misleadingly showed Accepted.
- Added direct in-app W-9 access to the organization-side workflow drawer, made misclassified files show an actionable incomplete state on both member and coach surfaces, excluded them from readiness, and blocked Form B files from being uploaded or connected as supporting documents.
- Added and applied migration `20260730223700_repair_misclassified_fiscal_documents.sql`. The three affected records now retain their evidence but are marked `needs_info` with specific replacement instructions; local and remote migration history match.
- Focused ESLint, `30` fiscal/workspace acceptance tests, RLS, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Repository-wide TypeScript remains blocked by unrelated dirty-worktree errors, with no scoped fiscal sponsorship errors. No application deployment, commit, or push occurred.
- Next test step: the assigned applicant completes the W-9 in Coach House, the assigned coach accepts it, prepares and sends Form B, the applicant signs, then the coach countersigns.

2026-07-30 19:50 EDT - restored the human-reviewed fiscal sponsorship test flow.

- Removed the mistaken filename-based document rejection and mismatch UI. Uploaded PDFs are again governed by the existing human review decision; accepting a PDF in the W-9 slot classifies it as the executed tax-form prerequisite.
- Added and applied compensating migration `20260730223800_restore_human_reviewed_fiscal_documents.sql`. It restored only the records changed by the prior repair. The live Coach House Solutions Group application is approved, its W-9 prerequisite is accepted and executed, and agreement preparation is unlocked.
- Confirmed the workflow uses native Coach House signing rather than DocuSeal: the applicant can correct 13 mapped Form B fields, consent, and type or draw a signature; the assigned coach countersigns after the applicant. Added an immediate read-only coach signing preview while the applicant is pending.
- Rendered and visually inspected all four generated Form B pages with both signatures. Fields and signature blocks fit without clipping or overlap. Focused ESLint and `12` signing/W-9 tests passed. The broader fiscal suite passed `46/47`; its unrelated existing budget-prefill expectation still expects `$25,000` while current budget rows total `$12,000`. No application deployment, commit, or push occurred.

2026-07-30 20:19 EDT - routed applicants into native Form B signing.

- Corrected the organization workflow drawer's Agreement action to prefer the assigned applicant or coach native signing URL over the raw agreement PDF once a signature packet is available.
- Kept native signing navigation in the current app tab across the drawer, phase timeline, and workbench. The action now says `Review and sign` for applicants and `Countersign` for Coach House; raw PDF viewing remains a separate document action.
- Focused ESLint, `13` signing/W-9 tests, boundaries, interaction locks, React Grab, raw-button, and diff checks passed. No signature packet or user data was created during QA. No deployment, commit, or push occurred.

2026-07-30 20:38 EDT - corrected fiscal signature status colors.

- Replaced link-availability-based completion with explicit signer completion state in the fiscal workflow drawer. `Needs signature` and `Waiting for applicant` remain amber; only signed applicant or coach actions render green.
- Focused formatting, ESLint, `10` signing/drawer tests, boundaries, React Grab, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-30 20:48 EDT - reset workspace geometry when switching organizations.

- Keyed the workspace canvas by organization so an active-organization switch creates a fresh React Flow instance, reloads the correct organization-scoped viewport and drawer preferences, and remeasures the drawer portal against the current canvas frame.
- Focused formatting, ESLint, `16` organization-switch/drawer/app-shell tests, boundaries, thresholds, workspace storage, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated browser measurement was unavailable because the connected Chrome session could not be reached. No deployment, commit, or push occurred.

2026-07-30 20:58 EDT - routed fiscal readiness fixes to their fields.

- Replaced the Activity card's generic missing-info link with criterion-aware drawer requests. Impact, location, funding, and mission rows open the selected Activity at the relevant wizard step; tax and mailing opens the Organization editor at EIN or Address. Repeated requests reopen the requested editor state.
- Focused formatting, ESLint, TypeScript, `23` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Authenticated browser verification was unavailable. No deployment, commit, or push occurred.

2026-07-30 21:20 EDT - made organization workspace switching atomic.

- Diagnosed the reported clipped canvas as mixed organization state: the shell showed Southside while the canvas, drawer, and saved camera still rendered Coach House.
- Replaced the partial router refresh after the active-organization cookie update with a full document reload so the shell, workspace seed, React Flow instance, drawer, and organization-scoped preferences switch together.
- Authenticated browser verification passed in both directions between Coach House and Southside. The shell, drawer, nodes, camera, and settled control insets matched each organization. Focused ESLint, `3` organization-switch tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-30 21:31 EDT - outlined the organization profile header.

- Added a thin theme-aware border to the empty organization profile header surface.
- Focused ESLint and diff checks passed. No deployment, commit, or push occurred.

2026-07-30 21:34 EDT - stabilized program budget editing.

- Stopped the TanStack budget grid from rebuilding its column renderers on every row update, which had remounted the active input and dropped focus after each character.
- Kept row totals and edit handlers current through stable callbacks. Added focused regression coverage.
- Authenticated browser verification entered a full phrase continuously while the input retained focus; the temporary value was cleared and remained cleared after autosave. Focused ESLint, tests, boundaries, thresholds, interaction locks, React Grab, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-30 21:59 EDT - corrected the organization header border.

- Moved the border from the clipped gradient child to the rounded header container so the outline follows the top corners.
- Focused ESLint and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 15:25 EDT - enabled trackpad canvas panning.

- Enabled React Flow pan-on-scroll for the workspace canvas so two-finger trackpad gestures move horizontally and vertically. Pinch zoom, mouse dragging, and viewport controls remain enabled.
- Focused ESLint, `2` viewport-control tests, interaction locks, React Grab, workspace-surface, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 15:28 EDT - accelerated trackpad canvas panning.

- Increased the workspace canvas pan-on-scroll speed from React Flow's `0.5` default to `0.8`.
- Focused ESLint, `2` viewport-control tests, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 15:35 EDT - focused canvas cards on double-click.

- Connected React Flow node double-clicks to the existing focus-card camera command, preserving its padding, zoom bounds, motion, and reduced-motion behavior. Double-clicks on nested buttons, links, and form controls remain available to those controls.
- Authenticated browser verification confirmed the Organization card moved from an off-center position to the exact canvas center at the configured `1.15` zoom cap. Focused ESLint, `11` camera tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 15:36 EDT - updated workspace canvas help gestures.

- Added two-finger trackpad panning and double-click card focus to Canvas help, and corrected zoom guidance for pinch or Meta/Control plus scroll.
- Focused ESLint, `3` viewport-help tests, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 15:50 EDT - exposed workspace tabs in the collapsed drawer.

- Kept Organization, People, Documents, and Accelerator visible at the collapsed snap point, reduced the space above them, and switched the collapsed peek to a fixed 68px height so it stays usable across canvas sizes.
- Kept drawer panels unmounted while collapsed; clicking any visible tab selects it and immediately opens the drawer to its normal height. The active-tab indicator remains visible in the collapsed row.
- Authenticated browser verification confirmed all four tabs are visible when collapsed and People opens its full panel. Focused ESLint, TypeScript, `17` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:00 EDT - removed pointer-selection outlines from workspace cards.

- Stopped React Flow's mouse-selected state from drawing an outline around workspace, person, and ontology cards. Keyboard focus-visible outlines remain.
- Authenticated browser verification confirmed a clicked card remains selected without an outline. Focused ESLint, `27` tests, interaction locks, workspace-surface, React Grab, raw-button, and diff checks passed.
- Cleared generated `.next` output and restarted the existing `coach-house-dev` localhost session because Turbopack was serving the stale global stylesheet. No deployment, commit, or push occurred.

2026-07-31 16:07 EDT - tightened the collapsed workspace drawer tabs.

- Reduced the collapsed drawer snap from 68px to 44px, compressed its handle band to 8px, and enforced the intended 28px tab-list height. Existing 52px and 68px saved preferences migrate to the new collapsed snap.
- Hid the fullscreen control only while collapsed; Organization, People, Documents, and Accelerator remain visible and selecting one restores the normal drawer header and opens its panel.
- Authenticated browser measurement reduced the space above the tabs from 35px to 9px. Focused ESLint, `17` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:17 EDT - restored the workspace drawer handle position.

- Restored the drawer's original 68px collapsed snap, 32px handle header, and visible fullscreen control. Tightened only the tab row with a collapsed-state negative margin and retained its intended 28px height.
- Authenticated browser measurement confirmed the handle remains at its original 15.5px inset while the tabs move from 35px to 21px below the drawer top. Selecting People still opens the full panel.
- Focused ESLint, `17` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:21 EDT - restyled the canvas card edit action.

- Replaced the primary text treatment with the Notifications-style 36px ghost icon button while preserving the pencil icon, edit route, drawer-opening behavior, and accessible `Edit card` label.
- Authenticated browser verification confirmed a transparent background, no shadow, one icon, and the expected ghost icon-button classes. Focused ESLint, acceptance coverage, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:24 EDT - hid the collapsed drawer tab separator.

- Removed the full-width tab-row divider only at the collapsed snap point. The active-tab indicator remains, and the divider returns when a tab opens the drawer.
- Authenticated browser verification confirmed a 0px collapsed divider and 1px open divider. Focused ESLint, `10` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:26 EDT - lowered the workspace drawer fullscreen toggle.

- Shifted only the fullscreen toggle down 4px with a static transform. Its 32px size, drawer layout, and handle position remain unchanged.
- Authenticated browser verification confirmed the new 4px top inset. Focused ESLint, `10` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:28 EDT - opened collapsed drawer tabs at full height.

- Changed Organization, People, Documents, and Accelerator tab clicks at the collapsed snap point to open the drawer directly at its full-height snap while preserving tab selection.
- Authenticated browser verification confirmed Documents opened flush to the canvas top with fullscreen state active. Focused ESLint, `10` acceptance tests, boundaries, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:36 EDT - tightened workspace card header spacing.

- Reduced the shared canvas-card header inset by 4px above and below without changing the global Card or CardTitle primitives.
- Authenticated browser verification confirmed Organization and Activity use 0px internal top padding, a 4px negative top inset, and 8px bottom padding. Focused ESLint, `7` acceptance tests, boundaries, thresholds, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-07-31 16:49 EDT - made canvas focus drawer-aware and opened Organization in edit mode.

- Centered double-clicked cards within the usable React Flow area above an overlapping half-height drawer while preserving the existing focus padding, zoom limits, animation, and reduced-motion handling.
- Made the Organization card edit action open the Organization drawer at its normal height with the profile already in edit mode. Direct editor URLs now preserve the same initial editing state.
- Focused ESLint, TypeScript, `28` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, and raw-button checks passed. The full structure check remains blocked by existing oversized files elsewhere in the dirty worktree. Browser verification could not complete because the local browser connection stalled. No deployment, commit, or push occurred.

2026-07-31 16:56 EDT - added adaptive Organization editor scrolling.

- Made the Organization drawer editor detect constrained-height overflow and activate the existing vertical scroll-fade treatment only when content no longer fits. The scroll state updates across viewport resizing, editor mode changes, and dynamic content changes.
- Preserved native vertical scrolling, overscroll containment, stable scrollbar spacing, and momentum scrolling on touch devices.
- Focused ESLint, `11` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Repository-wide TypeScript remains blocked by unrelated existing errors outside the touched files. No deployment, commit, or push occurred.

2026-07-31 17:05 EDT - color-coded accelerator lesson and picker icons.

- Shared the progress rail's Build amber, Fundable emerald, and Verified sky palette with the accelerator checklist and class picker.
- Colored each lesson and class-picker option icon by its global accelerator segment. The selected picker icon retains the selected option's segment color. Existing icons and status badges remain as non-color state cues.
- Kept the checklist and picker icon containers neutral; only the SVG icons receive the progress-segment color.
- Brightened the SVG strokes to saturated `600` shades in light mode and `400` shades in dark mode without adding icon backgrounds.
- Recolored checklist icon containers that were already filled to the matching segment fill and switched their SVG strokes to white. Icons without a filled container retain their saturated stage-colored strokes.
- Removed the colored circular wrappers from progress-rail tooltip icons and rendered larger stage-colored SVGs directly.
- Replaced the class picker's purple-tinted badge background with the checklist's muted neutral surface and lightened its dropdown panel with the same muted surface family.
- Added the missing vertical scroll owner to the Accelerator drawer checklist so constrained-height and resized layouts scroll with mouse, trackpad, and touch while the shared tab shell retains overflow containment.
- Moved the Accelerator banner into that scroll viewport so the banner, progress controls, and lesson list scroll together as one drawer page.
- Matched the Documents banner's one-time 600ms soft-pop animation on the Accelerator banner's leading icon, including the reduced-motion fallback.
- Replaced the Accelerator drawer's always-on overflow with measured overflow detection. The shared ScrollFadeEffect and vertical scrolling now activate only when resized or dynamic content exceeds the available height.
- Removed the sidebar's forced full-height minimum inside the banner scroll stack, preventing the banner plus an artificially full-height checklist from creating false overflow when unused space remains.
- Normalized the Activity card's Add and fiscal sponsorship actions to the card Edit action's 36px neutral ghost icon-button treatment. Add now uses an accessible plus icon, and the transparent FS mark preserves tooltips, eligibility state, and click behavior.
- Corrected the invisible hover caused by the canvas card's `muted` background matching the standard `accent` hover token. Card-surface header actions now use a contrasting background hover in light and dark modes, covering Add, FS, Edit, and overflow actions without changing frame-surface buttons.
- Removed the unexplained “Signals only. Coach House reviews.” subtitle from the fiscal sponsorship action tooltip and tightened the remaining title row.
- Focused ESLint, `14` acceptance tests, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed after the Activity action correction; earlier thresholds, boundaries, and TypeScript filtering remain valid. Repository-wide TypeScript remains blocked by unrelated existing errors outside the touched files. No deployment, commit, or push occurred.

2026-07-31 19:17 EDT - stabilized requested-field drawer collapse and fullscreen transitions.

- Kept the active workspace drawer panel mounted while collapsing, delayed its visual hiding until the 500ms snap animation completes, and made the collapsed body inert so double-click collapse no longer produces an interactive blank half-height drawer or discards form state.
- Contained Organization deep-link focus scrolling inside the existing editor scroll viewport and disabled Vaul input repositioning for this canvas-local drawer, preventing requested fields from scrolling the canvas drawer header above the visible frame when fullscreen opens.
- Focused ESLint, `17` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, touched-file TypeScript filtering, formatting, and diff checks passed. No deployment, commit, or push occurred.
