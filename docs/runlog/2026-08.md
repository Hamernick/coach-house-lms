# RUNLOG — 2026-08

2026-08-10 17:51 EDT - established the production-readiness wave contract.

- Reconciled the Finance/Find root PRD with merged PRs #118-127 and replaced
  conflicting completion percentages with evidence-based wave statuses.
- Ranked paid lifecycle and data durability as P0, then free signup/legal,
  existing release completion, `/find` loading/caching, theme quality, qualified
  resource cohorts, varied guides, and integrated production verification.
- Recorded the live resource endpoint snapshot of 853 records and about 2.52 MB.
  The local 5,046-record curated preview has only 741 records meeting the current
  publishability contract, so the remaining candidates stay out of `/find`.
- Deferred NWS weather promotion, required relevant-branch reuse or a focused
  branch from current `origin/main`, and prohibited automatic work on `main`.
- `pnpm check:quality` passed: all guardrails, snapshots, `2,030` acceptance
  tests with one intentional skip, fiscal and Finance RLS matrices, the
  `108/108` route build, `30/30` visuals, and performance budgets. The generic
  connected RLS suite skipped because this worktree has no Supabase credentials.
- This branch changes documentation only. Continue with Wave 1 on a separately
  selected paying-lifecycle branch after this PR merges.

2026-08-01 15:18 EDT - unified organization Mission, Vision, and Values with roadmap sections.

- Kept `mission_vision_values` as the canonical Mission id and route, added first-class Vision and Values sections, and routed roadmap, organization-profile, curriculum, public-map, search, readiness, and admin readers through one sanitized narrative contract.
- Replaced organization Mission, Vision, and Values textareas with compact rich-text editors, added section and organization-row optimistic concurrency checks, retained failed local drafts, and surfaced save and assignment-sync failures.
- Fixed assignment profile synchronization to use the invited member's active organization and existing staff/admin authorization instead of the member user id.
- Added copy-only, hash-recorded, idempotent migration planning; one-organization-only apply protection; and explicit-heading manual-review proposals that never modify combined Mission content.
- Production dry-run only: 56 organizations, 10 profile-only, 8 combined-only, 8 both, 30 neither; 41 safe field copies; 16 manual reviews; 5 Vision and 2 Values heading extracts. Defy Ventures remains manual-only because its unchanged 5,646-character content has no explicit section headings. No production write occurred.
- Full lint, snapshots, acceptance tests, build, visual regression, performance budgets, structure, routes, features, scaffold, thresholds, boundaries, storage, interaction, React Grab, workspace-surface, raw-button, supply-chain, large-file, SQL helper, and browser checks passed. RLS test execution skipped without a local test Supabase environment; existing membership policies were inspected. No deployment or data migration occurred.
- Continue by reconciling the isolated branch with the dirty root's overlapping organization `brandVoice*`, roadmap `budgetRows`, and existing August runlog work. Deploy compatible code before any migration; then review and approve Defy manually before running copy-only migrations.

2026-08-01 17:28 EDT - optimized pre-push lint.

- Added content-based ESLint caching for full local and CI lint runs, plus a change-scoped pre-push runner that compares the branch with the remote default branch.
- Pre-push now skips pushes with no JavaScript or TypeScript changes, lints only changed lintable files for normal code pushes, and falls back to full lint when configuration, dependency, or TypeScript project inputs change or the comparison range is uncertain.
- Kept the canonical CI `check:quality` gate on full repository lint. Benchmarks on the clean worktree improved an unchanged repeat lint from `92.8s` to `5.2s`; ESLint concurrency was rejected because it consumed substantially more CPU and performed inconsistently.
  2026-08-01 16:53 EDT - restored production migration source history.

- Ran the explicitly approved Defy Ventures canary in single-organization apply mode. Mission remained unchanged, Vision and Values had no safe source content, the plan reported `changed: false` and `applied: false`, and the organization profile and revision remained identical.
- Restored exact source copies of five migrations already applied to production but missing from `main`: `20260729223500`, `20260730223700`, `20260730223800`, `20260801130500`, and `20260801143000`.
- Byte parity checks passed and the linked Supabase dry-run reported the remote database is up to date. No database or organization data changed during this source-history repair.

2026-08-01 18:49 EDT - completed the copy-only Mission, Vision, and Values production migration.

- Reconfirmed the approved inventory before writing: 56 organizations, 18 organizations with safe changes, 41 field copies, 16 original combined-content reviews, and no existing Vision or Values roadmap content.
- Migrated all 18 eligible organizations and immediately verified each result. The idempotent resume path safely completed after the first test harness exceeded its five-second test timeout; the final audit reported 41 copied fields, zero remaining copies, and exact migration markers for every write.
- Preserved all legacy organization fields and populated roadmap content. Defy Ventures' 5,646-character combined Mission remained unchanged and had no reliable automatic Vision or Values extract.
- Generated read-only proposals for the 16 original combined-content organizations: five reliable Vision extracts and two reliable Values extracts. No proposal was written without human approval.
- Verified production while authenticated: Southside Community Table showed the same migrated Values content in its roadmap and organization-profile editors; Defy Ventures' public profile rendered the unchanged canonical Mission. No browser write was submitted and no console errors appeared.

2026-08-02 21:57 EDT - fixed stale public-organization map data after profile saves.

- Added tagged public-map cache invalidation whenever a public organization profile changes, while leaving private-only profile saves out of the public cache path.
- Advanced the public-organization cache generation once so the corrected FOCUOS address and coordinates load immediately when this release reaches production.
- Focused formatting, lint, cache acceptance tests, all `1,576` acceptance tests, snapshots, build, isolated `/find` visual checks, and performance budgets passed. RLS execution skipped without local test Supabase credentials. The branch remains gated by CI before merge and production verification.

2026-08-05 15:35 EDT - prepared durable organization-setup completion recovery.

- Persisted accelerator completion when organization setup saves, recovered saved organizations during workspace loads, and advanced stale canvas state past the recovered setup step.
- Added an idempotent migration for existing saved-organization owners and members without rewriting completed timestamps. Production read-only verification found 40 of 41 eligible users needed reconciliation, including Karissa.
- The linked migration dry-run selected only `20260805145856_reconcile_organization_setup_progress.sql`; no production database write occurred.
- `pnpm check:quality` passed against a dedicated clean-worktree server: lint, structure and boundary guards, snapshots, 1,583 acceptance tests, build, 29 visual tests, and performance budgets. RLS execution safely skipped because the isolated worktree had no test Supabase credentials.

2026-08-01 10:19 EDT - restored collapsed drawer tab visibility.

- Allowed the workspace drawer tab wrapper to overflow only at the collapsed snap point so the intentionally raised tab row is no longer cropped.
- Preserved expanded-state overflow containment, the mounted inactive drawer body, and existing drawer snap behavior.
- Focused ESLint, `12` acceptance tests, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed. Authenticated visual verification was unavailable because the browser automation helper is not installed. No deployment, commit, or push occurred.

2026-08-01 12:49 EDT - improved People segment assignment and controls.

- Made table and mobile-list drags carry every selected person into a custom segment, deduplicate existing members, select the destination segment after drop, and clear drag state reliably.
- Added visible custom-segment drop targets, active-target feedback, dragged-row feedback, and read-only permission gating while preserving the existing checkbox and toolbar alternative.
- Replaced the second available-people table with a keyboard-searchable shadcn Popover and Command picker in the controls row, leaving one members table per custom segment and explicit empty/filter states.
- Moved the existing rename/delete popover into the right side of the selected custom-segment pill and removed the duplicate segment header control below the rail.
- Made the add-person picker multi-select, moved each result's Add affordance to the right, kept the menu open while selecting, and added one batch confirmation action.
- Removed the duplicate search field inside the picker popover; the visible anchored Command input now searches the menu directly while retaining keyboard navigation and multi-select.
- Focused Prettier, ESLint, `10` drawer acceptance tests, targeted TypeScript diagnostics, thresholds, boundaries, interaction locks, React Grab, workspace-surface, raw-button, and diff checks passed. Full TypeScript remains blocked by unrelated existing errors elsewhere in the dirty worktree; the browser helper is not installed, so authenticated visual verification was unavailable. Segment persistence remains outside this interaction-only pass. No deployment, commit, or push occurred.

2026-08-01 13:08 EDT - persisted People segments and memberships.

- Replaced the People drawer's session-only custom segments with organization-scoped Supabase segment and membership records while retaining optimistic create, rename, delete, drag/drop, add, and remove interactions with rollback error handling.
- Loaded saved segments with the workspace server data, synchronized client state when the active organization changes, and filtered stale member IDs against the organization's current People records.
- Added server-side active-organization authorization, RLS policies for readable segments and editor-only writes, schema types, and a compatibility profile fallback for environments that have not received the migration yet.
- Applied `20260801130500_add_organization_people_segments.sql` to the linked Supabase project; focused acceptance tests and live RLS tests passed, including staff writes and board read-only enforcement.
- Focused ESLint, TypeScript diagnostics, thresholds, routes, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, and raw-button checks passed. Absolute structure and feature-contract checks remain blocked by unrelated pre-existing dirty-worktree violations. No deployment, commit, or source push occurred.

2026-08-01 13:25 EDT - made People relationships editable from the table.

- Replaced the editable relationship pill with an inline shadcn Select while preserving the existing compact pill, category dots, and a static read-only presentation.
- Added optimistic relationship updates with failure rollback and a category-only server action that preserves every other person field, enforces active-organization edit access, persists to the organization profile, and revalidates People/workspace surfaces. No schema change was required.
- Focused ESLint, `11` acceptance tests, targeted TypeScript diagnostics, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed. Full TypeScript remains blocked by unrelated existing dirty-worktree errors. No authenticated browser verification, deployment, commit, or push occurred.

2026-08-01 13:50 EDT - added table-managed People roles, segments, and tags.

- Renamed the People table's Relationship column to Role and updated its filter and mutation copy while retaining the compact single-select control.
- Added a Segments column with a searchable multi-select that uses the existing persisted segment memberships, supports membership changes from desktop and mobile tables, and respects read-only access.
- Added reusable, searchable, creatable person tags with normalized case-insensitive deduplication, organization-wide suggestions, per-person limits, optimistic display, failure rollback, and database persistence inside the existing organization People profile records. Existing person edits preserve tags.
- Focused ESLint, `13` acceptance tests, targeted TypeScript diagnostics, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed. Full TypeScript remains blocked by unrelated existing dirty-worktree errors. No authenticated browser verification, schema migration, deployment, commit, or push occurred.

2026-08-01 14:21 EDT - made People tags reusable, colored, and manageable.

- Replaced person-level string chips with shadcn Badge tags backed by organization-scoped tag definitions and memberships; tags now retain their color and update across every assigned person when renamed or recolored.
- Added the table and mobile tag picker, a create/edit dialog with nine accessible preset colors, optimistic assignment updates, and confirmed tag deletion that removes all memberships.
- Added server-side organization authorization, schema types, RLS policies, workspace loading and search hydration, and a migration that promotes existing `org_people[].tags` values without losing assignments.
- Applied `20260801143000_add_organization_people_tags.sql` to the linked Supabase project. Focused ESLint, `14` acceptance tests, snapshots, live RLS tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, and raw-button checks passed.
- Full TypeScript and full acceptance remain blocked by unrelated existing dirty-worktree failures in public-map, resource-map, fiscal-sponsorship, and other test surfaces; no diagnostics referenced the People tag files. No authenticated browser verification, deployment, commit, or source push occurred.

2026-08-01 14:27 EDT - tightened the People tag color picker.

- Replaced the oversized circular color controls with compact rounded-rectangle swatches and tighter wrapping inside the tag editor dialog.
- Focused ESLint, `13` acceptance tests, thresholds, React Grab, workspace-surface, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 14:31 EDT - fixed legacy People tag mutations.

- Prevented compatibility IDs such as `legacy:test` from reaching UUID database filters by resolving them to their migrated organization tag before update, delete, add-member, or remove-member operations.
- Added UUID and legacy-ID regression coverage. Focused ESLint, `14` acceptance tests, thresholds, boundaries, workspace storage, React Grab, workspace-surface, formatting, and diff checks passed.
- Full TypeScript remains blocked by unrelated existing dirty-worktree errors; no diagnostics referenced the changed People tag files. No deployment, commit, or push occurred.

2026-08-01 14:34 EDT - scoped Reports To to the Staff segment.

- Built the Reports To column only for the built-in Staff segment, removing it from All, other roles, custom segments, their column menus, and mobile cards.
- Focused ESLint, `10` acceptance tests, thresholds, boundaries, React Grab, workspace-surface, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 14:47 EDT - fixed the conditional Reports To lookup.

- Passed the Staff-only visibility flag into the mobile People list so TanStack never looks up the absent Reports To column outside Staff.
- Focused ESLint, `10` acceptance tests, thresholds, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 15:05 EDT - consolidated People row actions.

- Replaced the inline segment button with an always-available three-dot shadcn menu in the Action column for adding to or removing from the canvas and adding to or removing from the active custom segment.
- Wired Remove from canvas to the existing persisted person-placement controller and added the same action menu to mobile People cards while retaining canvas status and drag affordances.
- Focused ESLint, `10` acceptance tests, targeted TypeScript diagnostics, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace-surface, raw-button, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 16:08 EDT - removed unreliable LinkedIn photo fetching.

- Removed public-profile HTML scraping, automatic LinkedIn photo fetching on person save, both refresh-photo menu actions, and the UI promise that a LinkedIn URL would supply a photo.
- Preserved LinkedIn profile links, manual photo uploads, existing stored photos, and signed avatar rendering.
- Focused Prettier, ESLint, raw-button, React Grab, stale-reference, and diff checks passed. No authenticated browser verification, deployment, commit, or push occurred.

2026-08-01 16:11 EDT - integrated canonical Mission, Vision, and Values.

- Preserved the dirty root's brand-voice, roadmap budget-table, embedded navigation, workspace presentation, and August runlog work while merging the isolated Mission, Vision, and Values implementation.
- Kept `mission_vision_values` as Mission, added Vision and Values, unified roadmap and organization-profile editing, added sanitized rich content, optimistic concurrency, retained drafts, canonical readers, and active-organization assignment synchronization.
- Added dry-run-first copy migration and human-approved combined-content review tooling. The prior production inventory and Defy canary plan remain unchanged; no production data write occurred.
- Full ESLint, snapshots, `52` focused acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, localhost public Defy rendering, and anonymous login gating passed. The full dirty-root acceptance suite reached `1641` passes with `7` unrelated existing failures; full TypeScript likewise reported only pre-existing errors outside the merged files.
- RLS tests were not run against the linked production project. No deployment, commit, push, schema application, or data migration occurred. Continue with a clean focused release artifact before deploying compatible code, then require explicit Defy review approval before any canary write.

2026-08-01 16:19 EDT - added adjustable People table sizing.

- Added drag-resizable People table columns with accessible keyboard controls, double-click width reset, constrained widths, and locally persisted preferences.
- Added compact, default, and spacious row-height choices plus a reset-width action to the existing Columns menu; mobile cards retain their existing layout.
- Full TypeScript, focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. No authenticated browser verification, visual baseline update, deployment, commit, or push occurred.

2026-08-01 16:36 EDT - hardened People table column and row resizing.

- Reworked live column resizing to follow TanStack's performant React pattern: column widths are calculated once, delivered through CSS variables, and the complex table body remains memoized during column drag updates.
- Removed per-cell size calculations and per-frame storage writes, constrained and clipped cell content to prevent overlap, and debounced local preference persistence.
- Added full-width draggable row-boundary handles with pointer and keyboard resizing, per-person persisted heights, double-click reset, and global compact/default/spacious resets.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. Full TypeScript remains blocked by unrelated dirty-worktree errors; no diagnostic referenced the People table files. Headless browser verification reached localhost but redirected to login, so authenticated drag verification was unavailable. No deployment, commit, or push occurred.

2026-08-01 16:25 EDT - packaged Mission, Vision, and Values release PR.

- Committed the isolated release as `15bc3e1`, pushed `codex/mvv-unification`, and opened PR `#113` with the required `[STEP S20]` title.
- Pre-push checks passed `315` files and `1565` tests; GitHub quality, title validation, GitGuardian, and both Vercel builds passed. Supabase Preview was cancelled only because the project reached its concurrent preview-branch limit, matching its non-required historical behavior.
- The primary Vercel preview is access-protected from the available signed-in account; local Defy rendering passed. The alternate preview is reachable but has no matching Defy data.
- PR `#113` is mergeable but blocked on one human approval. Repository auto-merge is disabled, so no merge or production deployment occurred. No schema or organization-data migration occurred.

2026-08-01 16:42 EDT - deployed Mission, Vision, and Values unification.

- PR `#113` merged as `d8b4537`; both production Vercel deployments, the GitHub quality suite, build, and deploy jobs passed. Anonymous production checks returned `200` for Defy's public profile and the Vision roadmap login gate with zero console errors.
- The production migration planner ran read-only across `56` organizations: `10` profile-only, `8` combined-only, `8` both, and `30` neither. It proposed `41` automatic copy-only actions and `16` manual reviews, with `0` organization writes.
- Applied linked migrations `20260801190000_split_mission_vision_values_roadmap_sections.sql` and `20260801191000_update_search_index_canonical_mission.sql` after a successful dry-run. Production migration history now records both versions; curriculum mapping and canonical Mission search resolution are live.
- Verified Defy Ventures after schema rollout: its existing `5,646`-character combined content and hash `0914cd87` remain unchanged, Vision and Values remain empty, and the verification runner applied nothing. No organization-content migration or manual review write occurred.
- The linked migration audit exposed five earlier production-applied migrations present only in the dirty root and absent from `main`: `20260729223500`, `20260730223700`, `20260730223800`, `20260801130500`, and `20260801143000`. Exact temporary copies allowed the dry-run and push without history repair, then were removed. Restore that source history in a separate focused release before future linked migration work.
- The non-required Supabase Preview status reported failure, while direct linked migration validation and application succeeded. Defy's canary content migration still requires explicit approval.

2026-08-01 17:07 EDT - improved People table context, sizing, and performance.

- Kept the header, selection and Person columns, plus the Action and Canvas controls, visible while the desktop People table scrolls in either direction.
- Added per-column reset, content auto-fit, saved width layouts, persistent row sizing, and a 100-row page option without reintroducing overlapping cell content.
- Added conditional TanStack Virtual row rendering at 100 visible rows while retaining the simpler semantic table body for smaller result sets and the existing mobile card layout.
- Full TypeScript, focused ESLint, `10` People drawer acceptance tests, routes, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. Structure and feature checks remain blocked only by unrelated existing dirty-worktree violations. Authenticated visual verification was unavailable; no deployment, commit, or push occurred.

2026-08-01 17:26 EDT - added adaptive People table previews and wrapping.

- Replaced the single-item Tags and Segments summaries with density-aware previews: compact rows show one item, default rows show two, and spacious wrapping can show four slots while retaining an overflow count.
- Made overflow counts reveal every hidden label and open the existing assignment editor, preserved each tag's configured color, and allowed Person titles, Tags, and Segments to wrap without overlapping adjacent rows.
- Added a persisted Wrap previews or Truncate previews choice to the existing Columns menu and remeasured virtual rows whenever that choice changes.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. Full TypeScript and structure remain blocked only by unrelated existing dirty-worktree violations; no deployment, commit, or push occurred.

2026-08-01 18:33 EDT - stabilized People table wrapping and column widths.

- Switched the desktop table to independent CSS-variable grid tracks so resizing one column no longer redistributes neighboring widths.
- Enabled resizing for the Action column and made wrapped names, titles, managers, emails, LinkedIn labels, tags, and segments grow their rows instead of overlapping adjacent cells.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. Full TypeScript and structure remain blocked only by unrelated existing dirty-worktree violations; no deployment, commit, or push occurred.

2026-08-01 18:36 EDT - tightened the People table leading columns.

- Centered row-selection checkboxes with equal horizontal space in the header and body, reduced the Person column default from `240px` to `216px`, and migrated only the prior default while preserving custom and explicitly saved widths.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:39 EDT - improved compact People tag previews.

- Made compact Tags cells show two tags before the overflow count, let inline tag pills share the full cell width, and added balanced horizontal padding to the `+N` badge.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:40 EDT - fixed Action header truncation.

- Raised the resizable Action column minimum to `72px`, tightened its left padding, and removed the extra label padding before the resize handle so the full header remains visible.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:42 EDT - restored People table header spacing.

- Restored vertically centered `36px` header cells with balanced padding after the grid-track conversion and centered the header and row-selection checkboxes through full-width wrappers.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:45 EDT - tightened the People drag column.

- Reduced the fixed Canvas and drag-handle column from `112px` to `48px`, centered the handle, and removed the redundant visible status pill while retaining its accessible status and drag description.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:47 EDT - precisely centered People selection controls.

- Replaced nested flex and padding alignment in the fixed selection track with full-width, zero-padding centering grids for both select-all and row checkboxes.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:54 EDT - closed the People action-to-drag gap.

- Restored the drag handle's right alignment, right-aligned the Action menu button with tighter cell padding, and applied TanStack min/max clamping before persisted widths reach table CSS variables so the Canvas track actually contracts to `48px`.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 18:59 EDT - fixed People table resize trailing space.

- Unified the table, header, row, sticky-column, and virtual-row widths around the same clamped visible-column total so resizing Action cannot leave row borders extending into an empty strip.
- Kept the drag handle right-aligned and the Action button aligned beside it. Moved sizing calculations into the existing table-sizing owner to retain component limits.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. Structure remains blocked only by unrelated existing dirty-worktree violations; no deployment, commit, or push occurred.

2026-08-01 19:00 EDT - stopped content bleed through pinned People actions.

- Replaced translucent hover and placed-state backgrounds on the sticky Action and drag cells with opaque theme surfaces so horizontally scrolled cell content cannot overlap their controls.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, and formatting passed. No deployment, commit, or push occurred.

2026-08-01 19:25 EDT - added inline People segment creation.

- Added persisted segment creation to each editable Segments cell: users can search, type a new segment name, create it from the same popover, and assign the current person in one action on desktop or mobile.
- Extended segment creation to validate and persist initial members with the segment, including the existing profile fallback, and aligned the picker with the wider Tags menu.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. Full TypeScript and structure remain blocked by unrelated existing dirty-worktree failures; no deployment, commit, or push occurred.

2026-08-01 19:32 EDT - converted LinkedIn to Social Media profile access.

- Renamed the visible People table column to Social Media while retaining its internal LinkedIn ID so saved table layouts remain compatible, narrowed the default column, and replaced URL text with a LinkedIn platform icon.
- Made the Social Media cell open the existing right-side person editor with the full profile fields on desktop and mobile; people without LinkedIn data can use the same icon to add it.
- Focused ESLint, targeted TypeScript, `10` People drawer acceptance tests, thresholds, boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 19:43 EDT - completed persisted People social profiles.

- Added editable LinkedIn, Instagram, Facebook, X, YouTube, and TikTok fields to the existing organization profile People data, preserving omitted values during partial updates and refreshing the workspace after saves.
- Social Media cells now show only the platform logos a person has and use a clear add affordance when empty. The editor uses bundled Simple Icons paths with a local LinkedIn icon, so production has no logo CDN, API, or storage-bucket dependency.
- Focused ESLint, `15` acceptance tests, existing People guardrails, formatting, and diff checks passed. No database migration was required because People records already persist in organization profile JSONB. No deployment, commit, or push occurred.

2026-08-01 22:38 EDT - made People cells open profile details.

- Converted the Person name and avatar cell into the full-profile sheet trigger on desktop and mobile.
- Kept authorized profiles editable and added a read-only details state for viewers without People-management access, eliminating the previous dead click.
- Focused ESLint, targeted TypeScript, `15` acceptance tests, thresholds, boundaries, raw-button checks, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 22:42 EDT - stabilized People badge widths.

- Made wrapped tag, segment, and overflow badges retain their intrinsic width when columns expand while still respecting the available width and truncating in narrow columns.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 22:44 EDT - stopped the Segments trigger from stretching.

- Changed the editable Segments popover trigger from full-column width to content width while retaining its minimum tap target and narrow-column containment.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 22:47 EDT - simplified People segment and tag previews.

- Removed the visible container chrome around the Segments and Tags popover triggers while retaining keyboard focus behavior and the existing picker interactions.
- Reduced both cell previews to one primary pill plus a compact `+N` overflow pill regardless of column or row size.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, raw-button checks, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 23:02 EDT - kept People multi-select menus open.

- Hoisted desktop Segments and Tags popover state above TanStack cell renderers so optimistic membership updates no longer remount and close the active menu.
- Existing tag creation and editing flows still dismiss the picker before opening their dedicated editor.
- Focused ESLint, targeted TypeScript, `10` People drawer acceptance tests, thresholds, boundaries, raw-button checks, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-01 23:06 EDT - accelerated People tag and segment redraws.

- Removed the full People-object hydration pass from tag membership updates. Current tag labels now live in separate memoized search metadata, leaving table row objects stable while pills and selection checks update optimistically.
- Kept tag-aware People search intact and extracted its memoization into a focused local hook.
- Focused ESLint, targeted TypeScript, `10` People drawer acceptance tests, thresholds, boundaries, raw-button checks, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:22 EDT - restored collapsed-sidebar coaching access.

- Overrode the tall-screen-only coaching-card rule in collapsed desktop sidebars so the existing icon-sized Coaching item and tooltip remain visible while the full card stays hidden.
- Focused ESLint, `12` sidebar acceptance tests, thresholds, React Grab ownership, raw-button checks, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:24 EDT - simplified the mini Coaching avatar.

- Limited the compact sidebar Coaching item to one coach avatar and added a bordered green availability indicator at its bottom-right while preserving the full coach group in the expanded card.
- Focused ESLint, `14` sidebar and coaching acceptance tests, thresholds, React Grab ownership, raw-button checks, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:32 EDT - separated organization switcher items.

- Added a small vertical gap between organization command items so their rounded hover and selected surfaces no longer touch.
- Focused ESLint, `3` organization-switching acceptance tests, thresholds, raw-button checks, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:36 EDT - removed the organization-switch drawer mount deadlock.

- Stopped gating the canvas drawer on the separate app-shell right rail being open. The drawer now mounts as soon as its organization-scoped canvas container exists, including the first load after switching organizations.
- Focused ESLint, `13` drawer and organization-switching acceptance tests, interaction locks, React Grab ownership, workspace-surface, raw-button, and diff checks passed. The unauthenticated local browser reached the login redirect, so the authenticated switch itself was not replayed. No deployment, commit, or push occurred.

2026-08-02 13:42 EDT - separated People actions from the pinned drag column.

- Removed horizontal pinning from the Action header and cells while preserving the right-pinned drag column.
- Left-aligned the Action header and centered each row's three-dot action trigger to match the selection controls.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, interaction locks, React Grab ownership, workspace surfaces, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:44 EDT - simplified empty People segment and tag cells.

- Replaced the editable `Add to segment` and `Add tags` placeholder text with the same muted plus-icon affordance used by empty Social Media cells while preserving accessible trigger labels.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, React Grab ownership, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:47 EDT - expanded empty People cell menu hit targets.

- Made empty Segment and Tags popover triggers fill their table cells so clicking anywhere across the plus-icon cell opens the existing assignment menu; populated pill triggers remain content-width.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, interaction locks, React Grab ownership, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 13:51 EDT - synchronized People segment drops with table cells.

- Kept users in their current People table after dropping a contact onto a custom segment pill while preserving the existing optimistic persisted assignment flow.
- Made the memoized table body observe changed column definitions so Segment and Tags cells rerender immediately when membership changes; the dropped row now replaces its plus icon with the assigned segment pill.
- Focused ESLint, `10` People drawer acceptance tests, thresholds, interaction locks, React Grab ownership, workspace surfaces, raw-button, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 14:15 EDT - added subtle `/find` map overscan.

- Extended the complete Mapbox viewport `12px` past every outer canvas edge so the existing rounded frame crops the map without adding an inner border or inset.
- Disabled Mapbox's native attribution control and hid its native logo for this local styling pass; replacement attribution will be designed and restored separately.
- Preserved the pre-change dirty checkout in `refs/codex/snapshots/find-style-before-20260802T180531Z` without changing the active index or branch.
- Focused ESLint, `25` public-map layout tests, interaction locks, React Grab ownership, diff checks, and local Playwright screenshots at desktop and mobile sizes passed. The broader `/find` route test retained `2` unrelated existing onboarding-copy failures. No visual snapshot covers the dynamic `/find` map, so no baseline changed. No deployment, commit, or push occurred.

2026-08-02 14:58 EDT - replaced `/find` clusters with unified pin markers.

- Removed the custom cluster worker/client from the active map lifecycle and now feed the existing point features directly into two WebGL marker layers while preserving filtering, labels, selection, organization clicks, and same-location group clicks.
- Replaced active logo, circular-dot, and special-pill rendering with one flat white teardrop pin system: category glyphs for resources, a generic organization glyph for organizations, category/group accent faces, bottom anchoring, and a restrained shadow.
- Archived the complete pre-change cluster and individual-marker implementation in `refs/codex/snapshots/find-markers-before-20260802T143539` at `c04e53d626944f665c2fe5539e07abb7bd8450ae`; `docs/archive/public-map-markers-2026-08-02.md` records the entry points and read-only inspection command.
- Focused ESLint, `85` marker/WebGL/runtime tests, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed. Full TypeScript remains blocked by existing dirty-worktree errors; no diagnostic referenced a new marker module, and the reported `public-map-index.tsx` filter-state mismatch predates this import-only change. Local Playwright verification at desktop and mobile sizes rendered `11` point features through only the two marker layers with zero cluster features and no page or console errors. The broader `/find` route test retains `2` unrelated onboarding-copy failures. No `/find` visual baseline exists, so no baseline changed. No deployment, commit, or push occurred.

2026-08-02 15:07 EDT - corrected `/find` pin proportions and collision behavior.

- Replaced the oversized rounded-square silhouette with a true circular head and a short, rounded, connected tail; reduced both normal and selected zoom ramps while preserving bottom-tip anchoring.
- Restored Mapbox collision placement for normal markers so dense points do not stack into a wall; selected markers remain visible above collisions, and marker sprites stay viewport-aligned while the map pitches or rotates.
- Focused ESLint, `85` marker/WebGL/runtime tests, thresholds, boundaries, interaction locks, React Grab ownership, formatting, diff checks, and local desktop/mobile Playwright verification passed. The live `/find` layers rendered without cluster features, page errors, or console errors. No deployment, commit, or push occurred.

2026-08-02 15:21 EDT - stabilized `/find` markers and attached titles.

- Replaced the pin's pinched quadratic tail with a short, wide, tangent-matched curve; enlarged the colored face, softened the shadow, and aligned the visible tip with its geographic anchor despite the sprite's transparent shadow padding.
- Consolidated each icon and title into one Mapbox symbol. Icons now remain visible through collision recalculation while optional titles stay collision-aware and sit directly below their marker.
- Playwright retained all `11` rendered point markers through five consecutive zoom steps, with stable overlap and anchor contracts, no clusters, and no page or console errors. Desktop and mobile checks, focused ESLint, `85` marker/WebGL/runtime tests, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 15:26 EDT - rounded the `/find` pin tip.

- Replaced the tail's narrow point with a wider semicircular cap and bumped the marker sprite version so Mapbox does not reuse the sharper cached artwork.
- Focused ESLint, `85` marker/WebGL/runtime tests, five-step zoom verification with all `11` markers retained, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 15:28 EDT - removed the `/find` map-save activity pill.

- Removed the transient `Saving map activity` status from the map surface and its unused saving-state plumbing while preserving silent preference persistence and the existing visible failure message.
- Focused ESLint, `3` preference tests, thresholds, React Grab ownership, formatting, diff checks, and a real marker-click browser check passed with no pill, page errors, or console errors. The broader `/find` route suite retains its `2` unrelated onboarding-copy failures. No deployment, commit, or push occurred.

2026-08-02 15:45 EDT - rebuilt the `/find` pin as one continuous silhouette.

- Removed the circle-plus-tail construction and replaced it with one symmetric Bézier outline that begins tapering through the lower head, ending in a short rounded cap without shoulders or a parallel-sided stem.
- Kept the head and colored face sizes stable, recalibrated the bottom anchor for the shorter silhouette, and bumped the sprite identity so Mapbox cannot reuse prior artwork.
- An isolated selected-marker browser render was inspected at close zoom. Focused ESLint, `85` marker/WebGL/runtime tests, five-step zoom verification with all `11` markers retained, desktop/mobile browser checks, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed with no page or console errors. No deployment, commit, or push occurred.

2026-08-02 15:53 EDT - replaced the `/find` pin contour with tangent geometry.

- Replaced the hand-tuned piecewise Bézier outline with exact shared tangents between the circular head and a real circular tip cap, eliminating both the lower-head seam and the flattened micro-curve at the bottom.
- Recalibrated the sprite anchor for the new contour and invalidated the previous cached marker image while leaving marker sizing, labels, clicks, and zoom behavior unchanged.
- Inspected an isolated selected marker at close zoom and retina resolution. Focused ESLint, `85` marker/WebGL/runtime tests, five-step zoom verification with all `11` markers retained, desktop/mobile browser checks, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed with no page or console errors. No deployment, commit, or push occurred.

2026-08-02 16:05 EDT - restored `/find` marker images and category faces.

- Kept the new tangent pin silhouette while restoring the prior content hierarchy: verified platform organizations asynchronously upgrade to their stored profile/logo image; external resources retain category-specific colors and glyphs; missing or failed images remain on the immediate organization fallback.
- Added a bounded active pin-image bitmap cache and in-place Mapbox image updates without reactivating the archived cluster or old marker-shape implementation.
- A real PLAN4SUCCESS marker upgraded from its green organization fallback to its stored image inside the pin. Focused ESLint, `86` marker/WebGL/runtime tests, five-step zoom verification with all `11` markers retained, desktop/mobile browser checks, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed with no page or console errors. The broad dirty-worktree typecheck was stopped after extended runtime; focused validation found no new issue. No deployment, commit, or push occurred.

2026-08-02 16:14 EDT - changed profile-image marker backing to white.

- Replaced the category/group color beneath verified profile and logo images with a pure white clipped face while leaving category marker colors and glyphs unchanged.
- Invalidated the marker sprite identity so existing Mapbox instances reload the corrected artwork. The real PLAN4SUCCESS marker rendered its stored logo on white; focused ESLint and `86` marker/WebGL/runtime tests passed. No deployment, commit, or push occurred.

2026-08-02 16:18 EDT - focused `/find` markers on click.

- Routed organization, external-resource, and same-location marker clicks through the existing map camera focus before opening their detail or grouped results.
- A real PLAN4SUCCESS canvas-marker click flew from the national view to its exact location and selected the marker with no page or console errors. Focused ESLint, `5` marker tests, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed. The broader `/find` route suite retained its `2` unrelated onboarding-copy failures. No deployment, commit, or push occurred.

2026-08-02 16:22 EDT - softened and shadowed `/find` pins.

- Shortened both pin variants by `3px`, widened the circular tip cap, and slightly strengthened the existing drop shadow without changing the circular head or marker content.
- Recalibrated the Mapbox icon offsets so the shorter visible tip remains on the same geographic anchor, then invalidated the prior sprite identity. Retina and real marker-click inspection confirmed the shorter rounded point, subtle shadow, retained focus behavior, and no page or console errors. Focused ESLint, `87` marker/WebGL/runtime tests, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed. No deployment, commit, or push occurred.

2026-08-02 16:33 EDT - switched `/find` to the Standard day/night basemap.

- Replaced the satellite style with Mapbox Standard and tied its `lightPreset` to the existing light/dark theme as `day`/`night`, including in-place theme changes without reloading the map.
- Preserved the intentionally quiet display by hiding roads, pedestrian roads, road/place/POI/transit labels, landmarks, administrative boundaries, and 3D objects; no Streets or satellite-streets style was added. Light, dark, and live theme-toggle browser checks retained all `11` markers with no page or console errors and no style reload on toggle.
- Focused ESLint, `87` marker/WebGL/runtime tests, the new basemap contract test, thresholds, boundaries, interaction locks, React Grab ownership, formatting, and diff checks passed. The broader `/find` route suite retained its `2` unrelated onboarding-copy failures. No deployment, commit, or push occurred.

2026-08-02 17:03 EDT - documented infrastructure cost control and account classification.

- Added the durable self-hosting plan at `docs/plans/2026-08-02-infrastructure-cost-control-and-self-hosting.md` and indexed it from the docs map and architecture contract so future agents can retrieve it for cost, scaling, Vercel, Supabase, hosting, and disaster-recovery work.
- A read-only production audit classified `194` Auth records as `11` clearly synthetic/demo accounts, `23` real-email human tester accounts, and `160` standard accounts. Of the `183` non-synthetic accounts, `80` have signed in, `69` have product activity, `103` have never signed in, and `95` remain unconfirmed. No account or production data was changed.
- Recorded the staged hybrid recommendation, target architecture, service decisions, economic triggers, delivery phases, acceptance gates, rollback requirements, and monthly update protocol. Documentation and diff validation only; no application, infrastructure, deployment, commit, or push occurred.

2026-08-02 17:20 EDT - added consent-first `/find` location entrance.

- Replaced mount-time geolocation with a one-shot, client-only permission flow: already-granted access animates to the validated position once per browser tab, prompt state waits for the in-map consent action, denied state stays quiet until requested, and late automatic results do not override manual map movement. No coordinates are stored, sent to an app API, logged, or added to a URL.
- Added the top-left location control, nearby consent/error card, reusable location dot, manual recenter/retry behavior, reduced-motion-safe Mapbox animation, and same-origin `/find` geolocation policy. Detail routes, curated seed previews, and an open member Welcome overlay suppress the automatic entrance.
- Moved the authorized admin intro control to the map's top center and renamed `Preview intro`/`Hide intro` to `Welcome`/`Hide welcome` without changing its authorization.
- TypeScript, focused ESLint, formatting, `30` focused location/marker tests, thresholds, boundaries, interaction locks, React Grab ownership, raw-button, workspace-surface, and `git diff --check` validation passed. Browser checks covered prompt, granted, denied, return-navigation, desktop, and mobile behavior with no page errors; prompt and denied states made no automatic native request, granted access made one, and returning to `/find` retained one total request. The broader public-map suite passed `216/218`; its two unrelated existing resource-catalog migration assertions remain failing, and the broader `/find` route suite retains one unrelated existing onboarding-copy assertion. No deployment, commit, or push occurred.

2026-08-02 17:32 EDT - joined the `/find` directory status to location control.

- Moved the active resource count from the directory heading into the map's top-left location-control row and matched both controls to the Welcome button's `32px` height; the rail retains only its Resources heading.
- Focused ESLint, `32` location/sidebar tests, thresholds, React Grab ownership, raw-button, formatting, and diff checks passed. Desktop and mobile browser checks confirmed one Active pill, adjacent `32px` controls, and no duplicate rail status. The broad dirty-worktree TypeScript check remains blocked by existing unrelated errors. No deployment, commit, or push occurred.

2026-08-02 17:39 EDT - restored streets to the `/find` Standard basemap.

- Restored Mapbox Standard's theme-aware road colors, pedestrian roads, road names, and route shields in both day and night presets while preserving the existing hidden POI, place, transit, landmark, boundary, and 3D settings.
- The focused basemap test, ESLint, formatting, and a real street-zoom browser check passed with no page errors. No deployment, commit, or push occurred.

2026-08-02 18:15 EDT - matched the `/find` location dot across themes.

- Made both layers of the user-location indicator fully emissive so Mapbox Standard's night lighting no longer darkens its blue core, white ring, or halo.
- Focused ESLint, formatting, `8` location tests, and direct light/dark browser comparison passed with no page errors. No deployment, commit, or push occurred.

2026-08-02 18:41 EDT - added the centered `/find` globe entrance.

- Opened the location consent card by default for users without a successful tab-scoped location grant and centered it exactly within the map on desktop and mobile; dismissing it lasts for the current visit, while returning to `/find` reopens it until location is granted.
- Widened the no-location starting view from zoom `3.15` to `1.5` and added a calm three-minute globe rotation that stops permanently on map interaction, stops after location succeeds, stays disabled for reduced-motion users, and remains suppressed for detail, curated-preview, and Welcome states.
- Stored only a tab-scoped granted boolean so returning after success neither reopens the card nor repeats the location request; coordinates remain unstored. Focused TypeScript, ESLint, formatting, `23` location/map tests, thresholds, React Grab ownership, raw-button, diff, desktop/mobile browser, rotation-stop, reduced-motion, prompt-return, and granted-return checks passed with no page errors. No deployment, commit, or push occurred.

2026-08-02 18:43 EDT - matched the `/find` consent card surface to Welcome.

- Replaced the centered location card's background with the same `bg-card/92` light surface and `dark:bg-input/30` dark surface used by the Welcome control, including its matching theme border treatment, without changing the shared Card primitive.
- Focused ESLint, formatting, `9` location tests, diff validation, and direct light/dark computed-style checks passed with no page errors. No deployment, commit, or push occurred.

2026-08-02 18:50 EDT - tightened the `/find` location focus.

- Raised the successful location and recenter camera target from zoom `10` to zoom `13`, moving the result from city scale to neighborhood scale without forcing the map to zoom out when a user is already closer.
- Focused ESLint, formatting, `10` location tests, diff validation, and a granted-location browser check passed with no page errors; the consent card closed and the location indicator remained active. No deployment, commit, or push occurred.

2026-08-02 18:53 EDT - flattened the `/find` category pills.

- Removed the drop shadow from every horizontal category pill. The initial left overflow mask remains correctly at `0px`, the right overflow cue remains visible, and the left cue appears only after horizontal scrolling.
- Focused ESLint, formatting, `26` sidebar tests, diff validation, and direct browser checks before and after horizontal scrolling passed; computed pill shadow is now `none`. No deployment, commit, or push occurred.

2026-08-02 18:57 EDT - replaced Saved row removal text with an icon.

- Kept the existing Saved organization row styling and replaced its `Remove` text action with a compact `32px` circular minus-icon button using the existing bordered action treatment and an organization-specific accessible label.
- Focused ESLint, formatting, `26` sidebar tests, diff validation, and a real Saved-tab browser check passed with one visible minus icon, no button text, the expected label, and no page errors. No deployment, commit, or push occurred.

2026-08-02 19:04 EDT - restored granted `/find` location focus on return.

- Reacquired the browser-cached position when a successful tab-scoped location grant already exists so returning to `/find` loads focused at neighborhood zoom instead of the no-location globe view. The consent card remains closed, and only the grant boolean—not coordinates—is stored.
- Focused ESLint, formatting, `36` location/sidebar tests, and a browser `/find` to home to `/find` navigation check passed. The return made one new browser geolocation lookup, rendered the active location state at zoom `13`, kept the consent card closed, and produced no page errors. No deployment, commit, or push occurred.

2026-08-02 19:11 EDT - delayed the `/find` category overflow fade.

- Removed the category row's initial right-edge fade and now enable its directional overflow mask only after the user has actually scrolled the row horizontally. Category pills remain flat and shadow-free.
- Focused ESLint, formatting, `26` sidebar tests, and direct browser checks passed. The initial row had no mask, scrolling activated the mask at the measured offset, and no page errors occurred. No deployment, commit, or push occurred.

2026-08-02 19:18 EDT - strengthened `/find` marker content and selection.

- Added a thin light-grey stroke around the inner circular marker content for both category-icon and profile-image pins without changing the outer pin silhouette or its border.
- Increased the selected Mapbox marker scale across the zoom ramp and recalibrated its vertical offset so a focused marker is clearly larger while retaining its geographic tip anchor. Bumped the marker sprite identities to prevent reuse of the previous artwork.
- Focused ESLint, formatting, `60` pin/marker/WebGL tests, and direct browser comparison at the same focused zoom passed. The selected layer used the new size ramp, the marker rendered larger with the inner ring, and no page errors occurred. No deployment, commit, or push occurred.

2026-08-02 19:29 EDT - reconciled the `/find` resource-count gap.

- Confirmed the current localhost public-resource API returns zero external resources because neither the public-database flag nor an explicit curated-preview file is enabled; the visible `16` are platform organizations.
- Read-only checks found `853` published public database rows with coordinates, while the live production API currently exposes only `500`, consistent with its older single-page cap. The local intake queue contains `4,249` geocoded candidates, including `2,445` marked for review; the `942`-row preview is a subset, not an additional public corpus.
- No application, environment, database, publication, deployment, commit, or push change occurred.

2026-08-02 19:40 EDT - populated local `/find` with the published resource catalog.

- Added the ignored development-only `RESOURCE_MAP_PUBLIC_DB_ENABLED=true` override and restarted localhost:3000. The local API now returns all `853` approved resources, and `/find` reports `869` total directory items with the `16` platform organizations.
- Reused marker sprites across external resources with the same category artwork and same-location count instead of registering two high-resolution sprites per record. This removed Mapbox image-atlas corruption when the full catalog loaded while preserving unique profile-image markers.
- Direct neighborhood-zoom browser verification rendered the populated map with no black atlas blocks, page errors, or console errors. The marker source represents all `852` mappable items in `809` same-location-aware features; `13` published resources and `4` platform organizations remain list-only because they have no coordinates.
- Focused ESLint, formatting, and `83` resource/marker/WebGL tests passed. The development server is healthy on port `3000`; no database, publication, deployment, commit, or push occurred.

2026-08-02 19:47 EDT - removed the stale local `/find` resource-feed cache.

- Confirmed localhost:3000 and its public-resource API were healthy with `853` external resources while an existing browser tab still displayed only the `16` platform organizations.
- Replaced the module-lifetime successful-response cache with concurrent-request-only deduplication and restored normal HTTP cache semantics. A previously empty response can no longer stay pinned for the lifetime of the tab after the feed becomes available.
- Focused ESLint, formatting, `84` resource/marker/WebGL tests, and a fresh browser check passed; `/find` displayed `Active 869` with no page errors. No database, publication, deployment, commit, or push occurred.

2026-08-02 19:58 EDT - audited list-only records and began candidate publication triage.

- Identified all `17` list-only entries: `13` published external resources and `4` public platform organizations. Nine already have exact street addresses and received exact-address Census matches during read-only validation; eight currently lack a specific mappable address or represent multiple locations and remain list-only pending provider evidence.
- Audited all `4,249` local candidates. Before enrichment, none pass the publication-readiness contract: all lack two completed comparisons and approved verification, `4,249` lack eligibility and access instructions, and `4,242` lack a sufficient public summary.
- Applied the existing deterministic official heat/cooling enrichment in memory to its `1,587` supported candidates. After tightening the publication gate to detect unresolved duplicates and conflicts stored in normalized `extractedFields.dedupe`, `660` records across seven current official datasets pass the automated data-readiness gates; `927` remain held. The fix correctly moved seven Chicago records with unresolved duplicate/hour conflicts out of the preliminary clear cohort.
- Focused Prettier, ESLint, the `7` cooling-center enrichment tests, and diff validation passed. No candidate output was promoted, no database or production data changed, and no deployment, commit, or push occurred.

2026-08-02 20:06 EDT - changed `/find` food markers to blue shopping carts.

- Replaced the top-level Food category's red bread identity with the existing Tailwind `blue-600` color and a Lucide shopping-cart icon across map pins, category controls, resource details, and category-aware clusters. Community-fridge-specific bread/cyan semantics remain unchanged.
- Invalidated standard marker and cluster sprite identities so an already-open map cannot reuse the prior red bread artwork.
- Focused Prettier, ESLint, `110` resource/marker/WebGL/sidebar tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. A direct localhost browser check found `29` rendered Food features using `#2563eb`, the shopping-cart paths in the Food control, and no page or console errors. No deployment, commit, or push occurred.

2026-08-02 20:38 EDT - increased `/find` marker-title spacing.

- Increased the active normal and selected Mapbox marker label offsets by roughly `3px`, adding clear separation below the pin without changing marker size or geographic anchoring. The archived clustering implementation remains unchanged.
- Focused ESLint, `32` active marker-layer tests, thresholds, boundaries, interaction locks, and React Grab ownership passed. A direct localhost browser check confirmed the live layers use offsets `[0, 0.45]` and `[0, 0.52]` with no page errors. No deployment, commit, or push occurred.

2026-08-02 20:51 EDT - corrected `/find` marker designations and advanced candidate review data.

- Confirmed four of the `17` list-only entries are real platform-account organizations with live auth owners: Caught Dreaming, FOCUOS, A Taste of Theatre, and Mommy's Gift to Me. The other `13` are imported records: six from the 211 Metro Chicago housing directory and seven from the Cook County immigrant/refugee directory.
- Replaced active marker titles with primary designations: platform markers use their primary organization group, while imported resources use their primary resource category or subcategory. The item title remains intact for selection and detail views.
- Fixed category alias inference to require whole words or phrases. This prevents the Animals alias `pet` from matching `competition` in Coach House content or `competent` in Cityline Care Center content; localhost now reports `Animals 0` instead of `Animals 2`.
- Repaired the local source-family enrichment runner so the missing NYC-specific enrichment no longer crashes the complete candidate pass. Its `1,955` NYC heat records remain explicitly held and unchanged. Persisted the separate ignored review artifact `data/resource-map/.engine/source-family-enriched.jsonl`: all `4,249` candidates are represented, `741` pass automated enrichment gates (`660` official heat records plus `81` Chicago library records), and `3,508` remain held. No candidate was approved, imported, promoted, or published.
- Focused Prettier, ESLint, `131` map/data tests, thresholds, boundaries, interaction locks, React Grab ownership, diff checks, and localhost browser verification passed with no page errors. No database write, deployment, commit, or push occurred.

2026-08-02 20:55 EDT - audited list-only platform organization addresses.

- Confirmed Caught Dreaming, FOCUOS, and A Taste of Theatre have complete structured profile addresses; Mommy's Gift to Me has no address. The address fields do not retain actor-level edit history, so their presence does not prove whether the account holder or an administrator entered them.
- Caught Dreaming and A Taste of Theatre are explicitly marked online-only, which correctly clears coordinates during profile saves and causes the repair sweep to skip them. FOCUOS is in-person, but its stored city is misspelled `Boliingbrook`; this likely prevented the configured geocoder from resolving it.
- Read-only Census checks returned exact matches for all three entered addresses, including a normalized FOCUOS match at `430 E Briarcliff Rd, Bolingbrook, IL 60440`. No organization profile, coordinates, database record, or production state changed.

2026-08-02 21:18 EDT - repaired FOCUOS public map location.

- Corrected the live FOCUOS profile from `430 E. Briarcliff, Boliingbrook, IL 60440` to the Census-normalized `430 E Briarcliff Rd, Bolingbrook, IL 60440` and added exact coordinates `41.69891848779, -88.052010949089`.
- Preserved every other profile field and used an `updated_at` concurrency guard. A fresh production read and direct localhost `/find` marker-source check confirmed the corrected address and coordinates.
- No other organization, candidate, application code, deployment, commit, or push changed.

2026-08-02 21:24 EDT - checked FOCUOS on production `/find`.

- Confirmed the live production directory lists FOCUOS, but its server-rendered organization snapshot still contains the old `Boliingbrook` address and null coordinates. FOCUOS therefore does not yet appear as a production map marker despite the corrected live database row and working localhost marker.
- Rechecked the uncached production route after the normal five-minute organization-cache interval; the stale snapshot remained. No production, database, code, deployment, commit, or push change occurred.

2026-08-02 22:39 EDT - loaded the complete staged candidate corpus on localhost `/find`.

- Pointed the ignored local environment at the explicit `4,249`-row `source-family-enriched.jsonl` review artifact. Added a separate opt-in that includes its `271` Wikidata discovery candidates only as `superadmin_preview` items; default and production public-result filtering remains unchanged.
- Restarted only Coach House localhost:3000 in the detached `coach-house-find-3000` screen. The API returned all `4,249` candidates with coordinates.
- Direct browser verification reported `Active 4,265`: `4,249` staged candidates plus `16` platform organizations. The map represented all `4,262` coordinate-bearing items in `4,098` same-location-aware features with no page errors.
- Focused formatting, ESLint, and all `15` public-resource query tests passed. No candidate was approved, imported, promoted, published, or written to the database, and no deployment, commit, or push occurred.

2026-08-02 22:41 EDT - clarified domestic-violence map designations.

- Renamed the shared `family_domestic_violence` display label from `Domestic Violence` to `Domestic Violence Support`. The category key, classification, filtering, and underlying resource data remain unchanged.
- Focused formatting, ESLint, and all `26` public-map item tests passed. The current local staged corpus contains no matching marker to exercise visually, but the shared category contract now rejects the stale label. No deployment, commit, or push occurred.

2026-08-02 22:47 EDT - replaced community-fridge bread icons.

- Changed `food_community_fridges` from the filled bread icon to a distinct Lucide shopping-basket stroke icon while preserving its cyan marker color, label, category key, and filtering behavior. Top-level Food retains its blue shopping-cart icon.
- Advanced active marker and archived cluster sprite identities so open maps cannot reuse the previous bread artwork.
- Focused formatting, ESLint, and `81` resource-item, marker-rendering, and WebGL tests passed. The current local staged corpus contains no community-fridge marker to exercise visually. No deployment, commit, or push occurred.

2026-08-02 23:21 EDT - staged and previewed the Brooklyn/NYC nonprofit workbook.

- Added deterministic ingestion for the supplied `762`-organization XLSX, including namespaced Excel parsing, row-5 headers, blank cells, preserved multiline evidence, stable record IDs, source metadata, and safe splitting of `22` multi-location organizations into `797` location-aware candidates.
- Geocoded `655` of `682` physical locations (`10` source coordinates, `632` Census matches, and `13` Nominatim matches). Kept `115` service-area/list-only records and `27` failed physical addresses list-only. Two ambiguous source-coordinate cells were not assigned directly. Tightened the shared geocoder to reject provider matches that disagree with the requested ZIP after catching a false Manhattan-to-Riverhead result; the final batch has no NYC-region coordinate outliers.
- Fixed taxonomy handling so human-service organizations with `Rescue` in their names are not classified as Animals without explicit animal-service context. The batch has zero Animal false positives, no strong duplicate against the existing `4,249` candidates, and `0/797` publishable records because provider comparisons and approval are still incomplete.
- Wrote ignored local candidate and merged preview artifacts, pointed ignored `.env.local` at the reversible `5,046`-record merged preview, and raised only the local-preview ceiling from `5,000` to `10,000`. Browser verification reported `Active 5,062`, all `5,046` external candidates, all `797` incoming records, `655` incoming map points, a visible Mapbox canvas, and no page or console errors.
- Validation passed for `47` ingestion/data-engine tests, `15` public-query tests, focused ESLint/formatting, thresholds, boundaries, local-file validation, import dry-run, and diff checks. The data was not imported, approved, promoted, published, or written to the database; production was unchanged.

2026-08-02 23:41 EDT - replaced every `/find` right rail with one permanent drawer.

- Routed public, authenticated, desktop, and mobile `/find` presentations through the existing bottom drawer. Preserved the retired right-rail components in place for reversal, but removed their Home Canvas and AppShell registrations from the live assembly.
- Moved Find, Guides, and Saved into the drawer above its active content. The minimum snap permanently shows those tabs plus resource search and category filters; selecting Guides or Saved opens the middle snap, opening details returns to Find, and collapsing resets to the searchable Find state.
- Made the drawer non-dismissible with no zero-height snap. Downward dragging and Escape retain the minimum snap instead of removing the surface.
- Matched the supplied Welcome-control treatment with `bg-card/92`, the corresponding light border, `dark:bg-input/30`, the dark input border, a small shadow, and backdrop blur. Updated onboarding copy and its icon so it describes the bottom drawer instead of the retired right rail.
- TypeScript, focused ESLint/formatting, `64` focused acceptance tests, four `/find` visual behavior checks, thresholds, boundaries, interaction locks, React Grab ownership, raw-button, workspace-surface, route, and diff checks passed. Desktop, mobile, light, and dark browser checks confirmed three tabs, visible minimum-snap search/filters, no right rail, correct theme surface values, non-dismissal, and no page errors. No `/find` screenshot baseline exists, so the existing behavioral visual spec was updated instead. The broad structure check remains blocked by unrelated existing over-budget files; touched `/find` files remain below the enforced limit. No deployment, commit, or push occurred.

2026-08-02 23:51 EDT - stopped `/find` from requesting location again on reload.

- Changed the consent card to stay closed until tab-scoped location state is resolved, removing the reload flash that previously remained visible while Mapbox initialized.
- Kept the existing privacy boundary: only the grant boolean is retained in session storage; coordinates remain unstored and never leave the browser location API.
- A stored grant now checks the browser permission state before requesting coordinates. Persistent permission silently restores neighborhood focus, while expired or one-time permission remains closed and makes zero automatic geolocation requests; the location button remains available for a deliberate retry. New visitors without a grant still receive the centered consent card.
- TypeScript, focused ESLint/formatting, `26` location and `/find` acceptance tests, thresholds, boundaries, interaction locks, React Grab ownership, diff checks, and three browser permission-state checks passed with no page errors. No deployment, commit, or push occurred.

2026-08-02 23:53 EDT - softened the permanent `/find` drawer surface.

- Increased the live bottom drawer's top-corner radius from the shared primitive's effective `10px` to `44px` by overriding the direction-specific Vaul state, and raised its backdrop blur from `8px` to `12px` without changing its opacity, snap behavior, or contents.
- Focused formatting, ESLint, all `16` `/find` route tests, diff validation, and direct mobile browser style inspection passed with three tabs retained and no page errors. No deployment, commit, or push occurred.

2026-08-03 00:01 EDT - unified `/find` overlay glass across themes.

- Replaced the separate opaque light-mode and translucent dark-mode treatments with one shared `border-input bg-input/30 backdrop-blur-md` surface for the permanent drawer, location control and consent card, Active status pill, Welcome control, map alerts, and shared drawer action surfaces.
- Focused formatting, ESLint, `52` `/find` acceptance tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. Direct light/dark browser inspection confirmed the drawer, location surfaces, and Active pill share the same class contract and `12px` backdrop blur. The full worktree typecheck remains blocked by unrelated existing errors. No deployment, commit, or push occurred.

2026-08-03 00:11 EDT - matched complete `/find` overlay theming to dark mode.

- Scoped dark theme tokens to every shared map-overlay glass root, so light-mode `/find` now uses the dark-mode border, foreground, muted text, icons, inputs, and descendant control styling in addition to the existing glass background and blur. The surrounding page and day basemap remain light.
- Focused formatting, ESLint, `52` acceptance tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. A direct light-page browser inspection confirmed the light root remains active while the drawer, search field, location control/card, and Active pill resolve to dark-mode colors and borders with the same `12px` blur. No deployment, commit, or push occurred.

2026-08-03 00:20 EDT - made `/find` result cards responsive and more legible.

- Replaced the full-width result stack with a container-driven auto-fill grid using roughly `22rem` minimum tracks. Narrow phones retain one column; wider drawers add columns without stretching a small number of cards across the complete surface. Pagination remains outside the grid.
- Increased card padding, spacing, identity artwork, titles, metadata, descriptions, activity labels, chips, and the View touch target. Grid cards now stretch to equal row height while preserving existing selection, details, pagination, and React Grab ownership behavior.
- Focused formatting, ESLint, `33` list/sidebar tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. Browser layout checks confirmed one `334px` column at `390px`, two `372px` columns at `820px`, and three `397px` columns at `1280px`; titles render at `16px`, metadata at `14px`, and standard card padding at `16px`. No deployment, commit, or push occurred.

2026-08-03 00:22 EDT - centered the `/find` drawer tabs.

- Changed the Find, Guides, and Saved tab list from a full-width left-aligned row to an intrinsic-width group centered within the drawer. Centered each tab label and removed responsive flex growth so the group stays centered consistently.
- Focused formatting, ESLint, `42` sidebar and route tests, thresholds, boundaries, React Grab ownership, and diff checks passed. Browser inspection measured identical `640px` drawer and tab-group centers at `1280px`, with a `0px` alignment delta. No deployment, commit, or push occurred.

2026-08-03 00:29 EDT - adapted `/find` Guides and Saved for the full-width drawer.

- Replaced the Guides tab's fixed two-column layout with an `18rem` container-driven auto-fill grid and the Saved tab's stretched vertical stack with a `20rem` auto-fill grid. Both remain one column when narrow and add bounded columns as drawer width grows.
- Increased Guides header/card spacing, titles, metadata, and padding. Increased Saved section/card padding, titles, descriptions, location text, and the minus control to a `44px` touch target; added consistent horizontal drawer insets to both tabs.
- Focused formatting, ESLint, `45` sidebar/list tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. Browser checks confirmed Guides uses one `326px` column at `390px` and four `293px` columns at `1280px`; Saved uses three `387px` columns at `1280px`, `16px` titles, and `44px` remove targets. No deployment, commit, or push occurred.

2026-08-03 00:49 EDT - added a full-canvas `/find` drawer reading mode.

- Changed the third drawer snap to the exact measured canvas height, placed its portal above every map control, and made the full state square, `95%` opaque, and `40px` blurred. Lower snaps retain the rounded floating treatment. Increased the shared map-overlay blur to `24px` and made list, guide, and detail cards more opaque with their own blur.
- Normalized platform-organization and imported-resource profiles onto the same responsive shell, identity layout, section surfaces, typography, spacing, and `44px` controls. Both profiles center in a `48rem` desktop reading column, remain full width on mobile, prevent horizontal overflow, and use the detail viewport as their only vertical scroll owner.
- Full lint, TypeScript, `71` focused tests, thresholds, boundaries, interaction locks, React Grab ownership, workspace-surface, raw-button, formatting, and diff checks passed. The broader public-map run passed `240/242`; two unrelated existing resource-catalog schema assertions remain out of sync with migration content. Desktop and mobile browser checks confirmed exact canvas-top alignment, hidden underlying map controls, `768px` desktop profile width, zero mobile/page overflow, working internal scrolling, and no page errors. No deployment, commit, or push occurred.

2026-08-03 01:04 EDT - darkened `/find` drawer tabs in light mode.

- Scoped Find, Guides, and Saved to dark zinc text on the light page while preserving the existing light-on-dark treatment in dark mode. Active and hover states now use the darkest zinc foreground; inactive tabs use zinc-700.
- Focused formatting, ESLint, and `42` route/sidebar tests passed. Browser-computed light-mode colors resolved dark for all three tabs, while dark-mode colors remained unchanged. No deployment, commit, or push occurred.

2026-08-03 01:08 EDT - darkened the `/find` Welcome control text in light mode.

- Scoped zinc-950 foreground text to the Welcome/Hide welcome button and its hover state on the light page. Dark mode and the control's existing surface treatment remain unchanged.
- Focused formatting, ESLint, `20` route/onboarding tests, and a browser-computed light-theme color check passed. No deployment, commit, or push occurred.

2026-08-03 01:37 EDT - corrected `/find` detail controls and directional scroll fades.

- Restyled organization and resource detail-header controls with white translucent surfaces, light borders, and dark foregrounds on the light page. Dark mode now uses a lighter zinc surface while preserving the active favorite treatment.
- Replaced shared scroll-fade timeline assumptions with measured scroll state. The top fade is explicitly hidden at the start and appears only after scrolling; the bottom fade clears at the end.
- Focused formatting, ESLint, `58` `/find` and scroll-fade tests, thresholds, boundaries, interaction locks, and React Grab ownership passed. Browser checks confirmed the light controls and top/mid-scroll fade states. The full worktree typecheck remains blocked by unrelated existing errors. No deployment, commit, or push occurred.

2026-08-03 01:48 EDT - fixed lower-snap profile scrolling and stable drawer color.

- Sized the `/find` drawer's internal content viewport to the active snap height while leaving the Vaul shell at its full drag height. Organization and resource profiles can now reach their true bottom at every drawer height.
- Removed the fullscreen-only background, border, and blur overrides; expanding the drawer now changes only its top-corner geometry.
- Focused formatting, ESLint, `42` route/sidebar tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. A mobile browser check reached the exact middle-snap scroll maximum and confirmed identical computed background and blur values before and after fullscreen expansion. No deployment, commit, or push occurred.

2026-08-03 01:57 EDT - stabilized `/find` drawer tab hovering.

- Removed the four-pixel dead zones between Find, Guides, and Saved while preserving their centered layout. Made the active underline ignore pointer hit-testing so it cannot interrupt a trigger hover.
- Focused formatting, ESLint, `42` sidebar/route tests, thresholds, boundaries, interaction locks, React Grab ownership, and diff checks passed. Browser inspection confirmed a `0px` tab gap, zero dead pointer positions across the group, and a stable hovered Guides trigger. No deployment, commit, or push occurred.

2026-08-03 01:59 EDT - removed `/find` category-pill icon containers.

- Removed the circular background and rounding behind every category-filter icon while preserving the icon size, taxonomy color, pill spacing, selection state, and button behavior. Selected icons now use the direct foreground color for contrast without the former backing circle.
- Focused formatting, ESLint, `42` sidebar/route tests, thresholds, boundaries, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 02:01 EDT - blended `/find` detail-header controls into their parent surface.

- Removed the separate light/dark background fills from organization and resource detail-header controls. Their idle and hover backgrounds now remain transparent so the drawer/profile surface shows through; size, border, icon feedback, focus behavior, and active-favorite styling remain intact.
- Focused formatting, ESLint, `49` detail/sidebar/route tests, thresholds, boundaries, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 12:45 EDT - lowered the collapsed `/find` drawer.

- Replaced the lowest drawer snap's height-based scaling with a fixed `168px` stop so the collapsed sheet shows its controls without exposing the first organization or resource card. Higher snaps retain the complete list and existing behavior.
- Focused ESLint, formatting, `19` snap-point and `/find` route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. Localhost returned HTTP 200, but automated visual inspection remained blocked by the existing dev session's invalid HMR response, so browser-visible confirmation is still pending. No deployment, commit, or push occurred.

2026-08-03 12:48 EDT - constrained and pill-shaped the `/find` search field.

- Centered the live `/find` search field with a `36rem` maximum width and changed its local radius from rounded rectangle to pill, without modifying the shared Input primitive or the category-filter width.
- Focused ESLint, formatting, `42` sidebar and route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 12:51 EDT - tightened wrapped `/find` card metadata.

- Removed the extra vertical flex-row gap when organization and resource metadata wraps onto a second line, without changing its horizontal spacing, typography, or card layout.
- Focused ESLint, formatting, `29` sidebar and React Grab tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 12:54 EDT - matched the Saved list viewport to `/find` drawer glass.

- Removed the Saved organization section's nearly opaque background in the live drawer and made its exact ScrollArea transparent, allowing the existing drawer glass and blur to show through without stacking another dark layer. Saved cards, borders, scrolling, and controls remain unchanged.
- Focused ESLint, formatting, `42` sidebar and route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 12:57 EDT - lightened `/find` detail action glass.

- Replaced transparent detail action backgrounds, which exposed the darker profile panel, with the drawer's `bg-input/35` glass and `backdrop-blur-xl` treatment. Back, share, and inactive favorite controls now use that lighter surface; the active favorite state remains blue.
- Focused ESLint, formatting, `49` detail/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:03 EDT - kept `/find` fullscreen drawer content aligned with its shell.

- Normalized released drawer snap values against the current measured snap points. Small viewport-height changes can no longer leave the shell fullscreen while its internal content viewport and corner state remain at the middle snap.
- Focused ESLint, formatting, `47` snap/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:08 EDT - aligned the `/find` drawer to the canvas frame.

- Reduced the floating drawer's top radius from `44px` to the canvas frame's `28px`. Fullscreen keeps no local radius so the canvas remains the single outer clipping boundary, consistent with the design guidance to keep one radius family per view.
- Replaced the rounded pixel fullscreen snap with a true `100%` container snap and sized the shell to its portal container. This removes the subpixel top gap and fully covers the map canvas.
- Focused ESLint, formatting, `47` snap/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. The existing development session returned HTTP 200, but its hydration/HMR issue blocked the attempted live drag measurement. No deployment, commit, or push occurred.

2026-08-03 13:13 EDT - raised the `/find` welcome overlay above the drawer.

- Moved the map onboarding fragment into a dedicated top overlay layer rendered after the drawer portal. The welcome card and preview toggle now stack above every drawer snap without changing the shared Card primitive or map controls.
- Focused ESLint, formatting, `46` onboarding/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:15 EDT - retained rounded `/find` drawer corners at fullscreen.

- Removed the fullscreen-only sharp-corner override. The drawer now keeps the canvas-matched `28px` top radius at every snap while retaining its exact `100%` fullscreen coverage.
- Focused ESLint, formatting, `47` snap/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:19 EDT - removed the false initial `/find` card selection.

- Removed card-wide `focus-within` accent styling from organization and resource results. Browser or drawer focus on the first nested action can no longer make the first result look selected; the card's own keyboard focus, hover, and real selected states remain intact.
- Focused ESLint, formatting, `45` list/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:23 EDT - separated the `/find` welcome trigger and dialog stack levels.

- Kept the closed Welcome/Hide welcome control at the normal map-control layer below the drawer. Only the opened onboarding dialog now rises to the drawer's top layer, so its content remains visible without leaving the trigger floating over a closed drawer.
- Focused ESLint, formatting, `46` onboarding/sidebar/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 13:39 EDT - stabilized intermittent `/find` reloads around React Grab.

- Replaced the stale hardcoded React Grab `0.1.34` browser runtime with the installed package version and upgraded the package and lockfile from `0.1.47` to `0.1.50`. Added an acceptance assertion so the browser runtime cannot silently drift from the dependency again.
- Restarted the existing port-3000 development session. A clean browser confirmed the `0.1.50` runtime, and three consecutive `/find` loads completed with HTTP 200, a complete document, and no page or console errors. The public resource-map request remains slow in development and was observed at `15–42s`, but it did not produce the reported client error.
- Dependency integrity, large-file, focused ESLint, `37` React Grab/onboarding/route tests, thresholds, interaction locks, React Grab ownership, and diff checks passed. No deployment, commit, or push occurred.

2026-08-03 14:09 EDT - documented the production user-audience overview.

- Created a partner-ready brief from live Supabase, Stripe, and Vercel aggregates. It separates 180 external non-synthetic accounts from sign-ins, tracked activity, testers, organization ownership, and strict current payers; it also documents the May signup wave and the historical acquisition-attribution gap.
- Saved a PII-free aggregate snapshot, reproducibility notebook, and validated interactive report artifact under `docs/briefs/2026-08-03-user-audience-overview/`. Aggregate reconciliation and artifact validation passed; Jupyter is not installed, so the notebook itself was not executed. No production data, deployment, commit, or push was changed.

2026-08-03 14:21 EDT - closed the user-audience analysis handoff safely.

- Replaced the ambiguous partner-facing “ever signed in” wording with “authenticated at least once” and documented that Supabase may record the session during signup or email confirmation; the saved report artifact still validates.
- Preserved the existing dirty worktree without cleanup, staging, commit, push, deployment, or product-code edits. The pre-existing modification to `src/components/public/public-map-index/member-rail.tsx` was not made or changed in this session. Repository files and the current monthly runlog contain the durable handoff for the next Codex chat.

2026-08-03 14:25 EDT - made `/find` drawer search responsive and complete.

- Search focus, typing, and category selection now expand the permanent drawer from its collapsed controls stop to the middle results stop. The field has a dedicated clear action and Escape handling; result cards retain their existing organization/resource detail behavior.
- Replaced per-keystroke Next.js route navigation with a debounced native history update, preserving shareable query/category URLs without server requests. Split immediate drawer filtering from deferred map updates, cached normalized resource search text, and prewarmed that cache when the catalog resolves.
- Added result counts, map-update feedback, full-directory loading feedback, structural loading placeholders, retryable load failures, and actionable query/category/zero-resource empty states. Resolved catalogs are cached per endpoint so drawer remounts do not redownload the `9.2 MB` payload.
- The current local endpoint measured `8.42s` for `9,209,360` bytes. Controlled mobile browser verification confirmed snap `0 → 1`, zero `/find` requests while typing, a `60.9ms` DOM result update, loading feedback while the catalog is pending, clearable empty results, working result-to-detail navigation, and no page errors. The actual full-payload headless-browser timing was not captured; the real endpoint and interaction states were verified separately.
- Focused ESLint/formatting, TypeScript through the focused test transforms, `79` search/sidebar/performance tests, thresholds, boundaries, interaction locks, React Grab ownership, raw-button, workspace-surface, and diff checks passed. The full worktree TypeScript check remains blocked by unrelated existing errors in admin actions, resource-map adapters/taxonomy, workspace fixtures, and other untouched files. No deployment, commit, or push occurred.

2026-08-03 15:08 EDT - added zoom-aware `/find` marker relevance.

- Kept the complete resource catalog in the existing Mapbox WebGL source while replacing all-zoom marker eligibility with deterministic regional, city, neighborhood, and full-detail tiers. Broad views show one representative per top-level category and map tile, neighborhood views add up to two per category, and zoom `15+` reveals every resource in the viewport.
- Added dedicated saved-marker priority and retained selected-marker priority at every zoom. When location access is available, the nearest resource wins each local category slot; search, category filters, same-location groups, and the full drawer directory remain unchanged and complete.
- On the real `4,734`-feature local catalog, the source-wide eligibility tiers contain `21` overview representatives, `41` cumulative city representatives, `264` cumulative neighborhood representatives, and all `4,734` close-detail resources. A settled broad Chicago-centered browser view rendered `10` overview pins; neighborhood and close views rendered only the points inside their progressively smaller viewports. No page or console errors occurred.
- Added the design record at `docs/plans/2026-08-03-public-map-marker-relevance-design.md`. Focused formatting, ESLint, `93` marker/WebGL/route/location tests, thresholds, boundaries, interaction locks, React Grab ownership, raw-button, and diff checks passed. Full-worktree TypeScript remains blocked by unrelated existing admin, resource-map, workspace, and test-fixture errors; none referenced this marker relevance work. `/find` has no dynamic visual baseline, so no snapshot was updated. No deployment, commit, or push occurred.

2026-08-04 11:49 EDT - investigated a reported production organization-onboarding block.

- Traced the likely newest external account through live Supabase auth, organization, membership, journey, activation, page-health, API, and Stripe checkout state without changing production data. Signup and free builder onboarding completed, but no organization write or slug-availability query reached Supabase for that account, and no membership/RLS conflict or matching client crash was recorded.
- Confirmed three Organization checkout sessions were created within two seconds but remain open and unpaid with no customer, subscription, organization record, or accelerator entitlement. The failure boundary is before organization submission or checkout completion, not a rejected database save.
- The production slug endpoint currently returns `200` for a fresh valid slug. Exact client-side validation state remains unverified without the affected user's identity or screenshot. No product code, account, database, billing, deployment, commit, or push changed.

2026-08-04 16:44 EDT - corrected the production onboarding diagnosis to Karissa's account.

- Re-ran the investigation against the user-confirmed account. Production shows five successful workspace-setup submissions, repeated `200` organization upserts and slug checks, a populated organization profile, and valid active Operations Support accelerator access. The prior 11:49 finding concerned the wrong account.
- Confirmed the published `Organization setup` accelerator module has no `module_progress` or assignment-submission row for Karissa. Her persisted workspace board still points to that module's lesson step with no completed step IDs because the embedded organization form redirects after saving without invoking the accelerator completion path.
- Found secondary stale metadata: workspace onboarding is active at stage 2 while an older completion timestamp remains set. No production account, database, billing, product-code, deployment, commit, or push change occurred; this remained a read-only diagnosis.

2026-08-04 17:53 EDT - designed the safe Finance, fundraising, Find, and release plan.

- Audited the staged release tree read-only and confirmed it is 50 commits behind `origin/main`, contains 989 staged paths, conflicts with current upstream across hundreds of files, rewrites applied migrations, removes released behavior, and fails required quality gates. A hard push would either lose the staged index or regress production, so no commit, push, merge, deployment, or production mutation occurred.
- Audited the existing workspace, Stripe billing, fiscal sponsorship, public-profile, map persistence, location, weather, and resource-publication boundaries. The recommended design reuses `economic-engine` as Finance, separates organization Stripe balances from sponsor-held restricted funds, uses connected-account direct charges, keeps `/find/[slug]` canonical, and gates public data at the server payload.
- Added `docs/plans/2026-08-04-finance-fundraising-find-release-design.md` with seven Mermaid-backed merge batches, permissions, data model, Connect webhook/ledger/reconciliation design, fiscal grant flow, map signup replay, lists, location repair, weather relevance, resource scaling, caching, observability, rollback, and production gates. Implementation remains blocked on product and fiscal-custody approval.

2026-08-04 17:59 EDT - completed the Finance PRD traceability pass.

- Re-read the source objective and all 869 lines of its Stripe Connect specification, then mapped every requested outcome to a named design section and verification artifact. No image references were present in the supplied attachment directory, so the PRD now has a three-pass visual-review gate for when they arrive.
- Closed planning gaps for fiscal custody options, minimal Finance states, campaign constraints, existing Payment Link assignment, API contracts, connected-event identity, non-Stripe imports with explicit provenance, public visibility, rate limiting, repository ownership, Stripe/Vercel environments, Dashboard setup, local CLI workflow, edge cases, and objective traceability.
- Kept implementation blocked on the sponsor-custody and Finance information-architecture decisions. No staging, commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-04 18:14 EDT - added the Finance system React Flow prototype.

- Added a new `Finance > Flows > Finance system canvas` entry to the admin Prototype Lab. The isolated canvas uses the shared workspace node frame for a zero-state Finance overview, opportunity finder, selected application progression, public fundraiser preview, and sponsor-held restricted-fund reporting.
- Kept nonzero values explicitly labeled as sample data. The prototype supports opportunity reveal/back navigation, Finance sub-tabs, Stripe connected/disconnected preview states, public fundraiser visibility, donation-checkout messaging, CSV export feedback, draggable nodes, and fit/zoom controls without changing production Finance or backend behavior.
- Focused ESLint, Prettier, 12 Prototype Lab acceptance tests, raw-button, React Grab, workspace-surface, and interaction-lock checks passed. The authenticated local admin route rendered without console errors, and browser checks confirmed the selected-opportunity, Stripe, visibility, and CSV interactions. No staging, commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-04 22:07 EDT - corrected the Finance Prototype Lab deliverable to a planning graph.

- Removed the mistaken product-interface prototype. Replaced it with `Finance > Roadmaps > Finance release plan`, a React Flow representation of the existing PRD rather than a simulation of Finance, fundraising, Stripe, or fiscal-fund UI.
- Organized the roadmap into guardrail/approval, release-sequence, and research lanes. It now shows the unsafe starting tree, exactly seven numbered merge batches, one proof gate after every batch, four approval decisions, seven batch-aligned research tracks with required evidence, rollback guidance, and the verified gradual-production end state.
- Added step navigation, fit/zoom controls, a minimap, solid merge-order edges, dashed research inputs, and dotted approval dependencies using the shared workspace node frame. The graph opens at the beginning and keeps the full-plan overview available without duplicating the PRD's product UI.
- Focused Prettier, ESLint, touched-file TypeScript filtering, `13` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, and interaction-lock checks passed. Authenticated browser checks confirmed the beginning, Batch 7, end state, full overview, and zero console errors. No product implementation, staging, commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-04 23:34 EDT - increased Finance planning graph trackpad pan speed.

- Raised the React Flow two-finger pan multiplier from its implicit `0.5` default to `1.2`, making navigation across the wide seven-batch plan 2.4 times faster without changing zoom behavior, controls, graph content, or production surfaces.
- Started a continuing goal to evolve the planning graph from user feedback and forthcoming reference screenshots. No commit, push, merge, deployment, or production mutation occurred.

2026-08-04 23:53 EDT - mapped the full Finance PRD diagram set into Prototype Lab.

- Kept the seven-batch roadmap as the default view and added six progressively disclosed React Flow views for system ownership, payment and fiscal custody, Connect webhook truth, canonical data boundaries, in-map signup replay, and cooling-center weather relevance. The seven selectable views now correspond to all seven Mermaid diagrams in the source PRD without introducing product-interface mockups.
- Added view-local navigation, readable initial focus groups, one consistent minimap/zoom/pan surface, semantic headings, pressed states, 44px controls, and a compact responsive header. Two-finger panning remains at the explicit `1.2` multiplier and was reverified after the view refactor.
- Focused Prettier, ESLint, `15` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, and interaction-lock checks passed. Authenticated desktop and mobile browser checks confirmed every view, deep fiscal-data navigation, exact 1.2x gesture movement, responsive controls, and zero console errors. No visual baseline covers this dynamic Prototype Lab entry. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 00:02 EDT - completed Finance planning graph section traceability.

- Added a separate cross-batch dependency view that reproduces the PRD's branching release prerequisites without crossing those lines through the detailed linear merge-and-proof roadmap. The Prototype Lab entry now contains eight planning views: one detailed roadmap plus the seven source diagrams.
- Added a 33-section PRD coverage manifest and acceptance proof. Every top-level PRD section now maps to existing nodes in a named view, and the test fails if a heading is added, removed, reordered, or points to missing graph evidence.
- Kept the dependency view focused on Batch 1 initially, with Public path, Finance path, and Cutover navigation plus the existing fit-all overview. Reduced nonessential mobile header copy so the focused baseline node no longer sits behind the overlay.
- Focused Prettier, ESLint, `16` Prototype Lab tests, touched-file TypeScript filtering, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop/mobile browser checks confirmed 7 dependency nodes, all 10 PRD dependency edges, no header/node overlap, exact 1.2x trackpad movement, and zero console errors. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 00:44 EDT - made Finance planning views shareable and reload-stable.

- Added validated `planView` URL state for all eight Finance planning graphs while preserving the existing Prototype Lab entry query, hash, and unrelated parameters. The default Roadmap keeps the URL clean, and unknown view IDs fall back safely to Roadmap.
- View switching now updates the canvas immediately without a server navigation. Direct links and reloads preserve the selected graph, and browser Back/Forward restores prior graph selections.
- Focused Prettier, ESLint, `17` Prototype Lab tests, and authenticated browser checks passed. Browser QA confirmed switching, reload persistence, Back navigation, invalid-link fallback, a clean default route, and zero console warnings or errors. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 00:57 EDT - added cross-view Finance plan search and exact-node links.

- Added an accessible `Find step…` command dialog that searches titles, summaries, statuses, dependencies, and evidence across all eight planning views. The empty query shows one start point per view, result lists are capped at 12, duplicate concepts remain disambiguated by view labels, and unmatched searches provide a clear recovery prompt.
- Added validated `planNode` URL state. Selecting a result switches views without server navigation, focuses the exact React Flow node, preserves the target across reload, and restores view/node state through browser Back/Forward. View-tab changes clear stale node targets.
- Followed the Geist token guidance in `docs/design.md`, reused shadcn command/dialog/button/badge primitives, retained the 1.2 trackpad pan multiplier, kept 44px controls, and enforced 16px search text on mobile. Focused Prettier, ESLint, touched-file TypeScript filtering, and `19` Prototype Lab tests passed. Authenticated browser checks covered mouse and Enter-key selection, direct reload, Back/Forward, empty results, a `390x844` viewport with no horizontal overflow, and zero console warnings or errors. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 01:07 EDT - added the Finance production-assurance graph.

- Added a ninth `Assurance` planning view that connects authorization/RLS, financial truth, public privacy/resilience, three required verification families, four open approvals, a clean release artifact, monitoring/rollback readiness, staff and organization canaries, and the verified definition of done. Its 12 nodes and 14 causal edges synthesize the PRD's security, failure-state, accessibility, observability, test-matrix, cutover, and completion sections without adding product mockups.
- Strengthened the 33-section coverage manifest so seven previously fragmented PRD sections point to dedicated Assurance evidence. Cross-view search includes every new node, including exact security controls such as raw-body Connect verification, tenant isolation, rate limits, public redaction, and CSV formula neutralization.
- Added automatic active-tab reveal for horizontally scrolled mobile view navigation; direct Assurance links no longer leave the selected tab off-screen. Focused Prettier, ESLint, touched-file TypeScript filtering, and `19` Prototype Lab tests passed. Authenticated browser checks confirmed all 12 nodes, all 14 edges, section focus, exact-node search/reload, `390x844` responsive navigation with no document overflow, and zero console warnings or errors. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 01:19 EDT - exposed the Finance PRD coverage audit in Prototype Lab.

- Added an accessible `33 sections…` coverage navigator beside the graph finder. Reviewers can search all top-level PRD sections and mapped node content, open one source section, inspect every evidence node grouped by view, and jump directly to an exact React Flow node and shareable URL.
- Built the navigator from the validated coverage manifest and current graph definitions, so section order, view names, node titles, and evidence references cannot drift into a separate hand-maintained UI. The empty state has a clear recovery action, section detail has explicit Back navigation, and the dialog resets cleanly between opens.
- Reused the existing dialog, input, scroll-area, button, and badge primitives; retained 44px controls and 16px mobile search text. Focused Prettier, ESLint, touched-file TypeScript filtering, and `20` Prototype Lab tests passed. Authenticated desktop and `390x844` browser checks confirmed all 33 sections, evidence search and grouping, Back and empty-state recovery, cross-view exact-node jumps, active-tab visibility, no document overflow, and zero console warnings or errors. No commit, push, merge, deployment, Stripe mutation, database mutation, or production change occurred.

2026-08-05 01:25 EDT - made Finance roadmap dependencies self-validating.

- Replaced hand-maintained approval and research lines with structured batch inputs, then generated the visible React Flow dependencies from that shared data. This repaired the missing Finance-approval path into Batch 2 and fiscal-policy research path into Batch 7.
- Added a Decisions roadmap target for the four unresolved approvals and topology tests covering every batch input, source kind, graph node, rendered edge, research track, approval, and the exact 31-edge total.
- Focused Prettier, ESLint, `15` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. Authenticated browser checks confirmed all 31 nodes, all 31 edges, both repaired paths, decision navigation, and zero console warnings or errors. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 01:35 EDT - exposed the Finance plan's open implementation inputs.

- Added an `11 inputs` navigator that derives four reviewer decisions and seven research tracks from the roadmap source instead of maintaining a second checklist. Each item names the batch or batches it blocks and jumps to the exact shareable roadmap node.
- Kept the queue inside the planning graph and separated `Need from you` from `Research before coding`; it does not invent implementation progress or product-interface mockups. The visual-reference decision remains explicitly pending until screenshots arrive.
- Focused Prettier, ESLint, `16` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed all 11 items, scrolling without document overflow, exact-node navigation, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 01:45 EDT - completed Finance objective-level graph traceability.

- Expanded the existing PRD coverage navigator with `Sections 33` and `Objectives 27` modes. Every requested outcome from the source Objective Traceability table now exposes its planned evidence, mapped graph nodes grouped by view, search, and exact shareable node navigation without adding another canvas or product mockup.
- Added a source-parity manifest and index generated against current graph definitions. Tests now fail when any of the 27 objective rows changes, an evidence mapping points to a missing node, duplicate evidence is introduced, or objective search loses coverage; the seven-diagram objective explicitly maps all seven source diagram views.
- Focused Prettier, ESLint, `18` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed all 27 objectives, search, grouped evidence, exact cross-view node jumps, scrolling without document overflow, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 02:32 EDT - made all seven Finance source diagrams reviewable and parity-checked.

- Added a third `Diagrams 7` mode to PRD Coverage. Each source Mermaid diagram now shows its section, diagram kind, source-connection count, complete React Flow node set, search, grouped evidence, and exact-node navigation without adding another canvas view.
- Parsed all seven source Mermaid blocks in acceptance tests and compared their 81 total flow, sequence, and entity connections with graph source-step counts. The audit found and restored the webhook's durable `2xx` acknowledgment, corrected signup's result source, and recorded all nine signup messages while keeping its two graph-only privacy inputs explicit and excluded from source parity.
- Focused Prettier, ESLint, `19` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed seven diagrams, eight webhook edges, nine signup edges, exact cross-view jumps, no document overflow, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 02:40 EDT - made the Finance graph a truthful living readiness plan.

- Added explicit source-controlled input states to all four decisions and seven research tracks, plus execution states to all seven batches. Open-input counts and batch readiness now derive from those shared definitions instead of labels or editable local-only UI.
- Replaced the standalone inputs dialog with `Implementation readiness`, retaining `Inputs 11` and adding `Batches 7`. The ordered batch view shows ready, blocked, in-progress, and merged counts; each batch exposes its open-input count and unresolved predecessor, then jumps to the exact shareable roadmap node.
- Added transition tests proving the current `0 ready / 7 blocked / 0 in progress / 0 merged` state and the next valid transition: after inputs resolve and Batch 1 merges, Batch 2 becomes ready while Batch 3 remains blocked by sequence. Focused Prettier, ESLint, `20` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed both readiness modes, seven ordered batch rows, exact blockers, node jumps, no document overflow, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 02:52 EDT - required proof gates in Finance batch readiness.

- Corrected the prior merge-only transition model: resolving inputs and merging Batch 1 no longer makes Batch 2 ready. The next batch remains blocked until its predecessor batch is merged and the intervening proof gate is explicitly `proven`.
- Added source-controlled `not_started`, `collecting`, and `proven` states to all seven gates. `Implementation readiness` now has Inputs, Batches, and Gates modes; batch rows name predecessor-batch and proof-gate blockers, gate rows jump to exact shareable roadmap nodes, and batch/gate cards show their current state directly on the canvas.
- Focused Prettier, ESLint, `20` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed all three readiness modes, seven not-started gates, corrected blockers, exact batch/gate links, mobile fit, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 03:03 EDT - made Finance proof gates evidence-driven.

- Replaced 21 compressed gate summaries with all 36 exact proof requirements from the seven PRD batch sections. Each proof item now has a source-controlled `not_started`, `collecting`, or `verified` state, and gate state derives from those items so a gate cannot be marked proven while required evidence remains open.
- Expanded the Gates readiness mode with total and per-gate proof counts plus the complete ordered evidence checklist. Roadmap gate cards show their current proof count, and exact gate links remain shareable without adding another canvas or product-interface mockup.
- Added source-parity and transition coverage for all 36 ordered requirements, unique evidence IDs, partial evidence collection, fully proven Gate 1, and the resulting Batch 2 unlock. Focused Prettier, ESLint, `21` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed all seven checklists, all 36 not-started items, mobile fit, exact Gate 7 navigation, preserved batch blockers, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 03:11 EDT - made Finance completion criteria explicit and traceable.

- Added all 12 exact PRD Definition of Done requirements as source-controlled `not_started`, `collecting`, or `verified` completion criteria. Each criterion links to one existing roadmap, custody, data, signup, weather, or assurance node; no new canvas or product-interface mockup was added.
- Added a `Done 12` mode to Implementation readiness with total progress, ordered criteria, current state, and evidence-view labels. The four readiness categories use a two-by-two mobile layout and four-column desktop layout, while the Definition of Done coverage mapping now exposes the complete cross-view evidence set.
- Added source-parity, unique-ID, state-transition, valid-node, and coverage-target tests for all 12 criteria. Focused Prettier, ESLint, `22` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and touched-file TypeScript filtering passed. Authenticated desktop and `390x844` browser checks confirmed all criteria, responsive categories, exact Custody and Signup node links, and zero console warnings or errors. The full dirty-tree TypeScript check remains blocked by unrelated existing errors outside this Finance surface. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 03:24 EDT - made Finance research requirements evidence-driven.

- Replaced seven manual research-track flags with 28 structured items: 14 questions and 14 required evidence artifacts. Each item has a source-controlled `not_started`, `collecting`, or `verified` state; a track resolves only when all four items verify, and batch readiness cannot bypass unfinished research through a direct track override.
- Expanded Inputs with `0/4 verified` progress and exact Question/Evidence checklists per research track. Research roadmap nodes now show `Open` plus `0/4 research items verified`; exact track links remain shareable. Extracted the completion list to preserve the repository's 600-line component guard.
- Focused Prettier, ESLint, `23` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed all seven research lists, all 28 not-started items, exact node navigation, responsive categories, preserved batch blockers, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 03:33 EDT - made all Finance batch scope work explicit and stateful.

- Replaced 21 compressed Build summaries as the execution source with all 38 exact PRD scope items across the seven batches: `6`, `4`, `4`, `4`, `8`, `6`, and `6`. Each work item has a stable source-controlled `not_started`, `in_progress`, or `complete` state and is included in cross-view plan search.
- Expanded Batches readiness with total and per-batch work progress plus the complete ordered scope checklist. Roadmap batch nodes show their current work count, and a direct `merged` state can no longer unlock the next batch unless every scoped work item is complete; research inputs and proof gates remain separate blockers.
- Focused Prettier, ESLint, `24` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed seven batch lists, all 38 not-started work items, exact Batch 7 navigation, responsive cards, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 03:41 EDT - made all Finance human approvals criterion-driven.

- Replaced four manual decision flags with 13 explicit approval criteria: three release-sequence choices, four fiscal-custody choices, three Finance information-architecture choices, and three screenshot-review passes. Each criterion has a source-controlled `pending`, `changes_required`, or `approved` state; a decision resolves only when every criterion is approved.
- Expanded Inputs with total and per-decision approval progress plus the complete criterion checklists. Roadmap decision nodes now show their current approval count, criteria participate in plan search, and a direct input-state override can no longer bypass unfinished human approval or screenshot review.
- Focused Prettier, ESLint, `25` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed four decision lists, all 13 pending criteria, exact visual-approval navigation, responsive cards, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 05:06 EDT - made Finance production cutover explicitly verifiable.

- Added all nine exact PRD Production Cutover Rules as source-controlled `not_started`, `collecting`, or `verified` proof items. Each rule links to its roadmap, Webhooks, or Assurance evidence node; source-parity tests now fail if the ordered cutover contract drifts.
- Added a fifth `Cutover 9` readiness mode with exact rule status and graph navigation. Desktop uses five balanced categories; mobile keeps two columns and gives Done a full-width final row. The final clean-canary completion criterion now remains collecting when marked verified until all nine cutover rules are verified.
- Extracted the gate checklist into its own component to preserve the repository's 600-line component guard. Focused Prettier, ESLint, `26` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed all five readiness modes, nine not-started cutover rules, exact Webhooks navigation, responsive layout, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 06:40 EDT - made the Finance test matrix individually verifiable.

- Added all 16 exact PRD Test And Edge-Case Matrix areas and required-case strings as source-controlled `not_started`, `collecting`, or `verified` proof items. Each area links to its most specific System, Webhooks, Data, Signup, Weather, Roadmap, or Assurance evidence node.
- Added a sixth `Tests 16` readiness mode. The six categories use a three-by-two desktop and two-by-three mobile layout. The final clean-canary criterion cannot verify until all 16 test areas and all nine cutover rules verify.
- Focused Prettier, ESLint, `27` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed all 16 areas, exact Weather navigation, responsive layout, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 06:53 EDT - made Finance security and privacy controls individually verifiable.

- Added all 18 exact PRD Security, Privacy, And RLS controls as source-controlled `not_started`, `collecting`, or `verified` proof items. Each control links to its most specific Assurance, System, Custody, Webhooks, Data, or Signup evidence node; source-parity tests now fail if the ordered security contract drifts.
- Added a seventh `Security 18` readiness mode and made the final clean-canary criterion depend on every security control, every test area, and every cutover rule. Responsive QA found the expanded category grid could crop the dialog top at `390x844`; the dialog now uses a viewport-bounded flex shell with a fixed header and independently scrolling checklist.
- Focused Prettier, ESLint, `28` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed all 18 controls, exact Data-node navigation, visible dialog bounds, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 14:52 EDT - made all Finance failure and empty states release-blocking.

- Added all 15 exact PRD Failure And Empty States requirements as source-controlled `not_started`, `collecting`, or `verified` evidence. Each state links to its most specific System, Webhooks, Data, Signup, Weather, or Assurance node; exact source-parity and valid-target tests prevent planning drift.
- Added an eighth `Failures 15` readiness mode and extracted the balanced eight-category selector. The final clean-canary criterion now remains collecting until every failure state, security control, test area, and cutover rule is verified.
- Focused Prettier, ESLint, `29` Prototype Lab tests, thresholds, raw-button, React Grab, workspace-surface, interaction-lock, and diff checks passed. Authenticated desktop and `390x844` browser checks confirmed all 15 states, exact Weather-node navigation, responsive four-by-two and two-by-four category layouts, and zero console warnings or errors. The full dirty-tree TypeScript check still exits with unrelated existing errors outside this Finance surface and no touched-file matches. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 15:09 EDT - implemented durable Accelerator organization-setup recovery.

- Confirmed the production incident read-only: Karissa has a saved organization and a published Organization setup module but no durable completion row. The same query found `40` incomplete candidates among `41` owners and members attached to `36` saved organizations.
- Completed the narrow recovery path: successful organization setup writes canonical module progress, legacy setup slugs remain supported, repeated completion preserves its original timestamp, saved organizations recover immediately on load, restarted onboarding clears conflicting completion metadata, and a new idempotent migration backfills existing owners and members without rewriting completed rows.
- Marked the two corresponding Batch 1 graph work items `in_progress`; the batch remains unmerged and its proof gate remains open. Focused ESLint, Prettier, `73` acceptance tests, thresholds, boundaries, workspace-storage, interaction-lock, React Grab, raw-button, workspace-surface, supply-chain, large-file, and diff checks passed. Authenticated browser QA confirmed `Organization setup Complete` survives reload with no console warnings or errors. Full lint, TypeScript, structure, route, and feature checks remain blocked by unrelated dirty-tree violations with no touched-file lint or TypeScript matches. RLS tests were not run because the configured target is production and that suite creates and deletes users. The corrective migration was not applied. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 15:57 EDT - shipped durable Accelerator organization-setup recovery.

- Released isolated PR `#118` as `[STEP S25]` after local and GitHub `check:quality`, pre-push, security, step-ID, and both Vercel project checks passed. Production merge commit `9843650` deployed successfully without shipping the dirty root.
- Applied only `20260805145856_reconcile_organization_setup_progress.sql` after two linked dry runs selected that single migration. Production verification now shows `41/41` eligible saved-organization owners and members completed, zero remaining incomplete candidates, and Karissa has canonical completed progress with a completion timestamp.
- Marked the two corresponding Batch 1 planning-graph work items `complete`. Batch 1 and Gate 1 remain open because the other baseline-reconciliation scope and full recovered/new-user browser proof are still unfinished.
- Live smoke checks returned `200` for the production home and the expected authenticated redirect from `/workspace`. The browser-workflow CLI was unavailable, so no new authenticated production browser session was created; authenticated reload behavior remains covered by the earlier local browser proof and the verified production progress rows.

2026-08-05 16:14 EDT - preserved the current upstream release baseline.

- Reconciled the dirty root against production merge `9843650` without changing its index or history. The root is one patch-equivalent commit and `51` commits behind `origin/main`; Git maps the local `[STEP S75]` commit exactly to merged PR `#66`, so it contains no unique unreleased commit.
- Created a clean detached reconciliation worktree at `9843650`. Of `1,038` compared dirty-root paths, `379` already match upstream, `5` match upstream deletions, `206` are root-only, and `448` diverge and require scoped feature review. No file present on current `main` is deleted by the dirty-root content.
- Marked `Preserve every current upstream release` complete in the Finance execution graph. No merge, rebase, reset, commit, push, deployment, production mutation, or dirty-root deletion occurred.

2026-08-05 16:25 EDT - restored applied migration immutability and removed generated artifacts.

- Confirmed the linked production migration history is aligned through `20260805145856`; a linked dry run reports the remote database is up to date and made no changes.
- Restored the `21` drifted applied migration files from their exact `origin/main` sources and staged the complete `30`-file reconciliation scope. Both the index and working tree now match `origin/main` byte-for-byte for `supabase/migrations`.
- Removed the staged generated `scripts/resource-map/__pycache__/extract-cook-county-directory.cpython-311.pyc` artifact and added Python bytecode ignore rules. Marked the corresponding Batch 1 graph item complete. No migration, database write, commit, push, merge, or deployment occurred.

2026-08-05 16:38 EDT - reconciled package, tooling, agent documentation, and runlog structure.

- Restored `package.json`, `pnpm-lock.yaml`, Next.js, visual-test, lint, large-file, performance-budget, and admin tooling to the current upstream baseline; feature-specific dependencies and resource-map commands remain deferred to their owning PRD batches instead of entering Batch 1 without their consumers.
- Preserved the active AGENTS resource-map rules, their detailed enrichment contract, and the linked infrastructure plan. Restored all six upstream August release entries as the unchanged prefix of the current monthly runlog, then retained every local session entry as append-only history; `docs/RUNLOG.md` and the remaining agent contracts match upstream.
- A clean-worktree frozen install completed with `994` packages. Supply-chain and large-file checks passed, and the Batch 1 planning item is complete. No application, dependency-resolution, production, commit, push, merge, or deployment change occurred.

2026-08-05 16:46 EDT - retained public-profile cache invalidation through reconciliation.

- Restored the released public-organization cache contract without removing unrelated organization-profile work: public or formerly public profile saves revalidate the `public-map-organizations` tag, curation writes use the current `max` profile, and the cache remains on generation `v6`.
- Restored fail-loud public-organization query handling and the acceptance proof for both profile-save and curation invalidation. Focused ESLint, six public-map cache/query tests, and diff checks passed.
- All six Batch 1 scope items are now complete in the planning graph. Batch 1 remains unmerged and Gate 1 remains open pending its explicit approval, research, and verification evidence. The cache behavior is already live from PR `#117`; no production, database, commit, push, merge, or deployment change occurred in this session.

2026-08-05 16:49 EDT - verified the Gate 1 clean-diff and install proof.

- Refetched `origin/main` at production merge `9843650` and confirmed the isolated Batch 1 donor worktree is on that exact commit with an empty status and zero diff.
- Re-ran `pnpm install --frozen-lockfile --ignore-scripts`; the lockfile was already current and all `994` packages were already installed without resolution or source changes.
- Marked only `Clean diff and install` verified in Gate 1. Four evidence items remain, so the gate is collecting rather than proven and Batch 2 remains blocked. No root-tree, dependency, production, commit, push, merge, or deployment change occurred beyond the planning evidence and runlog update.

2026-08-05 16:50 EDT - verified the Gate 1 supply-chain and large-file proof.

- Ran both checks on the isolated donor at `9843650`. `check:npm-supply-chain` confirmed package and lockfile integrity; `check:large-files` passed all `3,201` tracked files, with the approved `3.7MB` legacy runlog archive as the largest file.
- Marked only `Supply-chain and large-file checks` verified. Gate 1 is now `2/5` verified and remains collecting; Batch 2 remains blocked. No source, package, lockfile, production, commit, push, merge, or deployment change occurred beyond the planning evidence and runlog update.

2026-08-05 16:53 EDT - verified all Gate 1 structural guardrails.

- Passed structure, routes, features, feature-scaffold synchronization, thresholds, import boundaries, workspace storage, interaction locks, React Grab ownership, workspace surfaces, and raw-button checks on the clean donor. Structure reported only existing non-blocking approaching-budget warnings.
- Three checks initially observed the feature-scaffold check's temporary smoke fixture during parallel execution; each passed when rerun sequentially after the fixture cleaned up, and the donor status remained empty.
- Marked only `All structural guardrails` verified. Gate 1 is now `3/5` verified and remains collecting; Batch 2 remains blocked. No source, production, commit, push, merge, or deployment change occurred beyond the planning evidence and runlog update.

2026-08-05 16:56 EDT - advanced the focused Gate 1 test proof without overstating RLS coverage.

- Passed `55/55` tests across `13` focused acceptance files covering recovered organization setup, onboarding actions, organization profile/save contracts, public-map cache refresh, organization access, platform authorization, coach assignment, join flows, and static RLS policy contracts.
- The dynamic RLS suite exited safely without running because the clean donor has no database credentials. Local Supabase cannot start because no Docker-compatible runtime is installed; production is the linked live target and was not used for destructive test-user creation; the only existing non-production branch reports `MIGRATIONS_FAILED` and is not a valid test target.
- Marked `Focused onboarding, organization save, cache, authz, and RLS tests` as collecting rather than verified. Gate 1 remains `3/5` verified and Batch 2 remains blocked. No database, auth user, source, production, commit, push, merge, or deployment change occurred beyond planning evidence and this runlog entry.

2026-08-05 17:31 EDT - verified recovered organization-setup reload behavior.

- Used the existing authenticated local workspace without submitting forms or changing production data. `Organization setup Complete` remained visible after a full reload, and the browser reported zero warnings or errors.
- A safe new-user test account is unavailable, so the combined new-and-recovered browser proof is now collecting rather than verified. Gate 1 remains `3/5` verified and Batch 2 remains blocked.
- No account, organization, database, production, commit, push, merge, or deployment mutation occurred beyond planning evidence and this runlog entry.

2026-08-05 18:06 EDT - proved Gate 1 with isolated RLS and new-user browser evidence.

- Created a disposable, data-free Supabase preview branch from production schema. Repaired branch-only historical bootstrap drift, applied every pending migration through `20260805145856`, confirmed a clean migration dry run, and passed the complete dynamic RLS suite including its test-user cleanup.
- Completed a fresh signup in the isolated branch, simulated the paid Organization entitlement without contacting Stripe, finished `Create your organization`, and confirmed `Organization setup Complete` after reload. The successful run produced no application errors; only the existing React Grab version warning remained. This joins the earlier recovered-user reload proof.
- Marked the final two Gate 1 evidence items verified, making Gate 1 `5/5` and proven. Recorded the approved release boundary and four verified baseline/cohort research items. Batch 1 is now truthfully merged: its runtime changes are already live through PRs `#117` and `#118`, while the remaining reconciliation is a no-op against current main and does not justify a redundant PR. Batch 2 is blocked only by its ownership research. Deleted the disposable preview branch and its test account, organization, subscription, and credentials; this cleanup is irreversible and did not touch production. No new commit, push, merge, deployment, or production mutation occurred.

2026-08-05 18:18 EDT - completed the Batch 2 workspace and Finance ownership audit.

- Audited current main and the dirty donor across workspace composition, card identity, board persistence, drawer routing, People storage, authorization, RLS, and public projections. Documented the exact donor areas eligible for narrow extraction and excluded formatting-only moves, current-main owner deletions, unowned prototypes, generated artifacts, and migration rewrites.
- Assigned the future `finance` card to workspace composition and the new Finance feature to domain state and explicit capabilities while preserving `economic-engine`. Confirmed that board JSON remains layout-only, Finance nested drawer state remains feature-owned, and public output uses sanitized projections rather than private Finance rows.
- Found that current board normalization drops unknown card IDs. Made a forward-compatible reader/writer plus rollback test the first Batch 2 implementation slice, documented saved-layout and drawer-route rollback behavior, verified all four ownership-research items, and advanced Batch 2 to ready. Focused planning tests pass `29/29`. No product UI, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 18:30 EDT - started Batch 2 with forward-compatible workspace persistence.

- Added an opaque compatibility envelope for unknown workspace nodes, connections, and hidden-card IDs. Current rendering still receives only known card IDs, while a future `finance` layout survives normalization, repeat saves, and rollback to this compatibility release.
- Preserved the newest opaque layout records during concurrent whole-board saves and kept layout compatibility state separate from Finance domain data. Added round-trip and concurrent-save coverage without adding the Finance card or modifying `economic-engine`.
- Marked the workspace/canvas Batch 2 item in progress. Focused tests pass `83/83`; ESLint, thresholds, boundaries, workspace-storage, and interaction-lock checks pass. Full structure and TypeScript remain blocked by unrelated dirty-tree violations with no new touched-file errors. No visible UI, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 18:47 EDT - canonicalized existing workspace drawer routes and saved tab state.

- Centralized the existing Organization, People, Documents, Accelerator, and Roadmap drawer-tab names in the workspace route contract. Canvas activations now use one resolver for canonical URLs, including Roadmap without a section slug and Documents focus values.
- Made explicit route requests take priority over stored viewer tab preferences while preserving saved preferences for route-less opens. Reused the same normalizer in server page composition, seed types, editor data, and UI-preference persistence; no Finance route, card, or visible UI was added.
- Focused regression tests pass `109/109`; focused ESLint, Prettier, diff checks, thresholds, boundaries, workspace-storage, interaction-lock, workspace-surface, and React Grab checks pass. Full route and structure checks remain blocked by pre-existing dirty-tree line-budget violations, and the full TypeScript check remains blocked by unrelated errors with no errors in this slice's touched files. No browser check was needed because rendered behavior and styling did not change. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:01 EDT - replaced the blocking release-plan readiness modal with a source-backed implementation list.

- Expanded the active goal to cover the existing seven-batch Finance, Fundraising, Fiscal Sponsorship, Find, onboarding, data, security, and release PRD without adding new scope. The goal keeps the existing 38 work items, seven gates, seven research tracks, approvals, assurance matrices, user-input boundaries, and no-release constraint.
- Docked the implementation list beside the React Flow canvas. It shows all seven broad batches and their exact written substeps, `32` remaining steps, `6/38` complete, `1` in progress, `2` open user decision groups, and `5` agent-owned research tracks. Replaced the full-screen Dialog trigger and dense category-button grid with one compact category selector.
- Authenticated browser checks confirmed the full list, exact fiscal and visual input requirements, category navigation, usable desktop split, compact `390x844` fit, no document overflow, and no console warnings or errors. The tab remains open for review. Focused tests pass `29/29`; Prettier, ESLint, diff checks, thresholds, raw-buttons, boundaries, and React Grab pass. Feature/scaffold and structure checks remain blocked by unrelated dirty-tree violations, and full TypeScript remains blocked by unrelated errors with no touched-file errors. No visual baseline exists for this prototype. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:08 EDT - parallel-audited the replacement build brief for the living release plan.

- Ran three independent read-only sub-goals covering exact PRD scope, Prototype Lab interaction/design quality, and completion evidence. All three agreed that the planner must remain a source-backed progress view for the real seven-batch execution path, not become a substitute product prototype or release claim.
- Confirmed the authoritative local planning state: `6/38` work items complete, `1` in progress, `31` not started; one batch merged, one active, five blocked; one of seven gates proven; `7` pending user decisions across fiscal custody and visual references; five open research tracks; and no verified completion, test, security, failure, or cutover matrix yet.
- Identified the next planner design requirement as a synchronized non-overlay Plan/Graph experience with selected-node state, mobile view separation, fast direct panning, and accessible status announcements. The root goal system rejected replacement because the existing broader PRD goal remains active, so no new root goal, source change, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:09 EDT - clarified the replacement goal's required Build-template format.

- Reframed the three-agent synthesis into the requested `Build [thing] in [technology]` structure while preserving the existing PRD, exact progress records, interaction requirements, product boundaries, and completion proof. The still-active root goal prevents a second root-goal record, so the formatted statement is the implementation brief under the existing goal. No source, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:20 EDT - reconciled active organization editor deep links with the canonical workspace.

- Updated Accelerator readiness actions, the organization snapshot, program full-view links, and active/fallback search results to target the shared `/workspace?view=editor` route contract instead of producing legacy Organization aliases. Legacy aliases remain readable.
- Decoupled search logo enrichment from the retired `/organization` URL by matching the active organization result ID. Behavioral tests cover canonical program encoding, organization destinations, readiness actions, drawer parsing, and self-logo preservation.
- Updated the PRD progress view without overstating the broad Organization work item: `6/38` work items are complete, `2` are in progress, and `30` are not started. Focused tests pass `63/63`; focused ESLint, import boundaries, and raw-button checks pass. The full route check still fails on the unrelated pre-existing `accelerator/page.tsx` `310/280` line budget, and one broader workspace-program source assertion remains stale outside this slice. No product styling, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:26 EDT - enforced shared Brand Kit and MVV persistence validation.

- Added one server-owned validation contract for Brand voice, colors, typography, Mission, Vision, Values, and narrative revision tokens. Invalid inputs now fail before the organization row is read or written; the Organization editor imports the same constraints, preventing client/server limit drift.
- Verified both preservation directions: a Brand Kit-only save retains MVV and unknown future profile keys, while an MVV-only save retains Brand Kit and unknown future keys. Existing organization-row optimistic concurrency, MVV revision conflicts, public-cache invalidation, and forward-compatible profile merging remain unchanged.
- Updated the Batch 2 graph summary without marking the broad Organization item complete. Focused tests pass `31/31`; focused ESLint, thresholds, and import boundaries pass, with no touched-file TypeScript errors. No UI, migration, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:34 EDT - reconciled activity and primary-object persistence.

- Changed program builder snapshot updates from whole-object replacement to merge-on-current-row, preserving unknown future keys and unchanged media metadata. Explicit snapshot clearing remains backward compatible.
- Enforced the existing activity taxonomy at the server boundary: Campaign, Fundraiser, Grant application, and Re-grant request remain readable primary-object records but cannot be written through the activity wizard. Guarded snapshot/media updates with `updated_at`, so stale autosaves return a conflict instead of overwriting newer rows or deleting their assets.
- Updated the Batch 2 graph summary without marking the broad Organization work item complete. Focused tests pass `22/22`; focused ESLint, thresholds, import boundaries, and raw-button checks pass, with no touched-file TypeScript errors. The broader workspace-program source test still has the pre-existing stale Add-button assertion and was not changed because this slice has no UI scope. No UI, migration, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:43 EDT - completed the Batch 2 Organization work-item audit.

- Retained the reconciled organization profile and Brand Kit editor behavior, MVV persistence, canonical editor deep links, program entry points, and guarded primary-object persistence. Added canonical `/workspace` and `/my-organization` revalidation after profile saves.
- Confirmed the existing JSONB profile column supports additive Brand Kit keys. Removed the test dependency on the comment-only migration and excluded that migration plus unrelated visual-only donor churn from the scoped release extraction; the user-owned dirty-root migration remains untouched. Reconciled the stale Add-program source assertion with the existing accessible icon control.
- The complete focused Organization gate passes `108/108`; the final combined Organization and progress-view suite passes `131/131`. Focused ESLint, import boundaries, thresholds, and diff checks pass. The progress view now reports `7/38` complete, `1` in progress, and `30` not started, leaving `31` steps. No product UI, migration deletion, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:46 EDT - reconciled canonical workspace state after canvas saves.

- Applied server-preserved node positions and opaque future layout records to the latest client after successful whole-board saves instead of accepting only the returned timestamp. Newer local edits still win over a stale response.
- Kept personal ontology exploration outside shared board persistence and preserved current non-layout client state. Updated the Batch 2 progress-view summary without marking the broad workspace item complete.
- Focused workspace storage and ontology tests pass `128/128`; focused ESLint, workspace-storage, interaction-lock, import-boundary, and threshold checks pass. Progress remains `7/38` complete, `1` in progress, and `30` not started. No visible UI, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:52 EDT - preserved workspace drawer corners at full canvas height.

- Removed the fullscreen-only `rounded-none` override from the container-scoped workspace drawer. The existing `20px` top radius now remains consistent across normal and full-height states, following `docs/design.md`; canvas-relative insets, full coverage, snap points, overflow, and stacking behavior are unchanged.
- Focused drawer, route, preference, viewport-control, and viewport-command tests pass `36/36`. Focused ESLint, interaction-lock, workspace-surface, React Grab, raw-button, import-boundary, and threshold checks pass.
- Automated browser proof remains pending: `/workspace` redirects unauthenticated Playwright sessions to login, and no safe saved auth state is available. Progress remains `7/38` complete, `1` in progress, and `30` not started. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 19:58 EDT - added optimistic concurrency to roadmap calendar edits.

- Added an event revision to workspace calendar payloads and sent the loaded revision from both calendar editors. The server now rejects a stale revision before writing and conditions the update on the current `updated_at`, preventing the read/write race from overwriting a newer event.
- Reconciled one stale calendar assertion with the current shared scroll-fade ref-merging contract; no shared UI implementation changed. Updated the Batch 2 progress-view summary without marking the broad workspace item complete.
- The complete roadmap/calendar acceptance set passes `64/64`; focused ESLint, touched-file TypeScript filtering, import-boundary, and threshold checks pass. Progress remains `7/38` complete, `1` in progress, and `30` not started. No visible UI, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 20:09 EDT - reduced serialized global-search request work.

- Ran independent organization/admin, entitlement/ranked-search, active-organization, logo, and analytics/enrichment work concurrently while preserving search result order, filtering, canonical destinations, and existing client cancellation.
- Parallelized the five RPC-fallback sources behind per-source collectors, then retained their prior deterministic merge order. Updated the Batch 2 progress view without marking the broad workspace item complete.
- Focused search and route tests pass `19/19`; the combined search and progress-view suite passes `48/48`; focused ESLint, Prettier, import boundaries, thresholds, and diff checks pass. The route-entry guard remains blocked only by the unrelated existing `accelerator/page.tsx` `310/280` line budget. Full TypeScript remains blocked by unrelated dirty-tree errors and reports none in this slice's files. Progress remains `7/38` complete, `1` in progress, and `30` not started. No visible UI, commit, push, merge, deployment, or production mutation occurred.

2026-08-05 20:17 EDT - reconciled the Accelerator rail and fullscreen fallback.

- Normalized malformed custom checkpoints so Build, Fundable, and Verified remain positive-width, interactive segments totaling exactly `100%`; default thresholds and existing segmented visuals are unchanged.
- Reused the canonical workspace Accelerator constant for fullscreen links and imported it for ontology modules that have no openable step, removing a reachable undefined-route branch. Updated the Batch 2 progress view without marking the broad workspace item complete.
- The full focused Accelerator and ontology set passes `156/156`; focused ESLint, touched-file TypeScript filtering, import boundaries, thresholds, interaction locks, workspace surfaces, React Grab, and raw-button checks pass. Progress remains `7/38` complete, `1` in progress, and `30` not started. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 20:25 EDT - reserved the future Finance workspace identity.

- Added stable `finance` card and drawer-tab constants while intentionally keeping them out of the active card and drawer registries, so this compatibility release cannot render or open Finance UI.
- Verified opaque Finance layout state still round-trips and locked the existing `economic-engine` metadata and connection unchanged. No feature scaffold, Finance view, route activation, or `economic-engine` edit was added.
- The focused identity, layout, persistence, route, shortcut, connection, and canvas-contract set passes `95/95`; focused ESLint, touched-file TypeScript filtering, import boundaries, thresholds, workspace storage, interaction locks, and React Grab checks pass. The progress view remains `7/38` complete, `1` in progress, and `30` not started while final workspace proof remains open. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 20:44 EDT - completed the Batch 2 Workspace work-item audit.

- The integrated authenticated browser audit found that partial drawer snaps translated full-height tab content below the canvas without constraining its internal viewport. Bound the content viewport to the active snap height minus the fixed header and made all five tab panels constrained flex columns, so clipped People content now owns a real `318/733px` scroll viewport.
- Verified fullscreen covers the canvas with zero inset gaps, retains `20px` top corners, hides viewport controls, restores the prior height and Accelerator tab afterward, produces no document overflow, and logs no warnings or errors. The `ui-skills` constraints kept the fix within the existing shadcn/Radix drawer and added no animation or new primitive.
- The integrated Workspace, calendar, search, Accelerator, Finance-identity, and progress-view suite passes `290/290`; the final drawer/progress suite passes `40/40`. Focused ESLint, touched-file TypeScript filtering, thresholds, import boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks pass. The progress view now reports `8/38` complete, `1` in progress, and `29` not started, leaving `30` steps; People consolidation is next. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:07 EDT - isolated People taxonomy and table preferences.

- Removed runtime profile-JSON fallbacks from People segment and tag loaders/actions. Typed organization-scoped tables and RLS remain authoritative; append-only migrations retain their one-time legacy backfills.
- Scoped table widths, row heights, row sizing, wrapping, and saved layouts to one record per organization and viewer. A legacy unscoped layout can be claimed once without leaking to another workspace.
- The focused People regression passes `37/37`; ESLint, Prettier, touched-file TypeScript filtering, thresholds, import boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks pass. Authenticated browser verification confirmed persistence through reload, restored the prior default, and logged no warnings or errors. The route guard remains blocked only by the unrelated existing Accelerator page line budget. Progress remains `8/38` complete, `1` in progress, and `29` not started; People links and optimistic persistence remain under audit. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:16 EDT - ordered People optimistic persistence.

- Added a keyed mutation coordinator: writes for the same segment or tag run sequentially, unrelated records remain concurrent, and prop refreshes invalidate obsolete optimistic tokens without reordering queued server writes.
- Segment and tag confirmed state now advances only after successful actions. Stale failures cannot overwrite newer UI, while the latest failed intent reconciles labels, colors, memberships, and deletion state to the last confirmed result.
- The complete focused People set passes `40/40`; ESLint, touched-file TypeScript filtering, thresholds, import boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks pass. An authenticated read-only browser reload rendered the People table with no warnings or errors; destructive failure injection was not run against connected organization data. Progress remains `8/38` complete, `1` in progress, and `29` not started; the People links audit remains. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:22 EDT - validated People social links.

- Restricted LinkedIn, Instagram, Facebook, X, YouTube, and TikTok values to their own HTTPS domains or normalized handles. Unsafe schemes and mismatched hosts now fail server validation, display inline, disable save, and never render as clickable platform badges; omitted links remain preserved during partial updates.
- Focused tests and ESLint pass. Authenticated browser verification confirmed an invalid LinkedIn host shows one inline error and disables save without mutating connected data. People remains in progress. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:43 EDT - protected interactive People profile writes from concurrency loss.

- Added bounded compare-and-retry persistence for People CRUD, roles, tags, deletion, canvas positions, and position resets. Conflicting writes re-read the latest organization profile before retrying, preserving unrelated profile keys and concurrent People changes; unchanged mutations skip writes.
- The focused People set passes `51/51`; ESLint, thresholds, import boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks pass. Full TypeScript remains blocked by unrelated dirty-worktree errors and reports none in these files. Progress remains `8/38` complete, `1` in progress, and `29` not started; automatic directory synchronization writers remain under audit. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:53 EDT - completed People consolidation locally.

- Converted onboarding organization setup, accepted-invite directory insertion, and member-page self synchronization to bounded revision-checked writes. Retries rebuild from the latest organization profile and preserve concurrent People records plus existing links, tags, images, placement, and unknown fields.
- The complete People proof gate passes `68/68` across `15` files; ESLint, thresholds, import boundaries, workspace storage, interaction locks, React Grab, workspace surfaces, and raw-button checks pass. Full TypeScript remains blocked only by unrelated dirty-worktree errors and reports none in these files. Progress is now `9/38` complete, `0` in progress, and `29` not started, leaving `29` steps. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 21:57 EDT - completed the Batch 2 prototype/demo exclusion boundary.

- Added a regression that allows Prototype Lab imports only from its authenticated admin route and owned admin-sidebar navigation. Any future production-feature import now fails; existing starter data, tutorial data, and simulated communication adapters retain their separate acceptance contracts.
- The focused exclusion set passes `15/15`; ESLint, Prettier, and diff checks pass. Batch 2 is locally complete; progress is `10/38` complete, `0` in progress, and `28` not started, leaving `28` steps. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 22:05 EDT - verified released fiscal review and native signing retention.

- Confirmed assigned-applicant and authorized Coach House signing, private storage, immutable and hashed evidence, versioned documents, W-9 redaction and human review, row and advisory locking, and service-only finalization remain covered.
- Repaired budget precedence so structured line items determine the canonical total while existing saved drafts remain ahead of prefill data. The complete fiscal set passes `57/57` across `15` files. Progress is `11/38` complete, `0` in progress, and `27` not started, leaving `27` steps. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 22:19 EDT - clarified Finance plan focus and status colors.

- Added a compact, source-derived focus strip across all graph views showing Batch 3, its next incomplete item, `11/38` complete, and `27` remaining; its action switches to and focuses the current roadmap batch.
- Green now consistently means complete, orange means needs information or in progress, blue means planned or reference architecture, and red remains risk or guardrail. Completed work icons are green and current/blocked work is orange. Authenticated localhost verification confirmed the System view, legend, and Batch 3 jump. Focused tests pass `32/32`; ESLint and all relevant UI guardrails pass. Full TypeScript remains blocked by unrelated dirty-worktree errors. Counts are unchanged. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 22:59 EDT - added a compact local response inbox to the Finance release plan.

- Replaced the blocking canvas header with compact view, jump, current-focus, information, coverage, and finder pills. Condensed duplicate implementation-panel status copy, reserved space around zoom and the desktop minimap, and hid the minimap on narrow screens.
- Added an animated rounded response dock with Enter/up-arrow save, Confirm, Deny, Agree, Add note, drag/paste/file attachments, safe HTTPS link previews, response history, orange in-progress state, and green resolved decisions. Replies persist atomically under ignored `.codex/prototype-plan-inbox/`, require admin access, reject unsafe identifiers and file types, and fail closed in production.
- Focused tests pass `36/36`; focused ESLint, Prettier, thresholds, import boundaries, interaction locks, workspace surfaces, React Grab, and raw-button checks pass. Clean authenticated desktop and `390x844` browser checks confirmed no page overflow or control overlap and no console warnings/errors. Full TypeScript was stopped after stalling on the dirty tree; route, structure, and feature checks retain unrelated pre-existing failures. Batch 3 remains at `11/38`; its next item is the remaining applicant/coach/project operations integration. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 22:29 EDT - adjusted Sade Brown-Simms billing and added local admin controls.

- Confirmed `brightbeginning60620@gmail.com` owns Stripe subscription `sub_1U1AQzGWpMHCiyVj2LJuI94r`. Changed its existing live subscription from `$58/month` Operations Support to `$20/month` Organization without proration, fully refunded the original `$58` charge, and verified the next invoice is `$20` on September 5, 2026. The refunded first month is free; Stripe retained its original processing fee.
- Added a developer-only Billing card to the super-admin organization detail page. It reads live Stripe plan, renewal, and latest-payment state; changes the existing subscription item without proration; refunds only the exact latest paid subscription charge behind confirmation; uses server-side admin authorization and Stripe idempotency; and retains an Open Stripe recovery path.
- Authenticated localhost verification confirmed the card, `$20/month` plan, and renewal date before the latest-payment resolver was corrected to use Stripe's Invoice Payments endpoint. Focused acceptance tests pass `4/4`; focused ESLint, import boundaries, thresholds, React Grab, raw-button, interaction-lock, and workspace-surface checks pass, with no touched-source TypeScript errors. The final browser reload is blocked by an unrelated dirty-tree Prototype Lab export error; repo-wide structure and route checks also retain unrelated existing failures. No commit, push, merge, or deployment occurred; the Stripe subscription and refund are live.

2026-08-05 23:12 EDT - made fiscal application submit and review transitions atomic.

- Added service-role-only, row-locked submission and assigned-coach review transactions with expected-revision checks. Submission, review, status, and immutable audit writes can no longer split; stale decisions fail safely, and idempotent submission retries do not repeat notifications.
- The fiscal/member-workspace proof set passes `60/60` across `16` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Local database RLS execution remains pending because Docker/Supabase is unavailable. The plan now shows the Batch 3 integration item orange/in progress: `11/38` complete, `1` in progress, `26` not started, `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:22 EDT - made fiscal document connection and review transactional.

- Added service-only, row-locked document connection and review transactions. Version allocation, document state, W-9 PDF classification, and immutable audit events now commit together; stale reviews fail safely and identical connection retries do not duplicate documents or notifications.
- Removed final-schema authenticated document insert/update policies so direct data API calls cannot bypass version or audit invariants. The fiscal/member-workspace set passes `65/65` across `17` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Progress remains `11/38` complete, `1` in progress, `26` not started, `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:29 EDT - connected generated plan work items to the response inbox.

- Added one compact Reply pill to every generated batch work item. Selecting a pill retargets the existing bottom dock; saved notes render orange In progress, while Confirmed, Denied, and Agreed render green without adding another panel.
- Moved response loading into one shared canvas provider so list pills, dock actions, and history stay synchronized without duplicate requests. Focused tests pass `36/36`; ESLint and desktop authenticated browser checks pass with no page overflow or sidebar/dock overlap. Counts remain `11/38` complete, `1` in progress, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:43 EDT - made fiscal draft and selected-program budget saves atomic.

- Added one service-only transaction that locks the project, application, and linked program; revision-checks existing drafts; enforces lifecycle locks; preserves unrelated program snapshot fields; and commits application fields, budget rows, and fundraising target together.
- Removed final-schema authenticated application insert/update policies so direct data API writes cannot bypass revision or lifecycle invariants. The fiscal/project proof set passes `96/96` across `21` files; focused ESLint, import boundaries, and thresholds pass. Full TypeScript retains unrelated dirty-tree failures; local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:50 EDT - made project-task creation atomic.

- Added one service-only transaction that locks the target project, validates the assignee, allocates task order, creates the task and assignment, and recalculates the project count from canonical task rows.
- Removed final-schema authenticated task inserts so UI writes cannot bypass the transaction. The fiscal/project/task proof set passes `101/101` across `24` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:54 EDT - made project-task edits and moves atomic.

- Added one service-only transaction that locks the task and affected projects in stable order, validates the destination assignee, updates or moves task details, replaces assignment, allocates destination order, and recalculates canonical counts for both projects.
- Removed final-schema authenticated task updates so data API writes cannot split task, assignment, order, and count state. The fiscal/project/task/plan proof set passes `137/137` across `26` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-05 23:57 EDT - made project-task deletion atomic.

- Added a service-only transition that revision-checks the task's authorized organization and project, locks both rows, deletes the task with cascade-owned assignments, and recalculates canonical project count in one transaction.
- Removed final-schema authenticated task deletes so a concurrent move cannot strand assignments or decrement the wrong project. The fiscal/project/task/plan proof set passes `139/139` across `26` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:01 EDT - made project-task reordering atomic.

- Added a service-only exact-set transition that locks the project and every current task, rejects duplicate, partial, or stale task sets, and performs one bulk ordinal update.
- A failed task row can no longer leave the project partially reordered. The fiscal/project/task/plan proof set passes `141/141` across `26` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:11 EDT - made native Form B generation atomic.

- Added one service-only transition that locks and revision-checks the approved application, re-verifies its accepted executed W-9, validates the generated PDF evidence, allocates the agreement version, and commits the document, agreement-ready status, and immutable audit event together.
- Rejected transitions remove the uploaded PDF. The broader fiscal, signing, project, task, and plan proof set passes `201/201` across `40` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript stalled on the dirty worktree and was stopped; local database RLS execution remains pending because Docker/Supabase is unavailable. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:16 EDT - made native Form B send setup atomic.

- Added one service-only transition that locks and revision-checks the application and generated agreement, rejects conflicting packets, validates canonical template evidence, and commits the native packet, applicant draft, document status, and immutable audit event together. Identical retries reuse the packet without repeating notifications.
- Removed direct authenticated signature-packet inserts and updates. The broader fiscal, signing, project, task, and plan proof set passes `205/205` across `41` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:21 EDT - made standard project creation atomic.

- Added one service-only transition that creates a standard project and its optional sanitized rich overview together. An overview write failure now rolls back the project instead of leaving partial workspace state.
- The broader fiscal, signing, project, task, and plan proof set passes `209/209` across `42` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:25 EDT - made full project edits atomic.

- Added one service-only transition that revision-checks and locks the scoped project, updates its canonical fields, and upserts its optional sanitized rich overview together. Stale edits now require refresh, and overview failures roll back project changes.
- The broader fiscal, signing, project, task, and plan proof set passes `213/213` across `43` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:28 EDT - protected quick project status changes from stale writes.

- Added one service-only transition that validates status, locks the scoped project, checks its expected revision, and updates status plus actor metadata together.
- The broader fiscal, signing, project, task, and plan proof set passes `217/217` across `44` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:31 EDT - protected quick project date changes from stale writes.

- Added one service-only transition that validates the date range, locks the scoped project, checks its expected revision, and updates start date, end date, and actor metadata together.
- The broader fiscal, signing, project, task, and plan proof set passes `221/221` across `45` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:52 EDT - made standard project deletion revision-safe.

- Added one service-only transition that locks the scoped project and rechecks its revision, standard-project kind, and absent canonical organization link before cascade deletion.
- The broader fiscal, signing, project, task, and plan proof set passes `225/225` across `46` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:55 EDT - made project asset batches failure-safe.

- Project asset uploads now validate every file before any write and remove every row and storage object created by the request if a later upload, insert, or unexpected route step fails.
- The broader fiscal, signing, project, task, and plan proof set passes `227/227` across `47` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 00:59 EDT - made native W-9 completion atomic.

- Added one service-only transition that locks and revision-checks the application, validates redacted signed-document evidence, allocates the tax-form version, advances the application revision, and commits the document plus immutable audit event together. Rejected transitions remove the uploaded PDF.
- The broader fiscal, signing, project, task, and plan proof set passes `231/231` across `48` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 01:01 EDT - prevented orphaned native-signing files.

- Applicant uploads are removed when applicant finalization fails. Coach House signing now removes the executed agreement if audit upload fails and removes both uploaded files when transactional finalization is rejected; files remain after committed finalization.
- Removed a duplicate signature validation branch. The broader fiscal, signing, project, task, and plan proof set passes `234/234` across `49` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and local database RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 01:13 EDT - preserved project-operation audit history.

- Project and task content, schedule, status, completion, and deletion changes now emit system-owned activity events. Project deletion preserves prior events by nulling only their project reference, while task and project deletion records retain the requesting actor.
- A temporary PostgreSQL 14 runtime confirmed that nine events survived individual task and cascading project deletion with three deletion records. The focused fiscal, project, task, and plan proof set passes `229/229` across `48` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Full TypeScript and final-schema Supabase RLS execution retain the limitations above. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 01:27 EDT - added deterministic fiscal role browser journeys.

- Added a development-only route that renders the production fiscal workbench for applicant, assigned-coach, sponsor-operator, and denied-role journeys without external data writes; production returns not found.
- Playwright passes `4/4`, and the focused role-contract and authorization set passes `16/16` across `3` files. Prettier, ESLint, import boundaries, thresholds, and diff checks pass. Full structure and route checks retain unrelated dirty-tree line-budget failures; authenticated production-route and final-schema Supabase RLS proof remain pending. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 01:53 EDT - proved the final-schema fiscal RLS role matrix locally.

- Added a final hardening migration that limits sponsor access to developers and assigned coaches, preserves organization-role and W-9 visibility, and revokes authenticated direct writes to authoritative fiscal tables while retaining applicant signing drafts.
- A temporary PostgreSQL runtime passed owner, staff, board, sponsor-operator, assigned-coach, unassigned-coach, unrelated-user, direct-write-denial, privilege, and forced-RLS assertions. The connected Supabase suite now matches final service-owned write semantics but was not run against production.
- All `81/81` fiscal tests across `21` files and `159/159` member-workspace tests across `40` files pass; focused ESLint, import boundaries, and thresholds pass. Gate 3 now shows `2/5` verified and `2/5` collecting. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 03:15 EDT - proved authenticated fiscal production routes on a disposable preview branch.

- Added explicit signed-out redirects for Form B and W-9 routes, plus a production-guarded Playwright harness that seeds only a Supabase preview branch. Browser proof passes `5/5`: return-path login, applicant Form B/PDF hash/W-9, assigned coach, sponsor operator, and denied coach.
- Preview builds now allow the configured branch storage host and the known production storage host used by existing static assets. The fiscal acceptance set passes `87/87` across `23` files; focused ESLint, import boundaries, thresholds, and diff checks pass. Global route and structure checks retain unrelated dirty-tree line-budget failures.
- Gate 3 now shows `3/5` verified, `1/5` collecting, and `1/5` not started. Progress remains `11/38` complete, `1` in progress, `26` not started, and `27` remaining. The disposable branch, credentials, and temporary test copies were deleted. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 03:28 EDT - completed fiscal PDF download integrity and authorization proof.

- A private stored PDF now passes real authenticated download checks for the applicant, assigned coach, and sponsor operator, including attachment, no-store, MIME, response hash, and downloaded-byte hash. Signed-out access returns `401`, unassigned-coach access returns `404`, and tampered hash metadata returns `409`.
- Closed a server-side authorization bypass where any platform staff member could reach the document service client. Fiscal document downloads now recheck developer or organization-assigned-coach authority before storage access.
- Playwright passes `5/5`; fiscal acceptance passes `88/88` across `23` files; focused ESLint, boundaries, thresholds, and diff checks pass. Global route and structure checks retain unrelated dirty-tree line-budget failures. Gate 3 is `4/5` verified. Progress is `13/38` complete, `1` in progress, `24` not started, and `25` remaining. The disposable branch and temporary test copy were deleted. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 03:50 EDT - prepared fiscal policy and copy approval Draft 1.

- Added a counsel, fiscal-operations, accounting, and product approval packet backed by IRS, NNFS, and Stripe sources. It identifies the current handbook's conflicting 7%-on-contributions language, preserves the approved grant-allocation-only rule, drafts replacement copy, and keeps sponsored payments disabled until sign-off.
- Updated the plan graph to `7/13` approved decisions and `10/28` verified research items. The fiscal approval and research nodes now show every action and evidence requirement without internal scrolling. Gate 3 remains `4/5` verified; overall progress remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining.
- Prototype acceptance passes `31/31`; focused ESLint, Prettier, import boundaries, thresholds, React Grab ownership, diff checks, and browser layout verification pass. No public agreement or handbook copy changed. No commit, push, merge, deployment, or production mutation occurred.

2026-08-06 04:04 EDT - measured the Resource 4 inventory baseline.

- Recorded separate exact local and production funnels: `4,249` local discovered, `741` locally contract-complete/verified, `2,184` production staged, and `853` production verified, administrator-approved, promoted, and public. Aggregate database checks were read-only.
- Added the provider gap, duplicate/fingerprint, review-state, and 30-source freshness evidence. Corrected stale local documentation so the raw candidate queue is never described as an automatic `/find` input. Research advances to `12/28` verified; implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining.
- Focused prototype, enrichment-coverage, and public-query tests pass `50/50`; ESLint, Prettier, import boundaries, thresholds, React Grab ownership, diff checks, and browser layout verification pass. No source fetch, import, review, promotion, public-data change, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 04:25 EDT - completed the public-map performance research contract.

- Measured the explicit local 5,046-item preview at `9,210,384` raw response bytes and a `4,318.49 ms` median across three current requests. Its 4,722-point GeoJSON is `4,248,843` bytes; feature and relevance construction each exceed a 16 ms frame while warmed client search remains below `2.25 ms` p95.
- Verified that the deployed production endpoint currently returns only `500/853` public rows. Recorded this as a Gate 4 release-parity failure and set bbox/index, cursor pagination, on-demand detail, request ordering, marker-density, latency, payload, cache, and measured vector-tile escalation budgets without changing production.
- Research 4 now shows `4/4` evidence items verified and the graph totals `14/28` verified research items with four open research tracks. Overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. The focused prototype and public-map set passes `59/59`; ESLint, Prettier, import boundaries, thresholds, React Grab ownership, diff checks, and browser no-overflow verification pass. No `/find` UI, source, database, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 06:48 EDT - completed the contact/privacy half of public-map Research 5.

- Measured anonymous contact exposure without printing values: `897` public and `166` private resource contacts, plus representative details on `16` public platform organizations. Confirmed that the initial anonymous resource JSON and public organization payload currently include values that should be split from list, marker, and search projections.
- Added a threat model and implementation contract separating intentional public provider intake from opt-in authenticated representative reveal. It specifies default-private classification, value-free audits, minimal payloads, a 10-minute one-time auth intent, object validation, shared atomic bootstrap limits, no process-memory limiter, and no hCaptcha. Location, NWS, and cooling-center policy remain open.
- Research now shows `16/28` items verified; overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. Prototype acceptance passes `34/34`; ESLint, Prettier, import boundaries, thresholds, React Grab ownership, and browser no-overflow verification pass. No `/find` UI, source, database, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 07:02 EDT - completed public-map Research 5.

- Added the location, NWS, coarse-cache, heat-threshold, and cooling-center implementation contract plus a 12-case source-backed weather fixture. It keeps exact coordinates in browser memory, uses a 0.05-degree weather cell, current NWS Heat Advisory/Extreme Heat event names, bounded cache/stale rules, and a 100°F-for-two-hours soft relevance threshold.
- Read-only evidence found `22` local cooling source cohorts and `3,542` current candidates with zero `lastVerifiedAt` values; production has zero public cooling-center rows. Weather promotion must remain inert until Batch 4 review and promotion gates pass. The audit also found the current auto-spin, permission-revocation, missing city/ZIP, and missing `/find` Permissions-Policy defects.
- Research now shows `18/28` items verified with three open research tracks; overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. Prototype and cooling-enrichment acceptance pass `42/42`; ESLint, Prettier, boundaries, thresholds, React Grab ownership, and browser graph/no-overflow checks pass. The separate current location suite is `9/10`: its existing same-origin geolocation-header assertion correctly fails because `next.config.ts` lacks the expected policy. No `/find` UI, resource promotion, database write, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 07:21 EDT - completed Stripe Connect Research 6.

- Added the source-backed Accounts v1, full-Dashboard, direct-charge contract, typed SDK request spike, 24-event destination matrix, 24-case behavior fixture, country gate, Account Link rules, and strict local/preview/live isolation.
- Verified `stripe@18.5.0` pins `2025-08-27.basil`, while current stable `stripe@22.4.0` pins `2026-07-29.dahlia`; local CLI `1.23.3` reports `1.45.1`. An isolated SDK/CLI upgrade and existing-billing compatibility pass is required before Batch 6 implementation.
- Research now shows `22/28` verified with two open tracks. Overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. Focused tests pass `40/40`; ESLint, Prettier, boundaries, thresholds, React Grab ownership, and authenticated browser graph/no-overflow/error checks pass. No Stripe configuration, key, account, event, database, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 10:03 EDT - completed the operational answer for Research 7.

- Added an unsigned Finance operations/cutover contract with zero-tolerance integrity and privacy invariants, six provisional SLOs, alert thresholds, monitoring views, independent server controls, a six-stage organization canary, rollback order, ten drills, support ownership, cache-age gates, and a signed approval record. Current optional tracing, static status copy, coarse flags, and unverified alerts/backups are recorded as gaps.
- Updated the PRD and Prototype graph to `23/28` research items verified, `1/28` collecting, and `4/28` not started. Research 7 remains orange/open because dashboards, alerts, named owners, drills, signatures, and the user’s screenshot review do not yet exist. Overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining.
- Focused acceptance passes `42/42`; ESLint, Prettier, boundaries, thresholds, React Grab ownership, diff checks, and authenticated browser rendering/no-horizontal-overflow checks pass. Global structure, route, and feature checks retain unrelated dirty-tree line-budget and incomplete-feature failures. No UI implementation, Stripe, database, commit, push, merge, deployment, or production mutation occurred.

2026-08-06 10:12 EDT - verified Gate 2 focused workspace regression evidence.

- Ran one exact current-worktree suite covering Organization persistence/routes, People authorization/concurrency/optimistic rollback, canvas state/persistence, drawer routes/preferences, inactive future Finance identity, preserved `economic-engine`, and Prototype Lab isolation. All `192/192` tests pass across `34/34` files; the proof document records the exact test set and evidence boundary.
- Marked only Gate 2’s focused-test item verified. Gate 2 is orange/collecting at `1/3`; guardrails are collecting because the scoped checks pass while unrelated global structure/route/feature failures remain, and authenticated desktop/mobile, light/dark, fullscreen, overflow, and reduced-motion browser evidence is not started. Overall progress remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining.
- Prototype acceptance passes `36/36`; ESLint, Prettier, boundaries, thresholds, workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, diff checks, and authenticated graph rendering/no-horizontal-overflow checks pass. No product UI, database, production, commit, push, merge, or deployment change occurred.

2026-08-06 10:40 EDT - verified Gate 2 workspace browser evidence.

- Authenticated `/workspace` passes desktop/mobile and light/dark checks. Fullscreen drawer bounds match the canvas at both sizes, preserve `20px` top corners, hide viewport controls, restore the prior People tab/snap, and create no document overflow or application errors. The existing `390x844` reduced-motion React Flow browser case passes with `0s` transition duration.
- Responsive reflow exposed a missing accessible description on the mobile app-shell contextual-details sheet. Added a screen-reader-only `SheetDescription`; its source guard passes, and a fresh desktop-to-mobile reflow produces no new warning or error logs.
- Gate 2 is now orange/collecting at `2/3`; only its full guardrail item remains open. The exact focused suite passes `195/195` across `35/35` files, Prototype acceptance passes `42/42`, the reduced-motion browser case passes `1/1`, and focused ESLint and Prettier pass. Overall progress remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. No database, production, commit, push, merge, or deployment change occurred.

2026-08-06 12:17 EDT - closed Workspace Gate 2.

- Resolved the full route, feature, scaffold, and structure failures through composition and responsibility splits across Accelerator, public-map panels and layer contracts, shared audio, workspace drawer/People/canvas state, project assets, and workspace page/action orchestration. No threshold or allowlist was weakened.
- All eleven Gate 2 guardrails pass: workspace storage, interaction locks, React Grab, workspace surfaces, raw buttons, routes, features, feature scaffold, structure, boundaries, and thresholds. The exact Organization, People, canvas, drawer, rollback, future-Finance, and prototype-isolation suite passes again at `195/195` across `35/35` files; focused ESLint and Prettier pass.
- Gate 2 is green/proven at `3/3`. Gate evidence is now `12/36` verified and `24/36` not started; gate readiness is `2/7` proven, `1/7` collecting, and `4/7` not started. Overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. No database, production, commit, push, merge, or deployment change occurred.

2026-08-06 12:29 EDT - made pending plan approvals directly answerable.

- Added compact Reply controls to each of the six pending fiscal and visual approval criteria. They reuse the existing response dock and its orange in-progress and green resolved states; approved criteria remain deduplicated, and UI responses do not silently rewrite source-backed gate evidence.
- Focused plan and response tests pass `47/47` across `3/3` files. Authenticated browser proof confirms the six controls, criterion targeting, Confirm/Deny/Agree actions, the approved 7% grant-allocation-only rule, and no horizontal overflow.
- Gate 3 remains `4/5` verified pending counsel and fiscal-operations sign-off. Overall implementation remains `13/38` complete, `1` in progress, `24` not started, and `25` remaining. No policy, database, production, commit, push, merge, or deployment change occurred.

2026-08-06 14:18 EDT - required a new-chat branch and worktree checkpoint.

- Added a mandatory startup pointer to `AGENTS.md` and a detailed workflow requiring every new chat to summarize the current branch, upstream, dirty state, relevant worktrees, overlap, and known release status before writes.
- When the user has not already chosen a strategy, the agent must recommend and ask whether to continue the current branch, use a named existing branch/worktree, or create a clean isolated branch/worktree from current `origin/main`. Dirty worktrees must be preserved, and no branch or task-file write may occur before the choice.
- Added acceptance coverage; the focused continuation-contract suite passes `3/3`, focused ESLint and Prettier pass, `AGENTS.md` remains exactly `50` lines, and diff checks pass. No branch switch, commit, push, PR, deployment, database, or production mutation occurred.
  2026-08-06 16:58 EDT - synchronized Batch 2 Prototype Lab proof status.

- Marked the locally completed guardrail and focused-test evidence verified;
  kept Gate 2 orange with visual/authenticated preview proof still collecting.
- Updated the gate copy to show connected RLS and non-persisting authenticated
  preview as the remaining work. Prototype plan acceptance passes `36/36`;
  focused ESLint and diff checks pass. No commit, push, or deployment occurred.

2026-08-07 01:32 EDT - approved the records-only Finance product boundary.

- Recorded the product-owner confirmation that counsel approved the current
  fiscal document. Counsel approval is no longer a release blocker; document
  metadata may be added later without blocking UI implementation.
- Removed Stripe Connect, payment processing, bank linking, custody-account,
  transfer, refund, and automated-disbursement work from the active plan. The
  application now covers signatures, storage, requests, approvals, imported or
  manually entered external finance records, corrections, reconciliation,
  source-labeled summaries, and reporting only. Money moves externally.
- Updated the root PRD, approval packet, roadmap, gates, research, diagrams,
  traceability, security, failure states, test matrix, and acceptance contract.
  Batch 2 is merged. Batch 3 is `13/37` work items complete with only non-author
  review and merge of PR #120 in progress; Batches 4-7 remain not started.
- Focused acceptance passes `36/36`; focused ESLint and Prettier pass. The
  localhost server responds, but authenticated visual proof was not rerun
  because the browser CLI is unavailable and the admin route redirects an
  anonymous request. No commit, push, merge, deployment, database write, or
  production mutation occurred.

2026-08-07 12:46 EDT - added the empty Finance workspace drawer tab.

- Added Finance between Documents and Accelerator in the existing workspace
  drawer. The selected tab persists and `/workspace?drawer=finance` opens it.
  Its panel is intentionally empty.
- Added no Finance card, page, copy, campaign, public fundraising surface,
  schema, or data behavior. The inactive `economic-engine` identity remains
  unchanged.
- Focused acceptance passes `29/29` across four files. Focused ESLint,
  Prettier, workspace-surfaces, interaction-locks, React Grab, and raw-button
  checks pass. The live route redirects anonymous requests to login, so
  authenticated visual proof was not rerun. No commit, push, deployment,
  database write, or production mutation occurred.

2026-08-07 13:05 EDT - fixed collapsed workspace drawer tab clipping.

- Confirmed the screen-reader-only drawer title does not occupy layout or
  overlap the tabs. The collapsed tab header moves upward by 12 pixels, but its
  immediate viewport clipped that overflow.
- The viewport now allows overflow only at the collapsed snap point and keeps
  overflow hidden when expanded. No tab sizing, content, or z-index changed.
- Drawer acceptance passes `11/11`; focused ESLint, Prettier, interaction-lock,
  React Grab, and workspace-surface checks pass. Authenticated visual proof
  remains user-side because the shell route redirects to login. No commit,
  push, deployment, database write, or production mutation occurred.

2026-08-07 14:10 EDT - reordered the workspace drawer tabs.

- Set the visible and canonical order to Finance, Organization, People,
  Documents, and Accelerator. Finance remains empty.
- Focused acceptance passes `22/22` across three files; focused ESLint and
  Prettier pass. No commit, push, deployment, database write, or production
  mutation occurred.

2026-08-07 14:34 EDT - added the first Finance drawer component.

- Added a read-only Raising card to Finance using the new `workspace-finance`
  feature slice. It uses one accessible goal ring, the amount still needed,
  recorded-versus-goal context, and up to three connected programs with known
  program-budget totals.
- The component derives only from existing program goals and raised values. It
  has explicit empty, raising, complete, and over-goal behavior, clamps the ring
  at 100%, and never fabricates transaction history or a zero-value chart.
- Deferred Activity and History navigation until History has a real table and
  data contract. No Finance card, Stripe connection, payment behavior, schema,
  or new page was added.
- Focused acceptance passes `25/25` across four files; TypeScript, focused
  ESLint, Prettier, feature, scaffold, structure, threshold, boundary,
  workspace-surface, interaction-lock, React Grab, and raw-button checks pass.
  Authenticated visual proof was not available from the shell. No commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 14:50 EDT - replaced the rejected Finance metric with view structure.

- Removed the Raising card and its program-goal data wiring from the rendered
  Finance drawer.
- Added one centered Activity and History segmented control using the existing
  shadcn/Radix tabs primitive with 48-pixel triggers. Both content regions are
  intentionally empty until their information contracts are approved.
- Focused acceptance passes `24/24`; focused ESLint and Prettier pass. Feature,
  scaffold, structure, threshold, boundary, workspace-surface,
  interaction-lock, React Grab, and raw-button checks pass. Full TypeScript is
  blocked by pre-existing errors elsewhere in the dirty worktree; none point to
  the Finance feature or drawer integration. Authenticated visual proof remains
  user-side. No commit, push, deployment, database write, or production
  mutation occurred.

2026-08-07 14:56 EDT - compacted and left-aligned the Finance view control.

- Changed the Activity and History control from a centered 320-pixel-wide
  element to a left-aligned content-width element. Reduced its height to 48
  pixels while preserving 44-pixel mobile touch targets.
- Focused acceptance passes `16/16`; focused ESLint, Prettier,
  workspace-surface, React Grab, and raw-button checks pass. No commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 15:07 EDT - added the Finance Activity graph frame.

- Added one left-aligned Contributions card for the last 30 days beneath the
  Activity view control. With no approved source data, it renders the shared
  empty-state primitive and no line, total, or fabricated `$0` value.
- Kept History empty and added no data source, record action, schema, page, or
  payment behavior. The next valid step is a real source or record contract.
- Focused acceptance passes `25/25`; focused ESLint and Prettier pass. Feature,
  structure, boundary, workspace-surface, React Grab, and raw-button checks
  pass. Authenticated visual proof remains user-side. No commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 15:39 EDT - centered Finance content and added Raising.

- Kept the Activity and History switcher left-aligned while centering the main
  Activity stack in a responsive `max-w-5xl` column. Mobile gutters are now 8
  pixels and card content uses reduced mobile inset instead of nested wide
  padding.
- Added a compact Raising list below Contributions. It reads existing program
  estimates, goals, raised estimates, and wizard budgets; active goals show the
  amount remaining, completed goals are labeled, and budget-only programs show
  `Goal needed`. It does not aggregate programs or imply a bank balance.
- Added the existing program-editor route as the editable empty-state recovery.
  Focused acceptance passes `27/27`; focused ESLint, Prettier, feature,
  scaffold, structure, threshold, boundary, workspace-surface,
  interaction-lock, React Grab, and raw-button checks pass. Three checks were
  rerun sequentially after a temporary scaffold-check race. Authenticated visual
  proof remains user-side. No commit, push, deployment, database write, or
  production mutation occurred.

2026-08-07 15:50 EDT - corrected Finance hierarchy and Donations naming.

- Replaced the full-width Raising list with one compact top metric. It shows
  remaining active-goal value and concise active, goal-needed, complete, and
  not-set states; program detail rows are deferred to a later drill-down.
- Renamed the graph frame from Contributions to Donations. Contributions was
  an assistant synthesis from the older external-record PRD, while Donations
  is the current product-owner wording for the intended tracked activity.
- Reduced and normalized the graph empty-state proportions: shared card insets,
  192-pixel mobile and 224-pixel desktop empty regions, and no nested narrow
  mobile content column.
- Focused acceptance passes `27/27`; focused ESLint, Prettier, feature,
  structure, boundary, workspace-surface, interaction-lock, React Grab, and
  raw-button checks pass. Authenticated visual proof remains user-side. No
  commit, push, deployment, database write, or production mutation occurred.

2026-08-07 16:07 EDT - rebuilt the Finance drawer as one lightweight dashboard.

- Replaced the stacked Finance cards with one centered `max-w-5xl` surface
  using equal responsive padding and section separators. Removed the drawer's
  extra desktop-only horizontal padding.
- Kept Raising as the only top metric. It combines program goals and connected
  budgets into one remaining amount and target; Budgeted, Recorded, Grants, and
  AI matches are not summary metrics.
- Added a horizontal Sources graph contract for Donations, Grants, Earned
  revenue, and Other. Bars render only when real source totals are supplied.
- Added a minimal Opportunities feed and a History table with Date, Source,
  Type, Amount, and Status. Both have explicit empty and populated states and
  no fabricated records.
- Focused Finance acceptance passes `12/12`; focused ESLint, Prettier,
  structure, feature, scaffold, boundary, route, workspace-surface, React Grab,
  and raw-button checks pass. Full TypeScript is blocked by unrelated errors in
  the dirty worktree; none point to the Finance feature or drawer integration.
  Browser visual proof was unavailable. No commit, push, deployment, database
  write, or production mutation occurred.

2026-08-07 16:11 EDT - unified Finance source and History data.

- Removed the separate source-total input. One normalized finance-record stream
  now powers both the History table and the Sources graph.
- Only inbound records with a funding-source category contribute to source
  bars. Outbound records remain visible in History without inflating funding.
- Confirmed there is no existing production finance-record or opportunity
  loader to reuse. Added no schema, migration, fake records, or external writes.
- Focused Finance acceptance passes `12/12`; focused ESLint and Prettier pass.
  No commit, push, deployment, database write, or production mutation occurred.

2026-08-07 16:14 EDT - aligned the Finance view switcher with its content.

- Moved the Activity and History switcher into the same centered `max-w-5xl`
  frame and matched the dashboard's 16/24-pixel responsive horizontal padding.
  Its left edge now aligns with Raising, the section separator, and Sources.
- Added focused acceptance coverage. No commit, push, deployment, database
  write, or production mutation occurred.

2026-08-07 16:17 EDT - completed the compact Raising progress state.

- Added one narrow progress track beneath the Raising amount using the existing
  shared progress primitive. It shows raised versus target and renders only
  when a real program goal or budget exists.
- Unified budget-only program math with goal-backed program math: budgets act
  as the raising target, raised estimates reduce the remaining amount, and a
  fully raised budget resolves to complete.
- Focused Finance acceptance passes `12/12`; focused ESLint and Prettier pass.
  No commit, push, deployment, database write, or production mutation occurred.

2026-08-07 16:20 EDT - reduced the Finance view switcher.

- Reduced the Activity and History pill from 48 to 36 pixels, its active
  segments from 44 to 32 pixels, and its label treatment from 14 to 12 pixels.
  Preserved the shared content-frame alignment.
- Added focused acceptance coverage. No commit, push, deployment, database
  write, or production mutation occurred.

2026-08-07 16:27 EDT - completed Finance data-display states.

- Added explicit idle, loading, ready, and error states for the shared record
  stream and Opportunities feed. Sources and History reuse the same record
  state so they cannot contradict each other.
- Added compact skeletons and plain, borderless error/empty treatments. Reduced
  Activity empty regions from 176 to 144 pixels to keep the drawer lightweight.
- Focused Finance acceptance passes `12/12`; focused ESLint and Prettier pass.
  Added no storage, controls, fake data, or external writes. No commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 16:31 EDT - added the Raising program drill-down.

- Added one quiet program-count disclosure beside the Raising context. It opens
  a compact read-only popover listing each connected program, its goal or
  budget target, remaining amount, and Raising or Complete state.
- Kept the breakdown out of the default layout so the dashboard remains light
  and added no new card, page, or edit control.
- Focused Finance acceptance passes `12/12`; focused ESLint, Prettier, and the
  raw-button guard pass. No commit, push, deployment, database write, or
  production mutation occurred.

2026-08-07 16:48 EDT - expanded Finance History to the approved activity feed.

- Generalized the read-only History view model to accept views, clicks,
  conversions, donations, and external financial records in one ordered feed.
- Engagement events render without fabricated amounts or reconciliation
  statuses. Only categorized inbound records with actual amounts contribute to
  the Sources graph.
- Focused Finance acceptance passes `12/12`; focused ESLint and Prettier pass.
  No storage, tracking collection, commit, push, deployment, database write, or
  production mutation occurred.

2026-08-07 17:00 EDT - centralized the Finance program-data adapter.

- Replaced drawer-owned goal, raised, and wizard-budget conversion with the
  public `buildWorkspaceFinanceProgramInputs` adapter in the Finance feature.
  Existing organization programs now enter Finance through one tested read-only
  seam; future records and opportunities can join without duplicating mapping
  logic in the drawer.
- Focused Finance and drawer acceptance passes `24/24`; focused ESLint and
  Prettier pass. Added no schema, mutation, fake data, or external write. No
  commit, push, deployment, database write, or production mutation occurred.

2026-08-07 17:06 EDT - made Finance History responsive.

- Kept the five-column History table at `sm` and above; narrow drawers now show
  the same records as compact activity rows with type, amount, source, date,
  and status. Rows use one bordered region with dividers, not individual cards.
- Loading, error, and empty states share the same mobile and desktop contract.
- Focused Finance acceptance passes `13/13`; focused ESLint and Prettier pass.
  No commit, push, deployment, database write, or production mutation occurred.

2026-08-07 17:10 EDT - added Finance read-model orchestration.

- Added a server-only read orchestrator for records and Opportunities. Each
  provider resolves independently to ready or error, while an unconfigured
  provider remains idle; failures cannot hide Raising or the other feed.
- The orchestrator performs no writes and is not yet connected to storage,
  analytics, Stripe, or an opportunity provider.
- Focused Finance acceptance passes `14/14`; focused ESLint, Prettier, and the
  import-boundary guard pass. No commit, push, deployment, database write, or
  production mutation occurred.

2026-08-07 17:16 EDT - added the private Finance-record storage contract.

- Added a local migration for organization-scoped Finance access and external
  finance records. Records use UTC timestamps, integer cents, currency and
  state constraints, provider idempotency, tenant/date/source indexes, and no
  donor PII or bank credentials.
- Enabled and forced RLS. Organization owners and explicitly granted Finance
  viewers can read; platform-admin status grants no implicit access. The client
  receives select-only privileges for records, with no insert, update, or
  delete path.
- Added schema typings and a read-only server provider that selects at most 500
  scoped records and maps only drawer-safe fields. The provider is not wired to
  the live drawer until the migration is applied through an approved release.
- Focused Finance acceptance passes `19/19`; focused ESLint, Prettier,
  structure, and import-boundary checks pass. No migration was applied and no
  commit, push, deployment, database write, or production mutation occurred.

2026-08-07 17:18 EDT - added the private Finance Opportunities storage contract.

- Added a local organization-scoped opportunity queue for grants, contracts,
  sponsorships, awards, partnerships, and other provider-discovered
  possibilities. It stores only the display and workflow fields the drawer
  needs, plus private provider identifiers for idempotent imports.
- Reused the explicit Finance access boundary with forced RLS and authenticated
  select-only privileges. Platform-admin status grants no implicit access, and
  no raw AI reasoning, match scores, donor data, or client write path was added.
- Added schema typings and a read-only server provider that excludes dismissed
  items and maps only title, source, due date, and status into the drawer model.
  The provider remains unwired until the migrations are deliberately released.
- Focused Finance acceptance passes `23/23`; focused ESLint, structure, and
  import-boundary checks pass. No migration was applied and no commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 17:30 EDT - added the private Finance engagement-history contract.

- Added organization-scoped storage for the view, click, and conversion stages
  that belong in Finance History. It deliberately does not reuse admin-only
  product telemetry or store user IDs, visitor IDs, or free-form metadata.
- Added tenant/date and funnel indexes, provider-event idempotency, and an
  optional reference from a conversion to its resulting Finance record without
  copying transaction data.
- Reused forced Finance RLS and authenticated select-only privileges. The
  read-only provider maps events into nonfinancial History rows, so they display
  no fabricated amount or status and never contribute to Sources.
- Focused Finance acceptance passes `27/27`; focused ESLint, structure, and
  import-boundary checks pass. No migration was applied and no commit, push,
  deployment, database write, or production mutation occurred.

2026-08-07 17:39 EDT - added executable Finance final-schema RLS proof.

- Added a disposable local Postgres harness that applies only the three Finance
  migrations in an isolated temporary cluster and deletes the cluster after the
  assertions finish. It does not connect to local Supabase, preview, or
  production.
- Proved organization-owner and explicit Finance-viewer reads, cross-tenant
  isolation, no implicit platform-admin access, anonymous denial, forced RLS,
  authenticated select-only privileges, and denial of direct Finance-record
  writes.
- Added `pnpm test:rls:finance` to the standard RLS chain. The isolated proof
  passes in 6.5 seconds. No shared migration was applied and no commit, push,
  deployment, database write, Stripe call, or production mutation occurred.

2026-08-07 17:54 EDT - added the bounded read-only Stripe import seam.

- Added a connected-account Balance Transactions reader capped at 500 records
  and scoped by a required start timestamp and connected Stripe account ID. It
  makes list requests only and has no payment, refund, transfer, payout, Product,
  Price, Payment Link, OAuth, or database mutation path.
- Added a pure adapter that requires an explicit Donation, Grant, Earned
  revenue, or Other classification before inbound Stripe money can enter
  Finance. Refunds and fees map to outbound History records; payouts and unknown
  movements are skipped rather than mislabeled. Stripe descriptions are not
  copied.
- Marked the old Connect/payment-link research and campaign-heavy root plan as
  superseded by the approved Activity/History, display/import-only product
  scope. Authorization remains unresolved because Stripe limits OAuth
  `read_only` scope to Extensions.
- Focused Finance acceptance passes `32/32`; focused ESLint, structure, and
  import-boundary checks pass. No Stripe request, OAuth flow, database write,
  migration, commit, push, deployment, or production mutation occurred.

2026-08-07 18:33 EDT - added the Finance Connections and CSV preview slice.

- Added one compact Connections control beside Activity and History in the
  shared `max-w-5xl` header. Its flat popover makes CSV the built-in path and
  presents Stripe as an optional read-only source without a fake Connect
  action.
- CSV selection now validates `.csv` type, a 2 MB file limit, quoted values,
  unique headings, malformed rows, and a 5,000-record preview limit. It reports
  the selected filename plus row and column counts and does not claim records
  were imported before mapping and server-side persistence exist.
- Chose a future Stripe App with default platform authentication over OAuth for
  the current read-only sync scope. Stripe lists no separate OAuth fee, but the
  Stripe application manifest, minimum read permissions, and real install
  endpoint remain approval gates.
- Focused Finance tests pass `18/18`; ESLint, Prettier, thresholds, boundaries,
  raw-button, React Grab, workspace-surface, and diff checks pass. Repository-
  wide TypeScript remains blocked by extensive unrelated dirty-tree errors. An
  authenticated browser was unavailable, so visual confirmation remains open.
  No Stripe request, database write, migration, commit, push, deployment, or
  production mutation occurred.

2026-08-07 18:35 EDT - corrected the empty Raising value.

- Replaced the empty Finance Raising value `Not set` with `$0.00`; the empty
  state remains visually numeric without adding a goal, progress bar, or fake
  funding data.
- Focused Finance acceptance passes. No external write, commit, push, migration,
  deployment, or production mutation occurred.

2026-08-07 18:36 EDT - reordered the workspace drawer tabs.

- Set the visible and canonical drawer order to Organization, Finance, People,
  Documents, Accelerator. The hidden Roadmap route remains available but is not
  exposed as a drawer tab.
- Updated focused drawer and route contracts. No external write, commit, push,
  migration, deployment, or production mutation occurred.

2026-08-07 18:53 EDT - changed Raising into the authoritative Raised view.

- Replaced the remaining-to-go Raising metric with Raised. Connected Finance
  records become authoritative and include only explicit recorded or reconciled
  USD inbound funding; drafts, opportunities, engagement events, outbound
  records, and other currencies do not inflate the amount.
- Preserved existing program `raised_cents` as a fallback estimate until record
  storage is connected. Raised-only programs remain included even without a
  goal or budget, and empty data remains `$0.00`.
- Consolidated the separate Sources chart into a slim, accessible segmented
  rail under Raised with labeled Donation, Grant, Earned revenue, and Other
  amounts. Potential opportunities are not represented as money.
- Replaced the separate opportunity column with one six-item paginated Activity
  feed built from shadcn Items. It chronologically combines transactions and
  discovered opportunities while History remains the complete responsive table;
  the page number is preserved in `financePage`.
- Added currency and opportunity-discovery fields to the private read model.
  Focused Finance and storage tests pass `32/32`; ESLint, Prettier, thresholds,
  boundaries, raw-button, React Grab, workspace-surface, interaction-lock, and
  focused TypeScript checks pass. No migration was applied and no database,
  Stripe, commit, push, deployment, or production mutation occurred.

2026-08-07 19:31 EDT - connected the migrated Finance read model to the workspace drawer.

- Confirmed the linked Supabase database contains all three Finance migrations:
  records `20260807171000`, Opportunities `20260807172000`, and engagement
  history `20260807173000`. The user applied them; this session made no database
  write.
- Added one feature-owned server loader that uses the authenticated workspace
  Supabase client under forced RLS. It loads organization-scoped records,
  engagement events, and Opportunities, adapts existing program finance data,
  and returns one serializable drawer input without service-role access or
  browser-side database queries.
- Passed that input through the existing workspace composition chain and
  replaced the drawer's program-only placeholder. Empty migrated tables now
  produce the real `$0.00`, empty History, and empty Opportunities states; later
  imported records populate the same Activity and History UI.
- Focused Finance and drawer tests pass `46/46`; the Finance RLS suite, ESLint,
  feature, structure, boundary, threshold, workspace-surface, and React Grab
  checks pass. Repository-wide TypeScript still fails on unrelated dirty-tree
  accelerator, public-map, fiscal-sponsorship, and test errors. Anonymous
  `/workspace` correctly redirects to login, so authenticated browser-visible
  confirmation remains open. No Stripe call, commit, push, or deployment
  occurred.

2026-08-08 13:33 EDT - completed Finance CSV mapping and persistence.

- Replaced the CSV preview dead end with one focused shadcn dialog that
  auto-detects common headings and maps Date, Amount, Source, Record type, and
  Currency. Record type can come from one CSV column or be applied explicitly
  to the whole file; unsupported values and row-level date, amount, source, and
  currency errors stop the import with the exact row number.
- Added server-side revalidation and organization-scoped persistence in bounded
  200-record batches. Owner, admin, staff, or an explicit Finance manager may
  import; the trusted client is created only after authorization, and browser
  table writes remain denied by forced RLS.
- Stored CSV rows as recorded Finance records with exact-file fingerprint and
  source-row identifiers. Retrying the same file skips duplicates, inbound
  records update Raised and its source rail, and all imported rows appear in
  History after the server refresh.
- Focused Finance and drawer acceptance passes `53/53`; Finance RLS, ESLint,
  feature, structure, boundary, threshold, workspace-storage, interaction-lock,
  raw-button, workspace-surface, React Grab, focused TypeScript, and diff checks
  pass. No new migration, database write, Stripe call, commit, push, deployment,
  or production mutation occurred. Authenticated browser confirmation remains
  open.

2026-08-08 13:50 EDT - combined Finance program targets with Raised.

- Corrected the program model so each program contributes one target: its
  explicit fundraising goal first, or its budget when no goal is set. Budget-
  only programs now correctly remain Raising instead of showing Goal needed.
- Kept one Raised metric and one source rail. The same Donations, Grants,
  Earned revenue, and Other segments now fill against the combined target, with
  the muted remainder representing the amount still needed.
- Added one compact program popover beside the target summary. It identifies
  the goal or budget behind each target and displays Raising or Complete from
  the existing program snapshot without adding another dashboard card.
- Focused Finance and drawer acceptance passes `29/29`; scoped ESLint, feature,
  structure, boundary, threshold, workspace-storage, interaction-lock,
  React Grab, workspace-surface, and raw-button checks pass. No migration,
  database write, Stripe call, commit, push, deployment, or production mutation
  occurred.

2026-08-08 13:53 EDT - Finance continuation and visual-reference checkpoint.

- Before starting new Finance UI, consult the eight user-supplied references:
  Agently's node canvas, the compact priority list, the dark integration graph,
  Evergreen's table/detail workspace, the Chat/History pill, Mission Control,
  deploy progress, and provider-allocation dashboard. Use them for hierarchy,
  density, proportions, connections, and interaction patterns without copying
  unrelated product concepts.
- Write a continuation checkpoint near 60% context remaining rather than
  waiting for automatic compaction. Current context was already near 15%, so
  this checkpoint was written immediately.
- Current Finance state: Activity and History, Connections with CSV import,
  authoritative Raised, one source/target rail, a program-target popover, and
  the combined transaction/opportunity feed are implemented locally. Next
  recommended slice is opportunity ingestion and workflow actions.
- Preserve the existing dirty worktree. No commit, push, deployment, database
  write, migration, or Stripe call occurred during this checkpoint.

2026-08-08 14:12 EDT - added Finance sample data and opportunity actions.

- Added a temporary Sample data switch beside the Finance controls. In local
  development it toggles a populated read model through `financeSample=1`, so
  Raised, source/target progress, program targets, opportunity states, Activity,
  and History can be reviewed together. It performs no database writes and is
  excluded from production unless explicitly enabled by environment variable.
- Kept the mobile header compact by shortening the Sample label and collapsing
  Connections to its existing plug icon on narrow drawers.
- Replaced static opportunity badges with one right-edge status control based
  on the supplied priority-list reference. Finance managers can select New,
  Reviewing, or Saved; Dismiss removes the row and provides an immediate Undo
  toast. Sample rows update locally and never call the server.
- Added an authenticated server mutation that validates IDs and statuses,
  checks organization editing or explicit Finance-manager access before
  creating the trusted client, scopes updates to the active organization, and
  revalidates only Finance routes. No migration was required.
- Focused Finance and drawer acceptance passes `31/31`; scoped ESLint,
  TypeScript, formatting, feature, structure, route, boundary, threshold,
  workspace-storage, interaction-lock, React Grab, workspace-surface,
  raw-button, and diff checks pass. No action was invoked, so no database,
  Stripe, commit, push, deployment, or production mutation occurred.

2026-08-08 14:19 EDT - removed Finance control route reloads.

- Traced the slow Activity Next button to `financePage` URL navigation, which
  rebuilt the authenticated workspace server route and remounted the drawer.
- Replaced Previous and Next links with shadcn Buttons backed by component-local
  page state. They now replace only the six visible Activity rows, retain the
  drawer and canvas, preserve keyboard labels, and announce the page number.
- Moved the temporary Sample data switch from query-string navigation to local
  state as well, so previewing populated and empty Finance states is immediate
  and non-persistent.
- Focused Finance acceptance passes `20/20`; focused ESLint and formatting pass.
  No database, Stripe, commit, push, deployment, or production mutation
  occurred.

2026-08-08 14:33 EDT - added the registered Finance opportunity ingestion seam.

- Added server-only opportunity source and scan-run tables. Sources record type,
  HTTPS base URL, allowed domains, authentication method, trust level, cadence,
  enablement, ownership, and terms notes. Scan runs record bounded counts,
  status, retries, trace IDs, and sanitized failures.
- Linked organization Finance opportunities to their registered source while
  retaining the existing provider/external-ID uniqueness contract and member
  workflow state.
- Added a pure 500-item normalizer that trims bounded display fields, converts
  timestamps to UTC, rejects malformed deadlines and duplicate external IDs,
  and never guesses an unsupported opportunity type.
- Added a server-only ingestion orchestrator that first resolves an enabled
  registered source, records the scan, preserves existing member statuses on
  upsert, and records succeeded or failed outcomes. It has no fetcher, arbitrary
  URL, model call, browser action, or live provider configured yet.
- Extended generated table types and Finance RLS coverage. Focused Finance,
  storage, and drawer acceptance passes `46/46`; Finance RLS, scoped ESLint, and
  focused TypeScript pass. The new migration has not been applied outside the
  disposable RLS test database. No external request, database mutation, commit,
  push, deployment, or production mutation occurred.

2026-08-08 14:39 EDT - added the disabled Grants.gov source adapter.

- Verified the official Grants.gov `search2` API is a public unauthenticated
  POST endpoint and that its terms permit searching and displaying grant data
  while requiring a prominent non-endorsement notice.
- Registered Grants.gov with its exact HTTPS host, public-API trust level,
  daily cadence, terms URL, and no-auth setting. It remains disabled by default,
  so migration alone cannot start external requests.
- Added a server-only adapter that accepts one bounded keyword, requests at most
  50 posted or forecasted results, rejects redirects, times out after 10 seconds,
  rejects responses over 2 MB, and ignores response tokens, descriptions,
  attachments, contacts, and unsupported statuses.
- Added deterministic response mapping for external ID, title, agency, type,
  and UTC deadline. Fetch failures now occur inside the registered scan run and
  therefore produce a sanitized failed-run record.
- Added the required Grants.gov notice once above Activity when relevant rows
  exist; no row repeats it. Focused Finance and storage acceptance passes
  `37/37`; Finance RLS, scoped ESLint, and focused TypeScript pass. No Grants.gov
  request, migration application, database mutation, commit, push, deployment,
  or production mutation occurred.

2026-08-08 14:41 EDT - completed Grants.gov adapter validation.

- Combined Finance, storage, and workspace-drawer acceptance passes `48/48`.
  Feature, structure, boundary, threshold, workspace-storage,
  interaction-lock, React Grab, workspace-surface, and raw-button guards pass;
  formatting and diff checks pass. Existing near-budget structure warnings are
  unchanged and non-blocking.

2026-08-08 15:00 EDT - linked Finance records to program targets.

- Added an optional Program field to the existing CSV mapping dialog. One file
  can remain organization-wide or assign every imported row to one displayed
  program target; no page, dashboard card, or additional primary metric was
  added.
- Added a nullable Finance-record program link. The authorized import action
  verifies the selected program belongs to the active organization, while a
  database foreign key and constraint trigger independently reject cross-org
  links and clear the link when a program is deleted.
- Changed each program's Raised value to derive from its linked recorded or
  reconciled USD inbound records whenever Finance storage is connected. The
  existing Programs popover now shows Raised versus goal or budget with a thin
  progress rail and identifies organization-wide funding separately. The main
  Raised metric and source rail remain organization-wide.
- Focused Finance, storage, CSV-action, and drawer acceptance passes `55/55`;
  Finance RLS, scoped ESLint and TypeScript, formatting, feature, structure,
  boundary, threshold, workspace-storage, interaction-lock, React Grab,
  workspace-surface, raw-button, and diff checks pass. Repository-wide
  TypeScript remains blocked by existing unrelated errors. Authenticated visual
  verification remains open because the browser runner is unavailable and a
  fresh session redirects. The new migration was not applied; no database
  write, Stripe call, commit, push, deployment, or production mutation occurred.

2026-08-08 15:45 EDT - exposed Finance record program context.

- Rechecked the current Stripe contract and current official Stripe App OAuth
  documentation. The approved repo plan still blocks OAuth and a visible
  Connect action until Coach House has a real Stripe App manifest, minimal
  approved permissions, allowed callback URI, install endpoint, and developer
  credentials. No placeholder connection behavior was added.
- Enriched normalized Finance records from the existing program input. Linked
  program names now appear as secondary context in Activity and in the existing
  History Source cell on desktop; mobile History keeps source, program, and date
  in one compact metadata line. No sixth column, new page, card, or metric was
  added.
- Focused Finance, storage, CSV-action, and drawer acceptance passes `55/55`;
  scoped ESLint and TypeScript, feature, structure, boundary, threshold,
  workspace-storage, interaction-lock, React Grab, workspace-surface,
  raw-button, and diff checks pass. No migration, database write, Stripe call,
  commit, push, deployment, or production mutation occurred.

2026-08-08 16:01 EDT - added in-place Finance program reassignment.

- Turned the existing secondary program context in each financial History row
  into one compact Shadcn radio menu. It stays inside the five-column Source
  area on desktop and the same compact record row on mobile; engagement events
  remain read-only and no card, column, page, or metric was added.
- Program selection updates History, Activity, and per-program Raised progress
  immediately from one browser record model. Sample changes remain local; real
  failures roll back the optimistic selection and report the server error.
- Added an authenticated mutation that validates both IDs, authorizes an
  organization editor or explicit Finance manager before creating the trusted
  client, verifies the program belongs to the active organization, and scopes
  the record update to that organization. The existing database trigger remains
  the cross-organization backstop; no migration was required.
- Focused Finance, storage, action, and drawer acceptance passes `59/59`;
  scoped ESLint and TypeScript, formatting, route, feature, scaffold, structure,
  boundary, threshold, workspace-storage, interaction-lock, React Grab,
  workspace-surface, raw-button, and diff checks pass. Authenticated browser
  verification remains unavailable in this checkout. No database write, Stripe
  call, commit, push, deployment, or production mutation occurred.

2026-08-08 16:31 EDT - added compact manual Finance record entry.

- Added one Manual record row to the existing Connections popover, following
  the supplied compact plugin-list reference. Its focused Shadcn dialog records
  Source, Type, optional Program, Date, and Amount in USD; it adds no card,
  metric, column, route, or page.
- Manual entry creates only a `recorded` external-activity row for money that
  already moved outside Coach House. It cannot reconcile activity, attach
  evidence, create corrections, or move funds. Record-type labels and direction
  rules now have one shared source used by CSV, manual entry, and History.
- The server validates every field, verifies the optional program belongs to
  the active organization, and returns the created row. Activity, History,
  Raised, source composition, and program progress update locally without
  `router.refresh`; later server reads deduplicate the optimistic row.
- Corrected the shared Finance write boundary to match the approved plan:
  organization owners may write, while admins, staff, board, and members require
  explicit Finance-manager access. Real record controls are disabled while the
  temporary sample-data view is active.
- Focused Finance, storage, action, and drawer acceptance passes `63/63`;
  scoped ESLint and TypeScript, formatting, feature, structure, route, boundary,
  threshold, workspace-storage, interaction-lock, React Grab,
  workspace-surface, raw-button, and diff checks pass. Authenticated browser
  verification remains unavailable. No migration, database write, Stripe call,
  commit, push, deployment, or production mutation occurred.

2026-08-08 16:59 EDT - added private Finance record reconciliation.

- Turned Recorded in the existing five-column History surface into one compact
  status action. Its focused Shadcn dialog confirms the existing record with an
  external reference and one PDF, JPG, or PNG up to 10 MB; it adds no page,
  card, metric, or table column and states that Coach House does not move money.
- Added a private Finance-evidence bucket, composite organization/record
  ownership, file-signature validation, SHA-256 verification, immutable
  evidence, and immutable audit events. One service-role-only database
  transition locks the row, stores evidence metadata, marks it reconciled, and
  records the event atomically. Failed transitions remove the uploaded object.
- Reconciled evidence is served only through a Finance-authorized, no-store
  route that re-verifies the file hash. Recorded and reconciled UI state updates
  locally without route navigation; sample data remains read-only.
- Corrected Raised, source composition, and per-program progress to count only
  reconciled USD inflows, as required by the approved Finance plan. Recorded
  manual and CSV rows remain visible in Activity and History until evidence is
  attached.
- Focused Finance/storage/import acceptance passes `61/61`; snapshots, scoped
  ESLint, Finance RLS/reconciliation in disposable Postgres, feature/scaffold,
  structure, route, boundary, threshold, workspace-storage, interaction-lock,
  React Grab, workspace-surface, and raw-button checks pass. The live dev route
  returned the expected same-origin rejection for an invalid POST. Full
  acceptance remains blocked by five unrelated public/resource-map failures;
  full TypeScript and the aggregate RLS command remain blocked by existing
  unrelated checkout failures. Authenticated visual verification remains
  unavailable. The new migration was not applied outside disposable Postgres;
  no database write, Stripe call, commit, push, deployment, or production
  mutation occurred.

2026-08-08 18:05 EDT - added append-only Finance record corrections.

- Changed the member-facing status language from Reconciled to Verified while
  retaining the existing `reconciled` database state. Recorded rows verify in
  the existing compact History dialog; no page, card, metric, or table column
  was added.
- Added an immutable correction link and service-role-only atomic transition.
  A correction preserves the verified original as Corrected, creates a linked
  verified replacement with its own private evidence, and records both audit
  events. Verified program assignment and financial fields cannot be rewritten.
- Corrected Activity, Raised, source composition, and program progress to retain
  the superseded original in History while excluding it from totals and counting
  only the verified replacement. The browser updates both rows without route
  navigation.
- Focused Finance/storage/drawer acceptance passes `131/131`; the focused
  correction subset passes `58/58`. Finance RLS, verification, correction,
  cross-tenant, service-role, and immutability checks pass in disposable
  PostgreSQL. Snapshots, scoped ESLint, feature/scaffold, structure, route,
  threshold, boundary, workspace-storage, interaction-lock, React Grab,
  workspace-surface, raw-button, formatting, and diff checks pass. Authenticated
  visual verification remains unavailable. Migration
  `20260808175223_add_finance_record_corrections.sql` is not applied outside the
  disposable test database. No Stripe call, commit, push, deployment, or
  production mutation occurred.

2026-08-08 18:22 EDT - confirmed Finance corrections and exposed sample states.

- Confirmed migration `20260808175223_add_finance_record_corrections.sql` is
  present in both the local and linked Supabase migration ledgers for project
  `vswzhuwjtgzrkxknrmxu`.
- Extended the existing development-only sample model with one Recorded row and
  one linked Corrected/Verified pair. This exposes all current History states
  through the existing switch while preserving the `$38,000.00` Raised total;
  no UI surface or production sample data was added.
- Focused Finance and storage acceptance passes `47/47`; scoped ESLint,
  formatting, and diff checks pass. No Stripe call, commit, push, or deployment
  occurred.

2026-08-08 19:10 EDT - made Finance Raised factual and added Stripe App ingestion.

- Removed legacy program estimates from Raised. Raised, source composition, and
  program progress now derive only from verified, non-corrected USD inflows;
  empty and unavailable states display `$0.00` without implying received funds.
- Replaced the ambiguous opportunity workflow with factual New, Saved, Applied,
  Awarded, Not awarded, and Dismissed stages. Existing Reviewing rows migrate to
  Saved, and stage changes update locally without reloading the workspace drawer.
- Added an optional read-only Stripe App connection using platform
  authentication and `balance_read`. The app stores no connected-user token or
  secret, verifies signed installs, requires an explicit income classification,
  bounds first sync to 90 days and 500 balance transactions, and imports only
  provider-verified immutable Finance records through a service-role-only atomic
  transition. Coach House does not create payments, refunds, transfers, or
  payouts.
- Added a compact plugin-style Stripe row to the existing Connections popover,
  with Connect and manual Sync flows that update the open drawer without route
  refresh. Added the Stripe manifest, callback/start/sync routes, connection and
  provider-evidence schema, RLS, generated types, and environment contracts.
- Focused Finance/storage/Stripe acceptance passes `55/55`; scoped ESLint and
  diff checks pass. Disposable Finance RLS passes access, cross-tenant,
  direct-write, verification, correction, provider-evidence, and immutability
  checks. The temporary Stripe CLI resolves to `1.45.1`; manifest upload was not
  attempted. Authenticated browser visual verification remains unavailable
  because the browser CLI is absent. The two migrations remain unapplied to the
  linked database; no Stripe call, commit, push, deployment, or production
  mutation occurred.

2026-08-08 19:16 EDT - recovered the local workspace route.

- Confirmed `/workspace`, its dashboard breadcrumb, and its Next route manifest
  were present with no workspace `notFound()` branch. The five-day-old port-3000
  development process was serving stale router state.
- Restarted only the confirmed Coach House development process on port 3000.
  The fresh Next process compiled `/workspace` and served authenticated workspace
  traffic with HTTP 200; no source, database, Stripe, or production state changed.

2026-08-08 21:33 EDT - reconciled builder onboarding before release packaging.

- Corrected the builder journey to preserve required setup: intent and plan
  selection now lead to the real workspace organization/account setup before
  onboarding is marked complete or the canvas tutorial begins. Workspace setup
  completion then starts the contextual tutorial and records the appropriate
  activation checkpoint.
- Updated tutorial copy for the current Organization, Finance, People,
  Documents, and Accelerator drawer, and clarified the post-setup foundation
  stage. Centralized reserved public organization slugs so current application
  routes cannot be claimed as organization URLs.
- Focused onboarding validation passes `72/72`; scoped ESLint passes. The first
  parallel run produced transformer timeouts in the large dirty checkout, while
  the clean serial rerun passed. Full-tree release validation and packaging
  continue next; no commit, push, merge, deployment, database, or Stripe change
  occurred in this checkpoint.

2026-08-09 04:02 EDT - completed integrated release validation.

- Integrated the authorized working tree with `origin/main`, preserved the
  corrected onboarding flow, reconciled workspace, Finance, fiscal sponsorship,
  public map, people, and platform-admin contracts, and retained `/workspace` as
  a generated production route.
- Stabilized browser validation by capping visual concurrency, waiting for canvas
  hydration before interaction, and updating the permanent public Find drawer
  assertion to its accessible dialog contract. Standard visual regression now
  passes `30/30`; the destructive authenticated fiscal suite remains isolated to
  its preview-only configuration.
- Removed the full Prototype Lab client bundle from ordinary admin navigation by
  extracting its lightweight sidebar tree. `/admin` first-load JavaScript fell
  from `1464.3KB` to `995.5KB`, within the `1000KB` budget.
- `pnpm check:quality` exits `0`: supply-chain, large-file, lint, structure,
  route/feature/boundary/workspace guardrails, snapshots, `2026/2027` acceptance
  tests with one intentional skip, connected fiscal/Finance/full RLS, production
  build, `30/30` visuals, and all performance budgets pass. No production
  database, Stripe, or deployment mutation occurred during validation.
- The first hosted preview exposed an undeclared `simple-icons` dependency that
  the long-lived local install had masked. Declared the exact validated
  `16.27.1` version in the manifest and lockfile; supply-chain validation, a
  clean production build, all `108` generated routes, and performance budgets
  pass after dependency reconciliation.
- GitHub's first corrected quality run exposed two Linux-only ontology snapshot
  mismatches. Restored the platform-specific baselines and geometry-stability
  wait that merge resolution had dropped; the two affected visuals pass locally
  without changing production UI or visual baselines.

2026-08-01 16:53 EDT - restored production migration source history.

- Ran the explicitly approved Defy Ventures canary in single-organization apply
  mode. Mission remained unchanged, Vision and Values had no safe source
  content, the plan reported `changed: false` and `applied: false`, and the
  organization profile and revision remained identical.
- Restored exact source copies of five migrations already applied to production
  but missing from `main`: `20260729223500`, `20260730223700`, `20260730223800`,
  `20260801130500`, and `20260801143000`.
- Byte parity checks passed and the linked Supabase dry-run reported the remote
  database is up to date. No database or organization data changed during this
  source-history repair.

2026-08-06 15:05 EDT - verified the Batch 2 organization and workspace release candidate.

- Extracted the approved Organization, Workspace, People, roadmap/calendar, search, and Accelerator work from the dirty root onto `origin/main` in an isolated release branch; preserved the root and restored fiscal-link card focus compatibility.
- Completed dependency and test extraction, including People taxonomy schema bindings, the React virtualizer dependency, and the approved ontology visual fixtures and baselines. No new migration is included.
- `pnpm check:quality` passed against the isolated candidate server: lint, all guardrails, snapshots, 1,680 acceptance tests, production build, 26 visual tests, and performance budgets.
- RLS execution skipped without Supabase credentials. Connected RLS, authenticated preview QA, rollback rehearsal, commit, push, PR, deployment, and live verification remain approval-gated. No remote or production change occurred.

2026-08-06 15:42 EDT - removed unplanned Accelerator release-candidate scope.

- Removed the donor-only test-fixture class filter and drawer introduction banner
  while preserving the existing legacy-class filter, Accelerator drawer routing,
  module selection, progress normalization, and responsive scroll behavior.
- Removed the banner-owned export, panel API, runtime callback, source guard
  assertions, and dedicated tests; no filter or banner references remain.
- Focused Prettier, ESLint, `24/24` Accelerator drawer tests, and diff checks
  passed. No commit, push, deployment, donor change, or production mutation
  occurred.

2026-08-06 16:19 EDT - hardened Batch 2 People rollout regressions.

- Replaced LinkedIn photo refresh's unrestricted server fetches with
  canonical LinkedIn validation, DNS-pinned public-address requests, manual
  redirect validation, private-network denial, supported-image enforcement,
  timeouts, and declared plus actual response-size limits.
- Added behavior coverage for safe LinkedIn refresh, SSRF and oversized-body
  rejection, hidden social-value preservation, and rollout-off People canvas
  placement and interaction suppression.
- Focused ESLint, import boundaries, threshold checks, diff checks, and `28/28`
  focused tests passed. Scoped TypeScript checks are clean; repository-wide
  TypeScript and structure checks retain unrelated candidate failures. No
  commit, push, deployment, donor change, or production access occurred.

2026-08-06 16:32 EDT - closed the Batch 2 organization-page source budget.

- Reduced `my-organization-page-content.tsx` from 615 to 412 lines by moving
  unchanged rollout-mode resolution, legacy-editor rendering, fiscal workflow
  loading, and onboarding-default construction into owned page helpers.
- Preserved request-context authentication, active-organization authorization,
  default-off rollout behavior, legacy redirects, parallel data loading,
  taxonomy gating, and workspace/Accelerator rendering paths.
- Focused page and route coverage passed `35/35`; focused ESLint, diff checks,
  scoped TypeScript, and the full structure check passed. No donor change,
  commit, push, deployment, or production access occurred.

2026-08-06 16:21 EDT - stopped Batch 2 authenticated QA after an unintended workspace autosave.

- An authenticated local visit to the rollout-enabled People drawer invoked
  `saveWorkspaceBoardStateAction` for Coach House Solutions Group through the
  configured remote Supabase project. The local candidate server was stopped
  immediately and the QA tab was closed.
- Read-only verification confirmed the board row and top-level state timestamp
  advanced at `2026-08-06T20:21:33Z`, attributed to the authenticated tester.
  No audit/history table exists in the repository, so the prior raw row is not
  recoverable through the application and no blind rollback was attempted.
- No deployment, migration, commit, push, PR, or other production write
  occurred. Further authenticated QA is paused until it cannot persist remote
  workspace state.

2026-08-06 16:50 EDT - completed Batch 2 local release-candidate validation.

- Prevented passive Accelerator mount, prop, storage, and step normalization
  from publishing workspace progress; only explicit navigation or completion
  can now enter the existing autosave path. Focused proof passes `39/39`.
- Removed an unapproved documents-banner animation and refreshed the app-shell
  rail source guard to match the intentional closed-rail full-width behavior.
- `pnpm check:quality` passes: lint, all guardrails, snapshots, `1,695/1,695`
  executed acceptance tests across `337/337` files, production build, `26/26`
  visual tests, and performance budgets. RLS skipped without credentials.
- The rollback compatibility subset passes `52/52`; a fresh fetch confirms
  `origin/main` still matches the candidate base at `9843650`.
- Connected RLS, non-persisting authenticated preview QA, rollback rehearsal,
  commit, push, PR, deployment, and live verification remain approval-gated.

2026-08-06 17:02 EDT - created the Batch 2 rollback checkpoint.

- Committed the exact eight-file forward-compatibility reader/writer and test
  set as reachable local commit `7609daa`; its focused proof passes `52/52`.
- Removed two formatting-only files from the remaining candidate diff. No push,
  preview, deployment, database, or production change occurred.

2026-08-06 17:08 EDT - opened the Batch 2 preview pull request.

- Committed the reviewed Organization and Workspace foundation after the
  non-squashed compatibility floor and pushed preview PR #119.
- Local quality remains green: `1,695/1,695` acceptance tests, production build,
  `26/26` visual tests, and performance budgets passed. GitGuardian passed;
  Vercel previews are building. Supabase Preview was cancelled, so connected
  RLS and authenticated preview evidence remain open. No merge or production
  deployment occurred.

2026-08-06 17:50 EDT - completed Batch 2 connected preview proof.

- Repaired the disposable no-data Supabase branch from the immutable Stripe
  webhook migration source, applied the complete migration history through
  `20260805145856`, and received the literal result `RLS tests passed.`
- Authenticated `/workspace` QA passes desktop/mobile, dark/light, fullscreen
  geometry with `20px` corners, zero document overflow, explicit node-position
  persistence, and a passive reload with an unchanged board timestamp. The
  temporary user was deleted; no production data was cloned or written.
- Preview QA exposed a missing production Storage image-host dependency. Added
  the compatibility host and a focused source guard. Acceptance passes
  `1,696/1,696`, the production build passes, candidate-only visual QA passes
  `26/26` on port `3020`, and performance budgets pass. The existing dirty-tree
  server on port `3000` was preserved. Supabase's integration badge retains its
  original automated replay failure despite the repaired branch and connected
  proof. No merge or production deployment occurred.

2026-08-06 18:34 EDT - closed Batch 2 review and preview-replay defects.

- Made partial organization-profile patch optionality explicit and added direct
  logo-only coverage.
- Staged LinkedIn avatar refreshes at unique non-overwriting paths, stopped on
  upload errors, removed only the staged object after conflicts, and deferred
  prior-image cleanup until the revision-checked profile write succeeds.
- Replaced two invalid backslash-escaped seed apostrophes with SQL-standard
  escaping and added a regression guard for the Supabase preview seed failure.
- Focused coverage passes `27/27`; full acceptance passes `1,701/1,701`
  executed tests with one skip at reduced local worker concurrency. Lint,
  guardrails, snapshots, production build, `26/26` split production/development
  visual checks, and performance budgets pass. Local RLS skipped without
  credentials; the prior connected disposable-preview RLS run remains green.
- No merge, deployment, donor change, or production mutation occurred.

2026-08-06 21:55 EDT - stabilized Batch 2 ontology visual proof.

- The recovered GitHub Actions run reached the full quality suite and exposed
  two deterministic ontology screenshot failures while the other 24 visuals
  passed. Added a bounded React Flow geometry-settling check before those two
  screenshots without changing product UI, baselines, or the 1% threshold.
- The two focused visuals pass `2/2`; three repeated executions each pass
  `6/6`; and the complete candidate visual suite passes `26/26` on the isolated
  port `3020` server. Prettier, ESLint, and diff checks pass. No merge,
  deployment, database, or production mutation occurred.

2026-08-06 22:07 EDT - refreshed the two intentional ontology node baselines.

- Hosted Linux CI reproduced the same stable `2%` differences after geometry
  settling, proving timing was not the cause. The Batch 2 node canvas and card
  styling had intentionally changed while these two older baselines remained
  untouched.
- Regenerated only the light and dark ontology node snapshots from the reviewed
  fixture. Product UI and the `1%` visual threshold remain unchanged.
- Focused repeats pass `6/6`; the full candidate visual suite passes `26/26`
  on isolated port `3020`. No merge, deployment, database, or production
  mutation occurred.

2026-08-06 22:26 EDT - isolated ontology snapshots by rendering platform.

- Retained failed Playwright output in CI and inspected the exact Linux actual,
  expected, and diff images. Both retries were byte-identical; differences were
  confined to Linux versus macOS text rasterization.
- Added Linux-specific light and dark baselines for the two text-heavy ontology
  node fixtures. The product UI, assertions, and `1%` threshold remain intact;
  the existing baseline remains the macOS proof.
- Prettier and ESLint pass; focused repeats pass `6/6`; the complete local
  visual suite passes `26/26` on isolated port `3020`. Hosted validation remains
  pending. No merge, deployment, database, or production mutation occurred.

2026-08-07 00:03 EDT - completed the Batch 3 fiscal and project-operations release candidate.

- Reconciled the applicant, coach, project, task, document, Form B, W-9,
  review, signing, audit, and rollback work onto merged Batch 2 in the isolated
  `feat/batch3-fiscal-project-operations-20260806` worktree; the dirty root was
  not modified.
- Replaced multi-write fiscal, project, and task mutations with restricted
  service-role transaction RPCs. Closed direct task-assignee write policies so
  assignment and project-count changes cannot escape their atomic transition.
- Preserved assigned-coach authorization at the document route without pulling
  server-only modules into the client feature entrypoint. Added staged-upload
  rollback, document hash enforcement, final-schema role coverage, and a
  dedicated authenticated preview browser configuration.
- Repaired fresh database creation by making the legacy `published` backfill
  conditional. A disposable, data-free Supabase branch rebuilt successfully
  from the complete migration chain through `20260806010000`; production data
  was never cloned or written.
- Validation passes: lint, all structural guardrails, snapshots, `1,769/1,769`
  executed acceptance tests with one intentional skip, connected RLS, production
  build, `30/30` general visuals, `5/5` authenticated fiscal role/PDF journeys,
  and performance budgets.
- No commit, push, PR, merge, deployment, Stripe mutation, or production
  database change occurred. Counsel and fiscal-operations approval of
  `docs/plans/2026-08-06-fiscal-sponsorship-policy-approval-packet.md` remains
  the only Batch 3 release gate; after approval, commit and open the replacement
  PR, then retire stale PR #105.

2026-08-09 11:06 EDT - reconciled PR #121 with the Batch 3 production merge.

- Merged current `origin/main` into the integrated platform release and
  resolved the overlapping fiscal, workspace, schema, test, visual, package,
  policy, and runlog changes without discarding the Finance or onboarding work.
- Consolidated the duplicate fiscal authorization export exposed by the merge.
  Restored current-main RLS coverage while retaining the dedicated Finance RLS
  matrix and the newer fiscal workflow behavior.
- Found that Batch 3 had added task-assignee policy removals to migrations that
  were already applied remotely. Preserved the applied migration sources and
  added forward migration
  `20260809110500_harden_organization_task_assignee_writes.sql`. The product
  owner applied it, the local and remote migration histories align, and the
  connected suite confirms direct task-assignee writes are denied.
- Final validation: lint and all repository
  guardrails pass; `2,028` acceptance tests pass with one intentional skip;
  fiscal, Finance, and full connected RLS matrices pass; the production build
  generates all `108` routes; `30/30` visuals pass; and performance budgets
  pass.

2026-08-10 12:05 EDT - repaired the production Finance drawer deep link.

- Added the omitted `finance` branch to the workspace initial-drawer resolver,
  so authenticated visits to `/workspace?drawer=finance` open the existing
  Finance drawer instead of leaving the canvas closed.
- Added focused acceptance coverage for URL resolution and initial drawer
  initialization. The focused suite passes `11/11`.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots,
  `2,028` acceptance tests with one intentional skip, connected fiscal and
  Finance RLS matrices, the `108`-route production build, `30/30` visuals, and
  performance budgets. The optional generic RLS suite skipped without its
  separate credentials; the scoped connected Finance matrix passed.
- Public production `/find` was independently confirmed healthy with `870`
  active resources and no browser errors before this fix. NWS weather and
  cooling-center promotion remains intentionally deferred.

2026-08-10 13:16 EDT - enabled the scoped workspace drawer rollout and repaired Documents entry.

- Confirmed the merged S27 code was live but the entire workspace-foundation
  drawer remained disabled by its production rollout gate. Added
  `WORKSPACE_FOUNDATION_ROLLOUT_ENABLED=1` and the active Southside Community
  Table organization to `WORKSPACE_FOUNDATION_ROLLOUT_ORG_IDS` in the
  `coachhouse` Vercel project for Production and Preview, then redeployed the
  merged `9d5cd2bf` production build. Rollback is to disable the enabled flag.
- Authenticated production verification confirms the drawer now renders on
  `/workspace`, Documents opens the document index, Finance opens Activity and
  History, and both `/workspace?drawer=documents` and
  `/workspace?drawer=finance` select the requested tab.
- Replaced the Documents ontology-group legacy route with the canonical
  workspace Documents drawer URL, preserving link semantics and in-canvas
  activation. Focused coverage passes `25/25`.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots,
  `2,028` acceptance tests with one intentional skip, connected fiscal and
  Finance RLS matrices, the `108`-route production build, `30/30` visuals, and
  performance budgets. The optional generic RLS suite skipped without its
  separate credentials.

2026-08-10 13:58 EDT - prepared the workspace drawer for global rollout.

- Added an explicit `*` allowlist mode so the enabled workspace foundation can
  serve every authenticated organization while retaining UUID-scoped rollout
  and the existing kill switch.
- Added acceptance coverage for global organization and user wildcard modes and
  documented the production configuration contract.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,029`
  acceptance tests with one intentional skip, connected fiscal and Finance RLS,
  the `108`-route production build, `30/30` visuals, and performance budgets.
  The optional generic RLS suite skipped without its separate credentials.

2026-08-10 14:35 EDT - removed the organization-scoped workspace restriction.

- Simplified the workspace foundation gate so the existing enabled flag serves
  every authenticated organization member. The global kill switch remains the
  rollback control; organization and user allowlists no longer limit access.
- Updated acceptance coverage and the release contract for the all-user mode.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,028`
  acceptance tests with one intentional skip, connected fiscal and Finance RLS,
  the `108`-route production build, `30/30` visuals, and performance budgets.
  The optional generic RLS suite skipped without its separate credentials.

2026-08-10 15:39 EDT - repaired organization-list console failures.

- Prevented recreated but equivalent coach-assignment props from repeatedly
  replacing local state and triggering React's maximum-update-depth guard.
- Applied the same semantic-signature synchronization to organization Kanban
  visibility after live localhost verification exposed the adjacent loop.
- Classified missing linked Stripe subscriptions as a recoverable unavailable
  billing state with warning-level telemetry. Unexpected billing failures still
  emit error telemetry; no Stripe or subscription data was changed.
- Added focused acceptance coverage for stable assignment comparison and the
  missing-link billing recovery contract.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,031`
  acceptance tests with one intentional skip, connected fiscal and Finance RLS,
  the `108`-route production build, `30/30` visuals, and performance budgets.
  The optional generic RLS suite skipped without its separate credentials.

2026-08-10 16:35 EDT - removed the failing Space Grotesk production build dependency.

- Reused the existing Inter font instance for the legacy public-home body copy
  after both Vercel production projects failed to resolve Space Grotesk's
  generated `next/font` asset imports.
- Preserved the existing typography loading behavior without adding a new
  remote font request.
- Clean `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,030`
  acceptance tests with one intentional skip, fiscal and Finance RLS, the
  `108`-route production build, `30/30` visuals, and performance budgets. The
  optional generic RLS suite skipped without its separate credentials.

2026-08-10 19:01 EDT - self-hosted Inter after the external font failed hosted CI.

- PR #129's billing code and both Vercel previews passed, but hosted CI failed
  during the production build because Google returned `404` for every requested
  Inter file. Turbopack then could not resolve its generated font module. The
  failure was unrelated to the billing diff and repeated the hosted-font class
  fixed incompletely by PR #127.
- Added the packaged Inter variable font, loaded its weight axis from the root
  layout, and routed the app and legacy-home Inter styles through that local
  asset. Sora, Fraunces, Geist, and JetBrains Mono remain unchanged.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,030`
  acceptance tests with one intentional skip, fiscal and Finance RLS fixtures,
  the `108`-route production build without Inter download warnings, `30/30`
  local visuals, and performance budgets. The generic
  connected RLS suite skipped without its separate credentials. No deployment,
  Stripe mutation, or database change occurred.
- Hosted CI then confirmed the build fix and produced the same dark ontology
  screenshot twice. Self-hosted Inter changed only text glyph rendering, so the
  Linux baseline was updated; layout, color, and node geometry were unchanged.

2026-08-11 00:11 EDT - clarified the Find and Build loops.

- Updated the root Finance, Fundraising, Find PRD to describe one product. Find
  is available to everyone; Build is the existing organization workflow;
  Builders retain Find and My Map. This does not create separate personas,
  routes, onboarding, permissions, metrics, or test systems.
- Replaced the active rollout order with seven small waves covering existing
  live regressions, signup/legal, `/find` performance, completion of the current
  saved-organizations foundation as Collect/My Map, read-only Finance reporting,
  qualified resources/guides, and integrated release proof. NWS promotion
  remains deferred.
- Removed paid-billing and Builder rebuild projects from the active plan. Both
  are existing baseline behavior and reopen only for verified regressions.
- Kept the Finder alpha minimal: collect/uncollect nonprofits and resources,
  show them in My Map, and persist them. Lists, notes, tags, ordering, and guest
  import are out of scope.
- Defined Finance as source-labeled external activity with a simple graph,
  list, history, export, and board sharing. Coach House subscription billing
  remains separate and Stripe manages the money.
- Marked the original seven-batch Prototype Lab roadmap and dependency map as
  historical. No product behavior, schema, deployment, Stripe data, or database
  data changed.
- Focused Prototype Lab acceptance passes `36/36`.
  2026-08-11 02:02 EDT - started Wave 1 with the public Find light-mode repair.

- Confirmed production forced the public-map drawer and organization detail
  subtree into dark mode even when the application theme was light, producing
  a dark profile card and low-contrast text over a light map.
- Removed the forced dark-theme boundary and gave the shared overlay glass
  explicit semantic light and dark surfaces. The existing drawer, organization
  profile, resource profile, location prompt, status pill, and map alerts retain
  one shared theme contract.
- Browser proof passed for authenticated desktop and mobile light mode and
  desktop and mobile dark mode. Opening the organization profile intentionally
  added it to the tester's recent-organization preference through the existing
  account API; no favorite, organization, billing, or Finance data changed.
- Focused coverage passed `34/34`. Full `pnpm check:quality` passed: lint and all
  guardrails, snapshots, `2,030` acceptance tests with one intentional skip,
  connected fiscal, Finance, and generic RLS, the `108`-route production build,
  `30/30` visuals, and performance budgets. Production remains unchanged until
  the focused branch is reviewed, merged, deployed, and smoke-tested.
- Continue Wave 1 with the remaining paid/free/member/coach/admin journey audit
  after this narrow light-mode repair ships.

2026-08-11 02:56 EDT - reduced organization-detail server read latency.

- Confirmed the merged Find light-mode repair is live. Authenticated production
  checks also confirmed the Workspace Documents and Finance deep links render
  their intended drawers without changing organization, billing, or Finance
  data.
- Found that organization detail pages waited serially for fiscal permission,
  fiscal workflow, and developer billing reads after the main project load.
  Started those independent reads together while preserving the existing role
  checks, missing-subscription recovery, and rendered data.
- Added acceptance coverage for the parallel route contract. Focused checks
  pass `13/13`; the broader Wave 1 route and billing slice passes `108/108`.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,030`
  acceptance tests with one intentional skip, connected fiscal and Finance RLS,
  the `108`-route production build, `30/30` visuals, and performance budgets.
  The optional generic RLS suite skipped without its separate credentials.
- Authenticated rendering passed in production light mode and local dark mode.
  Cold development compilation was excluded from the performance conclusion;
  hosted proof still requires the focused PR preview. Continue Wave 1 with
  role-complete browser journeys after this slice ships.

2026-08-11 10:29 EDT - protected organization document references from stale writes and partial storage failures.

- Confirmed organization documents, policies, policy documents, and public
  documents wrote the entire organization profile without a revision check.
  Concurrent changes could be overwritten, and replacement or deletion removed
  storage objects before the new profile state was committed.
- Added one revision-aware organization-profile mutation helper with bounded
  conflict retries. The four routes now stage uploads, commit metadata, roll
  back failed staged uploads, and remove replaced or deleted objects only after
  the profile commit. Storage cleanup failures leave harmless orphaned objects
  instead of broken user-facing references.
- Added concurrency and route-ordering acceptance coverage. Focused tests pass
  `6/6`. The full `pnpm check:quality` gate passes: lint and all guardrails,
  snapshots, `2,036` acceptance tests with one intentional skip, fiscal and
  Finance RLS, the `108`-route production build, `30/30` visuals against the
  isolated branch preview, and performance budgets. The optional generic RLS
  suite skipped without its separate credentials.
- An authenticated read-only browser smoke rendered the Documents index and
  existing organization files on the isolated preview. No organization,
  document, storage, billing, or Finance data was changed. Hosted preview,
  merge, deployment, and production proof remain required.

2026-08-11 11:14 EDT - removed organization writes from Workspace and Roadmap reads.

- Confirmed PR #134 is merged, both production deployments completed, and the
  authenticated production Documents index renders its existing files.
- Found that loading Workspace or Roadmap normalized legacy organization
  profile content and silently upserted the whole profile during rendering.
  A concurrent organization edit could be overwritten by a read.
- Removed both render-time upserts while preserving the same in-memory HTML and
  roadmap-section cleanup. Explicit save paths keep their existing sanitation.
- Added source-contract coverage that prevents insert, update, or upsert calls
  from returning to either rendering loader. Focused tests pass `13/13`.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,040`
  acceptance tests with one intentional skip, fiscal and Finance RLS, the
  `108`-route production build, `30/30` visuals against the isolated preview,
  and performance budgets. The optional generic RLS suite skipped without its
  separate credentials.
- Authenticated Roadmap and Workspace reads left the connected organization's
  `updated_at` revision unchanged. No organization, document, storage, billing,
  or Finance data was changed. Hosted preview, merge, deployment, and
  production proof remain required.

2026-08-11 11:52 EDT - kept People page loads from rewriting member data.

- Confirmed PR #135 merged, then rebased the next focused Wave 1 repair onto
  current `origin/main`.
- Found that loading People synchronized the signed-in member's account name,
  headline, email, avatar, and membership category into the organization
  profile. Merely opening the page could overwrite directory fields that an
  organization had set explicitly.
- Kept the existing account-derived display synchronization in memory and
  removed its organization-profile mutation. Explicit People edits, onboarding,
  invites, and position writes retain their revision-aware persistence.
- Added read-safety coverage and updated the existing People concurrency
  contract. Focused tests pass `7/7`.
- Full `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,038`
  acceptance tests with one intentional skip, connected fiscal, Finance, and
  generic RLS, the `108`-route production build, `30/30` visuals, and
  performance budgets.
- The authenticated platform People directory rendered `61` organizations and
  `108` people. The connected organization revision remained unchanged. No
  organization, People, billing, Finance, or storage data was changed. Hosted
  preview, merge, deployment, and production proof remain required.
- Both PR #135 production deployments completed. Authenticated production
  Workspace and Roadmap renders passed, and the connected organization revision
  remained unchanged across both reads.

2026-08-11 12:59 EDT - protected Workspace board saves from concurrent overwrites.

- Confirmed PR #136 is merged and deployed through both production projects.
  People page loading no longer persists account-derived fields over the
  organization directory; explicit People editing remains available.
- Found that whole-board and node-position saves read the current board and then
  upserted the complete row without checking its revision. Two users saving at
  once could silently replace the earlier save.
- Added bounded compare-and-swap retries using the board row's `updated_at`
  revision. Each retry rebuilds from the latest state; first-row insert races
  retry after the unique conflict. Personal organization setup now ignores an
  existing row instead of touching its revision during a board save.
- Focused concurrency and persistence coverage passes `16/16`. Full
  `pnpm check:quality` passes: lint and all guardrails, snapshots, `2,043`
  acceptance tests with one intentional skip, connected fiscal, Finance, and
  generic RLS, the `108`-route production build, `30/30` visuals against the
  isolated preview, and performance budgets.
- The signed-in isolated Workspace request returned `200` and loaded the Coach
  House page at the expected route. No Workspace, organization, People,
  billing, Finance, or storage data was changed. PR #137 merged, hosted CI and
  both Vercel previews passed, and both production deployments completed.
  Authenticated production smoke remains required.

2026-08-11 19:12 EDT - restored durable current PRD completion tracking.

- Replaced the production-stability override's prohibition on one percentage
  with a fixed 35-criterion denominator: five source-controlled completion
  criteria for each of seven current waves. Only production-verified checked
  items count; current progress is `4/35` (`11%`), with `3` in progress and
  `28` not started.
- Added stable criterion IDs, evidence, explicit wave states, automatic counts,
  and exact PRD-to-source parity tests. The prior 37-item batch tracker remains
  available as labeled historical evidence and no longer drives the current
  percentage.
- Made Current waves the default Prototype Lab progress view, surfaced the
  computed percentage and next criterion, retained the historical readiness
  views, and followed the existing Geist/shadcn layout and accessible
  text-plus-icon status contract from `docs/design.md`.
- Focused finance-plan proof passes `37/37`; focused ESLint, thresholds,
  boundaries, React Grab, workspace-surface, raw-button, and diff checks pass.
  The full quality components pass: lint and all guardrails, snapshots,
  `2,044` acceptance tests with one intentional skip, connected fiscal,
  Finance, and generic RLS, the `108`-route production build, all `30` visual
  cases, and performance budgets. The canonical command's visual step first
  reused an unrelated hung port-3000 dev server; isolated development proof
  passed `29/30`, and the sole cold-compile `/find` timeout passed on its warmed
  focused rerun.
- No database, Stripe, or production mutation occurred during this work.

2026-08-11 19:58 EDT - completed Wave 1 production Workspace and role proof.

- Verified the canonical production site with authenticated paid/member,
  free/admin, and existing tester-coach sessions. Workspace, Platform admin,
  and coach Organizations journeys rendered without browser warnings or
  errors; the tester coach did not receive developer-only Platform navigation.
- On the seeded Southside case-study Workspace, moved the Organization card
  from `(866, 82)` to `(907, 107)`. The production board revision advanced,
  `updated_by` matched the authenticated actor, and a full refresh preserved
  the new position.
- Restored the card exactly to `(866, 82)` and confirmed that restoration in
  the production database. Restored the original admin browser session,
  revoked the temporary tester-coach token session, and closed the QA tabs.
  No organization content, membership, subscription, Stripe, or billing data
  changed.
- Marked `wave-1-criterion-5` complete, Wave 1 production-verified, and Wave 2
  current. Durable PRD progress is now `5/35` (`14%`), with `2` in progress and
  `28` not started.

2026-08-11 21:07 EDT - started Wave 3 with a compact public resource index.

- Parked draft PR #139 after its green technical preview checks; legal review
  and end-to-end signup journey QA remain intentionally open. No signup,
  account, database, or production mutation occurred.
- Added the cached versioned `/api/public/resource-map/index` contract in an
  isolated feature slice. It retains resource identity, coordinates, location
  summary, categories, verification, visibility, and useful availability
  status while excluding descriptions, street addresses, services, contacts,
  links, source URLs, hours, timestamps, and other detail-only fields.
- The current production endpoint returns `853` records in `2,519,680` bytes.
  The connected local compact endpoint returns the same `853` records in
  `415,659` bytes, an `83.5%` reduction, with five-minute shared caching and a
  ten-minute stale-while-revalidate window.
- Focused acceptance passes `18/18`; feature, scaffold, route, structure,
  threshold, and boundary guards pass. The production build passes with `109`
  routes. The existing full endpoint and `/find` client remain unchanged until
  detail-on-demand is available, so this slice introduces no UI regression.
- Both connected Chrome profiles blocked localhost, so visual browser display
  was unavailable; direct HTTP verified the live local response and exact field
  boundary. Production remains unchanged. Continue with bounded detail lookup,
  then switch `/find` to the compact index without losing selected records.

2026-08-11 21:30 EDT - started bounded Find index loading.

- Marked PR #143 review-ready after quality, step-ID, security, and both Vercel
  preview checks passed. The hosted endpoint is protected by Vercel SSO and the
  connected browser blocked it client-side, so no hosted payload claim was made.
- Added deterministic cursor pagination to the compact index on stacked branch
  `feat/find-bounded-loading-20260811`: 200 records by default, 500 maximum,
  explicit invalid-limit responses, stable public-ID ordering, and cursor
  recovery when a previously returned item disappears.
- Added a shared five-minute server snapshot. Connected local traversal returned
  all 853 production records exactly once across five pages; the first page was
  97,986 bytes and the cached remaining pages completed in 25-38 ms. The cache
  response remains `s-maxage=300, stale-while-revalidate=600`.
- Focused acceptance passes 21/21; feature, route, boundary, lint, formatting,
  and invalid-limit checks pass. The existing `/find` client and production are
  unchanged, so Wave 3 criterion 2 remains in progress pending client adoption.

2026-08-11 21:52 EDT - started Find resource detail on demand.

- Marked stacked PR #144 review-ready after hosted quality, step-ID, security,
  and both Vercel preview checks passed.
- Added a read-only, versioned `/api/public/resource-map/items/[id]` response.
  It resolves one exact UUID from the existing anonymous sanitized view, reuses
  the established public full-item sanitizer, rejects malformed IDs before the
  database query, returns 404 for missing records, and caches each sanitized
  detail separately for five minutes.
- Connected local proof returned the same ID selected from the compact index in
  a 2,783-byte response with no favicon, raw logo, mission, values, or vision
  fields. The cold request spent 1.2 seconds in application code; a repeated
  request completed in 46 ms end to end and 9 ms in application code.
- An attempted shared raw-dataset cache was rejected during iteration because
  its 3,061,721-byte value exceeded Next.js's 2 MB per-entry limit. It was never
  committed; the compact index cache stayed unchanged and per-item sanitized
  caching replaced it. No migration, database write, UI, or production change
  occurred. Criterion 3 remains in progress pending client selection and
  collection adoption.

2026-08-11 22:23 EDT - started the compact Find client cutover.

- Marked stacked PR #145 review-ready after hosted quality, step-ID, security,
  and both Vercel preview checks passed.
- Switched both public `/find` compositions from the 2.5 MB full-resource
  endpoint to the versioned compact index. The client publishes each page as it
  arrives, continues through the stable cursor, and fetches full sanitized
  detail only after a resource is selected. Repeated detail requests reuse the
  five-minute client/server cache.
- Browser iteration exposed that immediate page requests could outrun the
  earlier whole-index cache write and repeat the 853-row source query. Replaced
  that path with direct bounded reads against the existing anonymous sanitized
  view: each request reads at most 201 rows. Connected traversal returned all
  853 unique records in five ordered pages; warmed application times were
  0.18-0.89 seconds per page on the final local run.
- Progressive refresh now merges incoming pages with the prior directory so a
  selected later-page resource is not temporarily dropped; final completion
  reconciles removed records. Focused client, query, index, and map acceptance
  passes 57/57.
- Displayed the local `/find` surface in Chrome. The page and all five compact
  requests rendered, but full DOM inspection timed out after the directory grew;
  the connected Chrome profile also surfaced a Supabase auth lock timeout. No
  browser settings, database data, production, styles, or controls
  changed. Criteria 2 and 3 remain in progress pending hosted and production
  browser proof.

2026-08-11 23:22 EDT - verified the bounded Find client slice.

- Rechecked the four-PR Wave 3 stack and kept work limited to criterion 2's
  client adoption. PRs #143-#146 have green hosted quality and deployment
  checks; PR #143 remains blocked only by required review.
- Ran the exact `feat/find-compact-client-20260811` branch against the existing
  curated local preview without copying or changing its data. The browser
  rendered the connected `/find` map shell while 200-item cursor pages arrived,
  and server logs confirmed continued bounded page requests.
- The connected `coachhouse` preview remains protected by Vercel access control;
  no access request, account switch, signup, database write, or production
  change occurred. Continue by making PR #146 review-ready, then merge the
  stack in order after PR #143 receives its required approval.

2026-08-11 23:56 EDT - merged the first three Wave 3 Find slices.

- Reviewed and sole-maintainer merged PRs #143, #144, and #145 in order. The
  compact public index, bounded cursor endpoint, and exact sanitized detail
  endpoint are now on `main`; PR #146 remains the client-adoption slice.
- Rebased each child branch onto current `main` after its parent merged, keeping
  each PR limited to its intended diff. Standard pre-push hooks passed with
  2,050 tests for #144 and 2,053 tests for #145, each with one intentional skip.
- PR #145's first hosted quality run failed on the existing Google-hosted
  Fraunces Turbopack module while both Vercel builds and local checks passed.
  The unchanged rerun passed in 6m35s, confirming unrelated CI flakiness.
- Restacked PR #146 onto current `main`. No signup, account, database, resource,
  or production data was changed. Continue with the client slice's standard
  checks and merge, then production verification for criteria 1-3.

2026-08-12 00:18 EDT - production-verified Wave 3 Find criteria 1-3.

- Reviewed and sole-maintainer merged PR #146 after its focused 32/32 checks,
  full pre-push suite, hosted quality, and both Vercel previews passed. Both
  production deployments for commit `648b0885` completed successfully.
- Traversed the live anonymous compact index in five stable cursor pages of
  200/200/200/200/53 records. All 853 IDs were unique; page responses were
  25,990-97,986 bytes and completed in 304-1,749 ms from the external check.
- The live exact-item endpoint returned the selected compact-index ID in a
  2,783-byte sanitized response. Focused coverage confirms later-page
  selections remain resolvable while progressive refresh reconciles results.
- Displayed production `/find` in Chrome. The visible combined result count
  progressed from 618 to 871 while the loaded map and directory context stayed
  available. No signup, account, database, or resource data was changed.
- Marked Wave 3 criteria 1-3 complete at 8/35 durable criteria. Criterion 4,
  state recovery without losing map context, is next.

2026-08-12 00:29 EDT - started Wave 3 Find state recovery.

- Kept criterion 4 limited to the existing Find loading-state contract. Initial
  loading now describes progressive resources instead of a monolithic full
  directory, while existing search and category empty states remain intact.
- Added explicit offline detection, preserved loaded resource items as visibly
  labeled stale results, retained the map and directory shell, and automatically
  revalidates after the browser reconnects. Manual retry remains available.
- Focused client, sidebar, and route acceptance checks pass 51/51. No map,
  resource, database, authentication, or collection behavior changed.

2026-08-12 00:53 EDT - production-verified Wave 3 Find state recovery.

- Reviewed and sole-maintainer merged PR #148 after focused 51/51 coverage,
  the 2,059-test pre-push suite, hosted quality, and both Vercel previews passed.
- The first `coachhouse` preview failed while the identical
  `coach-house-platform` build and hosted quality passed. Its logs were not
  available to the current Vercel CLI account; an unchanged-tree retry passed
  both previews, so no speculative code change was made.
- Both production deployments for commit `a149d763` completed successfully.
  Loading, empty, unavailable, offline, stale-result, reconnect, and manual
  retry rendering are covered without clearing loaded map resources or the map
  shell. Marked criterion 4 complete at 9/35; criterion 5 is next.

2026-08-12 01:48 EDT - removed hosted font-build network dependencies.

- Confirmed PR #145 did not change fonts; its transient Fraunces failure exposed
  a repository-wide `next/font/google` build dependency. A later hosted retry
  failed on Sora, confirming the issue was not limited to one route or family.
- Replaced the remaining Google loaders for Fraunces, Sora, JetBrains Mono,
  Geist, and Geist Mono with versioned Fontsource assets. Route-only families
  remain scoped to `/home`, `/legacy-home`, and the internal Platform Lab so
  unrelated routes do not inherit their stylesheets.
- Added focused source guards against restoring Google font downloads. Focused
  acceptance passes 7/7, targeted lint passes, and the production build passes.
  Browser checks displayed `/home` and `/legacy-home` with Fraunces, Sora, and
  Inter loaded in their intended roles. No signup, data, migration, or
  production change occurred; Wave 3 criterion 5 remains next.

2026-08-12 03:32 EDT - started Wave 3 Find performance budgets.

- Replaced the anonymous Find route's generic home preview with a dedicated
  shell that preserves the visible header, navigation, map frame, and rails
  without preloading hidden home, pricing, and authentication panels.
- Reduced compact index pages from 200 to the established 50-item maximum and
  batches intermediate client updates every 200 records while publishing the
  first page immediately. The account sheet now loads only when opened.
- Reduced `/find` first-load JavaScript from 2,085.6KB to 1,852.6KB, an 8.6%
  improvement, and added a 1,900KB route budget to `check:perf`. The production
  build, focused 28-test suite, targeted lint, and all route budgets pass.
- Displayed the local production build in the browser without opening signup.
  Its shell and search controls render correctly; this isolated worktree lacks
  the public Mapbox token, so connected map, LCP, and interaction proof remain
  for the hosted preview and production before criterion 5 can be completed.

2026-08-12 04:08 EDT - deployed Find budgets and isolated the LCP blocker.

- Reviewed and sole-maintainer merged PR #151 after 2,066 pre-push tests,
  hosted quality, and both Vercel previews passed. Main CI and both production
  deployments for commit `77862637` passed.
- Production `/find` HTML measured 137,592 bytes raw and 37,171 bytes
  compressed. Its 50-item index page measured 24,964 bytes raw and about 3,970
  bytes compressed; four warm samples completed in 185-249 ms.
- Browser production proof displayed the loaded Mapbox surface with all 871
  combined results. No signup, location permission, account, or data mutation
  occurred.
- A representative mobile Lighthouse run with software WebGL measured FCP at
  1.46 s but LCP at 37.26 s. The LCP element is the existing location consent
  card, which currently loses its first paint to synchronous Mapbox startup.
- Started a narrow follow-up that lets the initial shell and location consent
  state paint before Mapbox initialization. Wave 3 criterion 5 remains active
  until the preview and production LCP reruns pass.

2026-08-12 04:24 EDT - tightened the Find initial-paint boundary.

- PR #152 passed 2,067 pre-push tests, hosted quality, both previews, and both
  production deployments before its mobile WebGL Lighthouse rerun.
- Yielding one paint reduced production LCP from 37.26 s to 13.52 s, while FCP
  remained 1.52 s. The existing location consent card remained the LCP element,
  proving Mapbox startup still began before React received a reliable paint.
- Started a final narrow follow-up that reserves one second for the initial
  shell and consent state before Mapbox startup. Map behavior and permission
  behavior are unchanged; criterion 5 remains active pending hosted reruns.

2026-08-12 04:42 EDT - moved intended Find consent into the initial render.

- PR #153 passed 2,067 pre-push tests, hosted quality, both previews, and both
  production deployments. Its mobile WebGL Lighthouse rerun measured FCP at
  1.51 s and LCP at 13.77 s; the existing location consent card remained the
  late LCP candidate.
- Preserved the existing automatic consent-card contract while moving its open
  state into the initial render. Browser geolocation still requires the same
  explicit action and no location, account, or production data was changed.
- Wave 3 criterion 5 remains active pending the hosted numeric rerun.

2026-08-12 05:12 EDT - isolated the final Find render blocker.

- PR #154 passed 2,068 pre-push tests, hosted quality, both previews, and both
  production deployments. Rendering consent with the initial shell reduced
  production mobile WebGL LCP from 13.77 s to 4.76 s.
- The trace measured FCP at 1.53 s and 2.44 s of LCP element render delay.
  Mapbox still began at 1 s, before first paint, and then occupied the throttled
  main thread. Increased the bounded map-start delay to 2.5 s so the usable
  search shell and consent card paint before Mapbox startup.
- Criterion 5 remains active pending the final hosted rerun.

2026-08-12 14:17 EDT - replaced the missing legal routes and generic policy draft.

- Confirmed production `/privacy` and `/terms` fell through the dynamic
  organization route and redirected to `/find/privacy` and `/find/terms`, so
  neither URL displayed a legal document. Replaced the deleted and conflicted
  PR #139 branch with isolated branch `feat/legal-policy-consent-20260812` from
  current `origin/main`.
- Added canonical Terms and Privacy routes, readable section navigation, and
  required linked acceptance on the shared normal, contextual, and tester
  signup form. Acceptance is bound to version `2026-08-12.1`, exact document
  SHA-256 hashes, user, server UTC time, and source in an append-only RLS table.
- Rewrote both documents around actual Coach House data and product behavior,
  including organization/public sharing, financial and tax evidence, location,
  Vercel analytics, Supabase, Stripe, Mapbox, Resend, hCaptcha, OpenAI, retention,
  security, state privacy rights, marketing email, automated call/text consent,
  subscriptions, acceptable use, professional-advice boundaries, warranties,
  liability, and resource-directory limitations.
- Reviewed current official FTC, FCC, California, and Illinois guidance. The
  documents reduce disclosure gaps but cannot guarantee compliance. Qualified
  counsel must confirm the exact legal entity, postal address, retention
  schedule, vendor agreements, governing-law terms, and applicable state or
  international requirements before approval. Marketing email also requires a
  verified physical postal address outside these pages.
- Focused lint and legal, signup-schema, and seed tests pass `9/9`. All full
  quality components pass: lint and guardrails, snapshots, `2,073` acceptance
  tests with one intentional skip, fiscal and Finance RLS, the production build
  with real `/privacy` and `/terms` routes, `30/30` visuals on isolated port
  `3015`, and performance budgets. The canonical visual step first reused an
  unrelated port-3000 server, so it was stopped and rerun against this branch;
  the optional generic connected RLS suite skipped without its credentials.
- Chrome opened both local legal routes and the server returned `200`; no form
  was submitted. No account, database, payment, email, or production data was
  changed. Connected migration proof, hosted checks, and legal approval remain.

2026-08-12 14:45 EDT - moved legal documents into the public canvas shell.

- Replaced the standalone `PublicHeader` on `/privacy` and `/terms` with the
  home canvas visual language: the Coach House brand rail, padded rounded
  canvas, dark intro surface, and an internally scrolling document body.
- Removed the theme-toggle button from both legal routes without changing the
  shared Button or theme-toggle primitives. Kept the canonical routes, policy
  text, document version, consent hashes, and signup evidence unchanged.
- Focused lint and all five legal acceptance tests pass. Chrome confirmed both
  routes render the canvas owner and correct heading; the updated Privacy page
  was left open for review.

2026-08-12 14:49 EDT - removed the unrequested legal-page hero treatment.

- Removed the dark-blue background, radial gradients, translucent dark panel,
  and forced white text from the legal document header. The normal themed
  background, rounded home-canvas frame, routes, and legal content remain.
- Focused lint and all five legal acceptance tests pass. Chrome confirmed the
  Privacy canvas contains no `#07111f` background and displayed the update.

2026-08-12 15:05 EDT - recorded legal approval for the current policy copy.

- The product owner reported that legal review is complete and the current
  Terms and Privacy copy is approved for release.
- Marked Wave 2 criterion 4 complete. PR #156 still requires its hosted gates,
  connected migration proof, merge, deployment, and production smoke evidence.

2026-08-12 15:11 EDT - verified the legal migration plan and signup paths.

- Used the existing production Supabase link for read-only checks. Remote
  migration history is aligned through `20260809110500`, public-schema lint
  reports no errors, and `supabase db push --dry-run --linked` would apply only
  `20260811160000_add_signup_legal_acceptances.sql`. No migration was applied.
- Ran the focused direct and contextual signup, required-denial, confirmation,
  safe-return, retry, and recovery matrix: 12 files and 43 tests pass. No
  account was created and no email was sent.
- Marked Wave 2 criterion 3 in progress pending hosted and production proof.

2026-08-12 15:34 EDT - production-released the approved legal and consent slice.

- Sole-maintainer merged PR #156 as `9cacf375` after quality, step validation,
  GitGuardian, and both hosted previews passed. Main CI and both Vercel
  production deployments then passed.
- Applied only migration `20260811160000_add_signup_legal_acceptances.sql` after
  its connected dry-run. Remote history is aligned, public-schema lint reports
  no errors, the new table exists with zero rows, and anonymous REST reads are
  denied with `401`. No account was created and no email was sent.
- Production `/privacy` and `/terms` now return canonical `200` routes with
  document version `2026-08-12.1` and the legal canvas marker instead of
  redirecting through `/find/*`. Marked Wave 2 criterion 1 complete at 11/35;
  criteria 2, 3, and 5 remain in progress pending one controlled signup and
  tested rollback.

2026-08-12 15:36 EDT - recovered the canonical local preview on port 3000.

- The prior 23-hour Next dev process accepted TCP connections but returned no
  bytes, consumed more than one CPU core, and retained a 20-hour unbounded curl
  probe. Stopped its exact process tree and the unwanted port-3015 server.
- Quarantined the generated 2.5 GB `.next` directory at
  `/private/tmp/coach-house-legal-policy-next-cache-20260812-1530`; nothing was
  deleted. Linked the ignored root `.env.local` into the isolated worktree
  without printing or committing secrets and restarted one detached
  `coach-house-dev-3000` screen session.
- Future local checks use one port-3000 server, bounded curl probes, full
  process-tree shutdown, and recoverable cache quarantine only after repeated
  stalls. The focused browser tab displays `http://localhost:3000/find`.

2026-08-12 16:31 EDT - implemented the first local Collect and My Map slice.

- Started isolated branch `feat/my-map-guest-collection-20260812` from current
  `origin/main`. Visitors can now collect and remove nonprofits without a
  signup interruption, collect and remove public resources, and retain both
  collections locally. The existing Saved tab is presented as My Map on the
  shared desktop rail and mobile drawer without adding Finder onboarding,
  lists, notes, tags, ordering, or import.
- Kept the existing signed-in saved-organization sync intact. Resource
  collection remains local in this slice; cross-device account persistence and
  replay remain Wave 4 criterion 2 work. Collected-resource resolution during
  partial and failed loading remains criterion 4 work.
- Focused lint, all source guardrails, snapshots, 2,074 acceptance tests, all
  RLS suites, the production build, 30 visual cases, and performance budgets
  pass. The two Find visual cases initially used the obsolete Saved label and
  passed after their selectors were changed to My Map. The repository-wide
  standalone TypeScript command still reports unrelated pre-existing
  test-fixture errors; the production build's TypeScript validation passed.
- Replaced the port-3000 proof server with this exact worktree, kept port 3015
  closed, and opened `http://localhost:3000/find` in Chrome. The first compile
  completed in 51 seconds and the subsequent browser request returned `200` in
  5.9 seconds. A partial dependency install was moved to
  `/private/tmp/coach-house-my-map-partial-node-modules-20260812-1625`; nothing
  was deleted.

2026-08-12 17:29 EDT - released guest My Map and started account persistence.

- PR #158 merged as `8f5196ab`; main quality and deployment passed, and
  production `/find` returned `200`. Marked Wave 4 criteria 1 and 3 complete:
  visitor nonprofit/resource collection is released through the existing My
  Map foundation with no separate Finder onboarding.
- Started isolated branch `feat/my-map-account-sync-20260812` from that merge.
  Resource IDs now use the existing authenticated map-preference endpoint and
  the same bounded remote/local merge and idempotent desired-state PATCH used
  by saved nonprofits. No migration or new data system was added.
- Focused account-route and preference coverage passes 6/6 plus lint. The
  tracker and focused safeguard set passes 43/43; all source guardrails,
  snapshots, all RLS suites, 2,077 acceptance tests, and the production build
  pass. No visual baseline changed because this slice changes persistence only.
  Marked Wave 4 criterion 2 in progress; merge, deployment, and authenticated
  cross-device proof remain.
- Quarantined the incomplete dependency tree at
  `/private/tmp/coach-house-my-map-account-sync-partial-node-modules-20260812-1727`
  and replaced it with a full copy from the lockfile-matched release worktree;
  nothing was deleted.

2026-08-12 18:25 EDT - started My Map loading and recovery resilience.

- PR #159 merged as `6b2ac883`; started isolated branch
  `feat/my-map-resilience-20260812` from that merge for Wave 4 criterion 4.
- My Map now retains previously resolved collected resource cards while a
  refresh is loading or fails, but a confirmed successful refresh can still
  remove an unpublished resource. Unresolved collected IDs render loading
  or offline/unavailable recovery feedback instead of the false-empty
  “Nothing collected yet” state, with an accessible retry action on errors.
- Focused tracker, resource, preference, and sidebar coverage passes 78/78 plus
  focused lint, routes, and boundaries. RLS and the production build pass. The
  exact branch is running on `localhost:3000/find` and focused in Chrome;
  normal loaded-state visuals are unchanged.

2026-08-12 20:01 EDT - production-verified My Map release and rollback.

- PR #160 merged as `ae0deda7`; main quality, build, Supabase preview, GitHub
  Pages, and both Vercel production deployments passed. Production `/find`
  returns `200` and displays the merged My Map surface.
- In the existing authenticated Builder/admin session, collected Instituto del
  Progreso Latino, reloaded `/find`, confirmed it in My Map, removed it, then
  reloaded and confirmed the account returned to zero collected resources.
  No account was created and no unrelated production record was changed.
- Production browser logs contained no application errors; the only warning
  came from the local React developer extension. All RLS suites and 2,079 full
  acceptance tests pass. Marked Wave 4 criteria 4 and 5 complete.
- Criterion 2 remains in progress because reload proves production persistence
  but is not an independent second-client observation. The account route and
  replay tests are green; only that bounded cross-client evidence remains.
- Non-destructive rollback is to revert PR #160 (`ae0deda7`), then PR #159
  (`6b2ac883`), then PR #158 (`8f5196ab`) as needed. Existing saved
  organizations and account data are preserved; no destructive data rollback
  is required.

2026-08-12 22:16 EDT - advanced Wave 5 Finance reporting toward production proof.

- Started isolated branch `feat/finance-reporting-wave5-20260812` from
  `origin/main` at `c6b96ef2`; preserved all existing worktrees and avoided the
  stale root branch.
- Confirmed PR #121 merged as `a6016231`, its quality gate passed, both Vercel
  production deployments succeeded, and the authenticated rollout opened the
  released Activity and History views.
- Kept the existing Finance surface compact while making a connected Stripe
  row explicitly read-only and exposing its loaded last-sync timestamp. The
  freshness copy handles never-synced, running, successful, and failed states
  and updates immediately after a manual sync.
- Focused Finance acceptance passes `70/70`, and the Finance plan plus feature
  set passes `107/107`. The first full gate correctly rejected stale Prototype
  Lab counts; aligning its Wave 5 states resolved the only failure.
- Final `pnpm check:quality` passes all lint and source guardrails, snapshots,
  `2,080` acceptance tests with one intentional skip, fiscal and Finance RLS,
  the 111-page production build, `30/30` visuals, and performance budgets.
- Read-only production browser proof displayed Activity, Connections, the
  accessible empty Raised graph, and History with no console errors. Stripe is
  not configured for the current organization, so no connection, sync, record,
  account, database, migration, or production mutation occurred.
- Marked Wave 5 criteria 2 and 3 complete from merged production and connected
  RLS evidence. Criterion 1 remains in progress until this branch merges and
  deploys. Progress is `17/35` complete (`49%`), with `5` in progress and `13`
  not started.

2026-08-13 15:03 EDT - production-verified Finance freshness and measured Find budgets.

- PR #162 merged as `09810abd`; main quality and GitHub Pages passed, both
  Vercel production deployments succeeded, and the production Finance route
  serves the released read-only source and freshness-label implementation.
- Marked Wave 5 criterion 1 complete. The underlying read-only Stripe activity
  path remains bounded and the 70 focused Finance tests cover never-synced,
  running, successful, and failed freshness states.
- Ran one bounded production mobile Lighthouse check for `/find`. First
  contentful paint was 1.7s and the 1,900 KB route budget remains enforced, but
  LCP was 4.7s and time to interactive was 35.0s because Mapbox script
  evaluation dominated main-thread work. Marked Wave 3 criterion 5 in progress
  rather than falsely completing it or expanding this proof slice into a map
  rewrite.
- Progress is `18/35` complete (`51%`), with `5` in progress and `12` not
  started. No account, connection, sync, database, migration, or production
  data mutation occurred.

2026-08-13 15:16 EDT - implemented authorized Finance CSV and PDF exports.

- Rebasing branch `feat/finance-report-exports-20260813` onto merged PR #163
  preserved the canonical `18/35` (`51%`) production-backed completion count.
- Added a compact Export menu to Finance with CSV and PDF downloads backed by
  the existing authenticated organization and explicit Finance-view access
  boundary. No service-role read, migration, or new data model was added.
- CSV exports preserve signed cents, UTC timestamps, source, type, program,
  correction state, and verification reference while neutralizing formulas in
  user-controlled text fields. PDF exports provide the same authorized history
  in a paginated landscape report with record count and generation timestamp.
- Nine focused export, authorization, route, and bounded-failure tests pass;
  route, feature, boundary, raw-button, React Grab, and threshold guardrails
  pass; the 112-route production build passes. Criterion 4 is in progress until
  merge, both production deployments, and one authorized production download.
- Progress remains `18/35` complete (`51%`), with `6` in progress and `11` not
  started. No account, Finance record, connection, database, or production data
  mutation occurred.

2026-08-13 15:35 EDT - completed Finance exports and implemented revocable board sharing.

- PR #164 merged as `f6ceb0c4`; main quality and both production deployments
  passed, and authenticated production displays the released CSV/PDF Export
  control. Nine focused tests parse the PDF and verify exact CSV fields,
  authorization, formula neutralization, pagination, routes, and failures.
- Started isolated branch `feat/finance-board-sharing-20260813` from that merge.
  The owner can assign Viewer, Manager, or No access to existing board members
  from one compact Finance control. No invitation or public-link scope was added.
- Grants and revocations use the existing organization-scoped access table.
  A forward RLS migration requires current board membership and removes a grant
  when that membership is inserted, removed, or changes role, preventing stale
  access from surviving or reviving. It does not purge unrelated records.
- Eight focused sharing tests, 30 existing Finance tests, Finance RLS, targeted
  source guardrails, and the 112-route production build pass. Merge, production
  role proof, and the exact revert remain before sharing is complete.
- Progress is `19/35` complete (`54%`), with `6` in progress and `10` not
  started. No account, membership, Finance record, connection, or production
  data mutation occurred.

2026-08-13 15:56 EDT - production-verified Finance sharing release and refreshed exact resource counts.

- PR #165 merged as `2387591e`; main quality, Supabase production migration,
  GitHub Pages, and both Vercel production deployments passed. The disposable
  Supabase preview had returned `400 Resource has been removed`, but the same
  migration applied successfully to the linked production ledger.
- Finance sharing remains in progress until one authenticated owner-to-board
  grant/revoke journey and a forward database rollback are complete. No
  production access grant, membership, or Finance record was mutated here.
- Started isolated branch `docs/wave6-resource-counts-20260813` from the merge.
  A read-only audit reports the expanded local curated snapshot separately at
  `5,046` candidates and `741` complete, independently verified, publishable
  records. The primary intake queue remains `4,249` records.
- Exact production aggregates report `2,184` staged records and parity at `853`
  verified, administrator-approved, publishable, promoted, and anonymous public
  rows. Artifact hashes, definitions, and reproduction steps are recorded in a
  dated count report; no source fetch, import, review, promotion, unpublish, or
  production data mutation occurred.
- Marked Wave 6 criterion 1 complete. Progress is `20/35` (`57%`), with `6` in
  progress and `9` not started.

2026-08-13 16:25 EDT - repaired the first Chicago housing source group.

- Audited the 14 Chicago housing listings missing a second provider source.
  Current provider pages resolved seven, raising the group from `92/106` to
  `99/106` listings with two sources. All 106 already have the required service,
  summary, category, location, contact, eligibility, and access fields.
- Replaced nine stale provider links during deterministic 211 ingestion, raised
  the bounded provider-page response limit from 750 KB to 1.5 MB for current
  provider sites, and restored the documented package command for the checker.
- Four AIDS Foundation of Chicago listings and one Child Link listing remain
  held after HTTP 403 responses; L.A.M. House remains held without a provider
  website; Willis House remains held because its site redirects to a 404 page.
- Focused importer and provider-page tests pass `11/11`. No source record was
  imported, approved, published, or changed in production. Wave 6 criterion 2
  is now in progress; overall completion remains `20/35` (`57%`), with `7` in
  progress and `8` not started.

2026-08-13 16:39 EDT - repaired source-provided phone numbers for food listings.

- Found 12 Feeding Illinois listings whose public source records contain a
  `contactPhone` value while the importer only read the empty `phone` field.
  The importer now uses the provider contact phone as a fallback without
  exposing contact names.
- Contact coverage increased from `644/752` to `656/752`. Listings with both
  contact information and two sources increased from `615/752` to `626/752`;
  second-source coverage remains `649/752`.
- Eleven repaired listings now have contact information and two sources. Church
  of the Holy Spirit–Lake Forest remains held because it still lacks a second
  source. Nothing was imported, approved, published, or changed in production.
- Focused importer and coverage tests pass `8/8`. Overall PRD completion remains
  `20/35` (`57%`).

2026-08-13 16:49 EDT - added a real intake path to Cook County food listings.

- Identified 20 food listings without a direct provider phone, email, or
  website whose source rows place them in the Greater Chicago Food Depository
  region. Added the food bank's official find-food page as a clearly labeled
  intake link, not as the individual provider's website or independent proof.
- Food listings with contact information or an intake link increased from
  `656/752` to `676/752`. Listings with that access path and two sources
  increased from `626/752` to `645/752`; second-source coverage remains
  `649/752`.
- Nineteen repaired listings already have two sources. One remains held, and 76
  food listings still lack direct contact information or a verified intake
  link. Nothing was imported, approved, published, or changed in production.
- Focused importer and coverage tests pass `9/9`. Overall PRD completion remains
  `20/35` (`57%`).

2026-08-13 16:56 EDT - added a real intake path to Northern Illinois food listings.

- Identified 72 food listings without a direct provider phone, email, or
  website whose source rows place them in the Northern Illinois Food Bank
  region. Added the food bank's official groceries and resources page as a
  clearly labeled intake link, not as the individual provider's website or
  independent proof.
- Food listings with contact information or an intake link increased from
  `676/752` to `748/752`. Listings with that access path and two sources
  increased from `645/752` to `647/752`; second-source coverage remains
  `649/752`.
- Two repaired listings already have two sources. The other 70 remain held, and
  four food listings still lack direct contact information or a verified intake
  link. Nothing was imported, approved, published, or changed in production.
- Focused importer and coverage tests pass `10/10`. Overall PRD completion
  remains `20/35` (`57%`).

2026-08-13 17:28 EDT - repaired immigrant and refugee provider identity evidence.

- Refreshed only the 16 Cook County directory records that lacked a second
  provider identity comparison. Nine current provider pages now fetch and
  match under the existing rules.
- Added strict four-to-ten-letter provider acronym matching for whole page-text
  tokens and same-site domains. This corroborates HIAS Chicago and CASL without
  treating a successful fetch alone as evidence.
- Two-source identity coverage increased from `68/84` to `79/84`.
  `74/79` service-eligible records now have that evidence. Five eligible records
  remain held behind HTTP `403`, oversized-page, missing-site, or unusable
  redirect results; no override was added.
- Focused provider comparison and Cook County importer tests pass `10/10`. No
  source record was imported, approved, published, or changed in production.
  Overall PRD completion remains `20/35` (`57%`).

2026-08-13 17:37 EDT - completed the selected Wave 6 evidence-gap cohorts.

- PRs #167-#171 merged the bounded housing, food, and immigrant/refugee
  repairs. #171 hosted quality and both deployments passed. Each selected
  record now has repaired evidence or an explicit hold backed by a concrete
  provider-source failure.
- Housing two-source coverage increased from `92/106` to `99/106`; food contact
  or verified intake coverage increased from `644/752` to `748/752`; and
  immigrant/refugee two-source identity coverage increased from `68/84` to
  `79/84`.
- Marked Wave 6 criterion 2 complete. Completion does not override held records
  or authorize publication. No record was imported, approved, published, or
  changed in production.
- Overall PRD completion is now `21/35` (`60%`), with `6` in progress and `8`
  not started.

2026-08-13 18:25 EDT - guarded the first Wave 6 publication canary.

- Found that the existing source publication command could create approved
  verification, administrator review, and public promotion in one operation.
  Reworked it to consume stored review evidence only, require a clean approved
  verification ledger, cap canaries at five explicit IDs, require exact source
  confirmation, and preserve private contact/link visibility.
- The separate review command now requires an existing administrator actor for
  every applied decision. No command can use an arbitrary actor ID to approve
  records through this workflow.
- A connected read-only Cook County cooling-center dry run found `34/35` local
  records passing the publication contract, one held, 25 matching unapproved
  staging rows, and nine publishable local records not yet staged. Zero records
  were selected for publication because the staged rows retain older incomplete
  fields and lack final review and verification evidence.
- Focused import and guard coverage passes `18/18`. Wave 6 criterion 3 is in
  progress. Overall PRD completion remains `21/35` (`60%`), with `7` in
  progress and `7` not started. No review, verification, visibility,
  publication, database, or production state changed.

2026-08-13 18:45 EDT - prepared the bounded Cook County staging refresh.

- Added an existing-only importer mode that selects publishable records from
  one exact local source, reads the existing official source, caps refreshes at
  25, skips unstaged rows, and refuses approved, ready, or promoted matches.
- Apply requires an explicit flag and exact source confirmation. A second
  preflight prevents a staging race from inserting a missing import record.
- The connected read-only plan selected exactly 25 existing unapproved Cook
  County cooling-center rows, reported nine unstaged publishable rows and zero
  protected matches, and made no write.
- Focused importer coverage passes `19/19`. Wave 6 criterion 3 remains in
  progress and overall completion remains `21/35` (`60%`). No review,
  verification, visibility, publication, database, or production state changed.

2026-08-13 19:25 EDT - refreshed Cook County staging and caught live source drift.

- Confirmed PR #174 merged with quality and both deployments green, then
  applied the exact existing-only plan. It refreshed 25 unapproved staging
  rows, inserted zero records, preserved 25 raw payloads, and added 803 field
  evidence rows. Review and promotion state did not change.
- The post-refresh publication dry run selected zero records because all 25
  still lack an identified administrator review and stored approved
  verification ledger. Production remained at 853 public resources with zero
  Cook County cooling centers.
- Compared the first five canary rows with Cook County's live official Socrata
  dataset. Bremen Township, Brookfield Library, and Calumet Township still
  match. Markham and Maywood courthouses were removed.
- The full source changed from 35 retained rows to 33 live rows: Markham,
  Maywood, and Skokie courthouses were removed and LaGrange Park Public Library
  was added. The targeted acquisition dry run parsed 33 rows with zero
  duplicates; the ignored local source cohort was refreshed for later review.
- Added a bounded live verifier and changed publication to trust only the latest
  verification result, preventing a newer removal from being overridden by an
  older approval. Focused resource coverage passes `20/20`. Wave 6 criterion 3
  and overall PRD completion remain `21/35` (`60%`).

2026-08-13 21:39 EDT - stored the bounded Cook County verification ledger.

- Confirmed PR #175 merged as `767c9aee`; quality, the step check, and both
  production deployments passed.
- Ran the released verifier for exactly five existing unapproved records.
  Bremen Township, Brookfield Library, and Calumet Township received approved
  verification results. Removed Markham and Maywood courthouse rows received
  needs-review results. Review and publication state did not change.
- A post-write guarded publication dry run selected `0/5`. All five remain
  blocked by missing identified administrator review; the two removed records
  are additionally blocked because their latest verification is not approved.
- The administrator review dry run matched only the three current records.
  Wave 6 criterion 3 remains in progress and overall completion remains
  `21/35` (`60%`). No record was reviewed, promoted, or made public.

2026-08-13 22:28 EDT - closed the obsolete public resource bulk-detail path.

- Production inspection found the released compact index contains no contact,
  source, street-address, or detail fields, while the obsolete bare
  `/api/public/resource-map/items` route still returned all `853` detailed
  records, including `700` records with public contacts.
- Replaced only that unused bare route with a temporary redirect to the bounded
  200-record compact index. The existing per-record detail route and its
  user-initiated phone and email actions remain unchanged.
- Added a focused regression that locks the redirect and cache contract. No
  CAPTCHA, contact relay, consent redesign, database write, review, promotion,
  or publication change occurred. Overall PRD completion remains `21/35`
  (`60%`).

2026-08-13 22:29 EDT - completed the first guarded Wave 6 publication canary.

- Confirmed PR #177 merged as `de031dde`; its `2,115` acceptance tests, hosted
  quality, and both deployments passed. Production now redirects the obsolete
  bulk-detail route to the bounded compact index.
- The product owner reviewed and approved only Bremen Township, Brookfield
  Library, and Calumet Township. A fresh read-only check matched all three to
  the current 33-row official Cook County source; the guarded plan selected
  exactly those records with zero accepted duplicate matches.
- The production canary promoted all three atomically. Anonymous public parity
  increased only from `853` to `856`; each exact detail route returns its
  intended record, while all three contacts and six links remain private.
  Removed courthouse rows remain held.
- Marked Wave 6 criterion 3 complete. Overall PRD completion is now `22/35`
  (`63%`), with `6` in progress and `7` not started. No raw intake, synthetic
  seed, unverified discovery record, deletion, or unrelated production state
  changed. Reversible hide controls remain available for the three canonical
  services and organizations.

2026-08-13 22:42 EDT - added data-backed Chicago resource guides.

- Measured the current 856-row production catalog read-only. Chicago has 394
  public resources across food (`201`), community (`114`), housing (`69`),
  legal (`31`), family (`23`), and health (`7`).
- Added six derived Chicago guides for food access, housing and shelter,
  immigration and legal help, libraries and community centers, youth and family
  support, and health care. Each guide requires at least five current matching
  records and disappears safely below that threshold.
- Preserved existing cooling-center guides and the current guide-selection/map
  behavior. Updated the Guides description so it no longer implies that every
  guide is heat-related. No resource, review, contact, link, or production data
  changed.
- Wave 6 criterion 4 is in progress. Overall completion remains `22/35` (`63%`),
  with `7` in progress and `6` not started. Continue with focused tests, browser
  proof, hosted deployment, live guide counts, and link checks.

2026-08-13 23:12 EDT - completed the varied Chicago guide release proof.

- Confirmed PR #179 merged as `729ec3f5`; its quality check and both production
  deployments passed, and `coachhouse.app` serves the released guide bundle.
- Read the same canonical paginated public API consumed by `/find`. All `856`
  resources loaded with exact guide counts: food `201`, housing `69`, legal
  `31`, libraries/community centers `103`, family `23`, and health `7`.
- One exact item-detail route per guide returned `200`. The production `/find`
  page was opened for review; Mapbox made the attached Chrome automation
  unresponsive, so no extended visual test loop was attempted.
- Marked Wave 6 criterion 4 complete. Overall completion is now `23/35` (`66%`),
  with `6` in progress and `6` not started. No resource or database data changed.

2026-08-13 23:40 EDT - proved signup legal acceptance through connected RLS.

- Added the current legal version and content hashes to the existing ephemeral
  RLS member fixture so the real `auth.users` trigger records consent.
- Connected production RLS verified the user-linked version, both hashes,
  server UTC acceptance time, and `signup` source; the user could read only
  their row, another authenticated user and anonymous caller could not read it,
  and authenticated insert, update, and delete attempts were denied.
- The existing user cleanup cascaded the acceptance row. Production acceptance
  count was `0` before and `0` after, so no account or consent record remains.
- Focused legal acceptance passed `5/5`; Fiscal, Finance, and the full connected
  RLS matrix passed. No email was sent and no lasting production data changed.

2026-08-13 23:31 EDT - completed Wave 6 release safety and rollback proof.

- Rechecked the three approved Cook County canaries in production. The complete
  anonymous catalog loaded `856/856` unique records; Bremen, Brookfield, and
  Calumet were present, their exact detail routes returned `200`, and their
  shared official Cook County source returned `200`.
- Rehearsed reversible unpublishing against only the approved Bremen service.
  The canonical public projection changed from `856` to `855` with Bremen
  absent, then a `finally` restoration returned it exactly to `856` and present.
- Final state is published with no hidden, suppressed, or deleted marker.
  Identified-administrator `hide` and `restore` curation events preserve the
  before and after states. No record was deleted and no other record changed.
- Production `/find` returned `200`; PR #180 quality and both production
  deployments passed. Marked Wave 6 criterion 5 and Wave 6 complete. Overall
  completion is now `24/35` (`69%`), with `6` in progress and `5` not started.

2026-08-13 23:49 EDT - repaired the Supabase preview seed CTE scope.

- Supabase Preview for PR #182 exposed a pre-existing seed failure because a
  `toc_module` CTE was referenced by a second statement outside its SQL scope.
- Repeated the bounded CTE for the `module_assignments` insert and added a
  regression locking both statement scopes. A second preview run exposed module
  upserts rewriting migration-owned slugs through the index conflict path.
- Kept slug identity migration-owned while retaining seed inserts and metadata
  updates. Production read-only inspection confirmed the canonical module
  slugs. No migration, production data, or PRD completion state changed.

2026-08-14 00:10 EDT - completed immutable signup consent proof.

- Confirmed PR #182 merged as `e7e3b393`. Its connected production RLS fixture
  invoked the real signup trigger and verified the current legal version, both
  content hashes, user linkage, server UTC acceptance time, and `signup` source.
- Verified owner-only reads, denied authenticated mutations, cleanup cascade,
  and an acceptance count of `0` before and after. No email was sent and no
  account or consent row remains.
- Confirmed PR #183 merged as `5e694663` after quality, Supabase Preview, and
  both Vercel previews passed. Marked Wave 2 criterion 2 complete. Overall PRD
  completion is now `25/35` (`71%`), with `5` in progress and `5` not started.

2026-08-14 00:32 EDT - protected Find interaction readiness from Mapbox startup.

- Used the latest bounded production baseline: FCP `1.7s`, LCP `4.7s`, and TTI
  `35.0s`, with Mapbox evaluation dominating the throttled main thread.
- Extended automatic Mapbox startup to an `8s` interaction-ready boundary so
  the shell can establish a five-second quiet window. A pointer action on the
  map still starts Mapbox immediately, preserving explicit user intent.
- No payload, map behavior after startup, signup, account, or production data
  changed. Wave 3 criterion 5 remains in progress pending preview and production
  mobile performance measurements.

2026-08-14 01:07 EDT - measured Find performance and completed the integrated quality gate.

- Confirmed PR #185 merged as `c1144707`; main quality and the `coachhouse`
  production deployment passed.
- One bounded production mobile Lighthouse 12.8.2 run measured FCP `1.69s`,
  LCP `4.60s`, TTI `48.0s`, TBT `44.23s`, and `9.43 MB` transferred. Wave 3
  criterion 5 remains open; no extended tuning or visual-test loop followed.
- GitHub Actions run `31771538852` passed the full quality gate. The latest full
  connected production RLS matrix passed with ephemeral cleanup, and no later
  change touched database policy.
- `supabase migration list --linked` showed exact local and production parity
  through `20260813153500`. No database or account data changed.
- Marked Wave 7 criterion 1 complete. Overall completion is now `26/35` (`74%`),
  with `5` in progress and `4` not started.

2026-08-14 02:02 EDT - completed the release security review and contained its high-risk paths.

- Reviewed merged commit `6dcabc1b` across signup and consent, public routes,
  My Map, Finance tenant boundaries, telemetry, status, RLS, and release
  operations. The standard scan validated ten findings: three high, six
  medium, and one low. Finance record/report scoping, My Map account scoping,
  and the public Find projection had no confirmed cross-tenant leak.
- The local candidate disables unauthenticated service-role tester
  provisioning, requires platform administration on all legacy class APIs,
  disables arbitrary server-side link preview fetching, binds Stripe install
  completion to the expected organization and current Finance authority,
  hardens post-auth redirects, and removes unmeasured public health claims.
- Added a support, ownership, monitoring, incident, and forward-rollback
  runbook. Four medium findings, named primary/backup ownership, a delivered
  alert test, and production retention evidence remain open.
- Focused security, legal, link-preview, status, and Finance tests pass `18/18`.
  Wave 7 criterion 2 is in progress. Overall completion remains `26/35`
  (`74%`), with `6` in progress and `3` not started. No account, production
  data, deployment, or remote database changed.

2026-08-14 02:19 EDT - repaired the Finance RLS migration test sequence.

- PR #187 merged as `4d0a0fcf`, but its quality job failed because the Finance
  RLS harness asserted the new six-argument Stripe completion function without
  first loading the forward migration that defines it.
- Added the missing migration to the existing ordered Finance test setup. The
  focused Finance RLS suite and complete local RLS command pass. No migration,
  application behavior, production data, or PRD completion state changed.

2026-08-14 02:39 EDT - closed the saved roadmap rich-HTML injection path.

- Reproduced that an authorized roadmap editor could save a named-entity
  `javascript:` link that the regex sanitizer preserved for public rendering.
- Replaced regex rewriting with a parsed allowlist for the rich-text editor's
  supported tags, attributes, styles, classes, and URL schemes. Encoded script
  URLs, executable tags and handlers, HTML data URLs, and unsafe CSS are now
  removed while HTTPS, relative, fragment, email, phone, image, list, table,
  alignment, and formatting output remain supported.
- Added focused encoding and bypass regressions. All 44 sanitizer and direct
  consumer tests pass, as does the npm supply-chain check. Wave 7 criterion 2
  remains in progress with three medium findings plus ownership, alert, and
  retention evidence open. Overall completion remains `26/35` (`74%`). No
  account, production data, migration, or deployment changed.

2026-08-14 03:17 EDT - bounded public roadmap analytics ingestion.

- Confirmed the public service-role route accepted unlimited forged event types,
  private organization slugs, nonexistent sections, oversized fields, and
  silent insert failures.
- Added strict body, type, length, public-roadmap, and section validation. Valid
  events now use a service-role-only database function with an atomic
  per-organization lock and caps of 120 events per minute and 5,000 per rolling
  day. No visitor IP address, fingerprint, account, or new tracking identifier
  is stored.
- Added eight focused regressions covering rejection, recording, rate-limit,
  database-failure, and migration-boundary behavior. The complete quality gate
  passes with 2,134 acceptance tests, the Finance and fiscal RLS suites, the
  production build, 30 visual tests, and performance budgets. Wave 7 criterion
  2 remains in progress with two medium findings plus ownership, alert, and
  retention evidence open. Overall completion remains `26/35` (`74%`). No
  production data, migration, deployment, or remote service changed.

2026-08-14 04:20 EDT - enforced signup legal consent at the database boundary.

- Reproduced that direct public authentication could create an account with no
  legal consent because the existing trigger silently skipped invalid browser
  metadata instead of rejecting the user insert.
- Added a forward trigger-function migration that rolls back account creation
  unless the current legal version, document hashes, and acceptance signal are
  present. The immutable record continues to use the trusted auth user creation
  time rather than the browser timestamp.
- Preserved service-role fixtures and administrative provisioning through an
  explicit app-metadata exemption that public user metadata cannot set. The
  exemption creates no false legal-acceptance row.
- A real PostgreSQL trigger harness proves missing, empty, and forged consent
  fail; valid consent and trusted provisioning pass. The complete quality gate
  passes with 2,135 acceptance tests, all signup-consent, Finance, and fiscal
  RLS suites, the production build, 30 visual tests, and performance budgets.
  Wave 7 criterion 2 remains in progress with one medium finding plus ownership,
  alert, and retention evidence open. Overall completion remains `26/35`
  (`74%`). No account, production data, migration, deployment, or remote
  service changed.

2026-08-14 14:20 EDT - strengthened the trusted signup-consent release candidate.

- Rebased the isolated `fix/trusted-signup-consent-20260814` branch onto current
  `origin/main` at `3c254d7f`; no unrelated worktree was changed.
- Extended the disposable PostgreSQL harness to reproduce the pre-fix bypass
  before applying the forward migration. Post-fix proof now requires the exact
  `22023` rejection for missing, empty, non-string, and user-metadata-forged
  consent while preserving valid signup and trusted service provisioning.
- Focused consent RLS and six legal acceptance tests pass. The full local gate
  passes supply-chain, lint, guardrails, snapshots, `2,135` acceptance tests
  with one intentional skip, signup/fiscal/Finance RLS, the 112-route build,
  all `30/30` visuals through isolated production/development runs, and
  performance budgets. The first combined visual run was starved by a separate
  long-running Next server; isolated production and webpack development runs
  passed without changing baselines.
- The optional generic connected RLS suite skipped because its separate
  credentials are absent. Supabase Preview and hosted CI remain required. No
  account, production data, migration, deployment, or remote service changed.
