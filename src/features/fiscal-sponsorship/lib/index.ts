import type { FiscalSponsorshipInput } from "../types"

export function normalizeFiscalSponsorshipInput(input: FiscalSponsorshipInput): FiscalSponsorshipInput {
  // Keep this layer pure and framework-agnostic.
  return input
}
